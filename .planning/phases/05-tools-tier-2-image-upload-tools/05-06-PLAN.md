---
phase: 05-tools-tier-2-image-upload-tools
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx
  - mystiqor-build/src/app/api/tools/compatibility/route.ts
autonomous: true
requirements: [TOOL-04]

must_haves:
  truths:
    - "User can enter birth data for two people and receive compatibility analysis"
    - "Compatibility analysis returns astrology + numerology combined scores"
    - "Compatibility types include romantic, friendship, professional, family"
    - "Results are saved to analyses table with tool_type='compatibility'"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/compatibility/route.ts"
      provides: "POST endpoint for two-person compatibility analysis"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx"
      provides: "Two-person form + compatibility results page"
      min_lines: 100
  key_links:
    - from: "mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx"
      to: "/api/tools/compatibility"
      via: "fetch POST with person1/person2 data"
      pattern: "fetch.*api/tools/compatibility"
    - from: "mystiqor-build/src/app/api/tools/compatibility/route.ts"
      to: "invokeLLM"
      via: "text-only LLM call (no imageUrls)"
      pattern: "invokeLLM"
---

<objective>
Build the compatibility analysis tool — dual-person birth data form, LLM analysis combining astrology + numerology, structured compatibility scores.

Purpose: TOOL-04 requires compatibility analysis between two people. This is a TEXT-ONLY tool (no image upload) — it follows the numerology compatibility pattern from Phase 4 but expands to include astrology aspects.

Output: Compatibility API route and page with two-person form, type selector (romantic/friendship/professional/family), and formatted results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md

<interfaces>
From RESEARCH.md — Compatibility Zod Schema:
```typescript
const PersonSchema = z.object({
  name: z.string().min(1, 'שם חובה'),
  birthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  birthTime: z.string().regex(/^\d{2}:\d{2}$/).optional(),
})
const CompatibilityInputSchema = z.object({
  person1: PersonSchema,
  person2: PersonSchema,
  compatibilityType: z.enum(['romantic', 'friendship', 'professional', 'family']),
})
```

From STATE.md line 126:
- tool_type='compatibility' is the correct value (NOT 'numerology_compatibility')
- This is already in the ToolType union in src/types/analysis.ts

From src/types/analysis.ts:
```typescript
export type ToolType = ... | 'compatibility' | ...
```

Pattern: This is text-only (no imageUrls). Similar to numerology compatibility route.
The existing numerology/compatibility/route.ts pattern can be referenced.

From github-source/src/pages/Compatibility.jsx (688 lines):
- Two-person form with name, birthDate, birthTime for each
- Type selector: romantic, friendship, professional, family
- LLM returns: overall_score (0-100), category_scores, strengths, challenges, advice, astrological_aspects, numerological_match
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compatibility API route</name>
  <files>
    mystiqor-build/src/app/api/tools/compatibility/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/palmistry/route.ts,
    mystiqor-build/src/services/analysis/llm.ts,
    mystiqor-build/src/types/analysis.ts,
    github-source/src/pages/Compatibility.jsx
  </read_first>
  <action>
    Create POST /api/tools/compatibility/route.ts:
    1. Auth check: createClient() + getUser() — return 401
    2. Zod input schema:
       PersonSchema = z.object({ name: z.string().min(1, 'שם חובה'), birthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'תאריך לא תקין'), birthTime: z.string().regex(/^\d{2}:\d{2}$/).optional() })
       CompatibilityInputSchema = z.object({ person1: PersonSchema, person2: PersonSchema, compatibilityType: z.enum(['romantic','friendship','professional','family']) })
    3. Zod response schema for validation:
       CompatibilityResponseSchema = z.object({
         overall_score: z.number().min(0).max(100),
         category_scores: z.array(z.object({ category: z.string().min(1), score: z.number().min(0).max(100), description: z.string().min(1) })).min(1),
         strengths: z.array(z.string().min(1)).min(1),
         challenges: z.array(z.string().min(1)).min(1),
         advice: z.string().min(1),
         summary: z.string().min(1),
       })
    4. Build system prompt in Hebrew:
       "אתה אסטרולוג ונומרולוג מומחה. נתח את התאימות בין שני אנשים על בסיס נתוני הלידה שלהם.
        סוג תאימות: {compatibilityType}
        אדם 1: {person1.name}, נולד/ה {person1.birthDate} {person1.birthTime || ''}
        אדם 2: {person2.name}, נולד/ה {person2.birthDate} {person2.birthTime || ''}
        נתח תאימות אסטרולוגית (מזלות, אלמנטים, היבטים) ונומרולוגית (מספרי חיים, גורל).
        החזר ציון כולל 0-100, ציונים לפי קטגוריות, חוזקות, אתגרים ועצות."
    5. invokeLLM: NO imageUrls (text-only), systemPrompt, prompt: 'נתח תאימות', responseSchema object, zodSchema: CompatibilityResponseSchema, maxTokens: 2000
    6. Save to analyses: tool_type: 'compatibility' (Pitfall 7 — NOT 'numerology_compatibility'), input_data: { person1, person2, compatibilityType }, results: validated response, summary: response.summary
    7. Return { data: { ...validatedResult, analysis_id } }
    8. try/catch — return 500 with 'שגיאה בניתוח תאימות'
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "export async function POST" src/app/api/tools/compatibility/route.ts returns match
    - grep -r "tool_type.*compatibility" src/app/api/tools/compatibility/route.ts returns match (correct tool_type per Pitfall 7)
    - grep -rL "imageUrls" src/app/api/tools/compatibility/route.ts returns match (no imageUrls — text only)
    - grep -r "CompatibilityInputSchema\|CompatibilityResponseSchema" src/app/api/tools/compatibility/route.ts returns match
    - grep -r "person1.*person2\|PersonSchema" src/app/api/tools/compatibility/route.ts returns match
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Compatibility API accepts two-person birth data + compatibility type, calls LLM for astrology + numerology analysis, validates response, saves with tool_type='compatibility', returns overall_score + category_scores + strengths + challenges + advice.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compatibility page with dual-person form and results</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/app/api/tools/compatibility/route.ts,
    github-source/src/pages/Compatibility.jsx
  </read_first>
  <action>
    Create compatibility/page.tsx:
    1. 'use client' directive
    2. Import SubscriptionGuard, React Hook Form, Zod, useMutation, ReactMarkdown
    3. Zod form schema matching CompatibilityInputSchema from the API route
    4. useForm with zodResolver, useMutation for /api/tools/compatibility POST
    5. Form layout — two side-by-side cards (responsive: stacked on mobile):
       - Card 1 "אדם 1": name input, birthDate input (type="date"), birthTime input (type="time", optional)
       - Card 2 "אדם 2": same fields
    6. Compatibility type selector — 4 styled buttons or radio group:
       רומנטי (romantic), חברות (friendship), מקצועי (professional), משפחתי (family)
    7. Submit button: "נתח תאימות"
    8. Form wrapped in SubscriptionGuard feature="analyses"
    9. Results section (shown after success):
       a. Overall score — large circular percentage display (e.g., "78%" in a circle)
       b. Category scores — horizontal bar chart or progress bars for each category
       c. Strengths list — green check items
       d. Challenges list — amber triangle items
       e. Advice text — ReactMarkdown
    10. PageHeader with breadcrumbs: [דף הבית (/), כלים (/tools), תאימות]
    11. RTL dir="rtl", Hebrew labels, toast messages in Hebrew
    12. Loading state, framer-motion fadeInUp on results
    13. Max 300 lines
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - grep -r "/api/tools/compatibility" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - grep -r "person1\|אדם 1" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - grep -r "person2\|אדם 2" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - grep -r "romantic\|friendship\|professional\|family" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - grep -r "overall_score\|overallScore" src/app/\(auth\)/tools/compatibility/page.tsx returns match
    - wc -l src/app/\(auth\)/tools/compatibility/page.tsx shows <= 300 lines
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Compatibility page shows dual-person form with name + birth data for each, type selector (romantic/friendship/professional/family), and results with overall score, category breakdown, strengths, challenges, and advice — all wrapped in SubscriptionGuard.
  </done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -r "SubscriptionGuard" src/app/\(auth\)/tools/compatibility/page.tsx
grep -r "tool_type.*compatibility" src/app/api/tools/compatibility/route.ts
</verification>

<success_criteria>
- Compatibility API accepts dual-person data and returns structured compatibility analysis
- Compatibility page has dual-person form, type selector, and formatted results
- tool_type='compatibility' (per Pitfall 7 / STATE.md decision)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-06-SUMMARY.md`
</output>
