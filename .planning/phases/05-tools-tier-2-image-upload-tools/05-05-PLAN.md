---
phase: 05-tools-tier-2-image-upload-tools
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - mystiqor-build/src/components/features/graphology/GraphologyTimeline.tsx
  - mystiqor-build/src/components/features/graphology/GraphologyCompare.tsx
  - mystiqor-build/src/components/features/graphology/GraphologyReminder.tsx
  - mystiqor-build/src/app/(auth)/tools/graphology/page.tsx
  - mystiqor-build/src/app/globals.css
autonomous: true
requirements: [GRPH-02, GRPH-03, GRPH-04, GRPH-06]

must_haves:
  truths:
    - "User can view graphology progress across multiple sessions on a timeline"
    - "User can compare two graphology analyses side-by-side"
    - "User can export graphology analysis to PDF via browser print"
    - "User can set a graphology reminder for next session"
  artifacts:
    - path: "mystiqor-build/src/components/features/graphology/GraphologyTimeline.tsx"
      provides: "Timeline showing graphology sessions with score progression"
      min_lines: 40
    - path: "mystiqor-build/src/components/features/graphology/GraphologyCompare.tsx"
      provides: "Side-by-side comparison of two graphology analyses"
      min_lines: 50
    - path: "mystiqor-build/src/components/features/graphology/GraphologyReminder.tsx"
      provides: "Next-session reminder component with date picker"
      min_lines: 30
  key_links:
    - from: "mystiqor-build/src/components/features/graphology/GraphologyTimeline.tsx"
      to: "analyses table"
      via: "fetch past graphology analyses"
      pattern: "tool_type.*graphology"
    - from: "mystiqor-build/src/app/globals.css"
      to: "window.print()"
      via: "@media print styles hide nav, show content"
      pattern: "@media print"
---

<objective>
Build graphology timeline (GRPH-02), comparison (GRPH-03), PDF export via CSS print (GRPH-04), and reminder system (GRPH-06). Wire all into the graphology page as tabs.

Purpose: These features complete the graphology tool suite — users can track progress, compare sessions, export results, and set reminders for follow-up analysis.

Output: GraphologyTimeline, GraphologyCompare, GraphologyReminder components. CSS @media print rules. Graphology page updated with tab navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-04-SUMMARY.md

<interfaces>
From src/services/analysis/response-schemas/graphology.ts:
```typescript
export type GraphologyResponse = z.infer<typeof GraphologyResponseSchema>
// { summary, components: [{name, score_1_to_10, description}], overall_assessment, personality_traits[], insights[] }
```

PDF Export approach (from RESEARCH.md):
- No new library — use window.print() + CSS @media print
- Hide navigation, buttons, sidebars during print
- Show only the analysis content area
- RTL Hebrew renders correctly in browser print
- Pattern: add class "no-print" to elements that should be hidden, "print-only" to elements shown only in print

GraphologyTimeline reference (github-source/src/components/GraphologyTimeline.jsx, 89 lines):
- Shows a vertical timeline of past graphology sessions
- Each node: date, overall_assessment summary, average component score
- Connects nodes with a vertical line

GRPH-06 Reminder (from RESEARCH.md Open Question 3):
- v1 = simple localStorage-based reminder with next date
- Display in UI: "תזכורת הבאה: DD/MM/YYYY"
- No push notifications in Phase 5 (deferred to Phase 8 TRCK-06)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GraphologyTimeline, GraphologyCompare, GraphologyReminder components + print CSS</name>
  <files>
    mystiqor-build/src/components/features/graphology/GraphologyTimeline.tsx,
    mystiqor-build/src/components/features/graphology/GraphologyCompare.tsx,
    mystiqor-build/src/components/features/graphology/GraphologyReminder.tsx,
    mystiqor-build/src/app/globals.css
  </files>
  <read_first>
    mystiqor-build/src/services/analysis/response-schemas/graphology.ts,
    mystiqor-build/src/app/(auth)/tools/graphology/page.tsx,
    mystiqor-build/src/app/globals.css,
    github-source/src/components/GraphologyTimeline.jsx
  </read_first>
  <action>
    Create GraphologyTimeline.tsx:
    1. Props: { userId: string }
    2. useQuery fetching past graphology analyses: fetch('/api/analyses?tool_type=graphology&limit=20')
    3. Vertical timeline layout: each session is a dot on a vertical line with date and average score
    4. Each timeline node shows: date (DD/MM/YYYY format), average of component scores, brief summary (first 100 chars)
    5. Most recent at top
    6. If no analyses: "אין ניתוחים קודמים. בצע ניתוח ראשון!"
    7. Hebrew labels, RTL
    8. Port visual pattern from github-source GraphologyTimeline.jsx (89 lines — straightforward)

    Create GraphologyCompare.tsx:
    1. Props: { userId: string }
    2. Same pattern as DrawingCompare from Plan 03: useQuery for past analyses, two Select pickers, side-by-side display
    3. Side-by-side: comparison of the 9 component scores (show both scores per component with delta arrows)
    4. Highlight components where scores improved (green) or decreased (red)
    5. Show personality_traits comparison: traits in A only, in B only, in both
    6. Hebrew labels: "דגימה A", "דגימה B", "השוואה", "שיפור", "ירידה"

    Create GraphologyReminder.tsx:
    1. Props: none (uses localStorage internally)
    2. State: reminderDate (from localStorage key 'graphology-reminder-date')
    3. UI: if reminder set — show "תזכורת הבאה: DD/MM/YYYY" with button to clear
    4. If no reminder — show date input + button "הגדר תזכורת"
    5. On set: localStorage.setItem('graphology-reminder-date', dateString)
    6. On clear: localStorage.removeItem('graphology-reminder-date')
    7. Simple and small — no push notifications, no server-side storage
    8. Hebrew labels, RTL

    Update globals.css — append @media print rules:
    ```css
    @media print {
      .no-print { display: none !important; }
      .print-only { display: block !important; }
      body { background: white !important; color: black !important; }
      nav, header, aside, footer { display: none !important; }
    }
    ```
    IMPORTANT: Only append — do NOT modify existing CSS rules.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "export.*GraphologyTimeline\|export default.*GraphologyTimeline" src/components/features/graphology/GraphologyTimeline.tsx returns match
    - grep -r "export.*GraphologyCompare\|export default.*GraphologyCompare" src/components/features/graphology/GraphologyCompare.tsx returns match
    - grep -r "export.*GraphologyReminder\|export default.*GraphologyReminder" src/components/features/graphology/GraphologyReminder.tsx returns match
    - grep -r "tool_type.*graphology" src/components/features/graphology/GraphologyTimeline.tsx returns match
    - grep -r "localStorage" src/components/features/graphology/GraphologyReminder.tsx returns match
    - grep -r "@media print" src/app/globals.css returns match
    - grep -r "no-print" src/app/globals.css returns match
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    GraphologyTimeline shows vertical timeline with date+score per session. GraphologyCompare shows side-by-side with score deltas. GraphologyReminder saves/shows next date via localStorage. CSS print rules added to globals.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire timeline, comparison, reminder, and PDF into graphology page tabs</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/graphology/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/graphology/page.tsx,
    mystiqor-build/src/components/features/graphology/GraphologyTimeline.tsx,
    mystiqor-build/src/components/features/graphology/GraphologyCompare.tsx,
    mystiqor-build/src/components/features/graphology/GraphologyReminder.tsx
  </read_first>
  <action>
    Update graphology page to include tabs for all graphology features:
    1. Add tab navigation: "ניתוח חדש" (new analysis), "ציר זמן" (timeline), "השוואה" (compare), "תזכורת" (reminder)
    2. Tab 1 (ניתוח חדש): existing form + results — no changes except:
       - Add className="no-print" to nav/form elements
       - Add className="print-content" to results section (visible during print)
       - The window.print() button added in Plan 04 already works — now CSS rules from globals.css take effect
    3. Tab 2 (ציר זמן): render GraphologyTimeline component
    4. Tab 3 (השוואה): render GraphologyCompare component
    5. Tab 4 (תזכורת): render GraphologyReminder component
    6. Keep SubscriptionGuard only on Tab 1 (analysis form)
    7. Timeline and compare need userId — either get from a useQuery/session hook or pass as prop
    8. Maintain RTL, Hebrew labels
    9. Keep file under 300 lines — use dynamic imports for the tab content components
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "GraphologyTimeline" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "GraphologyCompare" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "GraphologyReminder" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "no-print" src/app/\(auth\)/tools/graphology/page.tsx returns match (print CSS class)
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - wc -l src/app/\(auth\)/tools/graphology/page.tsx shows <= 300 lines
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Graphology page has 4 tabs: new analysis (SubscriptionGuard + print-ready), timeline, comparison, and reminder. All GRPH requirements (01-06) are accessible from the graphology page.
  </done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -r "GraphologyTimeline" src/app/\(auth\)/tools/graphology/page.tsx
grep -r "@media print" src/app/globals.css
</verification>

<success_criteria>
- GraphologyTimeline shows session history with scores
- GraphologyCompare shows side-by-side with score deltas
- PDF export works via window.print() with @media print CSS
- GraphologyReminder saves next date to localStorage
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-05-SUMMARY.md`
</output>
