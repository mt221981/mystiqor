---
phase: 05-tools-tier-2-image-upload-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/tools/drawing/page.tsx
  - mystiqor-build/src/app/api/tools/drawing/route.ts
  - mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx
  - mystiqor-build/src/components/features/drawing/FDMVisualization.tsx
  - mystiqor-build/src/components/features/drawing/AnnotatedDrawingViewer.tsx
autonomous: true
requirements: [DRAW-01, DRAW-03, DRAW-04]

must_haves:
  truths:
    - "User can upload a drawing image and receive HTP psychological analysis"
    - "Koppitz indicators render with present/absent badges and significance"
    - "FDM categories display when returned by LLM"
    - "Annotated drawing viewer shows the uploaded image with feature list"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/drawing/route.ts"
      provides: "POST endpoint for drawing analysis via LLM vision"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/drawing/page.tsx"
      provides: "Drawing analysis page with upload + results display"
      min_lines: 100
    - path: "mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx"
      provides: "Koppitz indicator list with present/absent badges"
      min_lines: 40
    - path: "mystiqor-build/src/components/features/drawing/FDMVisualization.tsx"
      provides: "FDM categories display component"
      min_lines: 30
    - path: "mystiqor-build/src/components/features/drawing/AnnotatedDrawingViewer.tsx"
      provides: "Image viewer with analysis feature annotations"
      min_lines: 40
  key_links:
    - from: "mystiqor-build/src/app/(auth)/tools/drawing/page.tsx"
      to: "/api/tools/drawing"
      via: "fetch POST with imageUrl + drawingType"
      pattern: "fetch.*api/tools/drawing"
    - from: "mystiqor-build/src/app/api/tools/drawing/route.ts"
      to: "invokeLLM"
      via: "imageUrls for GPT-4o vision analysis"
      pattern: "invokeLLM.*imageUrls"
    - from: "mystiqor-build/src/app/api/tools/drawing/route.ts"
      to: "DrawingResponseSchema"
      via: "Zod validation of LLM response"
      pattern: "DrawingResponseSchema"
---

<objective>
Build the drawing analysis tool — upload drawing image, analyze via LLM vision (GPT-4o), display results with Koppitz indicators, FDM categories, and annotated drawing viewer.

Purpose: DRAW-01 (upload + HTP analysis), DRAW-03 (Koppitz + FDM), DRAW-04 (annotated viewer) are the core drawing analysis pipeline. This is the most complex tool in Phase 5 — image upload to LLM vision to structured psychological analysis.

Output: Working drawing analysis page with API route, Koppitz visualization, FDM display, and annotated viewer components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md

<interfaces>
<!-- Existing types and services the executor needs -->

From src/services/analysis/response-schemas/drawing.ts:
```typescript
export const DrawingResponseSchema = z.object({
  summary: z.string().min(1),
  analysis_type: z.enum(['house', 'tree', 'person', 'free']),
  features: z.array(z.object({
    name: z.string().min(1),
    present: z.boolean(),
    significance: z.string().min(1),
  })).min(1),
  koppitz_score: z.number().min(0).max(30).optional(),
  fdm_categories: z.array(z.string()).optional(),
  emotional_indicators: z.array(z.string()),
  insights: z.array(InsightSchema).min(1),
})
export type DrawingResponse = z.infer<typeof DrawingResponseSchema>
```

From src/services/drawing/analysis.ts:
```typescript
export interface DrawingFeatures { ... }
export function buildDrawingAnalysisPrompt(imageDescription: string, features: Partial<DrawingFeatures>): string
export async function extractDrawingFeatures(imageUrl: string): Promise<DrawingFeatures>
```

From src/services/analysis/llm.ts:
```typescript
export interface LLMRequest {
  prompt: string; systemPrompt?: string;
  responseSchema?: Record<string, unknown>;
  zodSchema?: z.ZodSchema;
  imageUrls?: string[];  // triggers gpt-4o
  maxTokens?: number; userId: string;
}
export async function invokeLLM<T>(request: LLMRequest): Promise<LLMResponse<T>>
```

From src/types/analysis.ts:
```typescript
export type ToolType = 'drawing' | 'graphology' | 'compatibility' | ... // 'drawing' is valid
```

From src/app/api/tools/palmistry/route.ts (pattern to follow):
- Auth check via createClient() + getUser()
- Zod input validation
- invokeLLM with imageUrls: [imageUrl]
- Save to analyses table with tool_type
- Return { data: { ...result, analysis_id } }

From src/app/(auth)/tools/palmistry/page.tsx (pattern to follow):
- SubscriptionGuard wrapping form
- File upload via /api/upload → gets URL → sets form field
- useMutation for API call
- ReactMarkdown for text results
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawing analysis API route + visualization components</name>
  <files>
    mystiqor-build/src/app/api/tools/drawing/route.ts,
    mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx,
    mystiqor-build/src/components/features/drawing/FDMVisualization.tsx,
    mystiqor-build/src/components/features/drawing/AnnotatedDrawingViewer.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/palmistry/route.ts,
    mystiqor-build/src/services/analysis/response-schemas/drawing.ts,
    mystiqor-build/src/services/drawing/analysis.ts,
    mystiqor-build/src/services/analysis/llm.ts,
    mystiqor-build/src/types/analysis.ts,
    github-source/src/components/KoppitzIndicatorsVisualization.jsx,
    github-source/src/components/FDMVisualization.jsx,
    github-source/src/components/AnnotatedDrawingViewer.jsx
  </read_first>
  <action>
    Create POST /api/tools/drawing/route.ts following the palmistry route pattern exactly:
    1. Auth check: createClient() + getUser() — return 401 if not authenticated
    2. Zod input schema: { imageUrl: z.string().url(), drawingType: z.enum(['house','tree','person','free']), userAge: z.number().int().min(1).max(120).optional(), userGender: z.enum(['male','female','other']).optional() }
    3. Call buildDrawingAnalysisPrompt() from src/services/drawing/analysis.ts with 'ניתח את הציור' and empty features
    4. Call invokeLLM with: imageUrls: [parsed.data.imageUrl], prompt from buildDrawingAnalysisPrompt, responseSchema object matching DrawingResponseSchema shape, zodSchema: DrawingResponseSchema, maxTokens: 3000
    5. CRITICAL: Both responseSchema AND zodSchema must be passed (Pitfall 4 from RESEARCH.md)
    6. Save to analyses table: tool_type: 'drawing', input_data: { imageUrl, drawingType }, results: validated LLM response, summary: LLM response summary field
    7. Return { data: { ...validatedResult, analysis_id } }
    8. Wrap in try/catch — return 500 with Hebrew error message on failure

    Create KoppitzVisualization.tsx:
    - Port from github-source KoppitzIndicatorsVisualization.jsx to TypeScript
    - Props: { features: DrawingResponse['features'], koppitzScore?: number }
    - Render features as a list with present/absent badges (green check / red X)
    - Show koppitz_score as a score badge with risk level: 0-10 normal (green), 11-20 moderate (yellow), 21-30 high (red)
    - Hebrew labels throughout, RTL layout
    - JSDoc in Hebrew on component and Props interface

    Create FDMVisualization.tsx:
    - Port from github-source FDMVisualization.jsx to TypeScript
    - Props: { categories: string[], emotionalIndicators: string[] }
    - Render FDM categories as tagged badges
    - Render emotional indicators as a list with colored dots by severity
    - Hebrew labels, RTL
    - JSDoc in Hebrew

    Create AnnotatedDrawingViewer.tsx:
    - Port from github-source AnnotatedDrawingViewer.jsx to TypeScript
    - Props: { imageUrl: string, features: DrawingResponse['features'], summary: string }
    - Display the uploaded image using next/image (or img with proper sizing)
    - Below image, show summary text
    - Show features as an overlay list on the side (not literally on the image — side panel)
    - Hebrew labels, RTL
    - JSDoc in Hebrew
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "export async function POST" src/app/api/tools/drawing/route.ts returns match
    - grep -r "imageUrls" src/app/api/tools/drawing/route.ts returns match (GPT-4o vision mode)
    - grep -r "DrawingResponseSchema" src/app/api/tools/drawing/route.ts returns match (Zod validation)
    - grep -r "tool_type.*drawing" src/app/api/tools/drawing/route.ts returns match
    - grep -r "export.*function KoppitzVisualization" src/components/features/drawing/KoppitzVisualization.tsx returns match
    - grep -r "export.*function FDMVisualization" src/components/features/drawing/FDMVisualization.tsx returns match
    - grep -r "export.*function AnnotatedDrawingViewer" src/components/features/drawing/AnnotatedDrawingViewer.tsx returns match
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    API route accepts imageUrl + drawingType, calls invokeLLM with imageUrls (GPT-4o vision), validates with DrawingResponseSchema, saves to analyses table with tool_type='drawing', returns structured DrawingResponse. Three visualization components are TypeScript-strict with typed Props interfaces and Hebrew JSDoc.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drawing analysis page with upload, results display, and SubscriptionGuard</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/drawing/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx,
    mystiqor-build/src/components/features/drawing/FDMVisualization.tsx,
    mystiqor-build/src/components/features/drawing/AnnotatedDrawingViewer.tsx,
    mystiqor-build/src/app/api/tools/drawing/route.ts
  </read_first>
  <action>
    Create drawing/page.tsx following the palmistry page pattern:
    1. 'use client' directive
    2. Import SubscriptionGuard from '@/components/features/subscription/SubscriptionGuard'
    3. Import KoppitzVisualization, FDMVisualization, AnnotatedDrawingViewer from '@/components/features/drawing/'
    4. Zod form schema: { imageUrl: z.string().url(), drawingType: z.enum(['house','tree','person','free']) }
    5. useForm with zodResolver, useMutation for /api/tools/drawing POST
    6. File upload handler matching palmistry pattern — FormData to /api/upload, sets imageUrl field
    7. Drawing type selector — 4 buttons or radio group: בית (house), עץ (tree), אדם (person), חופשי (free)
    8. Form wrapped in SubscriptionGuard feature="analyses"
    9. Results section (shown after successful mutation):
       a. AnnotatedDrawingViewer showing the image + features + summary
       b. KoppitzVisualization showing features + koppitz_score
       c. FDMVisualization showing fdm_categories + emotional_indicators
       d. Insights section — map over insights array, show category + text using ReactMarkdown
    10. PageHeader with breadcrumbs: [דף הבית (/), כלים (/tools), ניתוח ציורים]
    11. RTL dir="rtl", Hebrew labels, toast messages in Hebrew
    12. Loading state with Loader2 spinner
    13. framer-motion fadeInUp on results section
    14. Max 300 lines per file constraint
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "KoppitzVisualization" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "FDMVisualization" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "AnnotatedDrawingViewer" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "/api/tools/drawing" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "/api/upload" src/app/\(auth\)/tools/drawing/page.tsx returns match (file upload)
    - grep -r "drawingType" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - wc -l src/app/\(auth\)/tools/drawing/page.tsx shows <= 300 lines
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Drawing page allows user to upload an image (via /api/upload), select drawing type (house/tree/person/free), submit for analysis, and see Koppitz indicators, FDM categories, annotated viewer, and insights — all wrapped in SubscriptionGuard.
  </done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -r "SubscriptionGuard" src/app/\(auth\)/tools/drawing/page.tsx
grep -r "imageUrls" src/app/api/tools/drawing/route.ts
grep -r "DrawingResponseSchema" src/app/api/tools/drawing/route.ts
</verification>

<success_criteria>
- Drawing API route accepts image URL and returns DrawingResponse with Koppitz + FDM + insights
- Drawing page has file upload, drawing type selector, SubscriptionGuard, and all 3 visualization components
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-01-SUMMARY.md`
</output>
