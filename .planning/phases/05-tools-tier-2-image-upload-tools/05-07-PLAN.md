---
phase: 05-tools-tier-2-image-upload-tools
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified: []
autonomous: false
requirements: [TOOL-02, TOOL-03]

must_haves:
  truths:
    - "Human Design page (TOOL-02) has SubscriptionGuard wrapping"
    - "Palmistry page (TOOL-03) has SubscriptionGuard wrapping"
    - "TypeScript compiles with zero errors across all Phase 5 work"
    - "All Phase 5 tool pages are accessible and functional"
  artifacts: []
  key_links: []
---

<objective>
Verify Human Design (TOOL-02) and Palmistry (TOOL-03) already have SubscriptionGuard, run TypeScript compilation gate for all Phase 5 work, and get human approval.

Purpose: TOOL-02 and TOOL-03 are already built from Phase 4 — this plan audits them (per planning note "already built, audit only") and serves as the integration verification gate for all Phase 5 plans.

Output: Confirmation that TOOL-02 and TOOL-03 pass audit, TypeScript compilation clean, human verifier approves all Phase 5 tool pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-01-SUMMARY.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-02-SUMMARY.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-03-SUMMARY.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-04-SUMMARY.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-05-SUMMARY.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript compilation + TOOL-02/TOOL-03 SubscriptionGuard audit</name>
  <files></files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/human-design/page.tsx,
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx
  </read_first>
  <action>
    1. Run tsc --noEmit on mystiqor-build — must exit code 0 with zero errors across all Phase 5 work
    2. Audit TOOL-02 (Human Design):
       - Read human-design/page.tsx
       - Check for SubscriptionGuard import and usage wrapping the form
       - Per Pitfall 8 and Phase 4 audit: SubscriptionGuard was already confirmed present, but verify it is still there
       - If missing: add SubscriptionGuard wrapping the form content, following the pattern from palmistry/page.tsx
       - Note: Per STATE.md, Human Design page was built in Phase 4 and is confirmed complete — likely no changes needed
    3. Audit TOOL-03 (Palmistry):
       - Read palmistry/page.tsx
       - Check for SubscriptionGuard import and usage wrapping the form
       - Per Phase 4 audit: palmistry had SubscriptionGuard — verify still present
       - If missing: add SubscriptionGuard wrapping the form content
    4. Run grep to confirm SubscriptionGuard on ALL Phase 5 tool pages:
       grep -r "SubscriptionGuard" src/app/\(auth\)/tools/drawing/ src/app/\(auth\)/tools/graphology/ src/app/\(auth\)/tools/compatibility/ src/app/\(auth\)/tools/human-design/ src/app/\(auth\)/tools/palmistry/
       Must show matches for all 5 tool pages
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits 0 (zero TypeScript errors)
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/human-design/page.tsx returns match (TOOL-02 audit pass)
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/palmistry/page.tsx returns match (TOOL-03 audit pass)
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/compatibility/page.tsx returns match
  </acceptance_criteria>
  <done>
    TypeScript compiles cleanly across all Phase 5 work. All 5 Phase 5 tool pages (drawing, graphology, compatibility, human-design, palmistry) confirmed to have SubscriptionGuard wrapping.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 — Tools Tier 2: Image Upload Tools. The following tools were built/verified:
    1. Drawing Analysis — upload image or draw on canvas, HTP psychological analysis with Koppitz scoring + FDM visualization + annotated viewer + comparison + concept cards
    2. Graphology — upload handwriting, 9-component analysis with radar chart + timeline + comparison + PDF export + reminder
    3. Compatibility — two-person form with astrology + numerology compatibility scores
    4. Human Design (TOOL-02) — verified existing, SubscriptionGuard confirmed
    5. Palmistry (TOOL-03) — verified existing, SubscriptionGuard confirmed
  </what-built>
  <how-to-verify>
    Visit each tool page in the browser (dev server must be running: cd mystiqor-build && npm run dev):

    1. Drawing Analysis (/tools/drawing):
       - Upload an image via "העלאת תמונה" — verify analysis results appear
       - Switch to "ציור בדפדפן" — draw on canvas, save — verify analysis results appear
       - Check "השוואה" tab — verify comparison UI loads
       - Check "מושגים" tab — verify 4 educational concept cards

    2. Graphology (/tools/graphology):
       - Upload a handwriting image — verify radar chart + components appear
       - Click "ייצא PDF" — verify browser print dialog opens with content
       - Check "ציר זמן" tab — verify timeline UI loads
       - Check "השוואה" tab — verify comparison UI loads
       - Check "תזכורת" tab — verify date picker works

    3. Compatibility (/tools/compatibility):
       - Fill in both person forms (name + birth date)
       - Select compatibility type
       - Submit — verify overall score + category scores + strengths/challenges appear

    4. Human Design (/tools/human-design) — verify page loads, form works
    5. Palmistry (/tools/palmistry) — verify page loads, form works

    All pages should show SubscriptionGuard upgrade prompt for free users.
    All pages should be RTL Hebrew.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -rl "SubscriptionGuard" src/app/\(auth\)/tools/drawing/ src/app/\(auth\)/tools/graphology/ src/app/\(auth\)/tools/compatibility/ src/app/\(auth\)/tools/human-design/ src/app/\(auth\)/tools/palmistry/
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- All 5 tool pages have SubscriptionGuard
- Human verifier approves all Phase 5 pages
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-07-SUMMARY.md`
</output>
