---
phase: 05-tools-tier-2-image-upload-tools
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - mystiqor-build/src/components/features/drawing/DrawingCompare.tsx
  - mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx
  - mystiqor-build/src/app/(auth)/tools/drawing/page.tsx
autonomous: true
requirements: [DRAW-05, DRAW-06]

must_haves:
  truths:
    - "User can select two past drawing analyses and see them side-by-side"
    - "Drawing concept cards display educational HTP content"
    - "Comparison shows differences in Koppitz scores and emotional indicators"
  artifacts:
    - path: "mystiqor-build/src/components/features/drawing/DrawingCompare.tsx"
      provides: "Side-by-side comparison of two drawing analyses"
      min_lines: 60
    - path: "mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx"
      provides: "Static educational content about HTP drawing analysis"
      min_lines: 40
  key_links:
    - from: "mystiqor-build/src/components/features/drawing/DrawingCompare.tsx"
      to: "analyses table"
      via: "fetch /api/analyses?tool_type=drawing"
      pattern: "tool_type.*drawing"
---

<objective>
Build drawing comparison (DRAW-05) and concept cards (DRAW-06), and wire them into the drawing page.

Purpose: Comparison lets users track psychological development across drawing sessions. Concept cards educate users about HTP interpretation methods, making the tool more accessible.

Output: DrawingCompare component with two-analysis picker + side-by-side display. DrawingConceptCards with static educational content. Drawing page updated with tabs for these sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-01-SUMMARY.md

<interfaces>
From src/services/analysis/response-schemas/drawing.ts:
```typescript
export type DrawingResponse = z.infer<typeof DrawingResponseSchema>
// { summary, analysis_type, features[], koppitz_score?, fdm_categories?, emotional_indicators[], insights[] }
```

From analyses table (Supabase):
```typescript
// Query: supabase.from('analyses').select('id, created_at, summary, results, input_data').eq('tool_type', 'drawing').eq('user_id', userId).order('created_at', { ascending: false }).limit(10)
// Each row.results contains DrawingResponse shape
```

Pitfall 6 from RESEARCH.md: Single query with .select() — do NOT fetch each analysis separately (N+1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DrawingCompare and DrawingConceptCards components</name>
  <files>
    mystiqor-build/src/components/features/drawing/DrawingCompare.tsx,
    mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx
  </files>
  <read_first>
    mystiqor-build/src/services/analysis/response-schemas/drawing.ts,
    mystiqor-build/src/types/analysis.ts,
    mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx
  </read_first>
  <action>
    Create DrawingCompare.tsx:
    1. Props: { userId: string } (needed for fetching user's past analyses)
    2. useQuery to fetch past drawing analyses: GET /api/analyses?tool_type=drawing&limit=10 (single query, Pitfall 6)
    3. If no analyses or fewer than 2: show message "נדרשים לפחות 2 ניתוחים להשוואה"
    4. Two Select dropdowns (or simple list with radio) — let user pick analysis A and analysis B by date
    5. Side-by-side display in a 2-column grid (responsive: 1 col on mobile):
       - Left card: Analysis A date, drawing type, summary, koppitz_score, emotional_indicators
       - Right card: Analysis B date, drawing type, summary, koppitz_score, emotional_indicators
    6. Highlight differences: if koppitz_score differs, show delta with arrow (up/down). If emotional indicators differ, highlight new/removed ones with color.
    7. Hebrew labels: "ניתוח A", "ניתוח B", "בחר ניתוח", "השוואה", "ציון Koppitz", "מדדים רגשיים"
    8. TypeScript strict — type the analysis results as DrawingResponse from response schemas
    9. JSDoc in Hebrew

    Create DrawingConceptCards.tsx:
    1. Static educational content component — no API calls
    2. 4 concept cards about HTP drawing analysis:
       - Card 1: "מהו מבחן HTP?" — House-Tree-Person test explanation
       - Card 2: "מדדי Koppitz" — What Koppitz indicators measure
       - Card 3: "FDM — שיטת ציור משפחה" — Family Drawing Method explanation
       - Card 4: "כיצד לפרש את התוצאות" — How to interpret results responsibly
    3. Each card: icon (from lucide-react), Hebrew title, 2-3 sentence Hebrew description
    4. Grid layout: 2 cols on desktop, 1 on mobile
    5. Use Card/CardHeader/CardContent from shadcn/ui
    6. JSDoc in Hebrew
    7. No Props needed — purely static
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "export.*function DrawingCompare\|export default function DrawingCompare" src/components/features/drawing/DrawingCompare.tsx returns match
    - grep -r "export.*function DrawingConceptCards\|export default function DrawingConceptCards" src/components/features/drawing/DrawingConceptCards.tsx returns match
    - grep -r "tool_type.*drawing" src/components/features/drawing/DrawingCompare.tsx returns match (fetches drawing analyses)
    - grep -r "koppitz" src/components/features/drawing/DrawingCompare.tsx -i returns match (shows koppitz comparison)
    - grep -r "HTP" src/components/features/drawing/DrawingConceptCards.tsx returns match (educational content)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    DrawingCompare shows two picker dropdowns and side-by-side comparison with Koppitz score deltas and emotional indicator diffs. DrawingConceptCards shows 4 static educational cards about HTP methodology.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire comparison and concept cards into drawing page tabs</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/drawing/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/drawing/page.tsx,
    mystiqor-build/src/components/features/drawing/DrawingCompare.tsx,
    mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx
  </read_first>
  <action>
    Update drawing page to include tabs/sections for comparison and concept cards:
    1. Add a tab bar or section navigation at the page level with 3 tabs: "ניתוח חדש" (new analysis — default), "השוואה" (comparison), "מושגים" (concepts)
    2. Tab 1 (ניתוח חדש): the existing form (upload/canvas toggle + results) — no changes
    3. Tab 2 (השוואה): render DrawingCompare component, pass userId (get from session or pass as prop)
    4. Tab 3 (מושגים): render DrawingConceptCards component
    5. Use simple state-based tab switching (useState<'analyze'|'compare'|'concepts'>), not routing
    6. Keep SubscriptionGuard only on the analysis tab (tab 1) — comparison and concepts are viewable by all users
    7. Maintain RTL, Hebrew labels
    8. Keep file under 300 lines — the page is getting large, so use dynamic imports for DrawingCompare and DrawingConceptCards (ssr: false not strictly needed for these, but keeps bundle small)
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "DrawingCompare" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "DrawingConceptCards" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "compare\|comparison\|השוואה" src/app/\(auth\)/tools/drawing/page.tsx returns match
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/drawing/page.tsx returns match (still present)
    - wc -l src/app/\(auth\)/tools/drawing/page.tsx shows <= 300 lines
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Drawing page has 3 tabs: new analysis (with SubscriptionGuard), comparison (DrawingCompare), and concepts (DrawingConceptCards). All drawing requirements (DRAW-01 through DRAW-06) are accessible from a single page.
  </done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -r "DrawingCompare" src/app/\(auth\)/tools/drawing/page.tsx
grep -r "DrawingConceptCards" src/app/\(auth\)/tools/drawing/page.tsx
</verification>

<success_criteria>
- DrawingCompare loads past analyses and shows side-by-side with difference highlighting
- DrawingConceptCards shows 4 educational HTP cards
- Drawing page has tab navigation for all 3 sections
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-03-SUMMARY.md`
</output>
