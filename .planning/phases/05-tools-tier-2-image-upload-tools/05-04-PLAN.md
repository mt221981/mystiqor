---
phase: 05-tools-tier-2-image-upload-tools
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/tools/graphology/page.tsx
  - mystiqor-build/src/app/api/tools/graphology/route.ts
  - mystiqor-build/src/components/features/graphology/GraphologyQuickStats.tsx
autonomous: true
requirements: [GRPH-01, GRPH-05]

must_haves:
  truths:
    - "User can upload a handwriting sample and receive graphology analysis"
    - "API returns 9 graphology components with scores and descriptions"
    - "Quick stats render as a radar chart of the 9 scored components"
    - "Component scores have tooltips showing descriptions"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/graphology/route.ts"
      provides: "POST endpoint for graphology analysis via LLM vision"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/graphology/page.tsx"
      provides: "Graphology page with upload + results + radar chart"
      min_lines: 100
    - path: "mystiqor-build/src/components/features/graphology/GraphologyQuickStats.tsx"
      provides: "Radar chart of 9 graphology components with tooltips"
      min_lines: 50
  key_links:
    - from: "mystiqor-build/src/app/(auth)/tools/graphology/page.tsx"
      to: "/api/tools/graphology"
      via: "fetch POST with imageUrl"
      pattern: "fetch.*api/tools/graphology"
    - from: "mystiqor-build/src/app/api/tools/graphology/route.ts"
      to: "invokeLLM"
      via: "imageUrls for GPT-4o vision"
      pattern: "invokeLLM.*imageUrls"
    - from: "mystiqor-build/src/app/api/tools/graphology/route.ts"
      to: "GraphologyResponseSchema"
      via: "Zod validation"
      pattern: "GraphologyResponseSchema"
---

<objective>
Build the graphology analysis tool — upload handwriting image, analyze via LLM vision, display results with radar chart of 9 graphology components plus quick stats with tooltips.

Purpose: GRPH-01 (upload + analysis) and GRPH-05 (quick stats + tooltips) form the core graphology pipeline. Same pattern as drawing analysis but with graphology-specific response schema and radar chart visualization.

Output: Working graphology page with API route, quick stats radar chart, and full analysis display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tools-tier-2-image-upload-tools/05-RESEARCH.md

<interfaces>
From src/services/analysis/response-schemas/graphology.ts:
```typescript
export const GraphologyResponseSchema = z.object({
  summary: z.string().min(1),
  components: z.array(z.object({
    name: z.string().min(1),        // e.g., "לחץ", "שיפוע", "גודל"
    score_1_to_10: z.number().int().min(1).max(10),
    description: z.string().min(1),  // tooltip content
  })).min(1).max(12),
  overall_assessment: z.string().min(1),
  personality_traits: z.array(z.string().min(1)).min(1),
  insights: z.array(InsightSchema).min(1),
})
export type GraphologyResponse = z.infer<typeof GraphologyResponseSchema>
```

From github-source/src/pages/Graphology.jsx (936 lines) — 8-step prompt structure:
- Steps: 1. General overview, 2. Pressure analysis, 3. Slant, 4. Size, 5. Spacing, 6. Speed, 7. Margins, 8. Signatures/special features, 9. Overall personality assessment
- The prompt requests 9 named components with Hebrew names and 1-10 scores

Pitfall 5: Use maxTokens: 8000 for graphology — the response is larger than other tools (9 components + insights + assessment).
Pitfall 4: Both responseSchema AND zodSchema must be passed.

From palmistry route pattern (src/app/api/tools/palmistry/route.ts):
- Auth, Zod validation, invokeLLM with imageUrls, save to analyses, return data

Recharts RadarChart is already in the project (used in personality Big Five radar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graphology API route + QuickStats radar component</name>
  <files>
    mystiqor-build/src/app/api/tools/graphology/route.ts,
    mystiqor-build/src/components/features/graphology/GraphologyQuickStats.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/palmistry/route.ts,
    mystiqor-build/src/services/analysis/response-schemas/graphology.ts,
    mystiqor-build/src/services/analysis/llm.ts,
    mystiqor-build/src/types/analysis.ts,
    github-source/src/pages/Graphology.jsx,
    mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx
  </read_first>
  <action>
    Create POST /api/tools/graphology/route.ts:
    1. Auth check: createClient() + getUser() — return 401 if not authenticated
    2. Zod input schema: { imageUrl: z.string().url('כתובת URL לא תקינה') }
    3. Build graphology system prompt in Hebrew — request analysis of 9 handwriting components:
       לחץ (pressure), שיפוע (slant), גודל (size), ריווח (spacing), מהירות (speed), שוליים (margins), צורת אותיות (letter form), קישוריות (connectivity), חתימה (signature)
       Each component: name (Hebrew), score_1_to_10, description (Hebrew)
       Also request: summary, overall_assessment, personality_traits[], insights[]
    4. Call invokeLLM: imageUrls: [imageUrl], systemPrompt with graphology instructions, prompt: 'נתח את דגימת כתב היד בתמונה', responseSchema object matching GraphologyResponseSchema, zodSchema: GraphologyResponseSchema, maxTokens: 8000 (Pitfall 5 — graphology needs more tokens than other tools)
    5. CRITICAL: Both responseSchema AND zodSchema must be present (Pitfall 4)
    6. Save to analyses: tool_type: 'graphology', input_data: { imageUrl }, results: validated response, summary from response
    7. Return { data: { ...validatedResult, analysis_id } }
    8. try/catch wrapper — return 500 with 'שגיאה בניתוח גרפולוגי' on failure

    Create GraphologyQuickStats.tsx:
    1. Props: { components: GraphologyResponse['components'] }
    2. Use Recharts RadarChart (already installed) — same pattern as BigFiveRadarChart from personality
    3. Dynamic import with ssr: false for RadarChart (Recharts is SSR-incompatible)
    4. Data mapping: components.map(c => ({ subject: c.name, score: c.score_1_to_10, fullMark: 10 }))
    5. RadarChart with PolarGrid, PolarAngleAxis (component names), PolarRadiusAxis (0-10), Radar fill
    6. Below chart: grid of component cards showing name + score badge + description as tooltip (use title attr or custom Tooltip)
    7. Hebrew labels, RTL-safe layout
    8. JSDoc in Hebrew
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "export async function POST" src/app/api/tools/graphology/route.ts returns match
    - grep -r "imageUrls" src/app/api/tools/graphology/route.ts returns match (GPT-4o vision)
    - grep -r "GraphologyResponseSchema" src/app/api/tools/graphology/route.ts returns match
    - grep -r "maxTokens.*8000" src/app/api/tools/graphology/route.ts returns match (Pitfall 5)
    - grep -r "tool_type.*graphology" src/app/api/tools/graphology/route.ts returns match
    - grep -r "export.*GraphologyQuickStats\|export default.*GraphologyQuickStats" src/components/features/graphology/GraphologyQuickStats.tsx returns match
    - grep -r "RadarChart" src/components/features/graphology/GraphologyQuickStats.tsx returns match
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Graphology API route accepts imageUrl, calls invokeLLM with 8000 maxTokens and vision mode, validates with GraphologyResponseSchema, saves to analyses with tool_type='graphology'. QuickStats renders a Recharts RadarChart of 9 components with tooltip descriptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Graphology page with upload, results, radar chart, and SubscriptionGuard</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/graphology/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/app/api/tools/graphology/route.ts,
    mystiqor-build/src/components/features/graphology/GraphologyQuickStats.tsx
  </read_first>
  <action>
    Create graphology/page.tsx following palmistry page pattern:
    1. 'use client' directive
    2. Import SubscriptionGuard, GraphologyQuickStats, ReactMarkdown
    3. Zod form schema: { imageUrl: z.string().url('כתובת URL לא תקינה') }
    4. useForm + zodResolver, useMutation for /api/tools/graphology POST
    5. File upload handler — FormData to /api/upload, sets imageUrl field (same as palmistry pattern)
    6. Form wrapped in SubscriptionGuard feature="analyses"
    7. Results section (shown after successful mutation):
       a. Summary card with overall_assessment text
       b. GraphologyQuickStats radar chart of 9 components
       c. Personality traits as Badge list
       d. Insights section — map insights, show category + text via ReactMarkdown
       e. Print button: <Button onClick={() => window.print()} variant="outline">ייצא PDF</Button>
          (This is a placeholder for GRPH-04 — just the button, CSS print styles added in Plan 05)
    8. PageHeader with breadcrumbs: [דף הבית (/), כלים (/tools), גרפולוגיה]
    9. RTL dir="rtl", Hebrew labels, toast messages in Hebrew
    10. Loading state with Loader2 spinner
    11. framer-motion fadeInUp on results
    12. Max 300 lines
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "SubscriptionGuard" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "GraphologyQuickStats" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "/api/tools/graphology" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "/api/upload" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - grep -r "window.print" src/app/\(auth\)/tools/graphology/page.tsx returns match (PDF export button)
    - grep -r "personality_traits\|personalityTraits" src/app/\(auth\)/tools/graphology/page.tsx returns match
    - wc -l src/app/\(auth\)/tools/graphology/page.tsx shows <= 300 lines
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Graphology page allows image upload, displays radar chart of 9 components, personality traits badges, insights text, and has a print/PDF button — all wrapped in SubscriptionGuard.
  </done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
grep -r "SubscriptionGuard" src/app/\(auth\)/tools/graphology/page.tsx
grep -r "imageUrls" src/app/api/tools/graphology/route.ts
grep -r "GraphologyResponseSchema" src/app/api/tools/graphology/route.ts
</verification>

<success_criteria>
- Graphology API route accepts image URL and returns GraphologyResponse with 9 components
- Graphology page has file upload, SubscriptionGuard, radar chart, traits, insights, and PDF button
- maxTokens: 8000 set in API route (Pitfall 5)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-tier-2-image-upload-tools/05-04-SUMMARY.md`
</output>
