---
phase: 08-growth-monetization
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - mystiqor-build/src/app/(auth)/subscription/page.tsx
  - mystiqor-build/src/app/(auth)/subscription/success/page.tsx
  - mystiqor-build/src/app/api/subscription/portal/route.ts
  - mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx
autonomous: true
requirements: [SUBS-04, SUBS-05, SUBS-06]

must_haves:
  truths:
    - "Success page shows confirmation after Stripe checkout with session_id in URL"
    - "Subscription management page shows current plan, usage bar, and cancel/manage button"
    - "Cancel button creates a Stripe billing portal session and redirects user"
    - "SubscriptionGuard blocks 4th analysis for free users with upgrade prompt linking to /pricing"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/subscription/success/page.tsx"
      provides: "Post-checkout success confirmation page"
      contains: "session_id"
    - path: "mystiqor-build/src/app/(auth)/subscription/page.tsx"
      provides: "Subscription management page"
      contains: "SubscriptionManagement"
    - path: "mystiqor-build/src/app/api/subscription/portal/route.ts"
      provides: "Stripe billing portal session creator"
      contains: "billingPortal"
    - path: "mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx"
      provides: "Plan display + usage + manage button"
      contains: "UsageBar"
  key_links:
    - from: "mystiqor-build/src/app/(auth)/subscription/page.tsx"
      to: "SubscriptionManagement component"
      via: "import"
      pattern: "SubscriptionManagement"
    - from: "mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx"
      to: "/api/subscription/portal"
      via: "fetch POST for portal URL"
      pattern: "api/subscription/portal"
---

<objective>
Build the subscription management page, success page, and Stripe billing portal route. Verify SubscriptionGuard enforcement is working.

Purpose: Users can view their current plan, manage billing via Stripe portal, and see post-checkout confirmation.
Output: /subscription page, /subscription/success page, /api/subscription/portal route, SubscriptionManagement component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-growth-monetization/08-RESEARCH.md

Key existing files:
- mystiqor-build/src/components/features/subscription/UsageBar.tsx (reuse for usage display)
- mystiqor-build/src/components/features/subscription/PlanCard.tsx (reuse for current plan display)
- mystiqor-build/src/hooks/useSubscription.ts (subscription state, canUseFeature)
- mystiqor-build/src/components/features/subscription/SubscriptionGuard.tsx (already links to /pricing — verify works)
- mystiqor-build/src/app/api/subscription/route.ts (existing GET route for subscription data)
- mystiqor-build/src/lib/supabase/server.ts (createClient for auth)

<interfaces>
From src/hooks/useSubscription.ts:
```typescript
interface UseSubscriptionReturn {
  subscription: SubscriptionRow;
  planInfo: PlanInfo;
  hasUsageLeft: boolean;
  canUseFeature: (featureName: string) => boolean;
  incrementUsage: () => Promise<{ new_count: number }>;
  isLoading: boolean;
}
```

From src/components/features/subscription/UsageBar.tsx (needs props inspection):
// UsageBar likely takes usage data props — read before implementing

From database.generated.ts (subscriptions Row):
```typescript
{
  stripe_customer_id: string | null;
  stripe_subscription_id: string | null;
  plan_type: string;
  status: string;
  analyses_limit: number;
  analyses_used: number;
  cancel_at_period_end: boolean | null;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create success page + Stripe portal route</name>
  <files>mystiqor-build/src/app/(auth)/subscription/success/page.tsx, mystiqor-build/src/app/api/subscription/portal/route.ts</files>
  <read_first>
    mystiqor-build/src/app/api/subscription/route.ts
    mystiqor-build/src/lib/supabase/server.ts
    mystiqor-build/src/components/features/subscription/PlanCard.tsx
  </read_first>
  <action>
1. Create `mystiqor-build/src/app/(auth)/subscription/success/page.tsx` as a 'use client' page:
   - Use `useSearchParams()` to read `session_id` param (wrap in Suspense boundary as required by Next.js App Router, same as auth/login pattern from Phase 2)
   - Show a success card with:
     - Green checkmark icon (CheckCircle from lucide-react)
     - Title: `<h1>התשלום הושלם בהצלחה!</h1>`
     - Subtitle: `<p>המנוי שלך שודרג. כל הכלים המיסטיים זמינים עבורך.</p>`
     - Button linking to /tools: `<Link href="/tools"><Button>התחל לנתח</Button></Link>`
     - Button linking to /subscription: `<Link href="/subscription"><Button variant="outline">צפה בפרטי המנוי</Button></Link>`
   - Hebrew only. RTL compatible.

2. Create `mystiqor-build/src/app/api/subscription/portal/route.ts`:
   - Import `Stripe` from `stripe`, `createClient` from `@/lib/supabase/server`
   - Auth check (401 if no user)
   - Query subscriptions table for user's `stripe_customer_id` via createClient
   - Guard: if no stripe_customer_id, return 400 `{ error: 'אין מנוי פעיל' }`
   - Create Stripe billing portal session:
     ```
     const portalSession = await stripe.billingPortal.sessions.create({
       customer: stripe_customer_id,
       return_url: `${process.env.NEXT_PUBLIC_SITE_URL}/subscription`,
     });
     ```
   - Return `{ url: portalSession.url }`
   - Try/catch with 500 error: `{ error: 'שגיאה בפתיחת ניהול חשבון' }`
   - JSDoc in Hebrew
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "session_id" mystiqor-build/src/app/\(auth\)/subscription/success/page.tsx
    - grep -q "התשלום הושלם" mystiqor-build/src/app/\(auth\)/subscription/success/page.tsx
    - grep -q "Suspense" mystiqor-build/src/app/\(auth\)/subscription/success/page.tsx
    - grep -q "billingPortal" mystiqor-build/src/app/api/subscription/portal/route.ts
    - grep -q "stripe_customer_id" mystiqor-build/src/app/api/subscription/portal/route.ts
    - grep -q "NEXT_PUBLIC_SITE_URL" mystiqor-build/src/app/api/subscription/portal/route.ts
  </acceptance_criteria>
  <done>Success page shows confirmation with session_id, portal route creates Stripe billing portal session</done>
</task>

<task type="auto">
  <name>Task 2: Create SubscriptionManagement component + subscription page</name>
  <files>mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx, mystiqor-build/src/app/(auth)/subscription/page.tsx</files>
  <read_first>
    mystiqor-build/src/components/features/subscription/UsageBar.tsx
    mystiqor-build/src/hooks/useSubscription.ts
    mystiqor-build/src/components/features/subscription/PlanCard.tsx
    mystiqor-build/src/lib/constants/plans.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx` as a 'use client' component:
   - Import `useSubscription` from `@/hooks/useSubscription`
   - Import `UsageBar` from `./UsageBar`
   - Import `PlanCard` from `./PlanCard`
   - Import `PLAN_INFO` from `@/lib/constants/plans`
   - Import `useState` for loading state
   - Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
   - Import `Button` from `@/components/ui/button`
   - Import `Badge` from `@/components/ui/badge`
   - Import `useRouter` from `next/navigation`

   The component shows:
   a. Current plan section: PlanCard with `isCurrentPlan={true}` for the user's current plan
   b. Usage section: A card showing `analyses_used / analyses_limit` with UsageBar (read UsageBar props first to match its interface)
   c. Status badge: active (green), cancelled (red), trial (yellow), past_due (orange)
   d. If `cancel_at_period_end` is true, show warning: `<p>המנוי יבוטל בסוף התקופה הנוכחית</p>`
   e. Action buttons:
      - If plan_type !== 'free': "ניהול חשבון" button that POSTs to `/api/subscription/portal` and redirects to portal URL
      - If plan_type === 'free': "שדרג עכשיו" button linking to `/pricing`
   f. Loading state while portal session is being created

2. Create `mystiqor-build/src/app/(auth)/subscription/page.tsx` as a 'use client' page:
   - Simple page wrapper with title `<h1>ניהול מנוי</h1>`
   - Render `<SubscriptionManagement />`
   - Hebrew only, RTL compatible
   - Under 300 lines (component does the heavy lifting)
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SubscriptionManagement" mystiqor-build/src/app/\(auth\)/subscription/page.tsx
    - grep -q "useSubscription" mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx
    - grep -q "UsageBar" mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx
    - grep -q "api/subscription/portal" mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx
    - grep -q "cancel_at_period_end" mystiqor-build/src/components/features/subscription/SubscriptionManagement.tsx
    - grep -q "ניהול מנוי" mystiqor-build/src/app/\(auth\)/subscription/page.tsx
  </acceptance_criteria>
  <done>Subscription page shows current plan, usage, status badge, manage/upgrade buttons. Portal redirect works.</done>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` exits 0
- Success page exists at `src/app/(auth)/subscription/success/page.tsx`
- Subscription management page exists at `src/app/(auth)/subscription/page.tsx`
- Portal route exists at `src/app/api/subscription/portal/route.ts`
- SubscriptionManagement component exists with UsageBar integration
</verification>

<success_criteria>
- Success page renders post-checkout confirmation with session_id
- Subscription management page shows current plan + usage + status
- Manage button creates Stripe portal session and redirects
- Free users see upgrade button linking to /pricing
- SubscriptionGuard (already exists) correctly blocks and links to /pricing page (SUBS-04)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-growth-monetization/08-04-SUMMARY.md`
</output>
