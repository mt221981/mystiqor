---
phase: 08-growth-monetization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/rate-limit.ts
  - mystiqor-build/src/app/api/subscription/usage/route.ts
autonomous: true
requirements: [INFRA-07]

must_haves:
  truths:
    - "Rate limiter returns false after 10 LLM requests in 60 seconds from the same user"
    - "Rate limiter gracefully skips (allows all) when UPSTASH env vars are not configured"
    - "Usage route returns 429 with Hebrew error when rate limited"
  artifacts:
    - path: "mystiqor-build/src/lib/rate-limit.ts"
      provides: "Upstash rate limiter factory with env guard"
      contains: "Ratelimit"
    - path: "mystiqor-build/src/app/api/subscription/usage/route.ts"
      provides: "Usage route with rate limiting applied"
      contains: "checkRateLimit"
  key_links:
    - from: "mystiqor-build/src/lib/rate-limit.ts"
      to: "@upstash/ratelimit"
      via: "import { Ratelimit }"
      pattern: "Ratelimit"
    - from: "mystiqor-build/src/app/api/subscription/usage/route.ts"
      to: "mystiqor-build/src/lib/rate-limit.ts"
      via: "import checkRateLimit"
      pattern: "checkRateLimit"
---

<objective>
Install Upstash packages and create the rate limiting library, then wire it into the usage endpoint as proof of integration.

Purpose: Protect LLM and upload endpoints from abuse with serverless-safe sliding window rate limiting.
Output: src/lib/rate-limit.ts with llmRateLimit + uploadRateLimit + checkRateLimit helper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-growth-monetization/08-RESEARCH.md

Key existing files:
- mystiqor-build/src/app/api/subscription/usage/route.ts (existing — add rate limiting)
- mystiqor-build/package.json (add @upstash/ratelimit, @upstash/redis)

<interfaces>
From src/app/api/subscription/usage/route.ts (existing POST handler):
```typescript
export async function POST() {
  // Auth check → supabase.rpc('increment_usage') → return { new_count, limit }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Upstash packages + create rate-limit library</name>
  <files>mystiqor-build/src/lib/rate-limit.ts</files>
  <read_first>
    mystiqor-build/package.json
  </read_first>
  <action>
1. Run `cd mystiqor-build && npm install @upstash/ratelimit@2.0.8 @upstash/redis@1.37.0 --legacy-peer-deps` (same flag used for astronomy-engine in Phase 6 due to openai peerOptional zod conflict).

2. Create `mystiqor-build/src/lib/rate-limit.ts`:
```typescript
/**
 * מגביל קצב בקשות — Upstash Redis sliding window
 * מגן על נקודות קצה רגישות (LLM, העלאת קבצים) מפני שימוש יתר
 * אם משתני סביבת Upstash לא מוגדרים — מדלג ומאפשר הכל (Pitfall 5)
 */
import { Ratelimit } from '@upstash/ratelimit';
import { Redis } from '@upstash/redis';

/** יוצר מגביל קצב עם הגנה נגד חוסר env vars */
function createLimiter(prefix: string, requests: number, window: string): Ratelimit | null {
  if (!process.env.UPSTASH_REDIS_REST_URL || !process.env.UPSTASH_REDIS_REST_TOKEN) {
    return null;
  }
  const redis = new Redis({
    url: process.env.UPSTASH_REDIS_REST_URL,
    token: process.env.UPSTASH_REDIS_REST_TOKEN,
  });
  return new Ratelimit({
    redis,
    limiter: Ratelimit.slidingWindow(requests, window),
    prefix: `mystiqor:${prefix}`,
  });
}

/** 10 בקשות LLM ל-60 שניות למשתמש */
export const llmRateLimit = createLimiter('llm', 10, '60 s');

/** 5 העלאות קבצים ל-60 שניות למשתמש */
export const uploadRateLimit = createLimiter('upload', 5, '60 s');

/**
 * בודק הגבלת קצב — מחזיר true אם מותר, false אם חסום
 * מחזיר true אם המגביל לא מוגדר (env vars חסרים)
 */
export async function checkRateLimit(
  limiter: Ratelimit | null,
  identifier: string
): Promise<boolean> {
  if (!limiter) return true;
  const { success } = await limiter.limit(identifier);
  return success;
}
```

JSDoc in Hebrew. All comments in Hebrew per CLAUDE.md.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Ratelimit" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "slidingWindow" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "UPSTASH_REDIS_REST_URL" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "checkRateLimit" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "llmRateLimit" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "uploadRateLimit" mystiqor-build/src/lib/rate-limit.ts
    - grep -q "@upstash/ratelimit" mystiqor-build/package.json
  </acceptance_criteria>
  <done>@upstash/ratelimit and @upstash/redis installed, rate-limit.ts exports llmRateLimit, uploadRateLimit, checkRateLimit with env guard</done>
</task>

<task type="auto">
  <name>Task 2: Wire rate limiting into usage route</name>
  <files>mystiqor-build/src/app/api/subscription/usage/route.ts</files>
  <read_first>
    mystiqor-build/src/app/api/subscription/usage/route.ts
    mystiqor-build/src/lib/rate-limit.ts
  </read_first>
  <action>
Modify `mystiqor-build/src/app/api/subscription/usage/route.ts` to add rate limiting as the FIRST check after auth:

1. Add import: `import { llmRateLimit, checkRateLimit } from '@/lib/rate-limit';`
2. After the `if (!user)` auth check and BEFORE the `supabase.rpc('increment_usage')` call, add:
```typescript
// הגבלת קצב — חסימה לפני שימוש ב-RPC
const allowed = await checkRateLimit(llmRateLimit, user.id);
if (!allowed) {
  return NextResponse.json(
    { error: 'יותר מדי בקשות — נסה שוב בעוד דקה' },
    { status: 429 }
  );
}
```
3. Do NOT change any other logic in the file. The existing RPC call, Zod validation, and error handling remain unchanged.
4. Keep the file under 300 lines.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "checkRateLimit" mystiqor-build/src/app/api/subscription/usage/route.ts
    - grep -q "llmRateLimit" mystiqor-build/src/app/api/subscription/usage/route.ts
    - grep -q "429" mystiqor-build/src/app/api/subscription/usage/route.ts
    - grep -q "יותר מדי בקשות" mystiqor-build/src/app/api/subscription/usage/route.ts
  </acceptance_criteria>
  <done>Usage route returns 429 with Hebrew message when rate limited, passes through when under limit</done>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` exits 0
- rate-limit.ts exists with exports for llmRateLimit, uploadRateLimit, checkRateLimit
- Usage route has rate limiting as first check after auth
- @upstash/ratelimit and @upstash/redis in package.json dependencies
</verification>

<success_criteria>
- Rate limiting library compiles and exports 3 items
- Usage endpoint returns 429 when rate limited
- Missing Upstash env vars cause graceful skip (allow all) not crash
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-growth-monetization/08-03-SUMMARY.md`
</output>
