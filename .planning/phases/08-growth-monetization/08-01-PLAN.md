---
phase: 08-growth-monetization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/pricing/page.tsx
  - mystiqor-build/src/app/api/subscription/checkout/route.ts
  - mystiqor-build/src/types/subscription.ts
autonomous: true
requirements: [SUBS-01, SUBS-02]

must_haves:
  truths:
    - "Pricing page renders 3 plan cards (Free, Basic 49/mo, Premium 99/mo) with feature lists"
    - "Clicking Subscribe on Basic/Premium creates a Stripe checkout session and returns a URL"
    - "PLAN_CONFIG in types/subscription.ts is deprecated in favor of PLAN_INFO in plans.ts"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/pricing/page.tsx"
      provides: "3-tier pricing page with PlanCard components"
      contains: "PlanCard"
    - path: "mystiqor-build/src/app/api/subscription/checkout/route.ts"
      provides: "Stripe checkout session creation endpoint"
      contains: "stripe.checkout.sessions.create"
    - path: "mystiqor-build/src/types/subscription.ts"
      provides: "Deprecation comment on PLAN_CONFIG pointing to plans.ts"
      contains: "@deprecated"
  key_links:
    - from: "mystiqor-build/src/app/(auth)/pricing/page.tsx"
      to: "/api/subscription/checkout"
      via: "fetch POST on plan select"
      pattern: "fetch.*api/subscription/checkout"
    - from: "mystiqor-build/src/app/api/subscription/checkout/route.ts"
      to: "stripe.checkout.sessions.create"
      via: "Stripe SDK"
      pattern: "stripe\\.checkout\\.sessions\\.create"
---

<objective>
Create the pricing page and Stripe checkout session API, resolving the PLAN_CONFIG/PLAN_INFO inconsistency.

Purpose: Users can see plan options and initiate a subscription purchase via Stripe Checkout.
Output: /pricing page with 3 PlanCards + POST /api/subscription/checkout route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-growth-monetization/08-RESEARCH.md

Key existing files:
- mystiqor-build/src/components/features/subscription/PlanCard.tsx (reuse — accepts planType, planInfo, onSelect, highlighted)
- mystiqor-build/src/lib/constants/plans.ts (PLAN_INFO — source of truth: free=3, basic=20, premium=-1)
- mystiqor-build/src/types/subscription.ts (PlanType, PlanInfo types + PLAN_CONFIG to deprecate)
- mystiqor-build/src/hooks/useSubscription.ts (useSubscription hook — already uses PLAN_INFO)
- mystiqor-build/src/components/features/subscription/SubscriptionGuard.tsx (links to /pricing)
- mystiqor-build/src/lib/supabase/server.ts (createClient for auth)

<interfaces>
From src/types/subscription.ts:
```typescript
export type PlanType = 'free' | 'basic' | 'premium' | 'enterprise';
export interface PlanInfo {
  name: string;
  analyses: number;
  guestProfiles: number;
  features: string[];
}
```

From src/components/features/subscription/PlanCard.tsx:
```typescript
export interface PlanCardProps {
  planType: PlanType;
  planInfo: PlanInfo;
  isCurrentPlan?: boolean;
  onSelect?: (plan: PlanType) => void;
  highlighted?: boolean;
}
```

From src/lib/constants/plans.ts:
```typescript
export const PLAN_INFO: Readonly<Record<PlanType, PlanInfo>>;
// free: { analyses: 3 }, basic: { analyses: 20 }, premium: { analyses: -1 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing page + deprecate PLAN_CONFIG</name>
  <files>mystiqor-build/src/app/(auth)/pricing/page.tsx, mystiqor-build/src/types/subscription.ts</files>
  <read_first>
    mystiqor-build/src/components/features/subscription/PlanCard.tsx
    mystiqor-build/src/lib/constants/plans.ts
    mystiqor-build/src/types/subscription.ts
    mystiqor-build/src/hooks/useSubscription.ts
  </read_first>
  <action>
1. In `mystiqor-build/src/types/subscription.ts`, add `/** @deprecated Use PLAN_INFO from @/lib/constants/plans instead — values here are stale (basic shows 15 but should be 20) */` above the PLAN_CONFIG export. Do NOT remove it (would break any remaining importers) — just mark deprecated.

2. Create `mystiqor-build/src/app/(auth)/pricing/page.tsx` as a 'use client' page:
   - Import `PLAN_INFO` from `@/lib/constants/plans`
   - Import `PlanCard` from `@/components/features/subscription/PlanCard`
   - Import `useSubscription` from `@/hooks/useSubscription`
   - Import `useRouter` from `next/navigation`
   - Import `useState` for loading state
   - Page title: `<h1>בחר את התוכנית שלך</h1>` with subtitle `<p>גישה לכל 13 הכלים המיסטיים</p>`
   - Render 3 PlanCards in a responsive grid (`grid grid-cols-1 md:grid-cols-3 gap-6`):
     - free: PlanCard with planType='free', isCurrentPlan from subscription.plan_type === 'free'
     - basic: PlanCard with planType='basic', highlighted={true}, isCurrentPlan from subscription
     - premium: PlanCard with planType='premium', isCurrentPlan from subscription
   - Do NOT render enterprise card (not a user-facing tier per SUBS-01)
   - `onSelect` handler: POST to `/api/subscription/checkout` with `{ planId: plan }`, then `window.location.href = data.url` to redirect to Stripe Checkout
   - If planId is 'free', do nothing (no checkout for free plan)
   - Show loading spinner on the selected card button while checkout session is being created
   - Error toast via `sonner` if checkout fails: `toast.error('שגיאה ביצירת תשלום')`
   - Hebrew throughout. RTL compatible (use start/end not left/right).
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PlanCard" mystiqor-build/src/app/\(auth\)/pricing/page.tsx
    - grep -q "PLAN_INFO" mystiqor-build/src/app/\(auth\)/pricing/page.tsx
    - grep -q "api/subscription/checkout" mystiqor-build/src/app/\(auth\)/pricing/page.tsx
    - grep -q "בחר את התוכנית" mystiqor-build/src/app/\(auth\)/pricing/page.tsx
    - grep -q "@deprecated" mystiqor-build/src/types/subscription.ts
  </acceptance_criteria>
  <done>Pricing page renders 3 PlanCards (free/basic/premium), onSelect POSTs to checkout API, PLAN_CONFIG marked deprecated</done>
</task>

<task type="auto">
  <name>Task 2: Create Stripe checkout session API route</name>
  <files>mystiqor-build/src/app/api/subscription/checkout/route.ts</files>
  <read_first>
    mystiqor-build/src/lib/supabase/server.ts
    mystiqor-build/src/app/api/subscription/route.ts
    mystiqor-build/src/lib/constants/plans.ts
  </read_first>
  <action>
Create `mystiqor-build/src/app/api/subscription/checkout/route.ts`:
- Import `NextRequest`, `NextResponse` from `next/server`
- Import `Stripe` from `stripe`
- Import `createClient` from `@/lib/supabase/server`
- Import `z` from `zod`
- Instantiate: `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!)`
- Define Zod schema: `const CheckoutSchema = z.object({ planId: z.enum(['basic', 'premium']) })`
- Define `PRICE_IDS` record: `{ basic: process.env.STRIPE_PRICE_BASIC!, premium: process.env.STRIPE_PRICE_PREMIUM! }`
- Export async POST handler:
  1. Auth check via `createClient()` then `supabase.auth.getUser()`. Return 401 `{ error: 'לא מחובר' }` if no user.
  2. Parse body with `CheckoutSchema.safeParse()`. Return 400 `{ error: 'תוכנית לא תקינה' }` on failure.
  3. Call `stripe.checkout.sessions.create()`:
     - `mode: 'subscription'`
     - `line_items: [{ price: PRICE_IDS[planId], quantity: 1 }]`
     - `metadata: { user_id: user.id, plan_id: planId }` (CRITICAL: used by webhook)
     - `success_url: '${process.env.NEXT_PUBLIC_SITE_URL}/subscription/success?session_id={CHECKOUT_SESSION_ID}'`
     - `cancel_url: '${process.env.NEXT_PUBLIC_SITE_URL}/pricing'`
     - `customer_email: user.email`
  4. Return `{ url: session.url }`
  5. Wrap entire handler in try/catch. On Stripe error, return 500 `{ error: 'שגיאה ביצירת תשלום' }`.
- JSDoc comment in Hebrew per CLAUDE.md standards.
- All env vars are STRIPE_SECRET_KEY, STRIPE_PRICE_BASIC, STRIPE_PRICE_PREMIUM, NEXT_PUBLIC_SITE_URL.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "stripe.checkout.sessions.create" mystiqor-build/src/app/api/subscription/checkout/route.ts
    - grep -q "user_id" mystiqor-build/src/app/api/subscription/checkout/route.ts
    - grep -q "metadata" mystiqor-build/src/app/api/subscription/checkout/route.ts
    - grep -q "STRIPE_PRICE_BASIC" mystiqor-build/src/app/api/subscription/checkout/route.ts
    - grep -q "CheckoutSchema" mystiqor-build/src/app/api/subscription/checkout/route.ts
  </acceptance_criteria>
  <done>POST /api/subscription/checkout validates planId via Zod, creates Stripe checkout session with user metadata, returns session URL</done>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` exits 0
- Pricing page file exists at `src/app/(auth)/pricing/page.tsx`
- Checkout route file exists at `src/app/api/subscription/checkout/route.ts`
- PLAN_CONFIG in types/subscription.ts has @deprecated JSDoc
</verification>

<success_criteria>
- Pricing page renders 3 plan cards using existing PlanCard component
- Clicking Subscribe calls POST /api/subscription/checkout with planId
- Checkout route creates Stripe session with user_id in metadata
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-growth-monetization/08-01-SUMMARY.md`
</output>
