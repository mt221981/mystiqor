---
phase: 08-growth-monetization
plan: 06
type: execute
wave: 3
depends_on: [08-03, 08-04, 08-05]
files_modified:
  - mystiqor-build/src/app/api/notifications/route.ts
  - mystiqor-build/src/app/(auth)/notifications/page.tsx
  - mystiqor-build/src/components/layouts/Sidebar.tsx
autonomous: false
requirements: [TRCK-06]

must_haves:
  truths:
    - "User can create, view, and delete reminders"
    - "Notifications page shows list of upcoming and past reminders"
    - "Sidebar navigation includes links to pricing, referrals, and notifications"
    - "TypeScript compiles with zero errors across all Phase 8 work"
  artifacts:
    - path: "mystiqor-build/src/app/api/notifications/route.ts"
      provides: "CRUD API for reminders table"
      contains: "reminders"
    - path: "mystiqor-build/src/app/(auth)/notifications/page.tsx"
      provides: "Notifications/reminders UI page"
      contains: "reminders"
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Updated sidebar with pricing, referrals, notifications links"
      contains: "notifications"
  key_links:
    - from: "mystiqor-build/src/app/(auth)/notifications/page.tsx"
      to: "/api/notifications"
      via: "fetch CRUD"
      pattern: "api/notifications"
    - from: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      to: "/pricing"
      via: "href in nav items"
      pattern: "pricing"
---

<objective>
Build the notifications/reminders CRUD system and update sidebar navigation to include all new Phase 8 pages. Run final integration verification.

Purpose: Users can manage personal reminders and navigate to all monetization pages from the sidebar.
Output: Notifications API + page, updated sidebar, TypeScript verification across all Phase 8 work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-growth-monetization/08-RESEARCH.md

@.planning/phases/08-growth-monetization/08-01-SUMMARY.md
@.planning/phases/08-growth-monetization/08-02-SUMMARY.md
@.planning/phases/08-growth-monetization/08-03-SUMMARY.md
@.planning/phases/08-growth-monetization/08-04-SUMMARY.md
@.planning/phases/08-growth-monetization/08-05-SUMMARY.md

Key existing files:
- mystiqor-build/src/components/layouts/Sidebar.tsx (update nav items)

<interfaces>
From database.generated.ts (reminders):
```typescript
// Insert
{
  message: string;
  scheduled_date: string;
  type: string;
  user_id: string;
  is_recurring?: boolean | null;
  recurrence_rule?: string | null;
  status?: string | null;
}
// Row
{
  id: string;
  message: string;
  scheduled_date: string;
  type: string;
  user_id: string;
  is_recurring: boolean | null;
  recurrence_rule: string | null;
  status: string | null;
  created_at: string | null;
}
```

From Sidebar.tsx nav structure:
```typescript
const NAV_SECTIONS = [
  { title: 'חשבון', items: [
    { label: 'פרופיל', href: '/profile', icon: User },
    { label: 'הגדרות', href: '/settings', icon: Settings },
    { label: 'מנוי', href: '/subscription', icon: CreditCard },
  ]}
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notifications API + page + update sidebar</name>
  <files>mystiqor-build/src/app/api/notifications/route.ts, mystiqor-build/src/app/(auth)/notifications/page.tsx, mystiqor-build/src/components/layouts/Sidebar.tsx</files>
  <read_first>
    mystiqor-build/src/components/layouts/Sidebar.tsx
    mystiqor-build/src/types/database.generated.ts (lines 764-798 for reminders)
    mystiqor-build/src/lib/supabase/server.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/app/api/notifications/route.ts`:
   - Import `createClient` from `@/lib/supabase/server`, `NextRequest`, `NextResponse`, `z` from `zod`
   - **GET**: Auth check, query `reminders` where `user_id = user.id`, order by `scheduled_date` desc, limit 50. Return `{ reminders }`.
   - **POST**: Auth check, validate body with Zod schema:
     ```
     const ReminderSchema = z.object({
       message: z.string().min(1).max(500),
       scheduled_date: z.string().datetime(),
       type: z.enum(['analysis', 'mood', 'journal', 'goal', 'custom']),
       is_recurring: z.boolean().optional().default(false),
       recurrence_rule: z.string().optional().nullable(),
     });
     ```
   - Insert into `reminders` with `user_id: user.id` and validated fields.
   - Return `{ reminder }` with 201 status.
   - **DELETE**: Auth check, read `id` from `request.nextUrl.searchParams.get('id')`. Delete from `reminders` where `id = id` and `user_id = user.id` (prevent deleting other users' reminders). Return `{ success: true }`.
   - All error messages in Hebrew. JSDoc in Hebrew.

2. Create `mystiqor-build/src/app/(auth)/notifications/page.tsx` as a 'use client' page:
   - Title: `<h1>תזכורות והתראות</h1>`
   - Fetch reminders from GET /api/notifications using React Query (queryKey: ['reminders'])
   - Show list of reminders in cards: each with message, scheduled date (formatted DD/MM/YYYY), type badge, delete button
   - Add reminder form at top: message input, date picker (HTML date input), type select dropdown, submit button
   - Use mutation for POST /api/notifications to add, DELETE to remove
   - Optimistic UI: remove card immediately on delete, add card immediately on create
   - Empty state: `<p>אין תזכורות עדיין — הוסף תזכורת חדשה</p>`
   - Hebrew only, RTL, under 300 lines
   - Import `useQueryClient`, `useQuery`, `useMutation` from `@tanstack/react-query`
   - Import `Card`, `CardContent`, `CardHeader` from `@/components/ui/card`
   - Import `Button`, `Input`, `Badge` from shadcn components
   - Import `toast` from `sonner` for error feedback
   - Import `Trash2`, `Bell`, `Plus` from `lucide-react`

3. Update `mystiqor-build/src/components/layouts/Sidebar.tsx`:
   - Add 3 new imports from `lucide-react`: `Gift`, `Tag`, `Bell` (check which icons are already imported)
   - In the `חשבון` section, add after the existing `מנוי` entry:
     - `{ label: 'תמחור', href: '/pricing', icon: Tag }`
     - `{ label: 'הפניות', href: '/referrals', icon: Gift }`
     - `{ label: 'תזכורות', href: '/notifications', icon: Bell }`
   - Keep file under 300 lines. Do NOT change any other sidebar logic.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "reminders" mystiqor-build/src/app/api/notifications/route.ts
    - grep -q "ReminderSchema" mystiqor-build/src/app/api/notifications/route.ts
    - grep -q "תזכורות" mystiqor-build/src/app/\(auth\)/notifications/page.tsx
    - grep -q "api/notifications" mystiqor-build/src/app/\(auth\)/notifications/page.tsx
    - grep -q "pricing" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "referrals" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "notifications" mystiqor-build/src/components/layouts/Sidebar.tsx
  </acceptance_criteria>
  <done>Notifications CRUD API with Zod validation, notifications page with add/delete, sidebar updated with 3 new links</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 8 integration verification</name>
  <what-built>
Complete Phase 8 monetization system:
1. Pricing page with 3 plan tiers (08-01)
2. Stripe checkout session creation (08-01)
3. Stripe webhook with idempotency (08-02)
4. Rate limiting on usage endpoint (08-03)
5. Subscription management page with Stripe portal (08-04)
6. Post-checkout success page (08-04)
7. Referral program with code generation and rewards (08-05)
8. Email wiring: welcome, usage-limit, payment-failed, referral-accepted (08-05)
9. Notifications/reminders CRUD (08-06)
10. Sidebar navigation updates (08-06)
  </what-built>
  <how-to-verify>
1. Run `cd mystiqor-build && npx tsc --noEmit` — must exit 0 with no errors
2. Visit /pricing — should show 3 plan cards (Free, Basic ₪49, Premium ₪99) with features
3. Visit /subscription — should show current plan (free), usage, upgrade button
4. Visit /referrals — should show referral code generation
5. Visit /notifications — should show reminders page with add form
6. Check sidebar — should have pricing, referrals, notifications links in account section
7. Verify SubscriptionGuard still works on any tool page (e.g., /astrology) — should show upgrade prompt after 3 free uses
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` exits 0 — zero TypeScript errors across ALL Phase 8 files
- All 10 requirement IDs have been addressed:
  - SUBS-01: /pricing page (08-01)
  - SUBS-02: /api/subscription/checkout (08-01)
  - SUBS-03: /api/webhooks/stripe with idempotency (08-02)
  - SUBS-04: SubscriptionGuard enforcement (08-04 — existing, verified)
  - SUBS-05: /subscription page + portal route (08-04)
  - SUBS-06: /subscription/success page (08-04)
  - TRCK-06: /notifications page + API (08-06)
  - GROW-01: /referrals page + API (08-05)
  - INFRA-07: rate-limit.ts + usage route wiring (08-03)
  - INFRA-08: email wiring in onboarding, usage, webhook, referral (08-05)
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- All new pages are navigable from sidebar
- Notifications CRUD works (create, list, delete)
- Human reviewer approves the full Phase 8 feature set
</success_criteria>

<output>
After completion, create `.planning/phases/08-growth-monetization/08-06-SUMMARY.md`
</output>
