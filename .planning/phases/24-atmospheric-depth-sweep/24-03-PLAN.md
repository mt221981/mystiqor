---
phase: 24-atmospheric-depth-sweep
plan: "03"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx
  - mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  - mystiqor-build/src/app/(auth)/tools/human-design/page.tsx
  - mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx
  - mystiqor-build/src/app/(auth)/tools/personality/page.tsx
  - mystiqor-build/src/app/(auth)/tools/timing/page.tsx
  - mystiqor-build/src/app/(auth)/tools/daily-insights/page.tsx
  - mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx
autonomous: true
requirements: [ATMOS-01, ATMOS-02, ATMOS-03, ATMOS-04, CONTRAST-02]

must_haves:
  truths:
    - "All 11 remaining tool pages use StandardSectionHeader instead of PageHeader"
    - "All 11 pages have pageEntry fade+drift animation with reduced-motion respect"
    - "All pages with form buttons show MysticLoadingText with tool-specific Hebrew phrases"
    - "AI interpretation prose blocks display result-heading-glow on h2/h3 elements"
    - "Every tool page in the app now has consistent atmospheric styling"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx"
      provides: "Atmospheric solar-return page"
      contains: "StandardSectionHeader"
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx"
      provides: "Atmospheric transits page"
      contains: "StandardSectionHeader"
    - path: "mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx"
      provides: "AI interpretation with result-heading-glow"
      contains: "result-heading-glow"
  key_links:
    - from: "all 11 tool pages"
      to: "StandardSectionHeader.tsx"
      via: "import { StandardSectionHeader }"
      pattern: "import.*StandardSectionHeader"
    - from: "AIInterpretation.tsx"
      to: "globals.css"
      via: "result-heading-glow class on prose wrapper"
      pattern: "result-heading-glow"
---

<objective>
Migrate the remaining 11 tool pages to StandardSectionHeader + pageEntry + MysticLoadingText, and wire result-heading-glow onto AI interpretation prose blocks.

Purpose: Complete the atmospheric sweep so every tool page is consistent, and enable glowing result headings in AI output (CONTRAST-02).
Output: 11 modified tool pages + AIInterpretation component with result-heading-glow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-atmospheric-depth-sweep/24-UI-SPEC.md
@.planning/phases/24-atmospheric-depth-sweep/24-01-SUMMARY.md

<interfaces>
<!-- Same interfaces as Plan 02 -- all from Plan 01 -->

From mystiqor-build/src/components/layouts/StandardSectionHeader.tsx:
```typescript
interface StandardSectionHeaderProps {
  readonly title: string;
  readonly description?: string;
  readonly icon: ReactNode;
  readonly breadcrumbs?: readonly BreadcrumbItem[];
}
export function StandardSectionHeader(props: StandardSectionHeaderProps): JSX.Element;
```

From mystiqor-build/src/components/ui/mystic-loading-text.tsx:
```typescript
interface MysticLoadingTextProps {
  readonly text: string;
  readonly className?: string;
}
export function MysticLoadingText(props: MysticLoadingTextProps): JSX.Element;
```

From mystiqor-build/src/lib/constants/mystic-loading-phrases.ts:
```typescript
export const MYSTIC_LOADING_PHRASES: Record<string, { button: string; skeleton: string }>;
export const DEFAULT_LOADING_PHRASE: { button: string; skeleton: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate solar-return, transits, synastry, calendar, forecast, relationships pages</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  </files>
  <read_first>
    .planning/phases/24-atmospheric-depth-sweep/24-UI-SPEC.md,
    mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx,
    mystiqor-build/src/components/layouts/StandardSectionHeader.tsx,
    mystiqor-build/src/components/ui/mystic-loading-text.tsx,
    mystiqor-build/src/lib/constants/mystic-loading-phrases.ts
  </read_first>
  <action>
Apply the same 5-change pattern to each of the 6 pages:

1. **Swap PageHeader import to StandardSectionHeader**
2. **Swap `<PageHeader` JSX to `<StandardSectionHeader`**
3. **Wrap top container in motion.div with pageEntry** (using `shouldReduceMotion` from `useReducedMotion`): `initial={shouldReduceMotion ? false : { opacity: 0, y: 12 }}` and `animate={shouldReduceMotion ? false : { opacity: 1, y: 0 }}` with `transition={{ duration: 0.6, ease: 'easeOut' }}`
4. **Replace loading text with MysticLoadingText**
5. **Add result-heading-glow to any prose wrappers** that render AI output via ReactMarkdown

**Per-page loading phrase keys:**
- solar-return: `MYSTIC_LOADING_PHRASES['solar-return'].button`
- transits: `MYSTIC_LOADING_PHRASES['transits'].button`
- synastry: `MYSTIC_LOADING_PHRASES['synastry'].button` (replaces `'מחשב סינסטרי...'`)
- calendar: Uses `isLoading` not `isPending`. Replace loading indicator text with `MYSTIC_LOADING_PHRASES['astrology'].button` (calendar is an astrology sub-page).
- forecast: `MYSTIC_LOADING_PHRASES['forecast'].button`
- relationships: `MYSTIC_LOADING_PHRASES['relationships'].button` (replaces `'מנתח יחסים...'`)

**Icon assignments (replace if not already react-icons/gi):**
- solar-return: `GiSunrise` from `react-icons/gi`
- transits: `GiOrbital` from `react-icons/gi`
- synastry: `GiHearts` from `react-icons/gi`
- calendar: check current icon; use `GiCalendar` from `react-icons/gi` if not already gi
- forecast: `GiMagnifyingGlass` from `react-icons/gi`
- relationships: `GiTwoCoins` from `react-icons/gi`

**Critical rules:**
- Preserve all inner animations, form logic, API calls, state unchanged.
- Preserve idle button labels unchanged.
- No `left`/`right`, no `font-semibold`, no `any`.
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -l "StandardSectionHeader" src/app/\(auth\)/tools/astrology/solar-return/page.tsx src/app/\(auth\)/tools/astrology/transits/page.tsx src/app/\(auth\)/tools/astrology/synastry/page.tsx src/app/\(auth\)/tools/astrology/calendar/page.tsx src/app/\(auth\)/tools/astrology/forecast/page.tsx src/app/\(auth\)/tools/relationships/page.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files contain `import { StandardSectionHeader }` (no `import { PageHeader }`)
    - All 6 files contain `<StandardSectionHeader` in JSX
    - All 6 files contain `useReducedMotion` import
    - solar-return/page.tsx contains `MYSTIC_LOADING_PHRASES['solar-return']`
    - transits/page.tsx contains `MYSTIC_LOADING_PHRASES['transits']`
    - synastry/page.tsx contains `MYSTIC_LOADING_PHRASES['synastry']`
    - forecast/page.tsx contains `MYSTIC_LOADING_PHRASES['forecast']`
    - relationships/page.tsx contains `MYSTIC_LOADING_PHRASES['relationships']`
    - No file contains `import { PageHeader }` (old import removed)
    - TypeScript compiles: `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>6 tool pages (solar-return, transits, synastry, calendar, forecast, relationships) use StandardSectionHeader with atmospheric styling and mystical loading text.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate human-design, palmistry, personality, timing, daily-insights + wire AIInterpretation result-heading-glow</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/human-design/page.tsx,
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/personality/page.tsx,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx,
    mystiqor-build/src/app/(auth)/tools/daily-insights/page.tsx,
    mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx
  </files>
  <read_first>
    .planning/phases/24-atmospheric-depth-sweep/24-UI-SPEC.md,
    mystiqor-build/src/app/(auth)/tools/human-design/page.tsx,
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/personality/page.tsx,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx,
    mystiqor-build/src/app/(auth)/tools/daily-insights/page.tsx,
    mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx,
    mystiqor-build/src/components/layouts/StandardSectionHeader.tsx,
    mystiqor-build/src/components/ui/mystic-loading-text.tsx,
    mystiqor-build/src/lib/constants/mystic-loading-phrases.ts
  </read_first>
  <action>
**Part A: Migrate 5 remaining tool pages (same 5-change pattern):**

1. Swap PageHeader import to StandardSectionHeader
2. Swap `<PageHeader` JSX to `<StandardSectionHeader`
3. Wrap top container in motion.div with pageEntry (using `shouldReduceMotion`)
4. Replace loading text with MysticLoadingText
5. Add result-heading-glow to any prose wrappers rendering AI output

**Per-page loading phrase keys:**
- human-design: `MYSTIC_LOADING_PHRASES['human-design'].button`
- palmistry: `DEFAULT_LOADING_PHRASE.button` (palmistry not in phrase map)
- personality: `DEFAULT_LOADING_PHRASE.button` (personality not in phrase map)
- timing: `DEFAULT_LOADING_PHRASE.button` (timing not in phrase map; replaces `'מחשב תזמון...'`)
- daily-insights: Display page -- check if it has a form/mutation. If no submit button, skip loading text. Still swap header + add pageEntry.

**Icon assignments (replace if not already react-icons/gi):**
- human-design: `GiDna1` from `react-icons/gi`
- palmistry: check current icon; use `GiHand` from `react-icons/gi` if not gi
- personality: check current icon; use `GiBrain` from `react-icons/gi` if not gi
- timing: check current icon; use `GiSundial` or `GiHourglass` from `react-icons/gi` if not gi
- daily-insights: check current icon; use `GiStarShining` from `react-icons/gi` if not gi

**Part B: Wire result-heading-glow on AIInterpretation.tsx:**

In `mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx`:
Find the `<div className="prose prose-invert prose-sm max-w-none font-body text-on-surface-variant leading-relaxed">` on line 64.
Add `result-heading-glow` to its className:
```tsx
<div className="result-heading-glow prose prose-invert prose-sm max-w-none font-body text-on-surface-variant leading-relaxed" dir="rtl">
```

Also add `result-heading-glow` to prose wrappers in the 5 tool pages being migrated:
- human-design/page.tsx: 1 prose wrapper
- palmistry/page.tsx: 1 prose wrapper
- personality/page.tsx: 1 prose wrapper
- timing/page.tsx: check for prose wrapper
- daily-insights/page.tsx: check for prose wrapper

**Critical rules:**
- Preserve all inner animations, form logic, API calls, state.
- Preserve idle button labels.
- No `left`/`right`, no `font-semibold`, no `any`.
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -l "StandardSectionHeader" src/app/\(auth\)/tools/human-design/page.tsx src/app/\(auth\)/tools/palmistry/page.tsx src/app/\(auth\)/tools/personality/page.tsx src/app/\(auth\)/tools/timing/page.tsx src/app/\(auth\)/tools/daily-insights/page.tsx | wc -l && grep -c "result-heading-glow" src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 page files contain `import { StandardSectionHeader }` (no `import { PageHeader }`)
    - All 5 page files contain `<StandardSectionHeader` in JSX
    - All 5 page files contain `useReducedMotion` import
    - human-design/page.tsx contains `MYSTIC_LOADING_PHRASES['human-design']` or `DEFAULT_LOADING_PHRASE`
    - palmistry/page.tsx contains `DEFAULT_LOADING_PHRASE`
    - personality/page.tsx contains `DEFAULT_LOADING_PHRASE`
    - AIInterpretation.tsx contains the string `result-heading-glow`
    - human-design/page.tsx prose wrapper contains `result-heading-glow`
    - palmistry/page.tsx prose wrapper contains `result-heading-glow`
    - personality/page.tsx prose wrapper contains `result-heading-glow`
    - No file contains `import { PageHeader }` (old import removed)
    - TypeScript compiles: `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Final 5 tool pages migrated to StandardSectionHeader. AIInterpretation.tsx and tool page prose wrappers display result-heading-glow. All 22 tool pages now have consistent atmospheric styling.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` -- zero TypeScript errors
2. `grep -rl "import { PageHeader }" mystiqor-build/src/app/\(auth\)/tools/` -- should return ZERO matches (all pages migrated)
3. `grep -rl "StandardSectionHeader" mystiqor-build/src/app/\(auth\)/tools/` -- should list all 22 tool page files
4. `grep -c "result-heading-glow" mystiqor-build/src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx` -- returns >= 1
5. `grep -rl "result-heading-glow" mystiqor-build/src/app/\(auth\)/tools/` -- returns files with prose wrappers
</verification>

<success_criteria>
- All 11 remaining tool pages use StandardSectionHeader (combined with Plan 02, all 22 are done)
- All pages have pageEntry animation with reduced-motion fallback
- Pages with forms show mystical Hebrew loading phrases
- AI interpretation output blocks display result-heading-glow drop-shadow on h2/h3
- Zero TypeScript errors
- No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/24-atmospheric-depth-sweep/24-03-SUMMARY.md`
</output>
