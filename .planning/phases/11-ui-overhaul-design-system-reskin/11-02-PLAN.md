---
phase: 11-ui-overhaul-design-system-reskin
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - mystiqor-build/src/components/layouts/Header.tsx
  - mystiqor-build/src/components/layouts/Sidebar.tsx
  - mystiqor-build/src/components/layouts/MobileNav.tsx
  - mystiqor-build/src/components/layouts/PageHeader.tsx
  - mystiqor-build/src/app/(auth)/layout-client.tsx
autonomous: true
requirements: [UI-12]
must_haves:
  truths:
    - "Header has glass-nav effect with nebula glow shadow and Material Symbols icons"
    - "Sidebar has glass-panel background with MD3 color tokens for navigation items"
    - "Auth layout has stars-bg background and floating glow orbs"
    - "PageHeader uses font-headline and MD3 text colors"
    - "MobileNav overlay uses surface-container colors instead of gray-800"
  artifacts:
    - path: "mystiqor-build/src/components/layouts/Header.tsx"
      provides: "Reskinned header with glass-nav"
      contains: "glass-nav"
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Reskinned sidebar with MD3 colors"
      contains: "surface-container"
    - path: "mystiqor-build/src/components/layouts/MobileNav.tsx"
      provides: "Reskinned mobile nav"
      contains: "surface-container"
    - path: "mystiqor-build/src/components/layouts/PageHeader.tsx"
      provides: "Reskinned page header"
      contains: "font-headline"
    - path: "mystiqor-build/src/app/(auth)/layout-client.tsx"
      provides: "Stars background + floating glows"
      contains: "stars-bg"
  key_links:
    - from: "mystiqor-build/src/components/layouts/Header.tsx"
      to: "mystiqor-build/tailwind.config.ts"
      via: "MD3 color classes"
      pattern: "bg-\\[#131315\\]/60|glass-nav"
    - from: "mystiqor-build/src/app/(auth)/layout-client.tsx"
      to: "mystiqor-build/src/app/globals.css"
      via: "stars-bg utility class"
      pattern: "stars-bg"
---

<objective>
Reskin the app shell — Header, Sidebar, MobileNav, PageHeader, and auth layout wrapper — to use the new MD3 design system. This affects the frame visible on every page.

Purpose: The shell wraps all content. Reskinning it transforms the entire app's look without touching individual pages.
Output: 5 updated layout components with MD3 tokens, glass effects, and cosmic theming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-01-SUMMARY.md

@mystiqor-build/src/components/layouts/Header.tsx
@mystiqor-build/src/components/layouts/Sidebar.tsx
@mystiqor-build/src/components/layouts/MobileNav.tsx
@mystiqor-build/src/components/layouts/PageHeader.tsx
@mystiqor-build/src/app/(auth)/layout-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin Header + Sidebar + MobileNav</name>
  <files>mystiqor-build/src/components/layouts/Header.tsx, mystiqor-build/src/components/layouts/Sidebar.tsx, mystiqor-build/src/components/layouts/MobileNav.tsx</files>
  <read_first>
    - mystiqor-build/src/components/layouts/Header.tsx
    - mystiqor-build/src/components/layouts/Sidebar.tsx
    - mystiqor-build/src/components/layouts/MobileNav.tsx
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md
  </read_first>
  <action>
**Header.tsx — Class replacements (preserve all logic, only change classes):**

Header `<header>` tag classes:
- OLD: `sticky top-0 z-40 flex h-16 items-center justify-between border-b border-white/5 bg-gray-950/80 px-4 backdrop-blur-md md:px-6`
- NEW: `fixed top-0 w-full z-50 flex h-16 items-center justify-between px-4 md:px-6 glass-nav shadow-[0_20px_50px_rgba(143,45,230,0.08)]`
  (Note: changed from `sticky` to `fixed` and from z-40 to z-50 per RESEARCH.md header pattern)

Mobile menu button:
- OLD: `text-gray-400 hover:bg-white/5 hover:text-white`
- NEW: `text-on-surface-variant hover:bg-surface-container hover:text-on-surface`

Mobile logo Sparkles:
- OLD: `text-purple-400`
- NEW: `text-primary`

Mobile logo text:
- OLD: `bg-gradient-to-l from-purple-400 to-indigo-400 bg-clip-text`
- NEW: `font-headline text-xl font-bold text-primary`
  (Remove the gradient clip — use direct primary color per RESEARCH.md brand pattern)

Theme toggle button:
- OLD: `text-gray-400 hover:bg-white/5 hover:text-white`
- NEW: `text-on-surface-variant hover:text-primary p-2 rounded-full active:scale-95`

User menu avatar button:
- OLD: `bg-purple-600/20 text-purple-300 hover:bg-purple-600/30 hover:text-purple-200`
- NEW: `bg-primary-container/20 text-primary hover:bg-primary-container/30 hover:text-primary-fixed`

User dropdown container:
- OLD: `border border-white/10 bg-gray-900`
- NEW: `border border-outline-variant/10 bg-surface-container-high`

Dropdown menu items:
- OLD: `text-gray-300 hover:bg-white/5 hover:text-white`
- NEW: `text-on-surface-variant hover:bg-surface-container-highest hover:text-on-surface`

Logout button:
- OLD: `text-red-400 hover:bg-red-500/10 hover:text-red-300`
- NEW: `text-error hover:bg-error-container/10 hover:text-error`

**Sidebar.tsx — Class replacements:**

`<aside>` wrapper:
- OLD: `bg-gradient-to-b from-gray-950 via-gray-900 to-gray-950 border-e border-white/5`
- NEW: `bg-surface/95 glass-panel border-e border-outline-variant/10`

Logo section — Sparkles icon:
- OLD: `text-purple-400`
- NEW: `text-primary`

Logo text:
- OLD: `bg-gradient-to-l from-purple-400 to-indigo-400 bg-clip-text text-xl font-bold text-transparent`
- NEW: `font-headline text-xl font-bold text-primary`

Logo border:
- OLD: `border-b border-white/5`
- NEW: `border-b border-outline-variant/10`

Section title buttons:
- OLD: `text-purple-300/70 hover:text-purple-200`
- NEW: `text-on-surface-variant/70 hover:text-on-surface-variant font-label`

Active nav item:
- OLD: `bg-purple-600/30 text-purple-100 shadow-sm shadow-purple-500/20`
- NEW: `bg-primary-container/20 text-primary shadow-sm shadow-primary-container/20`

Inactive nav item:
- OLD: `text-gray-300 hover:bg-white/5 hover:text-white`
- NEW: `text-on-surface-variant hover:bg-surface-container-high hover:text-on-surface`

UsageBar border:
- OLD: `border-t border-white/10`
- NEW: `border-t border-outline-variant/10`

UsageBar text:
- OLD: `text-gray-400` and `text-gray-500`
- NEW: `text-on-surface-variant` and `text-on-surface-variant/60`

UsageBar progress track:
- OLD: `bg-white/10`
- NEW: `bg-surface-container-high`

UsageBar progress fill:
- OLD: `bg-gradient-to-l from-purple-500 to-indigo-500`
- NEW: `bg-gradient-to-l from-primary-container to-secondary-container shadow-[0_0_15px_rgba(143,45,230,0.4)]`

**MobileNav.tsx — Class replacements:**

Close button:
- OLD: `bg-gray-800 text-gray-400 hover:bg-gray-700 hover:text-white`
- NEW: `bg-surface-container-high text-on-surface-variant hover:bg-surface-container-highest hover:text-on-surface`
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx contains "glass-nav" class
    - Header.tsx contains "fixed top-0 w-full z-50"
    - Header.tsx contains shadow-[0_20px_50px_rgba(143,45,230,0.08)]
    - Header.tsx does NOT contain "bg-gray-950" or "border-white/5"
    - Sidebar.tsx contains "glass-panel"
    - Sidebar.tsx contains "text-primary" for logo
    - Sidebar.tsx contains "bg-primary-container/20" for active nav
    - Sidebar.tsx does NOT contain "from-gray-950" or "text-purple-400"
    - MobileNav.tsx contains "bg-surface-container-high" for close button
    - MobileNav.tsx does NOT contain "bg-gray-800"
  </acceptance_criteria>
  <done>Header has glass-nav with nebula shadow, Sidebar uses MD3 surface/primary tokens, MobileNav uses surface-container colors, all old gray/purple hardcoded colors replaced</done>
</task>

<task type="auto">
  <name>Task 2: Reskin PageHeader + auth layout wrapper</name>
  <files>mystiqor-build/src/components/layouts/PageHeader.tsx, mystiqor-build/src/app/(auth)/layout-client.tsx</files>
  <read_first>
    - mystiqor-build/src/components/layouts/PageHeader.tsx
    - mystiqor-build/src/app/(auth)/layout-client.tsx
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md
  </read_first>
  <action>
**PageHeader.tsx — Class replacements:**

Icon container:
- OLD: `bg-purple-600/20 text-purple-400`
- NEW: `bg-primary-container/20 text-primary`

Title:
- OLD: `text-2xl font-bold tracking-tight text-white`
- NEW: `text-2xl font-headline font-bold tracking-tight text-on-surface`

Description:
- OLD: `text-sm text-gray-400`
- NEW: `text-sm font-body text-on-surface-variant`

**layout-client.tsx — Add stars background and floating glows:**

The outer `<div>` wrapper:
- OLD: `flex min-h-screen bg-background`
- NEW: `flex min-h-screen bg-surface stars-bg relative`

Add floating glow orbs as the first children inside the outer `<div>`, before the sidebar div:
```tsx
{/* Floating background glows */}
<div className="fixed top-1/4 -left-20 w-96 h-96 bg-primary-container/5 rounded-full blur-[100px] -z-10 pointer-events-none" aria-hidden="true" />
<div className="fixed bottom-1/4 -right-20 w-96 h-96 bg-secondary-container/5 rounded-full blur-[100px] -z-10 pointer-events-none" aria-hidden="true" />
```

Main content area — add padding-top to compensate for fixed header (was sticky):
- OLD: `<main className="flex-1 overflow-auto">`
- NEW: `<main className="flex-1 overflow-auto pt-16">`
  (pt-16 = 4rem = matches h-16 header height. Since header is now `fixed` instead of `sticky`, content needs top padding.)

Content container:
- OLD: `mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8`
- NEW: `mx-auto max-w-7xl px-6 py-8 space-y-8`
  (Matches RESEARCH.md layout pattern: `pt-24 pb-32 px-6` — but we keep py-8 since pt-16 is on main)
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PageHeader.tsx contains "font-headline" for title
    - PageHeader.tsx contains "text-on-surface" for title color
    - PageHeader.tsx contains "bg-primary-container/20 text-primary" for icon
    - PageHeader.tsx does NOT contain "text-purple" or "text-white"
    - layout-client.tsx contains "stars-bg" class on wrapper
    - layout-client.tsx contains "bg-primary-container/5" floating glow div
    - layout-client.tsx contains "bg-secondary-container/5" floating glow div
    - layout-client.tsx contains "pt-16" on main element
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>PageHeader uses font-headline + MD3 colors, auth layout has stars-bg background with floating glow orbs and correct padding for fixed header</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. `grep -c "gray-950\|gray-900\|gray-800\|gray-700\|gray-400\|gray-300\|gray-500\|purple-400\|purple-600\|purple-500\|purple-300\|purple-200\|purple-100\|indigo-400\|indigo-500" mystiqor-build/src/components/layouts/Header.tsx` — returns 0
3. `grep "glass-nav" mystiqor-build/src/components/layouts/Header.tsx` — found
4. `grep "stars-bg" mystiqor-build/src/app/\(auth\)/layout-client.tsx` — found
5. `grep "font-headline" mystiqor-build/src/components/layouts/PageHeader.tsx` — found
</verification>

<success_criteria>
- Header renders with glass-nav effect and nebula glow shadow
- Sidebar renders with glass-panel background and MD3 navigation colors
- MobileNav uses surface-container colors
- PageHeader uses font-headline typography
- Auth layout has stars-bg pattern and floating glow orbs
- No hardcoded gray-* or purple-* colors remain in any shell component
- Content area properly accounts for fixed (not sticky) header
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-overhaul-design-system-reskin/11-02-SUMMARY.md`
</output>
