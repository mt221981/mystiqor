---
phase: 11-ui-overhaul-design-system-reskin
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - mystiqor-build/src/app/(auth)/dashboard/page.tsx
  - mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
  - mystiqor-build/src/components/features/dashboard/StatCards.tsx
  - mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
  - mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
  - mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
  - mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
  - mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx
  - mystiqor-build/src/components/features/onboarding/steps.tsx
  - mystiqor-build/src/app/(auth)/onboarding/page.tsx
autonomous: true
requirements: [UI-13]
must_haves:
  truths:
    - "Dashboard page uses bento grid layout (grid-cols-2 md:grid-cols-4)"
    - "DailyInsightCard has nebula gradient background"
    - "StatCards use the StatCard primitive with MD3 tokens"
    - "All dashboard charts have surface-container backgrounds"
    - "Onboarding wizard uses glass-panel styling with MD3 colors"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      provides: "Reskinned dashboard with bento grid"
      contains: "grid-cols-2 md:grid-cols-4"
    - path: "mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx"
      provides: "Nebula gradient daily insight"
      contains: "nebula-glow"
    - path: "mystiqor-build/src/components/features/dashboard/StatCards.tsx"
      provides: "MD3 stat cards"
      contains: "surface-container"
  key_links:
    - from: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      to: "mystiqor-build/src/components/ui/stat-card.tsx"
      via: "StatCard import"
      pattern: "import.*StatCard"
---

<objective>
Reskin the dashboard page to bento grid layout with nebula gradient DailyInsightCard, MD3-themed stat cards, chart containers, and period selector. Also reskin the onboarding wizard and steps.

Purpose: The dashboard is the first page users see after login — it sets the visual tone for the entire app.
Output: Reskinned dashboard + onboarding pages with MD3 design system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-01-SUMMARY.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-02-SUMMARY.md

@mystiqor-build/src/app/(auth)/dashboard/page.tsx
@mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
@mystiqor-build/src/components/features/dashboard/StatCards.tsx
@mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
@mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
@mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
@mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin dashboard page + DailyInsightCard + StatCards</name>
  <files>mystiqor-build/src/app/(auth)/dashboard/page.tsx, mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx, mystiqor-build/src/components/features/dashboard/StatCards.tsx, mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx</files>
  <read_first>
    - mystiqor-build/src/app/(auth)/dashboard/page.tsx
    - mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
    - mystiqor-build/src/components/features/dashboard/StatCards.tsx
    - mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md (Stat Cards, Layout sections)
  </read_first>
  <action>
Read each file, identify all Tailwind classes, and apply these replacements. Preserve ALL component logic, state, hooks, and data fetching.

**Dashboard page.tsx — Layout changes:**

The dashboard page layout should use the bento grid pattern from RESEARCH.md:

For the greeting section at the top:
- Title: replace any `text-white` or `text-foreground` with `text-on-surface font-headline`
- Subtitle/greeting: replace `text-gray-400` or `text-muted-foreground` with `text-on-surface-variant font-body`

For the stats grid (where StatCards are rendered):
- Ensure the grid wrapper uses: `grid grid-cols-2 md:grid-cols-4 gap-4`

For chart sections, each chart wrapper card:
- Replace `bg-card` or `bg-gray-*` with `bg-surface-container rounded-xl p-6 border border-outline-variant/5`

For the asymmetric layout (if present with larger/smaller columns):
- Use: `grid grid-cols-1 lg:grid-cols-3 gap-6` with `lg:col-span-2` on the wider section

**DailyInsightCard.tsx — Apply nebula gradient:**
- Card wrapper: replace existing bg class with `nebula-glow rounded-xl p-8 relative overflow-hidden`
- Add a subtle glow overlay: `<div className="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent pointer-events-none" aria-hidden="true" />`
- Text elements: use `text-white` for headings (on gradient background), `text-white/80` for body text
- Any icon containers: use `text-white/90`
- Any inner card sections: use `bg-white/10 rounded-lg p-4 backdrop-blur-sm`

**StatCards.tsx — Replace with MD3 tokens:**
- Each stat card container: `bg-surface-container rounded-xl p-4 h-32 relative overflow-hidden`
- Value text: `text-3xl font-headline font-black text-on-surface`
- Label text: `font-label text-xs text-on-surface-variant`
- Any badge/trend indicators: `bg-tertiary/10 text-tertiary px-2 py-0.5 rounded-full font-label text-[10px]`
- Add ambient glow: `<div className="absolute -bottom-4 -left-4 w-12 h-12 bg-tertiary/5 rounded-full blur-xl" aria-hidden="true" />`
- Replace any `text-purple-*`, `bg-purple-*` with corresponding MD3 tokens (primary, primary-container)

Alternatively, if StatCards is a wrapper that renders individual cards, refactor to import and use the `StatCard` primitive from `@/components/ui/stat-card`.

**PeriodSelector.tsx — Replace colors:**
- Active button: replace `bg-purple-*` or `bg-primary` styles with `bg-primary-container/20 text-primary font-label`
- Inactive button: `text-on-surface-variant hover:text-on-surface hover:bg-surface-container font-label`
- Container: `bg-surface-container rounded-lg p-1`
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx contains "grid grid-cols-2 md:grid-cols-4"
    - dashboard/page.tsx contains "font-headline" for greeting
    - dashboard/page.tsx does NOT contain "text-white" as a heading color (use text-on-surface instead)
    - DailyInsightCard.tsx contains "nebula-glow"
    - StatCards.tsx contains "surface-container" or imports StatCard from @/components/ui
    - PeriodSelector.tsx contains "primary-container" for active state
    - No gray-400, gray-300, purple-400, purple-600 remain in these files
  </acceptance_criteria>
  <done>Dashboard uses bento grid layout, DailyInsightCard has nebula gradient, StatCards use MD3 tokens, PeriodSelector themed with primary-container</done>
</task>

<task type="auto">
  <name>Task 2: Reskin dashboard charts + onboarding</name>
  <files>mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx, mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx, mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx, mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx, mystiqor-build/src/components/features/onboarding/steps.tsx, mystiqor-build/src/app/(auth)/onboarding/page.tsx</files>
  <read_first>
    - mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
    - mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
    - mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
    - mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx
    - mystiqor-build/src/components/features/onboarding/steps.tsx
    - mystiqor-build/src/app/(auth)/onboarding/page.tsx
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md
  </read_first>
  <action>
**Chart components (BiorhythmChart, MoodTrendChart, GoalsProgressChart):**

For each chart component, apply these class replacements:

Card/container wrapper:
- Replace `bg-card`, `bg-gray-*`, or shadcn Card with: `bg-surface-container rounded-xl p-6 border border-outline-variant/5`

Chart title:
- Replace `text-white`, `text-foreground` with: `text-on-surface font-headline text-lg font-semibold`

Chart subtitle/description:
- Replace `text-gray-400`, `text-muted-foreground` with: `text-on-surface-variant font-body text-sm`

Recharts color values (in stroke, fill props):
- Primary line/area: change any `#7c3aed` or `purple` references to `#ddb8ff` (primary)
- Secondary line/area: change to `#c3c0ff` (secondary)
- Tertiary line/area: change to `#4edea3` (tertiary)
- Grid lines: `#4a4455` (outline-variant)
- Axis text: `#ccc3d8` (on-surface-variant)
- Tooltip background: `#201f22` (surface-container)

For custom Tooltip components (if used — Phase 3 established this pattern):
- Container: `bg-surface-container border border-outline-variant/20 rounded-lg p-3 shadow-lg`
- Label text: `text-on-surface font-label text-xs`
- Value text: `text-primary font-headline font-bold`

**OnboardingWizard.tsx — Glass panel styling:**

Main wizard container:
- Replace existing bg/border with: `bg-surface-container/60 backdrop-blur-xl rounded-2xl p-8 border border-outline-variant/10`

Step indicator dots:
- Active: `bg-primary w-3 h-3 rounded-full`
- Inactive: `bg-surface-container-high w-2 h-2 rounded-full`

Progress bar (if present):
- Track: `h-1.5 w-full bg-surface-container rounded-full overflow-hidden`
- Fill: `h-full bg-gradient-to-l from-primary-container to-secondary-container rounded-full`

Navigation buttons:
- Next/Submit: `bg-gradient-to-br from-primary-container to-secondary-container text-white font-headline font-bold py-3 px-6 rounded-xl`
- Back: `border border-outline-variant/20 hover:bg-surface-container rounded-lg py-2 px-4 text-on-surface-variant`

**steps.tsx — Step content styling:**

Step titles: `text-on-surface font-headline font-semibold text-xl`
Step descriptions: `text-on-surface-variant font-body text-sm`
Input fields: replace any shadcn Input styling with `bg-surface-container-lowest border-none rounded-lg p-4 text-on-surface placeholder:text-outline/40 focus:ring-1 focus:ring-primary/40`
Labels: `text-on-surface-variant font-label text-sm font-medium`

**onboarding/page.tsx — Page background:**

Page wrapper: add `stars-bg` class if not inherited from layout. Replace any `bg-gray-*` or `bg-background` with `bg-surface`.
Title: `text-on-surface font-headline`
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - BiorhythmChart.tsx contains "surface-container" for card wrapper
    - MoodTrendChart.tsx contains "#ddb8ff" or "surface-container" (MD3 chart colors)
    - GoalsProgressChart.tsx contains "surface-container"
    - OnboardingWizard.tsx contains "backdrop-blur" and "surface-container"
    - OnboardingWizard.tsx contains "from-primary-container to-secondary-container" for button
    - steps.tsx contains "font-headline" or "font-label"
    - No purple-400, purple-600, gray-400, gray-800 remain in chart/onboarding files
  </acceptance_criteria>
  <done>Dashboard charts use MD3 chart colors and surface-container backgrounds, onboarding wizard has glass-panel effect with nebula gradient buttons</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. `grep -c "nebula-glow" mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx` — returns 1+
3. `grep "grid-cols-2 md:grid-cols-4\|grid grid-cols" mystiqor-build/src/app/\(auth\)/dashboard/page.tsx` — found
4. `grep "backdrop-blur" mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx` — found
</verification>

<success_criteria>
- Dashboard page uses bento grid layout matching RESEARCH.md
- DailyInsightCard renders nebula gradient
- Stat cards use font-headline, font-label, surface-container tokens
- Charts use MD3 color values (#ddb8ff, #c3c0ff, #4edea3)
- Onboarding wizard has glassmorphism styling
- No old color system remnants in any touched file
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-overhaul-design-system-reskin/11-03-SUMMARY.md`
</output>
