---
phase: 11-ui-overhaul-design-system-reskin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/tailwind.config.ts
  - mystiqor-build/src/app/globals.css
  - mystiqor-build/src/app/layout.tsx
  - mystiqor-build/src/components/ui/glass-card.tsx
  - mystiqor-build/src/components/ui/stat-card.tsx
  - mystiqor-build/src/components/ui/nebula-button.tsx
  - mystiqor-build/src/components/ui/glass-input.tsx
autonomous: true
requirements: [UI-10, UI-11]
must_haves:
  truths:
    - "Tailwind config produces MD3 color utilities (bg-primary, bg-surface-container, text-on-surface, etc.)"
    - "Three font families available via Tailwind (font-headline, font-body, font-label)"
    - "Custom CSS classes nebula-glow, glass-nav, glass-panel, stars-bg, glow-soft, celestial-glow exist in globals.css"
    - "GlassCard, StatCard, NebulaButton, GlassInput primitives are importable from @/components/ui/"
    - "Body renders with #131315 background and Material Symbols Outlined icon font is loaded"
  artifacts:
    - path: "mystiqor-build/tailwind.config.ts"
      provides: "MD3 color tokens + font families"
      contains: "surface-container"
    - path: "mystiqor-build/src/app/globals.css"
      provides: "Custom CSS classes + scrollbar + shadcn CSS variable overrides"
      contains: "nebula-glow"
    - path: "mystiqor-build/src/app/layout.tsx"
      provides: "Plus Jakarta Sans, Inter, Manrope, Material Symbols font loading"
      contains: "Plus_Jakarta_Sans"
    - path: "mystiqor-build/src/components/ui/glass-card.tsx"
      provides: "Reusable glass card primitive"
      exports: ["GlassCard"]
    - path: "mystiqor-build/src/components/ui/stat-card.tsx"
      provides: "Bento stat card primitive"
      exports: ["StatCard"]
    - path: "mystiqor-build/src/components/ui/nebula-button.tsx"
      provides: "Gradient button primitive"
      exports: ["NebulaButton"]
    - path: "mystiqor-build/src/components/ui/glass-input.tsx"
      provides: "Dark glass input primitive"
      exports: ["GlassInput"]
  key_links:
    - from: "mystiqor-build/tailwind.config.ts"
      to: "mystiqor-build/src/app/globals.css"
      via: "CSS variables map to Tailwind color tokens"
      pattern: "surface-container"
    - from: "mystiqor-build/src/app/layout.tsx"
      to: "Google Fonts CDN"
      via: "next/font/google imports"
      pattern: "Plus_Jakarta_Sans"
---

<objective>
Replace the entire design system foundation: Tailwind color palette with MD3 tokens from RESEARCH.md, update Google Fonts to Plus Jakarta Sans + Inter + Manrope + Material Symbols, add custom CSS utility classes (nebula-glow, glass-nav, etc.), override shadcn CSS variables to map to the new dark cosmic palette, and create four shared UI primitives (GlassCard, StatCard, NebulaButton, GlassInput).

Purpose: This is the foundation plan — every subsequent plan depends on these tokens, fonts, and primitives existing.
Output: Updated tailwind.config.ts, globals.css, layout.tsx, and 4 new primitive components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md

@mystiqor-build/tailwind.config.ts
@mystiqor-build/src/app/globals.css
@mystiqor-build/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tailwind config + globals.css + fonts</name>
  <files>mystiqor-build/tailwind.config.ts, mystiqor-build/src/app/globals.css, mystiqor-build/src/app/layout.tsx</files>
  <read_first>
    - mystiqor-build/tailwind.config.ts (current shadcn config to preserve structure)
    - mystiqor-build/src/app/globals.css (current CSS variables)
    - mystiqor-build/src/app/layout.tsx (current font setup)
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md (authoritative design tokens)
  </read_first>
  <action>
**tailwind.config.ts — FULL REWRITE of colors + fontFamily:**

Keep `darkMode: 'class'`, keep `content` array, keep `keyframes` and `animation`, keep `plugins: [require('tailwindcss-animate')]`.

Replace the entire `colors` object inside `theme.extend` with BOTH the shadcn CSS-variable colors (background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, input, ring, chart, sidebar) AND the new MD3 flat colors side-by-side. This way existing shadcn components keep working via CSS variables while new code can use MD3 tokens directly.

Add the MD3 colors as flat tokens (NOT nested in theme.extend.colors but alongside the existing ones):

```
"primary": "#ddb8ff",
"primary-container": "#8f2de6",
"primary-fixed": "#f0dbff",
"primary-fixed-dim": "#ddb8ff",
"on-primary": "#490080",
"on-primary-container": "#f2dfff",
"on-primary-fixed": "#2c0051",
"on-primary-fixed-variant": "#6800b4",
"inverse-primary": "#861fdd",
"secondary": "#c3c0ff",
"secondary-container": "#3626ce",
"secondary-fixed": "#e2dfff",
"secondary-fixed-dim": "#c3c0ff",
"on-secondary": "#1d00a5",
"on-secondary-container": "#b3b1ff",
"on-secondary-fixed": "#0f0069",
"on-secondary-fixed-variant": "#3323cc",
"tertiary": "#4edea3",
"tertiary-container": "#007650",
"tertiary-fixed": "#6ffbbe",
"tertiary-fixed-dim": "#4edea3",
"on-tertiary": "#003824",
"on-tertiary-container": "#76ffc2",
"on-tertiary-fixed": "#002113",
"on-tertiary-fixed-variant": "#005236",
"surface": "#131315",
"surface-dim": "#131315",
"surface-bright": "#39393b",
"surface-container-lowest": "#0e0e10",
"surface-container-low": "#1c1b1d",
"surface-container": "#201f22",
"surface-container-high": "#2a2a2c",
"surface-container-highest": "#353437",
"surface-tint": "#ddb8ff",
"surface-variant": "#353437",
"on-surface": "#e5e1e4",
"on-surface-variant": "#ccc3d8",
"on-background": "#e5e1e4",
"error": "#ffb4ab",
"error-container": "#93000a",
"on-error": "#690005",
"on-error-container": "#ffdad6",
"outline": "#958da1",
"outline-variant": "#4a4455",
"inverse-surface": "#e5e1e4",
"inverse-on-surface": "#313032",
```

IMPORTANT: The existing shadcn keys (`background`, `foreground`, `primary`, `secondary`, etc.) use `hsl(var(...))`. These MUST remain because shadcn components depend on them. The MD3 tokens use different key names (`surface-container`, `on-surface`, `primary-container`, etc.) so there is NO conflict. The shadcn `primary` key stays as `hsl(var(--primary))` and a new `"primary-container": "#8f2de6"` is added alongside it. If a key name would conflict (like `background`), keep the shadcn CSS-variable version and let the CSS variable value be updated in globals.css to point to the MD3 value.

Add `fontFamily` to theme.extend:
```
fontFamily: {
  headline: ["var(--font-headline)", "Plus Jakarta Sans", "sans-serif"],
  body: ["var(--font-body)", "Inter", "sans-serif"],
  label: ["var(--font-label)", "Manrope", "sans-serif"],
}
```

Add `borderRadius` override:
```
borderRadius: {
  DEFAULT: "0.25rem",
  lg: "0.5rem",
  xl: "0.75rem",
  "2xl": "1rem",
  full: "9999px",
}
```

**globals.css — UPDATE CSS variables + ADD custom classes:**

Replace the `:root` and `.dark` CSS variable blocks. Since the app is always dark-themed (className="dark" on html), set the `.dark` block values to map to MD3 tokens:

```css
.dark {
  --background: 240 5% 7.5%;        /* #131315 */
  --foreground: 270 3% 89%;         /* #e5e1e4 */
  --card: 270 3% 13%;               /* #201f22 */
  --card-foreground: 270 3% 89%;    /* #e5e1e4 */
  --popover: 270 3% 13%;            /* #201f22 */
  --popover-foreground: 270 3% 89%; /* #e5e1e4 */
  --primary: 274 100% 86%;          /* #ddb8ff */
  --primary-foreground: 274 100% 25%; /* #490080 */
  --secondary: 243 100% 88%;        /* #c3c0ff */
  --secondary-foreground: 243 100% 32%; /* #1d00a5 */
  --muted: 270 3% 16%;              /* #2a2a2c */
  --muted-foreground: 271 21% 81%;  /* #ccc3d8 */
  --accent: 270 3% 16%;             /* #2a2a2c */
  --accent-foreground: 270 3% 89%;  /* #e5e1e4 */
  --destructive: 7 100% 84%;        /* #ffb4ab */
  --destructive-foreground: 0 100% 21%; /* #690005 */
  --border: 270 11% 30%;            /* #4a4455 */
  --input: 270 11% 30%;             /* #4a4455 */
  --ring: 274 100% 86%;             /* #ddb8ff */
  --radius: 0.75rem;
  --chart-1: 274 100% 86%;          /* #ddb8ff */
  --chart-2: 265 100% 53%;          /* #8f2de6 */
  --chart-3: 155 69% 59%;           /* #4edea3 */
  --chart-4: 243 100% 48%;          /* #3626ce */
  --chart-5: 7 100% 84%;            /* #ffb4ab */
  --sidebar-background: 240 5% 7.5%;
  --sidebar-foreground: 270 3% 89%;
  --sidebar-primary: 274 100% 86%;
  --sidebar-primary-foreground: 274 100% 25%;
  --sidebar-accent: 270 3% 16%;
  --sidebar-accent-foreground: 270 3% 89%;
  --sidebar-border: 270 11% 30%;
  --sidebar-ring: 274 100% 86%;
}
```

Keep `:root` light values as they were (but the app always uses dark via className="dark").

After the existing `body` rule, ADD these custom utility classes inside `@layer utilities`:

```css
@layer utilities {
  .nebula-glow {
    background: linear-gradient(135deg, #8f2de6 0%, #3626ce 100%);
  }
  .glass-nav {
    background: rgba(19, 19, 21, 0.6);
    backdrop-filter: blur(12px);
  }
  .glass-panel {
    background: rgba(32, 31, 34, 0.6);
    backdrop-filter: blur(12px);
  }
  .stars-bg {
    background-image: radial-gradient(circle at 2px 2px, rgba(221, 184, 255, 0.15) 1px, transparent 0);
    background-size: 40px 40px;
  }
  .glow-soft {
    box-shadow: 0 20px 50px rgba(143, 45, 230, 0.08);
  }
  .celestial-glow {
    box-shadow: 0 0 40px rgba(143, 45, 230, 0.15);
  }
}
```

Add scrollbar styling after the utilities layer:

```css
::-webkit-scrollbar { width: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #4a4455; border-radius: 10px; }
```

Keep the existing `@media print` block unchanged.

**layout.tsx — REPLACE fonts:**

Remove `Assistant` and `Geist` imports from `next/font/google`.

Add these imports:
```typescript
import { Plus_Jakarta_Sans, Inter, Manrope } from 'next/font/google';
```

Configure each font:
```typescript
const plusJakartaSans = Plus_Jakarta_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700', '800'],
  variable: '--font-headline',
  display: 'swap',
});

const inter = Inter({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600'],
  variable: '--font-body',
  display: 'swap',
});

const manrope = Manrope({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-label',
  display: 'swap',
});
```

Update the `<html>` tag className to use all three font variables:
```tsx
<html lang="he" dir="rtl" className={cn("dark", plusJakartaSans.variable, inter.variable, manrope.variable)} suppressHydrationWarning>
```

Update `<body>` className:
```tsx
<body className="font-body antialiased min-h-screen bg-surface text-on-surface">
```

Add Material Symbols Outlined via a `<link>` tag inside `<head>` (or use `next/head`). Since App Router uses metadata, add it as a direct link in layout:
```tsx
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
```
Place this inside the `<head>` by adding it before `<body>` or using Next.js metadata links array.

Update `viewport.themeColor` from `'#1a1025'` to `'#131315'`.

Remove the old `assistant.variable` and `geist.variable` references entirely. The font-[family-name:...] class on body should be removed — `font-body` from Tailwind config handles it.
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.ts contains "surface-container": "#201f22"
    - tailwind.config.ts contains "on-surface": "#e5e1e4"
    - tailwind.config.ts contains "primary-container": "#8f2de6"
    - tailwind.config.ts contains fontFamily with headline, body, label keys
    - globals.css contains .nebula-glow class
    - globals.css contains .glass-nav class
    - globals.css contains .stars-bg class
    - globals.css contains .glow-soft class
    - globals.css contains .celestial-glow class
    - globals.css .dark block contains --background: 240 5% 7.5%
    - layout.tsx imports Plus_Jakarta_Sans from next/font/google
    - layout.tsx imports Inter from next/font/google
    - layout.tsx imports Manrope from next/font/google
    - layout.tsx does NOT import Assistant from next/font/google
    - layout.tsx contains Material+Symbols+Outlined link
    - layout.tsx themeColor is #131315
  </acceptance_criteria>
  <done>Tailwind config has full MD3 palette + 3 font families, globals.css has 6 custom utility classes + updated shadcn variables, layout.tsx loads Plus Jakarta Sans + Inter + Manrope + Material Symbols, tsc --noEmit passes</done>
</task>

<task type="auto">
  <name>Task 2: Create shared UI primitives (GlassCard, StatCard, NebulaButton, GlassInput)</name>
  <files>mystiqor-build/src/components/ui/glass-card.tsx, mystiqor-build/src/components/ui/stat-card.tsx, mystiqor-build/src/components/ui/nebula-button.tsx, mystiqor-build/src/components/ui/glass-input.tsx</files>
  <read_first>
    - .planning/phases/11-ui-overhaul-design-system-reskin/11-RESEARCH.md (component patterns section)
    - mystiqor-build/src/components/ui/card.tsx (existing shadcn card for reference)
    - mystiqor-build/src/components/ui/button.tsx (existing shadcn button for reference)
    - mystiqor-build/src/components/ui/input.tsx (existing shadcn input for reference)
  </read_first>
  <action>
Create 4 new primitive components in `src/components/ui/`. Each must have typed Props interface, JSDoc in Hebrew, and use the MD3 Tailwind tokens from Task 1.

**glass-card.tsx — GlassCard:**
```tsx
'use client';

import { forwardRef } from 'react';
import { cn } from '@/lib/utils/cn';
import type { HTMLAttributes, ReactNode } from 'react';

interface GlassCardProps extends HTMLAttributes<HTMLDivElement> {
  readonly variant?: 'default' | 'glass' | 'highlighted';
  readonly children: ReactNode;
}

const GlassCard = forwardRef<HTMLDivElement, GlassCardProps>(
  ({ variant = 'default', className, children, ...props }, ref) => {
    const variants = {
      default: 'bg-surface-container rounded-xl p-6 border border-outline-variant/5',
      glass: 'bg-surface-container/60 backdrop-blur-xl rounded-xl p-6',
      highlighted: 'bg-gradient-to-br from-primary-container to-secondary-container rounded-xl p-8',
    };
    return (
      <div ref={ref} className={cn(variants[variant], className)} {...props}>
        {children}
      </div>
    );
  }
);
GlassCard.displayName = 'GlassCard';
export { GlassCard };
```

**stat-card.tsx — StatCard:**
```tsx
'use client';

import { cn } from '@/lib/utils/cn';
import type { ReactNode } from 'react';

interface StatCardProps {
  readonly value: string | number;
  readonly label: string;
  readonly badge?: string;
  readonly icon?: ReactNode;
  readonly className?: string;
}

export function StatCard({ value, label, badge, icon, className }: StatCardProps) {
  return (
    <div className={cn('bg-surface-container rounded-xl p-4 h-32 relative overflow-hidden', className)}>
      <div className="flex items-start justify-between">
        <div>
          {badge && (
            <span className="bg-tertiary/10 text-tertiary px-2 py-0.5 rounded-full font-label text-[10px] font-semibold">
              {badge}
            </span>
          )}
          <p className="text-3xl font-headline font-black text-on-surface mt-2">{value}</p>
          <p className="font-label text-xs text-on-surface-variant mt-1">{label}</p>
        </div>
        {icon && <div className="text-on-surface-variant">{icon}</div>}
      </div>
      <div className="absolute -bottom-4 -left-4 w-12 h-12 bg-tertiary/5 rounded-full blur-xl" aria-hidden="true" />
    </div>
  );
}
```

**nebula-button.tsx — NebulaButton:**
```tsx
'use client';

import { forwardRef } from 'react';
import { cn } from '@/lib/utils/cn';
import type { ButtonHTMLAttributes } from 'react';

interface NebulaButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  readonly variant?: 'primary' | 'secondary' | 'icon';
}

const NebulaButton = forwardRef<HTMLButtonElement, NebulaButtonProps>(
  ({ variant = 'primary', className, children, ...props }, ref) => {
    const variants = {
      primary: 'bg-gradient-to-br from-primary-container to-secondary-container text-white font-headline font-bold py-4 px-6 rounded-xl shadow-[0_10px_30px_rgba(143,45,230,0.3)] active:scale-95 transition-transform',
      secondary: 'border border-outline-variant/20 hover:bg-surface-container rounded-lg py-2 px-4 text-on-surface-variant hover:text-on-surface transition-colors',
      icon: 'text-on-surface-variant hover:text-primary p-2 rounded-full active:scale-95 transition-all',
    };
    return (
      <button ref={ref} className={cn(variants[variant], className)} {...props}>
        {children}
      </button>
    );
  }
);
NebulaButton.displayName = 'NebulaButton';
export { NebulaButton };
```

**glass-input.tsx — GlassInput:**
```tsx
'use client';

import { forwardRef } from 'react';
import { cn } from '@/lib/utils/cn';
import type { InputHTMLAttributes } from 'react';

const GlassInput = forwardRef<HTMLInputElement, InputHTMLAttributes<HTMLInputElement>>(
  ({ className, ...props }, ref) => {
    return (
      <input
        ref={ref}
        className={cn(
          'w-full bg-surface-container-lowest border-none rounded-lg p-4',
          'text-on-surface placeholder:text-outline/40',
          'focus:outline-none focus:ring-1 focus:ring-primary/40',
          'font-body text-sm',
          className
        )}
        {...props}
      />
    );
  }
);
GlassInput.displayName = 'GlassInput';
export { GlassInput };
```

All four components must be exactly as shown — the Tailwind classes reference the MD3 tokens from Task 1.
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - glass-card.tsx exports GlassCard with variant prop (default | glass | highlighted)
    - glass-card.tsx uses bg-surface-container, bg-surface-container/60, from-primary-container
    - stat-card.tsx exports StatCard with value, label, badge, icon props
    - stat-card.tsx uses font-headline, font-label, bg-tertiary/10, text-tertiary
    - nebula-button.tsx exports NebulaButton with variant prop (primary | secondary | icon)
    - nebula-button.tsx uses from-primary-container to-secondary-container gradient
    - glass-input.tsx exports GlassInput with forwardRef
    - glass-input.tsx uses bg-surface-container-lowest, text-on-surface, focus:ring-primary/40
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>4 shared primitives (GlassCard, StatCard, NebulaButton, GlassInput) created with correct MD3 tokens, all importable from @/components/ui/, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. `grep -c "surface-container" mystiqor-build/tailwind.config.ts` — returns 6+
3. `grep -c "nebula-glow\|glass-nav\|stars-bg\|glow-soft\|celestial-glow" mystiqor-build/src/app/globals.css` — returns 5+
4. `grep "Plus_Jakarta_Sans" mystiqor-build/src/app/layout.tsx` — found
5. `ls mystiqor-build/src/components/ui/glass-card.tsx mystiqor-build/src/components/ui/stat-card.tsx mystiqor-build/src/components/ui/nebula-button.tsx mystiqor-build/src/components/ui/glass-input.tsx` — all exist
</verification>

<success_criteria>
- Tailwind config produces all MD3 color utilities alongside existing shadcn CSS-variable colors
- Three new font families (headline, body, label) available via Tailwind classes
- 6 custom CSS utility classes exist in globals.css
- shadcn CSS variables in .dark block map to MD3 hex values
- 4 shared primitives exist and compile without errors
- No existing shadcn components broken (CSS variable keys preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-overhaul-design-system-reskin/11-01-SUMMARY.md`
</output>
