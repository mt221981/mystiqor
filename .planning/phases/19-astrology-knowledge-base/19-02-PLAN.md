---
phase: 19-astrology-knowledge-base
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - mystiqor-build/src/app/(auth)/learn/astrology/dictionary/page.tsx
  - mystiqor-build/src/components/layouts/Sidebar.tsx
autonomous: false
requirements:
  - ASTRO-01
  - ASTRO-02
  - ASTRO-03
  - ASTRO-04

must_haves:
  truths:
    - "User navigating to /learn/astrology/dictionary sees a tabbed page with 4 sections"
    - "Clicking each tab (מזלות, כוכבים, בתים, אספקטים) shows the corresponding dictionary content"
    - "User can find 'מילון אסטרולוגי' in the sidebar under 'למידה' section"
    - "Clicking sidebar link navigates to the dictionary page"
    - "Existing /learn/astrology AI tutor page is untouched and still works"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/learn/astrology/dictionary/page.tsx"
      provides: "Dictionary page with 4 tabbed sections"
      contains: "TabsList"
      min_lines: 40
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Navigation entry for dictionary"
      contains: "מילון אסטרולוגי"
  key_links:
    - from: "dictionary/page.tsx"
      to: "ZodiacGrid.tsx"
      via: "import { ZodiacGrid }"
      pattern: "from '@/components/features/astrology/ZodiacGrid'"
    - from: "dictionary/page.tsx"
      to: "PlanetGrid.tsx"
      via: "import { PlanetGrid }"
      pattern: "from '@/components/features/astrology/PlanetGrid'"
    - from: "dictionary/page.tsx"
      to: "HouseList.tsx"
      via: "import { HouseList }"
      pattern: "from '@/components/features/astrology/HouseList'"
    - from: "dictionary/page.tsx"
      to: "AspectDictionary.tsx"
      via: "import { AspectDictionary }"
      pattern: "from '@/components/features/astrology/AspectDictionary'"
    - from: "Sidebar.tsx"
      to: "dictionary/page.tsx"
      via: "href: '/learn/astrology/dictionary'"
      pattern: "learn/astrology/dictionary"
---

<objective>
Assemble the dictionary page with tabbed navigation and add sidebar entry.

Purpose: Wire the 4 display components (from Plan 01) into a single tabbed page at `/learn/astrology/dictionary` and add a sidebar navigation link so users can discover and browse astrology reference content.
Output: Working dictionary page accessible from sidebar navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-astrology-knowledge-base/19-RESEARCH.md
@.planning/phases/19-astrology-knowledge-base/19-UI-SPEC.md
@.planning/phases/19-astrology-knowledge-base/19-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 — import these directly -->
From mystiqor-build/src/components/features/astrology/ZodiacGrid.tsx:
```typescript
export function ZodiacGrid(): JSX.Element;
```

From mystiqor-build/src/components/features/astrology/PlanetGrid.tsx:
```typescript
export function PlanetGrid(): JSX.Element;
```

From mystiqor-build/src/components/features/astrology/HouseList.tsx:
```typescript
export function HouseList(): JSX.Element;
```

From mystiqor-build/src/components/features/astrology/AspectDictionary.tsx:
```typescript
export function AspectDictionary(): JSX.Element;
```

From mystiqor-build/src/components/ui/tabs.tsx (base-ui, NOT Radix):
```typescript
// Uses @base-ui/react/tabs — confirmed API from existing usage in tools/astrology/page.tsx:
// <Tabs defaultValue="interpretation" dir="rtl">
//   <TabsList className="w-full grid grid-cols-3 bg-surface-container-high">
//     <TabsTrigger value="interpretation">label</TabsTrigger>
//   </TabsList>
//   <TabsContent value="interpretation" className="mt-4">content</TabsContent>
// </Tabs>
export { Tabs, TabsList, TabsTrigger, TabsContent }
```

From mystiqor-build/src/components/layouts/PageHeader.tsx:
```typescript
interface PageHeaderProps {
  readonly title: string;
  readonly description?: string;
  readonly icon?: ReactNode;
  readonly breadcrumbs?: readonly BreadcrumbItem[];
}
export function PageHeader(props: PageHeaderProps): JSX.Element;
```

From mystiqor-build/src/components/layouts/Sidebar.tsx:
```typescript
// NAV_SECTIONS array — 'למידה' section is at index 5
// GiAstrolabe is already imported from 'react-icons/gi'
// Each item: { label: string, href: string, icon: ComponentType }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dictionary page with tabbed sections</name>
  <files>mystiqor-build/src/app/(auth)/learn/astrology/dictionary/page.tsx</files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/astrology/page.tsx
    mystiqor-build/src/components/layouts/PageHeader.tsx
    mystiqor-build/src/components/ui/tabs.tsx
    mystiqor-build/src/app/(auth)/learn/astrology/page.tsx
    mystiqor-build/src/components/features/astrology/ZodiacGrid.tsx
    mystiqor-build/src/components/features/astrology/PlanetGrid.tsx
    mystiqor-build/src/components/features/astrology/HouseList.tsx
    mystiqor-build/src/components/features/astrology/AspectDictionary.tsx
    .planning/phases/19-astrology-knowledge-base/19-UI-SPEC.md
  </read_first>
  <action>
    Create directory `src/app/(auth)/learn/astrology/dictionary/` and file `page.tsx` inside it.

    The page is a `'use client'` component that:

    1. **Imports:**
       - `PageHeader` from `@/components/layouts/PageHeader`
       - `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs`
       - `GiAstrolabe` from `react-icons/gi` (for PageHeader icon)
       - `ZodiacGrid` from `@/components/features/astrology/ZodiacGrid`
       - `PlanetGrid` from `@/components/features/astrology/PlanetGrid`
       - `HouseList` from `@/components/features/astrology/HouseList`
       - `AspectDictionary` from `@/components/features/astrology/AspectDictionary`

    2. **PageHeader** with:
       - `title="מילון אסטרולוגי"`
       - `description="מדריך עיון מלא — מזלות, כוכבים, בתים ואספקטים"`
       - `icon={<GiAstrolabe className="h-5 w-5" />}`
       - `breadcrumbs={[{ label: 'למידה', href: '/learn/tutorials' }, { label: 'מילון אסטרולוגי' }]}`

    3. **Tabs** section (follow the exact pattern from `tools/astrology/page.tsx`):
       ```tsx
       <Tabs defaultValue="signs" dir="rtl">
         <TabsList className="w-full grid grid-cols-4 bg-surface-container-high">
           <TabsTrigger value="signs">מזלות</TabsTrigger>
           <TabsTrigger value="planets">כוכבים</TabsTrigger>
           <TabsTrigger value="houses">בתים</TabsTrigger>
           <TabsTrigger value="aspects">אספקטים</TabsTrigger>
         </TabsList>

         <TabsContent value="signs" className="mt-4">
           <ZodiacGrid />
         </TabsContent>

         <TabsContent value="planets" className="mt-4">
           <PlanetGrid />
         </TabsContent>

         <TabsContent value="houses" className="mt-4">
           <HouseList />
         </TabsContent>

         <TabsContent value="aspects" className="mt-4">
           <AspectDictionary />
         </TabsContent>
       </Tabs>
       ```

    4. **Wrapper:** Outer `<div className="space-y-6">` containing PageHeader + Tabs

    5. **JSDoc** in Hebrew: `/** עמוד מילון אסטרולוגי — מזלות, כוכבים, בתים ואספקטים בטאבים */`

    6. **Export** as default (Next.js page convention): `export default function AstrologyDictionaryPage()`

    CRITICAL: Do NOT modify the existing `/learn/astrology/page.tsx` (AI tutor) — it is a separate working page.

    File should be approximately 50-70 lines. Well under 300 limit.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(auth)/learn/astrology/dictionary/page.tsx`
    - Contains `'use client'` directive
    - Contains `export default function AstrologyDictionaryPage`
    - Contains `<Tabs defaultValue="signs" dir="rtl">`
    - Contains `<TabsTrigger value="signs">מזלות</TabsTrigger>`
    - Contains `<TabsTrigger value="planets">כוכבים</TabsTrigger>`
    - Contains `<TabsTrigger value="houses">בתים</TabsTrigger>`
    - Contains `<TabsTrigger value="aspects">אספקטים</TabsTrigger>`
    - Contains `<ZodiacGrid />` inside TabsContent value="signs"
    - Contains `<PlanetGrid />` inside TabsContent value="planets"
    - Contains `<HouseList />` inside TabsContent value="houses"
    - Contains `<AspectDictionary />` inside TabsContent value="aspects"
    - Contains `title="מילון אסטרולוגי"`
    - TypeScript compiles with zero errors
    - Existing `/learn/astrology/page.tsx` is NOT modified
  </acceptance_criteria>
  <done>Dictionary page at /learn/astrology/dictionary renders 4 tabbed sections with all astrology reference content.</done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar navigation entry for dictionary</name>
  <files>mystiqor-build/src/components/layouts/Sidebar.tsx</files>
  <read_first>
    mystiqor-build/src/components/layouts/Sidebar.tsx
  </read_first>
  <action>
    Modify `Sidebar.tsx` to add a single new nav item in the `למידה` section (index 5 of `NAV_SECTIONS`).

    Add this entry AFTER the existing `מורה אסטרולוגיה` item (which links to `/learn/astrology`):
    ```typescript
    { label: 'מילון אסטרולוגי', href: '/learn/astrology/dictionary', icon: GiAstrolabe },
    ```

    `GiAstrolabe` is already imported at the top of the file — no new import needed.

    The `למידה` section items array should become:
    ```typescript
    items: [
      { label: 'מדריכים', href: '/learn/tutorials', icon: GiGraduateCap },
      { label: 'בלוג', href: '/learn/blog', icon: GiNewspaper },
      { label: 'מורה אסטרולוגיה', href: '/learn/astrology', icon: GiAstrolabe },
      { label: 'מילון אסטרולוגי', href: '/learn/astrology/dictionary', icon: GiAstrolabe },
      { label: 'מורה ציור', href: '/learn/drawing', icon: GiPaintBrush },
    ],
    ```

    IMPORTANT: Only add ONE line. Do not modify any other section or item.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains `{ label: 'מילון אסטרולוגי', href: '/learn/astrology/dictionary', icon: GiAstrolabe }`
    - The entry is in the 'למידה' section
    - No other sections or items are modified
    - GiAstrolabe import is reused (no duplicate import)
    - TypeScript compiles with zero errors
    - File line count increased by exactly 1 line compared to original
  </acceptance_criteria>
  <done>Sidebar shows "מילון אסטרולוגי" link in the למידה section, navigating to /learn/astrology/dictionary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dictionary page</name>
  <files>mystiqor-build/src/app/(auth)/learn/astrology/dictionary/page.tsx</files>
  <action>
    Human verifies the complete astrology dictionary page visually. The following was built:
    - Dictionary page at /learn/astrology/dictionary with 4 tabbed sections (מזלות, כוכבים, בתים, אספקטים)
    - 12 zodiac sign cards with emoji, element, ruler, color, description
    - 10 planet cards with symbol, color, meaning, description
    - 12 house accordion items with expandable descriptions
    - 7 aspect cards with colored badges and strength bars
    - Sidebar navigation link under למידה

    Steps to verify:
    1. Start dev server: `cd mystiqor-build && npm run dev`
    2. Navigate to http://localhost:3000/learn/astrology/dictionary
    3. Verify PageHeader shows "מילון אסטרולוגי" with description and icon
    4. Verify 4 tabs visible: מזלות, כוכבים, בתים, אספקטים
    5. Click "מזלות" tab — verify 12 zodiac cards with emoji, Hebrew name, element, ruler badge, description (RTL)
    6. Click "כוכבים" tab — verify 10 planet cards with colored symbol, Hebrew name, meaning, description
    7. Click "בתים" tab — verify 12 accordion items; click one to expand and see description
    8. Click "אספקטים" tab — verify 7 aspect cards with colored badge, strength bar (filled proportionally), percentage, description
    9. Check sidebar — find "מילון אסטרולוגי" under למידה section, click it, confirm navigation
    10. Navigate to /learn/astrology — confirm AI tutor page still works (not broken)
    11. Verify RTL: all text is right-aligned, tabs flow right-to-left
    12. Verify hover: cards show mystic-hover effect on mouse over
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>User approved the visual appearance and all 4 dictionary sections display correctly with proper RTL, colors, and interactivity.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero TypeScript errors
2. `cd mystiqor-build && npx vitest run` — full test suite green
3. Dictionary page renders at /learn/astrology/dictionary with all 4 tabs
4. Sidebar shows "מילון אסטרולוגי" link
5. Existing /learn/astrology AI tutor page is unmodified and working
</verification>

<success_criteria>
- Dictionary page accessible at /learn/astrology/dictionary with 4 working tabs
- Sidebar contains link to dictionary page
- All astrology data (12 signs, 10 planets, 12 houses, 7 aspects) visible and browsable
- Existing astrology tutor page at /learn/astrology untouched
- User approved visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/19-astrology-knowledge-base/19-02-SUMMARY.md`
</output>
