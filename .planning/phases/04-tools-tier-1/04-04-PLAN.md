---
phase: 04-tools-tier-1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/api/tools/astrology/forecast/route.ts
  - mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx
  - mystiqor-build/src/components/features/astrology/DailyForecast/index.tsx
  - mystiqor-build/src/app/api/tools/astrology/calendar/route.ts
  - mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx
  - mystiqor-build/src/components/features/astrology/AstroCalendar/index.tsx
autonomous: true
requirements:
  - ASTR-06
  - ASTR-07
must_haves:
  truths:
    - "User sees a daily astrology forecast personalized to their zodiac sign with Hebrew content"
    - "Astro calendar shows a monthly grid with at least 3 astrological events per month"
    - "Calendar events include full moon, new moon, mercury retrograde, and zodiac sign changes"
    - "Both pages pre-fill user's zodiac sign from profile birth data"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/astrology/forecast/route.ts"
      provides: "GET endpoint for daily forecast"
      exports: ["GET"]
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx"
      provides: "Daily forecast page"
    - path: "mystiqor-build/src/app/api/tools/astrology/calendar/route.ts"
      provides: "GET endpoint for monthly calendar events"
      exports: ["GET"]
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx"
      provides: "Astro calendar page with monthly grid"
  key_links:
    - from: "forecast/page.tsx"
      to: "/api/tools/astrology/forecast"
      via: "useQuery GET with zodiac sign"
      pattern: "fetch.*astrology/forecast"
    - from: "calendar/page.tsx"
      to: "/api/tools/astrology/calendar"
      via: "useQuery GET with month/year"
      pattern: "fetch.*astrology/calendar"
---

<objective>
Build the daily astrology forecast page and astro calendar with monthly events.

Purpose: ASTR-06 provides a personalized daily zodiac forecast (AI-generated). ASTR-07 provides a monthly calendar view with astrological events (full moon, retrogrades, sign changes). Both are lightweight AI tools that don't require ephemeris (Phase 6). Both use LLM-generated content.
Output: Two new pages with API routes — daily forecast and monthly astro calendar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tools-tier-1/04-CONTEXT.md
@.planning/phases/04-tools-tier-1/04-RESEARCH.md

<interfaces>
From mystiqor-build/src/lib/constants/astrology.ts:
```typescript
export const ZODIAC_SIGNS: Record<ZodiacSignKey, { emoji: string; color: string; name: string; element: string }>
export const ZODIAC_SIGN_KEYS: readonly ZodiacSignKey[]
```

Established API route pattern:
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
// Zod validate -> service call -> invokeLLM -> save -> return
```

Profile zodiac lookup pattern:
```typescript
// User profile has birth_date -> derive zodiac sign
const { data: profile } = await supabase.from('profiles').select('birth_date').eq('user_id', user.id).single()
// Calculate zodiac from birth_date month/day
```

date-fns v4 (per STATE.md decision):
```typescript
import { format, eachDayOfInterval, startOfMonth, endOfMonth } from 'date-fns'
import { he } from 'date-fns/locale/he'  // v4 locale import path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create daily forecast API route + component + page</name>
  <files>
    mystiqor-build/src/app/api/tools/astrology/forecast/route.ts,
    mystiqor-build/src/components/features/astrology/DailyForecast/index.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/numerology/route.ts,
    mystiqor-build/src/lib/constants/astrology.ts,
    mystiqor-build/src/types/astrology.ts,
    mystiqor-build/src/app/(auth)/tools/dream/page.tsx
  </read_first>
  <action>
    1. Create `forecast/route.ts`:
       - GET handler (not POST — forecast is read-only for a given day)
       - Auth check
       - Load user profile to get birth_date -> derive zodiac sign from month/day using date ranges from ZODIAC_SIGNS constant
       - Check if forecast already cached: `SELECT * FROM daily_insights WHERE user_id=? AND insight_date=TODAY AND mood_type='forecast'`
       - If cached: return cached content
       - If not: call `invokeLLM` with Hebrew system prompt: "אתה אסטרולוג מומחה. צור תחזית יומית מפורטת למזל {sign} ליום {date}. כלול: אנרגיה כללית, אהבה, קריירה, בריאות, ומספר מזל. כתוב בעברית בגוף ראשון." maxTokens: 800
       - Save to `daily_insights` with `mood_type: 'forecast'` to differentiate from combined daily insights
       - Return `{ data: { content, zodiacSign, date } }`
       - Error handling: try/catch, 500 on LLM failure

    2. Create `DailyForecast/index.tsx` component:
       - Props: `DailyForecastProps { content: string; zodiacSign: string; date: string; isLoading: boolean }`
       - Render: Card with zodiac emoji + Hebrew sign name as header, date formatted DD/MM/YYYY, content via react-markdown
       - Loading state: animated skeleton
       - Sections: overall energy, love, career, health, lucky number (parsed from LLM markdown)
       - Star icons from lucide-react for visual appeal
       - Dark gradient background matching mystical theme

    3. Create `forecast/page.tsx`:
       - 'use client' directive
       - Title: "תחזית יומית" with zodiac emoji
       - useQuery GET to `/api/tools/astrology/forecast` — auto-fetches on mount
       - Render `<DailyForecast>` with response data
       - Wrap content in `<SubscriptionGuard feature="analyses">`
       - framer-motion page transition
       - Error state: toast via sonner
  </action>
  <acceptance_criteria>
    - grep -q "export async function GET" mystiqor-build/src/app/api/tools/astrology/forecast/route.ts
    - grep -q "daily_insights" mystiqor-build/src/app/api/tools/astrology/forecast/route.ts
    - grep -q "invokeLLM" mystiqor-build/src/app/api/tools/astrology/forecast/route.ts
    - grep -q "export function DailyForecast" mystiqor-build/src/components/features/astrology/DailyForecast/index.tsx
    - grep -q "react-markdown" mystiqor-build/src/components/features/astrology/DailyForecast/index.tsx
    - grep -q "SubscriptionGuard" mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Daily forecast API returns cached or freshly generated Hebrew forecast per zodiac sign. DailyForecast component renders content in a themed card. Page auto-fetches on mount with SubscriptionGuard. Compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create astro calendar API route + component + page</name>
  <files>
    mystiqor-build/src/app/api/tools/astrology/calendar/route.ts,
    mystiqor-build/src/components/features/astrology/AstroCalendar/index.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/astrology/forecast/route.ts,
    mystiqor-build/src/lib/constants/astrology.ts,
    github-source/src/pages/AstroCalendar.jsx
  </read_first>
  <action>
    1. Create `calendar/route.ts`:
       - GET handler accepting `?month=1&year=2026` query params
       - Auth check
       - Zod validate query: `z.object({ month: z.coerce.number().min(1).max(12), year: z.coerce.number().min(2020).max(2030) })`
       - Check cache: look for existing record in a calendar_events concept — store in `daily_insights` table with `mood_type: 'calendar'` and `insight_date: '{year}-{month}-01'`
       - If cached: return parsed JSON from `data_sources` column
       - If not: call `invokeLLM` with Hebrew prompt asking for 5-8 astrological events for the given month/year. Prompt: "צור רשימה של 5-8 אירועים אסטרולוגיים לחודש {month}/{year}. לכל אירוע ציין: תאריך, סוג (ירח מלא, ירח חדש, מרקורי רטרוגרד, כניסת מזל, ליקוי), תיאור קצר בעברית. החזר כ-JSON array." Use Zod response validation for the LLM output: `z.array(z.object({ date: z.string(), type: z.string(), title: z.string(), description: z.string() }))`
       - Save events JSON to `daily_insights.data_sources` with cache key
       - Return `{ data: { events, month, year } }`

    2. Create `AstroCalendar/index.tsx` component:
       - Props: `AstroCalendarProps { events: AstroEvent[]; month: number; year: number; onMonthChange: (month: number, year: number) => void }`
       - Define `AstroEvent { date: string; type: string; title: string; description: string }`
       - Render monthly grid:
         a. Header: Hebrew month name + year, prev/next month buttons (ChevronLeft, ChevronRight from lucide)
         b. Grid: 7 columns (Sun-Sat), Hebrew day names (ראשון, שני, שלישי, רביעי, חמישי, שישי, שבת)
         c. Day cells: date number, colored dot if event exists on that day
         d. Event types color coding: ירח מלא = silver, ירח חדש = dark, מרקורי רטרוגרד = orange, כניסת מזל = purple, ליקוי = red
         e. Clicking a day with event(s) shows event details in a popover or expandable section below
       - Use `date-fns` v4: `eachDayOfInterval`, `startOfMonth`, `endOfMonth`, `format`, `getDay`, `isSameDay`
       - Import Hebrew locale: `import { he } from 'date-fns/locale/he'` (v4 import path)

    3. Create `calendar/page.tsx`:
       - 'use client' directive
       - Title: "לוח שנה אסטרולוגי"
       - State: current month/year (default: current month)
       - useQuery GET `/api/tools/astrology/calendar?month=${month}&year=${year}` — refetch on month change
       - Render `<AstroCalendar>` with data
       - Wrap in `<SubscriptionGuard feature="analyses">`
       - Loading state: skeleton grid
       - framer-motion page transition
  </action>
  <acceptance_criteria>
    - grep -q "export async function GET" mystiqor-build/src/app/api/tools/astrology/calendar/route.ts
    - grep -q "invokeLLM" mystiqor-build/src/app/api/tools/astrology/calendar/route.ts
    - grep -q "z.coerce.number" mystiqor-build/src/app/api/tools/astrology/calendar/route.ts
    - grep -q "export function AstroCalendar" mystiqor-build/src/components/features/astrology/AstroCalendar/index.tsx
    - grep -q "eachDayOfInterval\|startOfMonth" mystiqor-build/src/components/features/astrology/AstroCalendar/index.tsx
    - grep -q "date-fns" mystiqor-build/src/components/features/astrology/AstroCalendar/index.tsx
    - grep -q "SubscriptionGuard" mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Astro calendar API returns cached or LLM-generated monthly events. AstroCalendar component renders monthly grid with event dots and details. Page navigates months with SubscriptionGuard. Compiles without errors.</done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
Daily forecast and astro calendar pages render with data from their API routes.
</verification>

<success_criteria>
- Daily forecast API caches per user per day, returns personalized Hebrew forecast
- Daily forecast page auto-fetches and shows formatted content with zodiac emoji
- Astro calendar API generates 5-8 events per month via LLM, caches in DB
- Astro calendar page renders monthly grid with event dots, month navigation
- Hebrew locale used for date formatting (date-fns v4 import path)
- Both pages wrapped in SubscriptionGuard
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-tools-tier-1/04-04-SUMMARY.md`
</output>
