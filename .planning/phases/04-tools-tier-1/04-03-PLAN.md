---
phase: 04-tools-tier-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/constants/big-five-questions.ts
  - mystiqor-build/src/services/personality/scoring.ts
  - mystiqor-build/src/components/features/personality/BigFiveQuestionnaire.tsx
  - mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx
  - mystiqor-build/src/app/api/tools/personality/route.ts
  - mystiqor-build/src/app/(auth)/tools/personality/page.tsx
  - mystiqor-build/src/app/(auth)/tools/dream/page.tsx
autonomous: true
requirements:
  - TOOL-07
  - TOOL-01
  - TOOL-06
must_haves:
  truths:
    - "User answers 20 Likert-scale questions and sees a Big Five radar chart with 5 OCEAN axes"
    - "Reverse-scored items are correctly inverted (6 - rawScore) before dimension summing"
    - "Personality results include AI interpretation per dimension in Hebrew"
    - "Dream analysis page has SubscriptionGuard wrapping the form submission"
    - "Tarot page SubscriptionGuard is verified present and correct"
  artifacts:
    - path: "mystiqor-build/src/lib/constants/big-five-questions.ts"
      provides: "20 Hebrew questions with dimension and reverse-score flags"
      exports: ["BIG_FIVE_QUESTIONS", "BigFiveDimension"]
    - path: "mystiqor-build/src/services/personality/scoring.ts"
      provides: "Big Five scoring with reverse items and normalization"
      exports: ["scoreBigFive"]
    - path: "mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx"
      provides: "Recharts RadarChart with 5 OCEAN axes"
      exports: ["BigFiveRadarChart"]
    - path: "mystiqor-build/src/app/api/tools/personality/route.ts"
      provides: "POST endpoint scoring + AI interpretation"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/personality/page.tsx"
      provides: "Personality analysis page with questionnaire and results"
  key_links:
    - from: "personality/page.tsx"
      to: "/api/tools/personality"
      via: "useMutation POST with answers array"
      pattern: "fetch.*tools/personality"
    - from: "personality/route.ts"
      to: "scoreBigFive"
      via: "import from services"
      pattern: "scoreBigFive"
    - from: "dream/page.tsx"
      to: "SubscriptionGuard"
      via: "wraps form content"
      pattern: "SubscriptionGuard"
---

<objective>
Build the Big Five personality analysis tool (questionnaire, scoring, radar chart, AI interpretation) and fix SubscriptionGuard on dream/tarot pages.

Purpose: Personality analysis (TOOL-07) is a new tool with a standardized psychology model. The existing tarot page has SubscriptionGuard but dream page is missing it (TOOL-01, TOOL-06 verification per D-14, D-15).
Output: Complete personality analysis flow + dream page SubscriptionGuard fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tools-tier-1/04-CONTEXT.md
@.planning/phases/04-tools-tier-1/04-RESEARCH.md

<interfaces>
From mystiqor-build/src/components/features/subscription/SubscriptionGuard.tsx:
```typescript
export interface SubscriptionGuardProps {
  feature: string;
  children: ReactNode;
  fallback?: ReactNode;
}
export function SubscriptionGuard({ feature, children, fallback }: SubscriptionGuardProps)
```

Recharts RadarChart pattern (from Phase 3 dashboard):
```tsx
import { RadarChart, Radar, PolarGrid, PolarAngleAxis, ResponsiveContainer } from 'recharts';
// Recharts v3 — no inline Tooltip formatter, use custom component
```

Big Five Scoring (from RESEARCH.md Pattern 4):
```typescript
// Reverse score: effectiveScore = isReversed ? (6 - rawScore) : rawScore
// Dimension score (4 items, max=20, min=4): normalize = ((sum - 4) / 16) * 100
```

Established API route pattern:
```typescript
// 1. Auth check (createClient + getUser)
// 2. Zod validate input
// 3. Pure calculation
// 4. invokeLLM for AI interpretation
// 5. Save to analyses table + return
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Big Five constants + scoring service + components</name>
  <files>
    mystiqor-build/src/lib/constants/big-five-questions.ts,
    mystiqor-build/src/services/personality/scoring.ts,
    mystiqor-build/src/components/features/personality/BigFiveQuestionnaire.tsx,
    mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx
  </files>
  <read_first>
    mystiqor-build/src/components/features/subscription/SubscriptionGuard.tsx,
    mystiqor-build/src/app/(auth)/tools/tarot/page.tsx,
    mystiqor-build/src/app/(auth)/tools/dream/page.tsx
  </read_first>
  <action>
    1. Create `big-five-questions.ts` (per D-09, D-10, D-11):
       - Export type `BigFiveDimension = 'openness' | 'conscientiousness' | 'extraversion' | 'agreeableness' | 'neuroticism'`
       - Export interface `BigFiveQuestion { id: number; text: string; dimension: BigFiveDimension; isReversed: boolean }`
       - Export `BIG_FIVE_QUESTIONS: readonly BigFiveQuestion[]` — 20 questions in Hebrew, 4 per dimension:
         * Openness (פתיחות): "אני נהנה/ת לחוות דברים חדשים", "יש לי דמיון חי ועשיר", "אני מעדיף/ה שגרה יציבה על פני הרפתקאות" (reversed), "אני מתעניין/ת ברעיונות חדשניים ויצירתיים"
         * Conscientiousness (מצפוניות): "אני מתכנן/ת את המשימות שלי מראש", "אני שמה לב לפרטים הקטנים", "לפעמים אני משאיר/ה דברים לרגע האחרון" (reversed), "אני עקבי/ת בביצוע ההתחייבויות שלי"
         * Extraversion (מוחצנות): "אני נהנה/ת מאירועים חברתיים", "אני יוזם/ת שיחות עם אנשים חדשים", "אני מעדיף/ה לבלות לבד מאשר בקבוצה" (reversed), "אני מרגיש/ה אנרגטי/ת כשאני מוקף/ת באנשים"
         * Agreeableness (נעימות): "אני מתחשב/ת ברגשות של אחרים", "אני נוטה לסמוך על אנשים", "לפעמים אני ביקורתי/ת כלפי אחרים" (reversed), "אני מוכן/ה לעזור גם על חשבון הזמן שלי"
         * Neuroticism (רגישות רגשית): "אני חש/ה מתח בקלות", "אני נוטה לדאוג יותר מדי", "בדרך כלל אני שלו/ה ורגוע/ה" (reversed), "לפעמים אני מרגיש/ה מוצף/ת מרגשות"
       - Export `DIMENSION_LABELS: Record<BigFiveDimension, string>` = { openness: 'פתיחות', conscientiousness: 'מצפוניות', extraversion: 'מוחצנות', agreeableness: 'נעימות', neuroticism: 'רגישות רגשית' }

    2. Create `scoring.ts`:
       - Export interface `BigFiveScores { openness: number; conscientiousness: number; extraversion: number; agreeableness: number; neuroticism: number }`
       - Export function `scoreBigFive(answers: number[]): BigFiveScores` — takes 20 answers (1-5 Likert), applies reverse scoring where `isReversed === true` (formula: `6 - rawScore`), sums 4 answers per dimension, normalizes to 0-100 scale (formula: `((sum - 4) / 16) * 100`). Returns scores object.
       - Validate that answers array has exactly 20 items, each 1-5. Throw typed error if invalid.
       - Hebrew JSDoc comments on all functions.

    3. Create `BigFiveQuestionnaire.tsx` (per D-09):
       - Props: `BigFiveQuestionnaireProps { onSubmit: (answers: number[]) => void; isSubmitting: boolean }`
       - Use React Hook Form with Zod schema: `z.object({ answers: z.array(z.number().min(1).max(5)).length(20) })`
       - Render 20 questions from `BIG_FIVE_QUESTIONS` constant
       - Each question: Hebrew text + 5 radio buttons labeled "מאוד לא מסכים" to "מאוד מסכים" (Likert 1-5)
       - Group by dimension with dimension header (Hebrew label)
       - Submit button disabled until all 20 answered
       - Use `z.input<typeof Schema>` for useForm generic (per Phase 3 pattern for schemas with .default())

    4. Create `BigFiveRadarChart.tsx` (per D-10):
       - Props: `BigFiveRadarChartProps { scores: BigFiveScores }`
       - Import `DIMENSION_LABELS` from constants
       - Transform scores into Recharts data format: `[{ dimension: 'פתיחות', score: 75 }, ...]`
       - Render `ResponsiveContainer > RadarChart > PolarGrid + PolarAngleAxis(dataKey="dimension") + Radar(dataKey="score")`
       - Use purple theme: fill="#8b5cf6" fillOpacity={0.4} stroke="#8b5cf6"
       - PolarAngleAxis tick: `{ fill: '#a1a1aa', fontSize: 12 }`
       - Height: 300px
       - IMPORTANT: Do NOT use inline Tooltip formatter — Recharts v3 type issue (per STATE.md decision)
  </action>
  <acceptance_criteria>
    - grep -q "BIG_FIVE_QUESTIONS" mystiqor-build/src/lib/constants/big-five-questions.ts
    - grep -q "BigFiveDimension" mystiqor-build/src/lib/constants/big-five-questions.ts
    - grep -q "isReversed" mystiqor-build/src/lib/constants/big-five-questions.ts
    - grep -q "DIMENSION_LABELS" mystiqor-build/src/lib/constants/big-five-questions.ts
    - grep -q "scoreBigFive" mystiqor-build/src/services/personality/scoring.ts
    - grep -q "6 - " mystiqor-build/src/services/personality/scoring.ts
    - grep -q "export function BigFiveQuestionnaire" mystiqor-build/src/components/features/personality/BigFiveQuestionnaire.tsx
    - grep -q "BIG_FIVE_QUESTIONS" mystiqor-build/src/components/features/personality/BigFiveQuestionnaire.tsx
    - grep -q "RadarChart" mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx
    - grep -q "DIMENSION_LABELS" mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>20 Hebrew questions defined with reverse-score flags. Scoring service normalizes to 0-100 per dimension. Questionnaire renders Likert form. RadarChart renders 5-axis chart. All compile.</done>
</task>

<task type="auto">
  <name>Task 2: Create personality API route + page + fix dream SubscriptionGuard</name>
  <files>
    mystiqor-build/src/app/api/tools/personality/route.ts,
    mystiqor-build/src/app/(auth)/tools/personality/page.tsx,
    mystiqor-build/src/app/(auth)/tools/dream/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/numerology/route.ts,
    mystiqor-build/src/services/personality/scoring.ts,
    mystiqor-build/src/lib/constants/big-five-questions.ts,
    mystiqor-build/src/components/features/personality/BigFiveQuestionnaire.tsx,
    mystiqor-build/src/components/features/personality/BigFiveRadarChart.tsx,
    mystiqor-build/src/app/(auth)/tools/dream/page.tsx,
    mystiqor-build/src/app/(auth)/tools/tarot/page.tsx
  </read_first>
  <action>
    1. Create `personality/route.ts` (per D-09, D-10):
       - Follow established 5-step API route pattern (see numerology/route.ts)
       - POST handler:
         a. Auth check: `createClient()` + `getUser()`, return 401 if not authenticated
         b. Zod validate: `z.object({ answers: z.array(z.number().min(1).max(5)).length(20) })`, return 400 on failure
         c. Score: call `scoreBigFive(parsed.data.answers)` from `@/services/personality/scoring`
         d. LLM interpretation: call `invokeLLM` with Hebrew system prompt asking for personality interpretation referencing the 5 dimension scores. Prompt should describe each dimension score and ask for insights about the user's personality profile. maxTokens: 1000.
         e. Save: insert into `analyses` table with `tool_type: 'personality'`, `data: { scores, answers }`, `result: { interpretation: aiText }`
         f. Return: `{ data: { scores, interpretation: aiText } }`
       - Error handling: try/catch around LLM + DB, return 500 with Hebrew message

    2. Create `personality/page.tsx`:
       - 'use client' directive
       - Page layout: Hebrew title "ניתוח אישיות — חמשת הממדים הגדולים", subtitle explaining the Big Five model
       - State management: `useState` for showing questionnaire vs results
       - Questionnaire phase: render `<BigFiveQuestionnaire>` inside `<SubscriptionGuard feature="analyses">`
       - useMutation: POST to `/api/tools/personality` with `{ answers }` body
       - Results phase: show `<BigFiveRadarChart scores={data.scores} />` + AI interpretation rendered via `react-markdown` below the chart
       - Each dimension: score badge + Hebrew label + AI interpretation paragraph
       - "ניתוח חדש" button to reset and show questionnaire again
       - Loading state: skeleton during mutation
       - Error handling: toast via sonner on mutation error
       - framer-motion page transition wrapper (per project pattern)

    3. Fix `dream/page.tsx` SubscriptionGuard (per D-15, Pitfall 5):
       - READ the existing dream page FIRST — preserve ALL existing functionality
       - Add `import { SubscriptionGuard } from '@/components/features/subscription/SubscriptionGuard'`
       - Wrap the form submission area (the main interactive content, not the page title) with `<SubscriptionGuard feature="analyses">`
       - Match the same pattern used in tarot/page.tsx (read it first to see exact wrapping position)
       - Do NOT rewrite the page — only add the import and the Guard wrapper
  </action>
  <acceptance_criteria>
    - grep -q "scoreBigFive" mystiqor-build/src/app/api/tools/personality/route.ts
    - grep -q "export async function POST" mystiqor-build/src/app/api/tools/personality/route.ts
    - grep -q "tool_type.*personality" mystiqor-build/src/app/api/tools/personality/route.ts
    - grep -q "BigFiveQuestionnaire" mystiqor-build/src/app/(auth)/tools/personality/page.tsx
    - grep -q "BigFiveRadarChart" mystiqor-build/src/app/(auth)/tools/personality/page.tsx
    - grep -q "SubscriptionGuard" mystiqor-build/src/app/(auth)/tools/personality/page.tsx
    - grep -q "SubscriptionGuard" mystiqor-build/src/app/(auth)/tools/dream/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Personality API route scores Big Five + returns AI interpretation. Personality page shows questionnaire -> radar chart + interpretation flow. Dream page has SubscriptionGuard wrapping form content. All compile without errors.</done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
Personality flow: questionnaire -> API -> radar chart + AI text. Dream page SubscriptionGuard added.
</verification>

<success_criteria>
- 20 Hebrew Big Five questions defined with 4 per dimension and reverse-score flags (per D-09, D-11)
- Scoring correctly applies reverse scoring (6 - raw) and normalizes to 0-100 (per D-09)
- RadarChart renders with 5 Hebrew-labeled axes (per D-10)
- Personality API route follows 5-step pattern, saves to analyses table
- Personality page wrapped in SubscriptionGuard
- Dream page now has SubscriptionGuard (was missing per RESEARCH.md Pitfall 5)
- Tarot page SubscriptionGuard verified present (per D-14)
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-tools-tier-1/04-03-SUMMARY.md`
</output>
