---
phase: 04-tools-tier-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx
  - mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx
  - mystiqor-build/src/components/features/astrology/BirthChart/PlanetPositions.tsx
  - mystiqor-build/src/components/features/astrology/BirthChart/AspectLines.tsx
  - mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
  - mystiqor-build/src/components/features/astrology/BirthChart/utils.ts
autonomous: true
requirements:
  - ASTR-01
must_haves:
  truths:
    - "BirthChart component renders an SVG with zodiac ring showing 12 sign emojis in correct positions"
    - "Planet positions appear as colored circles with symbols at the correct longitude on the chart"
    - "Aspect lines connect planet pairs with the correct color per aspect type (trine=green, square=orange, etc.)"
    - "House numbers 1-12 appear at correct angular positions inside the chart"
  artifacts:
    - path: "mystiqor-build/src/components/features/astrology/BirthChart/index.tsx"
      provides: "Composed BirthChart SVG component"
      exports: ["BirthChart"]
    - path: "mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx"
      provides: "Outer zodiac ring with 12 sign emojis and colored segments"
      exports: ["ZodiacRing"]
    - path: "mystiqor-build/src/components/features/astrology/BirthChart/PlanetPositions.tsx"
      provides: "Planet circles at longitude positions with symbols"
      exports: ["PlanetPositions"]
    - path: "mystiqor-build/src/components/features/astrology/BirthChart/AspectLines.tsx"
      provides: "Colored lines connecting aspecting planet pairs"
      exports: ["AspectLines"]
    - path: "mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx"
      provides: "12 house divider lines and house numbers"
      exports: ["HouseOverlay"]
  key_links:
    - from: "BirthChart/index.tsx"
      to: "ZodiacRing, PlanetPositions, AspectLines, HouseOverlay"
      via: "SVG composition inside single viewBox"
      pattern: "<ZodiacRing|<PlanetPositions|<AspectLines|<HouseOverlay"
    - from: "BirthChart/utils.ts"
      to: "all sub-components"
      via: "shared getPlanetPosition and geometry constants"
      pattern: "import.*from.*utils"
---

<objective>
Port the BASE44 BirthChart.jsx (922 lines) into 4 typed SVG sub-components plus a shared utils file, per D-02 and D-03.

Purpose: The natal chart is the visual centerpiece of the astrology tool. Decomposing into typed sub-components makes it maintainable, testable, and reusable for synastry (Phase 6).
Output: 6 files in BirthChart/ directory — ZodiacRing, PlanetPositions, AspectLines, HouseOverlay, utils, and index.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tools-tier-1/04-CONTEXT.md
@.planning/phases/04-tools-tier-1/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mystiqor-build/src/services/astrology/chart.ts:
```typescript
export interface HouseData {
  house_number: number
  cusp_longitude: number
  sign: string
}
export interface ChartData {
  ascendant: number
  midheaven: number
  houses: HouseData[]
  aspects: AspectResult[]
}
export function calculateHouses(date: Date, lat: number, lon: number): ChartData
export function assembleChart(date: Date, lat: number, lon: number, planets: PlanetPositions): ChartData
export function getSign(longitude: number): string
```

From mystiqor-build/src/services/astrology/aspects.ts:
```typescript
export interface AspectResult {
  planet1: string
  planet2: string
  type: string  // 'Conjunction' | 'Opposition' | 'Trine' | 'Square' | 'Sextile'
  orb: number
  strength: number
}
export interface PlanetPositions {
  [planetName: string]: { longitude: number }
}
```

From mystiqor-build/src/lib/constants/astrology.ts:
```typescript
export type ZodiacSignKey = 'Aries' | 'Taurus' | ... | 'Pisces'
export type PlanetKey = 'sun' | 'moon' | ... | 'pluto'
export type AspectTypeKey = 'Conjunction' | 'Opposition' | 'Trine' | 'Square' | 'Sextile' | 'Quincunx' | 'Semi-sextile'
export const ZODIAC_SIGNS: Record<ZodiacSignKey, { emoji: string; color: string; name: string; element: string }>
export const PLANET_SYMBOLS: Record<PlanetKey, { symbol: string; name: string; color: string; meaning: string }>
export const ASPECT_TYPES: Record<AspectTypeKey, { name: string; color: string; strength: number; meaning: string }>
export const ZODIAC_SIGN_KEYS: readonly ZodiacSignKey[]
export const PLANET_KEYS: readonly PlanetKey[]
```

SVG Geometry (from github-source/src/components/BirthChart.jsx):
- viewBox="0 0 500 500", center=(250,250)
- Outer circle: r=240 (stroke only)
- Zodiac ring dashed separator: r=200
- Planet orbit dashed ring: r=160
- Planet positions: radius=170
- House number positions: radius=135
- Zodiac sign emoji positions: radius=220
- Ascendant: horizontal gold line from center to outer edge
- MC: vertical silver line from center to top
- getPlanetPosition formula: angle = (longitude - 90) * (PI/180); x = 250 + radius * cos(angle); y = 250 + radius * sin(angle)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared utils + ZodiacRing + HouseOverlay components</name>
  <files>
    mystiqor-build/src/components/features/astrology/BirthChart/utils.ts,
    mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx,
    mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx
  </files>
  <read_first>
    github-source/src/components/BirthChart.jsx,
    mystiqor-build/src/lib/constants/astrology.ts,
    mystiqor-build/src/services/astrology/chart.ts,
    mystiqor-build/src/types/astrology.ts
  </read_first>
  <action>
    1. Create `utils.ts` with shared SVG geometry constants and helper functions (per D-03):
       - Export `CHART_CENTER = 250`, `OUTER_RADIUS = 240`, `ZODIAC_RADIUS = 220`, `ZODIAC_SEPARATOR_RADIUS = 200`, `PLANET_ORBIT_RADIUS = 170`, `PLANET_RING_RADIUS = 160`, `HOUSE_NUMBER_RADIUS = 135`
       - Export `getPlanetPosition(longitude: number, radius: number): { x: number; y: number }` — formula: `angle = (longitude - 90) * (Math.PI / 180)`, `x = 250 + radius * Math.cos(angle)`, `y = 250 + radius * Math.sin(angle)` (ported from BirthChart.jsx lines 94-99)
       - Export `getAngleForSign(signIndex: number): number` — returns center angle for a zodiac sign segment: `(signIndex * 30 - 90 + 15) * (Math.PI / 180)`
       - All functions must have Hebrew JSDoc comments

    2. Create `ZodiacRing.tsx` (per D-01, D-02):
       - Props interface: `ZodiacRingProps {}` (no props needed — signs are static)
       - Renders SVG `<g>` element containing:
         a. Outer circle: `<circle cx=250 cy=250 r=240 stroke="#6366F1" strokeWidth="2" fill="none" />`
         b. Zodiac separator dashed circle: `<circle cx=250 cy=250 r=200 stroke="#6366F1" strokeWidth="1" strokeDasharray="4 4" fill="none" opacity="0.3" />`
         c. 12 zodiac sign emojis: iterate `ZODIAC_SIGN_KEYS` from `@/lib/constants/astrology`, position each at radius=220 using `getAngleForSign(i)`. Use `<text>` with `textAnchor="middle"` `dominantBaseline="middle"` `fontSize="24"` `fill={ZODIAC_SIGNS[sign].color}`. Add `className="pointer-events-none"`.
         d. 12 sign separator lines: 30-degree-spaced lines from r=200 to r=240, stroke="#6366F1" opacity=0.3
       - IMPORTANT: SVG text does NOT respect CSS dir="rtl". Use explicit textAnchor="middle" on all <text> elements.

    3. Create `HouseOverlay.tsx` (per D-01, D-02):
       - Props interface: `HouseOverlayProps { ascendant: number; midheaven: number }`
       - Renders SVG `<g>` element containing:
         a. 12 house divider lines: from r=160 to r=200 at each house cusp angle (every 30 degrees), stroke="#6366F1" strokeWidth="1" opacity="0.5"
         b. 12 house numbers: positioned at r=135 at midpoint of each house (15-degree offset), `<text>` fill="#A5B4FC" fontSize="12" fontWeight="bold" textAnchor="middle" dominantBaseline="middle"
         c. Ascendant line: horizontal gold (#FFD700) line from center(250,250) to outer edge at ascendant longitude, strokeWidth="2"
         d. MC line: silver (#C0C0C0) line from center to outer edge at midheaven longitude, strokeWidth="2"
       - Use `getPlanetPosition` from utils for coordinate calculations.
  </action>
  <acceptance_criteria>
    - grep -q "getPlanetPosition" mystiqor-build/src/components/features/astrology/BirthChart/utils.ts
    - grep -q "CHART_CENTER" mystiqor-build/src/components/features/astrology/BirthChart/utils.ts
    - grep -q "export function ZodiacRing" mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx
    - grep -q "ZODIAC_SIGN_KEYS" mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx
    - grep -q "textAnchor" mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx
    - grep -q "export function HouseOverlay" mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx
    - grep -q "ascendant" mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx
    - grep -q "FFD700" mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>utils.ts exports getPlanetPosition and all radius constants. ZodiacRing renders 12 sign emojis with correct colors. HouseOverlay renders 12 house numbers, divider lines, ascendant line (gold), and MC line (silver). All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlanetPositions + AspectLines + BirthChart index</name>
  <files>
    mystiqor-build/src/components/features/astrology/BirthChart/PlanetPositions.tsx,
    mystiqor-build/src/components/features/astrology/BirthChart/AspectLines.tsx,
    mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
  </files>
  <read_first>
    github-source/src/components/BirthChart.jsx,
    mystiqor-build/src/components/features/astrology/BirthChart/utils.ts,
    mystiqor-build/src/components/features/astrology/BirthChart/ZodiacRing.tsx,
    mystiqor-build/src/components/features/astrology/BirthChart/HouseOverlay.tsx,
    mystiqor-build/src/lib/constants/astrology.ts,
    mystiqor-build/src/services/astrology/aspects.ts
  </read_first>
  <action>
    1. Create `PlanetPositions.tsx` (per D-01, D-02):
       - Props interface: `PlanetPositionsProps { planets: Record<string, { longitude: number }> }`
       - Renders SVG `<g>` element containing for each planet entry:
         a. Planet circle: `<circle>` at position from `getPlanetPosition(longitude, PLANET_ORBIT_RADIUS)` (radius=170 from BirthChart.jsx), r=12, fill with planet color from `PLANET_SYMBOLS[planetKey as PlanetKey]?.color ?? '#FFFFFF'`
         b. Planet symbol: `<text>` at same position, the symbol character from `PLANET_SYMBOLS[planetKey as PlanetKey]?.symbol ?? '?'`, fontSize="14" fill="#FFFFFF" textAnchor="middle" dominantBaseline="middle"
       - Handle unknown planet names gracefully (skip or use default color/symbol). Use optional chaining since planet names from PlanetPositions interface are `string`, not `PlanetKey`.
       - PITFALL: Do NOT use `parseFloat` — longitude is already `number` type in TypeScript.

    2. Create `AspectLines.tsx` (per D-01):
       - Props interface: `AspectLinesProps { aspects: AspectResult[]; planets: Record<string, { longitude: number }> }`
       - Import `AspectResult` from `@/services/astrology/aspects`
       - Import `ASPECT_TYPES` from `@/lib/constants/astrology` — use `AspectTypeKey` for type-safe color lookup
       - Renders SVG `<g>` element: for each aspect, draw a `<line>` between the two planets' positions at radius=170. Color from `ASPECT_TYPES[aspect.type as AspectTypeKey]?.color ?? '#888888'`. strokeWidth="1" opacity calculated from aspect.strength (e.g., `0.3 + aspect.strength * 0.5` to keep visible).

    3. Create `index.tsx` — the composed BirthChart component (per D-02):
       - Props interface: `BirthChartProps { planets: Record<string, { longitude: number }>; chartData: ChartData }`
       - Import `ChartData` from `@/services/astrology/chart`
       - Renders: dark background container div with aspect-square, then `<svg viewBox="0 0 500 500">` containing in this order:
         a. Dark background circle: `<circle cx=250 cy=250 r=240 fill="#0f172a" />` (dark navy)
         b. `<ZodiacRing />`
         c. `<HouseOverlay ascendant={chartData.ascendant} midheaven={chartData.midheaven} />`
         d. `<AspectLines aspects={chartData.aspects} planets={planets} />`
         e. `<PlanetPositions planets={planets} />`
         f. Planet orbit dashed ring: `<circle cx=250 cy=250 r=160 stroke="#6366F1" strokeWidth="1" strokeDasharray="2 4" fill="none" opacity="0.3" />`
       - Export as default and named export.
       - Use `next/dynamic` loading for lazy loading hint comment but do NOT wrap in dynamic() here — let the page decide.
  </action>
  <acceptance_criteria>
    - grep -q "export function PlanetPositions" mystiqor-build/src/components/features/astrology/BirthChart/PlanetPositions.tsx
    - grep -q "PLANET_SYMBOLS" mystiqor-build/src/components/features/astrology/BirthChart/PlanetPositions.tsx
    - grep -q "export function AspectLines" mystiqor-build/src/components/features/astrology/BirthChart/AspectLines.tsx
    - grep -q "ASPECT_TYPES" mystiqor-build/src/components/features/astrology/BirthChart/AspectLines.tsx
    - grep -q "export function BirthChart" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
    - grep -q "ZodiacRing" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
    - grep -q "HouseOverlay" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
    - grep -q "AspectLines" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
    - grep -q "PlanetPositions" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
    - grep -q "viewBox" mystiqor-build/src/components/features/astrology/BirthChart/index.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PlanetPositions renders planet circles with symbols at correct longitude positions. AspectLines draws colored lines between aspecting planets. BirthChart index composes all 4 sub-components inside a single SVG viewBox="0 0 500 500" with dark background. All compile without errors.</done>
</task>

</tasks>

<verification>
cd mystiqor-build && npx tsc --noEmit
All 6 BirthChart files compile. Components export correctly. No `any` types.
</verification>

<success_criteria>
- BirthChart/index.tsx assembles ZodiacRing + PlanetPositions + AspectLines + HouseOverlay inside SVG viewBox="0 0 500 500"
- All SVG geometry matches BASE44 BirthChart.jsx radii (240, 220, 200, 170, 160, 135)
- Zodiac ring shows 12 emojis with correct colors from ZODIAC_SIGNS constant
- Planet positions use getPlanetPosition formula: angle = (longitude - 90) * PI/180
- Aspect lines colored per ASPECT_TYPES constant
- House overlay shows numbers 1-12, ascendant line (gold), MC line (silver)
- TypeScript strict — zero `any`, all props typed
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-tools-tier-1/04-01-SUMMARY.md`
</output>
