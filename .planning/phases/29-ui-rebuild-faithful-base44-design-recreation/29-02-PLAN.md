---
phase: 29-ui-rebuild-faithful-base44-design-recreation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/layouts/Sidebar.tsx
autonomous: false
requirements:
  - SIDE-03

must_haves:
  truths:
    - "פריט ניווט פעיל בולט בצורה דרמטית עם גרדיאנט סגול-ורוד — כמו ב-BASE44"
    - "ה-hover state נשמר רגיש ועדין (bg-white/5)"
    - "כל הלוגיקה הפונקציונלית (localStorage, toggles, UsageBar, navigation) לא השתנתה"
    - "הסיידבר נשמר RTL-correct עם border-s (logical direction)"
  artifacts:
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Sidebar with BASE44-dramatic active gradient + side indicator"
      contains: "from-primary-container"
  key_links:
    - from: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      to: "layout-client.tsx"
      via: "Sidebar component export — unchanged API"
      pattern: "export function Sidebar"
---

<objective>
Enhance sidebar active navigation state from subtle `bg-primary/10` to a dramatic BASE44-inspired gradient, making the current page visually unmistakable in the sidebar navigation.

Purpose: The current active state (bg-primary/10 text-primary) is functional but subtle. The BASE44 original used a bold gradient (from-purple-600 to-pink-600) that was visually dramatic and immediately clear. Per user feedback about expecting DRAMATIC visual changes, the active state should be more energetic while keeping the established MD3 token approach.
Output: Modified Sidebar.tsx with dramatic active gradient on nav items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-rebuild-faithful-base44-design-recreation/29-RESEARCH.md

@mystiqor-build/src/components/layouts/Sidebar.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mystiqor-build/src/components/layouts/Sidebar.tsx:
```typescript
// Current active state classes (line 214):
isActive
  ? 'bg-primary/10 text-primary border-s-2 border-s-primary'
  : 'text-on-surface-variant hover:bg-white/5 hover:text-on-surface'
```

From github-source/src/Layout.jsx (BASE44 active state to replicate):
```jsx
// Lines 226-229:
isActive
  ? 'bg-gradient-to-r from-purple-600/90 to-pink-600/90 text-white shadow-lg border-purple-400/50 scale-105'
  : 'text-gray-100 border-purple-800/30 bg-gray-800/50 hover:bg-purple-900/40'
```

MD3 token equivalents for BASE44 colors:
- purple-600 -> primary-container (#8f2de6)
- pink-600 -> secondary-container
- text-white -> text-on-primary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance sidebar active state to BASE44-dramatic gradient</name>
  <files>mystiqor-build/src/components/layouts/Sidebar.tsx</files>
  <read_first>
    - mystiqor-build/src/components/layouts/Sidebar.tsx (full file — pay attention to line ~210-215 for the active/inactive class logic inside CollapsibleSection)
    - github-source/src/Layout.jsx (lines 224-233 for BASE44 active state reference)
  </read_first>
  <action>
In Sidebar.tsx, inside the CollapsibleSection component, find the nav item Link className (around line 210-215). The current active/inactive ternary is:

```tsx
isActive
  ? 'bg-primary/10 text-primary border-s-2 border-s-primary'
  : 'text-on-surface-variant hover:bg-white/5 hover:text-on-surface'
```

Replace the ACTIVE branch ONLY with a dramatic gradient using MD3 tokens:

```tsx
isActive
  ? 'bg-gradient-to-r from-primary-container/80 to-secondary-container/80 text-on-primary shadow-sm shadow-primary-container/20 border-s-2 border-s-primary'
  : 'text-on-surface-variant hover:bg-white/5 hover:text-on-surface'
```

This changes ONLY the active state:
- `bg-primary/10` -> `bg-gradient-to-r from-primary-container/80 to-secondary-container/80` (dramatic gradient like BASE44's purple-to-pink)
- `text-primary` -> `text-on-primary` (white text on the gradient for better contrast)
- Added `shadow-sm shadow-primary-container/20` (subtle glow like BASE44's shadow-lg)
- Kept `border-s-2 border-s-primary` (the side indicator from Phase 27)

The inactive/hover branch stays EXACTLY as is: `text-on-surface-variant hover:bg-white/5 hover:text-on-surface`

Do NOT change:
- NAV_SECTIONS array (labels, hrefs, icons)
- useState/useCallback/useEffect logic
- localStorage persistence
- UsageBar component
- Logo area
- Category headers or separators
- Any TypeScript types or interfaces
- Import statements
- Any inactive/hover state classes
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "from-primary-container/80 to-secondary-container/80" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "text-on-primary" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "shadow-sm shadow-primary-container/20" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "border-s-2 border-s-primary" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "hover:bg-white/5" mystiqor-build/src/components/layouts/Sidebar.tsx
    - grep -q "export function Sidebar" mystiqor-build/src/components/layouts/Sidebar.tsx
    - TypeScript compilation passes with zero errors
    - No changes to NAV_SECTIONS, localStorage logic, or UsageBar
  </acceptance_criteria>
  <done>
    - Active nav items display a dramatic purple-to-pink gradient background using MD3 tokens
    - Active items have white text (text-on-primary) for contrast on the gradient
    - Active items keep the purple side indicator (border-s-2 border-s-primary)
    - Inactive items unchanged — hover still shows bg-white/5
    - Zero TypeScript errors
    - All navigation still works (links, collapsible sections, localStorage persistence)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — sidebar active state gradient</name>
  <files>mystiqor-build/src/components/layouts/Sidebar.tsx</files>
  <action>
Human verifies the visual output of Task 1: dramatic BASE44-inspired gradient active state on sidebar navigation items.

Steps:
1. Run: cd mystiqor-build and npm run dev
2. Open http://localhost:3000/dashboard
3. Look at the sidebar — the current page item should show a DRAMATIC purple-to-pink gradient background (not the subtle purple tint from before)
4. Navigate to different pages (e.g., /tools/numerology, /coach, /profile) — the active item should stand out with the gradient
5. Check contrast — text should be white/bright on the gradient background
6. Hover over inactive items — should show subtle bg-white/5 highlight
7. Check that the purple side bar indicator (border-s) still appears on active items
8. Expand/collapse sidebar categories — should still work with smooth animation
9. Refresh the page — collapsed/expanded state should persist (localStorage)
10. Check mobile (resize) — sidebar should remain hidden on mobile (MobileNav handles that)
  </action>
  <verify>Human visual inspection confirms active state is dramatically visible with gradient, text is readable, and all sidebar functionality works</verify>
  <done>User approves visual output: sidebar active gradient is dramatic and unmistakable, all navigation functions correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a pure CSS class change on an existing client component. No data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | T (Tampering) | Sidebar nav links | accept | No change to href values or navigation logic — purely visual active state change |
</threat_model>

<verification>
1. TypeScript compilation: `cd mystiqor-build && npx tsc --noEmit` passes with 0 errors
2. Sidebar.tsx contains `from-primary-container/80 to-secondary-container/80` in the active state
3. Active items show `text-on-primary` (not `text-primary`)
4. Inactive items still show `hover:bg-white/5`
5. All navigation links still work correctly
6. localStorage persistence for section toggles still works
</verification>

<success_criteria>
- Sidebar active state is dramatically visible with gradient background
- White text on gradient provides strong contrast
- Inactive/hover states remain subtle
- Zero TypeScript errors, zero functional regressions
- Human verification confirms the active state is unmistakable
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-rebuild-faithful-base44-design-recreation/29-02-SUMMARY.md`
</output>
