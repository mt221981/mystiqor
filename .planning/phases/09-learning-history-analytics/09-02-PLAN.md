---
phase: 09-learning-history-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/005_schema_fixes.sql
  - mystiqor-build/src/app/api/blog/route.ts
  - mystiqor-build/src/app/api/learn/progress/route.ts
  - mystiqor-build/src/lib/validations/learning.ts
  - mystiqor-build/src/components/features/blog/BlogPostCard.tsx
  - mystiqor-build/src/components/features/learn/LearningPathCard.tsx
  - mystiqor-build/src/components/features/learn/ProgressTracker.tsx
  - mystiqor-build/src/app/(auth)/learn/blog/page.tsx
  - mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx
  - mystiqor-build/src/app/(auth)/learn/page.tsx
autonomous: true
requirements:
  - GROW-02
  - GROW-03

must_haves:
  truths:
    - "User can browse published blog articles with category filtering and search at /learn/blog"
    - "User can read a full article by expanding it inline"
    - "User sees structured learning paths (astrology, numerology, drawing) with progress tracking at /learn/tutorials"
    - "User can mark a learning topic as completed and see progress update"
    - "/learn page serves as a hub redirecting to tutorials, blog, astrology tutor, drawing tutor"
  artifacts:
    - path: "mystiqor-build/005_schema_fixes.sql"
      provides: "Blog seed data + learning_progress unique constraint"
      contains: "INSERT INTO blog_posts"
    - path: "mystiqor-build/src/app/api/blog/route.ts"
      provides: "GET blog_posts with category filter and search"
      exports: ["GET"]
    - path: "mystiqor-build/src/app/api/learn/progress/route.ts"
      provides: "GET/POST learning_progress upsert"
      exports: ["GET", "POST"]
    - path: "mystiqor-build/src/app/(auth)/learn/blog/page.tsx"
      provides: "Blog list page with category tabs and search"
      min_lines: 60
    - path: "mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx"
      provides: "Tutorials hub with learning paths"
      min_lines: 80
  key_links:
    - from: "mystiqor-build/src/app/(auth)/learn/blog/page.tsx"
      to: "/api/blog"
      via: "useQuery + fetch"
      pattern: "fetch.*api/blog"
    - from: "mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx"
      to: "/api/learn/progress"
      via: "useQuery for progress, useMutation for completion"
      pattern: "api/learn/progress"
---

<objective>
Build the blog page with seeded educational content and the tutorials hub with structured learning paths and progress tracking.

Purpose: Users need educational content about astrology, numerology, and drawing interpretation (GROW-02) and structured, progressive learning paths (GROW-03) with tracked completion.

Output: Working /learn/blog page with 4 seeded articles, /learn/tutorials page with 3 learning disciplines, /learn hub page, blog API, learning progress API, migration with seed data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-history-analytics/09-RESEARCH.md

Key existing files:
- mystiqor-build/src/types/database.generated.ts — blog_posts and learning_progress table types
- mystiqor-build/src/types/database.ts — custom types with LearningLevel
- mystiqor-build/004_schema_fixes.sql — last migration file

<interfaces>
From database.generated.ts (blog_posts):
```typescript
blog_posts: {
  Row: {
    author: string; category: string; content: string; excerpt: string | null;
    id: string; is_published: boolean | null; published_at: string | null;
    read_time_minutes: number | null; slug: string; tags: string[] | null; title: string;
  }
}
```

From database.generated.ts (learning_progress):
```typescript
learning_progress: {
  Row: {
    completed: boolean | null; created_at: string | null; discipline: string;
    id: string; last_studied: string | null; level: string | null;
    quiz_score: number | null; study_time_minutes: number | null;
    topic: string; user_id: string;
  }
}
```

From database.ts (custom):
```typescript
learning_progress: {
  Row: {
    id: string; user_id: string; discipline: string; level: LearningLevel;
    topic: string; completed: boolean; quiz_score: number | null;
    study_time_minutes: number; last_studied: string | null;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Blog API + Learning Progress API + Zod schemas</name>
  <files>
    mystiqor-build/005_schema_fixes.sql,
    mystiqor-build/src/app/api/blog/route.ts,
    mystiqor-build/src/app/api/learn/progress/route.ts,
    mystiqor-build/src/lib/validations/learning.ts
  </files>
  <read_first>
    mystiqor-build/004_schema_fixes.sql,
    mystiqor-build/src/types/database.generated.ts,
    mystiqor-build/src/types/database.ts,
    mystiqor-build/src/lib/supabase/server.ts,
    mystiqor-build/src/app/api/coach/conversations/route.ts
  </read_first>
  <action>
1. Create `mystiqor-build/005_schema_fixes.sql`:
   - Add unique constraint on learning_progress(user_id, discipline, topic) if not exists:
     ```sql
     DO $$ BEGIN
       IF NOT EXISTS (
         SELECT 1 FROM pg_constraint WHERE conname = 'learning_progress_user_discipline_topic_key'
       ) THEN
         ALTER TABLE learning_progress ADD CONSTRAINT learning_progress_user_discipline_topic_key UNIQUE (user_id, discipline, topic);
       END IF;
     END $$;
     ```
   - Seed 4 blog articles from BASE44 (Hebrew content):
     ```sql
     INSERT INTO blog_posts (title, slug, content, excerpt, author, category, tags, read_time_minutes, is_published, published_at)
     VALUES
     ('מדריך נומרולוגיה למתחילים', 'numerology-beginners-guide',
      'נומרולוגיה היא המדע העתיק של המספרים ומשמעותם. כל מספר מייצג אנרגיה ייחודית שמשפיעה על חיינו...\n\n## מספרי הליבה\n\nהמספרים הבסיסיים בנומרולוגיה הם 1-9, ולכל אחד מהם משמעות עמוקה:\n\n**מספר 1** - מנהיגות, עצמאות, התחלות חדשות\n**מספר 2** - שיתוף פעולה, דיפלומטיה, איזון\n**מספר 3** - יצירתיות, ביטוי עצמי, שמחה\n**מספר 4** - יציבות, סדר, עבודה קשה\n**מספר 5** - חופש, הרפתקה, שינוי\n**מספר 6** - אהבה, אחריות, משפחה\n**מספר 7** - רוחניות, חקירה, חוכמה\n**מספר 8** - כוח, הצלחה, שפע\n**מספר 9** - סיום, חמלה, אוניברסליות\n\n## מספר נתיב החיים\n\nזהו המספר החשוב ביותר. חשב אותו מתאריך הלידה שלך על ידי חיבור כל הספרות וצמצום למספר בודד.\n\n## מספרי מאסטר\n\nהמספרים 11, 22 ו-33 הם מספרי מאסטר — הם נושאים אנרגיה כפולה ומשמעות עמוקה יותר.',
      'גלה את עולם המספרים ומשמעותם העמוקה בחייך', 'MystiQor', 'נומרולוגיה',
      ARRAY['נומרולוגיה', 'מתחילים', 'מדריך'], 5, true, NOW()),

     ('אסטרולוגיה — מדריך מקיף לכוכבי הלכת', 'astrology-planets-guide',
      'כוכבי הלכת באסטרולוגיה מייצגים כוחות בסיסיים שמעצבים את האישיות שלנו...\n\n## השמש\nהשמש מייצגת את הזהות המרכזית שלנו, את האגו והחיוניות. מיקום השמש במפה שלנו מגלה את המהות שלנו.\n\n## הירח\nהירח שולט ברגשות, באינטואיציה ובצרכים הרגשיים העמוקים. הוא מייצג את הנשיות ואת הצד הלא-מודע.\n\n## מרקורי\nכוכב התקשורת, החשיבה והלמידה. מיקומו מגלה איך אנחנו חושבים ומתקשרים.\n\n## ונוס\nכוכב האהבה, היופי וההנאה. ונוס מגלה מה אנחנו מעריכים ביחסים ובאסתטיקה.\n\n## מאדים\nכוכב הפעולה, האנרגיה והתשוקה. מאדים מראה איך אנחנו פועלים ולוחמים.\n\n## צדק\nכוכב ההרחבה, המזל והחוכמה. צדק מגלה איפה אנחנו מתרחבים וגדלים.\n\n## שבתאי\nכוכב המבנה, המשמעת וההגבלה. שבתאי מלמד אותנו שיעורי חיים חשובים.',
      'הכר את כוכבי הלכת ואת השפעתם על חייך', 'MystiQor', 'אסטרולוגיה',
      ARRAY['אסטרולוגיה', 'כוכבי לכת', 'מדריך'], 7, true, NOW()),

     ('קריאת כף יד — מדריך בסיסי', 'palmistry-basic-guide',
      'כירומנטיה, או קריאת כף יד, היא אמנות עתיקה של פענוח תכונות אישיות ומסלול חיים דרך קווי כף היד...\n\n## הקווים העיקריים\n\n**קו החיים** — עוקב סביב בסיס האגודל. מייצג חיוניות ואיכות חיים, לא אורך חיים.\n\n**קו הלב** — חוצה את חלקו העליון של כף היד. מגלה את הגישה שלנו ליחסים ורגשות.\n\n**קו הראש** — חוצה את אמצע כף היד. מייצג את סגנון החשיבה והגישה האינטלקטואלית.\n\n**קו הגורל** — קו אנכי שעולה מבסיס כף היד. קשור לקריירה ולמסלול חיים.\n\n## טיפים לקריאה\n\n- בדוק תמיד את שתי הידיים — יד דומיננטית מציגה הווה ועתיד, יד לא-דומיננטית מציגה פוטנציאל מולד.\n- שים לב לעומק הקווים, לאורכם ולחיבורים ביניהם.\n- תרגל על ידיים רבות כדי לפתח את היכולת.',
      'למד לקרוא את הקווים העיקריים בכף היד', 'MystiQor', 'כירומנטיה',
      ARRAY['כירומנטיה', 'קריאת כף יד', 'מתחילים'], 4, true, NOW()),

     ('גרפולוגיה — מה כתב היד שלך חושף', 'graphology-handwriting-secrets',
      'גרפולוגיה היא מדע ניתוח כתב היד שחושף תכונות אישיות עמוקות...\n\n## גודל הכתב\n\n**כתב גדול** — אישיות חברתית, חיפוש תשומת לב, אקסטרוברטיות.\n**כתב קטן** — ריכוזיות, מחשבתיות, אינטרוברטיות.\n**כתב בינוני** — איזון ויכולת הסתגלות.\n\n## נטייה\n\n**נטייה ימינה** — פתיחות רגשית, חברתיות, ספונטניות.\n**נטייה שמאלית** — שמרנות, ריסון עצמי, חשיבה לפני פעולה.\n**כתב ישר** — שליטה עצמית, אובייקטיביות, רציונליות.\n\n## לחץ\n\n**לחץ חזק** — אישיות אינטנסיבית, רגשות עמוקים, נחישות.\n**לחץ קל** — רגישות, עדינות, גמישות.\n\n## מרווח בין מילים\n\n**מרווח רחב** — צורך בחופש אישי, עצמאות.\n**מרווח צפוף** — צורך בקרבה, חברתיות.',
      'גלה מה כתב היד שלך מספר על האישיות שלך', 'MystiQor', 'גרפולוגיה',
      ARRAY['גרפולוגיה', 'כתב יד', 'ניתוח אישיות'], 6, true, NOW())
     ON CONFLICT (slug) DO NOTHING;
     ```
   - NOTE: If blog_posts table does not have a unique constraint on slug, add it first:
     ```sql
     DO $$ BEGIN
       IF NOT EXISTS (
         SELECT 1 FROM pg_constraint WHERE conname = 'blog_posts_slug_key'
       ) THEN
         ALTER TABLE blog_posts ADD CONSTRAINT blog_posts_slug_key UNIQUE (slug);
       END IF;
     END $$;
     ```

2. Create `mystiqor-build/src/lib/validations/learning.ts`:
   - `LearningProgressSchema`: z.object with `discipline` (z.enum(['astrology', 'drawing', 'numerology'])), `topic` (z.string().min(1).max(100)), `completed` (z.boolean()), `quiz_score` (z.number().min(0).max(100).optional()).
   - `BlogQuerySchema`: z.object with `category` (z.string().optional()), `search` (z.string().optional()), `limit` (z.number().int().min(1).max(50).default(20)), `offset` (z.number().int().min(0).default(0)).
   - Export both schemas and their inferred types.
   - JSDoc in Hebrew.

3. Create `mystiqor-build/src/app/api/blog/route.ts`:
   - GET handler. Auth not required (blog is public content, but route is inside (auth) group for layout consistency).
   - Actually, per PROTECTED_PATHS and research, route is protected. Use standard auth check.
   - Parse query params with BlogQuerySchema.
   - Query `blog_posts` with `is_published = true`, ordered by `published_at DESC`.
   - If category provided, filter `.eq('category', category)`.
   - If search provided, filter with `.ilike('title', `%${search}%`)`.
   - Apply limit/offset pagination.
   - Return `{ data: BlogPost[], meta: { total: number } }`.

4. Create `mystiqor-build/src/app/api/learn/progress/route.ts`:
   - GET: fetch all learning_progress for current user, filtered by optional `discipline` query param. Return `{ data: LearningProgressRow[] }`.
   - POST: upsert learning_progress. Accept `{ discipline, topic, completed, quiz_score? }`. Upsert on `(user_id, discipline, topic)` conflict. Update `last_studied` to now. Return `{ data: row }`.
   - Both require auth (standard pattern).
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test -f 005_schema_fixes.sql
    - grep -q "INSERT INTO blog_posts" 005_schema_fixes.sql
    - grep -q "learning_progress_user_discipline_topic_key" 005_schema_fixes.sql
    - grep -q "export const LearningProgressSchema" src/lib/validations/learning.ts
    - grep -q "export const BlogQuerySchema" src/lib/validations/learning.ts
    - grep -q "export async function GET" src/app/api/blog/route.ts
    - grep -q "is_published" src/app/api/blog/route.ts
    - grep -q "export async function GET" src/app/api/learn/progress/route.ts
    - grep -q "export async function POST" src/app/api/learn/progress/route.ts
    - grep -q "onConflict" src/app/api/learn/progress/route.ts
  </acceptance_criteria>
  <done>Migration seeds 4 blog articles and adds unique constraint. Blog API serves published posts with filtering. Learning progress API supports GET and upsert POST. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Blog page + Tutorials hub page + Learn index page + UI components</name>
  <files>
    mystiqor-build/src/components/features/blog/BlogPostCard.tsx,
    mystiqor-build/src/components/features/learn/LearningPathCard.tsx,
    mystiqor-build/src/components/features/learn/ProgressTracker.tsx,
    mystiqor-build/src/app/(auth)/learn/blog/page.tsx,
    mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx,
    mystiqor-build/src/app/(auth)/learn/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/blog/route.ts,
    mystiqor-build/src/app/api/learn/progress/route.ts,
    mystiqor-build/src/lib/validations/learning.ts,
    mystiqor-build/src/types/database.generated.ts,
    mystiqor-build/src/lib/utils/dates.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/components/features/blog/BlogPostCard.tsx`:
   - Props: `{ post: { id: string; title: string; excerpt: string | null; author: string; category: string; tags: string[] | null; read_time_minutes: number | null; published_at: string | null; content: string }; expanded: boolean; onToggle: () => void }`.
   - Renders a Card with title, excerpt, author, category Badge, read_time_minutes + " דקות קריאה", published date.
   - When expanded, shows full content text below excerpt (use whitespace-pre-wrap for line breaks).
   - Tags rendered as small Badges.
   - Button "קרא עוד" / "סגור" toggles expanded.

2. Create `mystiqor-build/src/components/features/learn/LearningPathCard.tsx`:
   - Props: `{ discipline: string; title: string; description: string; icon: LucideIcon; topics: { id: string; name: string; completed: boolean }[]; totalTopics: number; completedTopics: number; onStartTopic: (topicId: string) => void }`.
   - Renders a Card with the icon, title, description.
   - Progress bar showing completedTopics / totalTopics.
   - List of topics with checkmark for completed, circle for incomplete.
   - Each topic is clickable (calls onStartTopic).

3. Create `mystiqor-build/src/components/features/learn/ProgressTracker.tsx`:
   - Props: `{ completed: number; total: number; label: string }`.
   - Horizontal progress bar with percentage text and label.
   - Uses shadcn Progress component if available, otherwise custom div with width percentage.

4. Create `mystiqor-build/src/app/(auth)/learn/blog/page.tsx`:
   - 'use client' page.
   - State: `selectedCategory` (string | null), `searchQuery` (string), `expandedId` (string | null).
   - useQuery: fetch `/api/blog?category=${selectedCategory}&search=${searchQuery}`.
   - Render:
     a. Title "בלוג" with Newspaper icon.
     b. Search input (Input component from shadcn).
     c. Category filter buttons: "הכל", "אסטרולוגיה", "נומרולוגיה", "כירומנטיה", "גרפולוגיה".
     d. Grid of BlogPostCard components (grid-cols-1 md:grid-cols-2 gap-4).
     e. Empty state: "אין מאמרים" if no results.

5. Create `mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx`:
   - 'use client' page.
   - Define 3 learning paths as static data:
     a. Astrology — topics: ['zodiac_signs', 'planets', 'houses', 'aspects', 'transits', 'solar_return'] with Hebrew names.
     b. Numerology — topics: ['life_path', 'destiny_number', 'soul_number', 'compatibility', 'master_numbers'] with Hebrew names.
     c. Drawing — topics: ['htp_basics', 'koppitz_scoring', 'fdm_model', 'color_meaning', 'composition'] with Hebrew names.
   - useQuery: fetch `/api/learn/progress` — get user's completed topics.
   - useMutation: POST to `/api/learn/progress` to mark topic completed.
   - Render:
     a. Title "מדריכים ומסלולי למידה" with GraduationCap icon.
     b. ProgressTracker showing overall completion across all disciplines.
     c. Grid of LearningPathCard components (grid-cols-1 md:grid-cols-3).
     d. Each card maps its topics to completed/incomplete based on progress query data.
     e. Clicking a topic calls mutation with `{ discipline, topic: topicId, completed: true }` and shows a toast.

6. Create `mystiqor-build/src/app/(auth)/learn/page.tsx`:
   - Hub page for the learn section.
   - 4 cards linking to /learn/tutorials, /learn/blog, /learn/astrology, /learn/drawing.
   - Each card has an icon, title in Hebrew, brief description.
   - Use Link component for navigation.
   - Title: "מרכז למידה" with BookOpen icon.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function BlogPostCard" src/components/features/blog/BlogPostCard.tsx
    - grep -q "export function LearningPathCard" src/components/features/learn/LearningPathCard.tsx
    - grep -q "export function ProgressTracker" src/components/features/learn/ProgressTracker.tsx
    - grep -q "useQuery" src/app/\(auth\)/learn/blog/page.tsx
    - grep -q "api/blog" src/app/\(auth\)/learn/blog/page.tsx
    - grep -q "useQuery" src/app/\(auth\)/learn/tutorials/page.tsx
    - grep -q "api/learn/progress" src/app/\(auth\)/learn/tutorials/page.tsx
    - grep -q "useMutation" src/app/\(auth\)/learn/tutorials/page.tsx
    - grep -q "מרכז למידה" src/app/\(auth\)/learn/page.tsx
  </acceptance_criteria>
  <done>Blog page shows seeded articles with category filter and search. Tutorials page shows 3 learning paths with progress tracking. Learn hub provides navigation to all learning pages. tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` exits 0
2. Blog API returns published articles with category/search filtering
3. Learning progress API supports GET and upsert POST
4. Blog page renders articles with expand/collapse
5. Tutorials page shows progress bars and topic lists
6. Migration file exists with seed data and unique constraint
</verification>

<success_criteria>
- /learn/blog shows 4 seeded articles filterable by category
- /learn/tutorials shows 3 learning paths with progress tracking
- /learn hub page links to all learning sections
- Blog API returns only is_published=true posts
- Learning progress API upserts on (user_id, discipline, topic)
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-learning-history-analytics/09-02-SUMMARY.md`
</output>
