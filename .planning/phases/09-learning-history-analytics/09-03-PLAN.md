---
phase: 09-learning-history-analytics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/api/learn/tutor/astrology/route.ts
  - mystiqor-build/src/app/api/learn/tutor/drawing/route.ts
  - mystiqor-build/src/lib/validations/tutor.ts
  - mystiqor-build/src/components/features/learn/TutorChat.tsx
  - mystiqor-build/src/components/features/learn/QuickConceptButtons.tsx
  - mystiqor-build/src/app/(auth)/learn/astrology/page.tsx
  - mystiqor-build/src/app/(auth)/learn/drawing/page.tsx
autonomous: true
requirements:
  - GROW-04
  - GROW-05

must_haves:
  truths:
    - "User can send a message to the astrology tutor and receive an educational response at /learn/astrology"
    - "User can send a message to the drawing tutor and receive an educational response at /learn/drawing"
    - "Quick concept buttons provide one-click topic prompts for common questions"
    - "Tutor responses reference user's existing analyses when available"
    - "Chat history displays in chronological order within the session"
  artifacts:
    - path: "mystiqor-build/src/app/api/learn/tutor/astrology/route.ts"
      provides: "POST handler for astrology tutor LLM chat"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/api/learn/tutor/drawing/route.ts"
      provides: "POST handler for drawing tutor LLM chat"
      exports: ["POST"]
    - path: "mystiqor-build/src/components/features/learn/TutorChat.tsx"
      provides: "Shared chat UI for both tutors"
      exports: ["TutorChat"]
    - path: "mystiqor-build/src/app/(auth)/learn/astrology/page.tsx"
      provides: "Astrology tutor page with chat and concept cards"
      min_lines: 50
    - path: "mystiqor-build/src/app/(auth)/learn/drawing/page.tsx"
      provides: "Drawing tutor page with chat and concept cards"
      min_lines: 50
  key_links:
    - from: "mystiqor-build/src/app/(auth)/learn/astrology/page.tsx"
      to: "/api/learn/tutor/astrology"
      via: "useMutation POST with message"
      pattern: "api/learn/tutor/astrology"
    - from: "mystiqor-build/src/app/(auth)/learn/drawing/page.tsx"
      to: "/api/learn/tutor/drawing"
      via: "useMutation POST with message"
      pattern: "api/learn/tutor/drawing"
    - from: "mystiqor-build/src/app/api/learn/tutor/astrology/route.ts"
      to: "invokeLLM"
      via: "import from @/services/analysis/llm"
      pattern: "invokeLLM"
---

<objective>
Build the astrology tutor and drawing tutor pages with AI-driven educational chat, reusing the coach conversation pattern from Phase 7.

Purpose: Users need to learn astrology concepts (GROW-04) and drawing interpretation concepts (GROW-05) through interactive AI tutoring that references their personal analysis history.

Output: Working /learn/astrology and /learn/drawing pages with tutor chat, quick concept buttons, two tutor API routes using invokeLLM.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-history-analytics/09-RESEARCH.md

Key existing files:
- mystiqor-build/src/app/api/coach/messages/route.ts — pattern for LLM chat (POST handler)
- mystiqor-build/src/services/analysis/llm.ts — invokeLLM function
- mystiqor-build/src/app/api/coach/conversations/route.ts — conversation creation pattern

<interfaces>
From src/services/analysis/llm.ts:
```typescript
export interface LLMRequest {
  prompt: string;
  systemPrompt?: string;
  responseSchema?: Record<string, unknown>;
  zodSchema?: z.ZodSchema;
  imageUrls?: string[];
  maxTokens?: number;
  userId: string;
}
// Returns: { success: boolean; data: unknown } or { data: string } in text mode
```

From src/app/api/coach/messages/route.ts (pattern to replicate):
```typescript
// 1. Auth check
// 2. Validate input (Zod)
// 3. Fetch user's relevant analyses for context
// 4. Build system prompt with persona + context
// 5. Call invokeLLM with prompt + systemPrompt + maxTokens + userId
// 6. Return { data: { role: 'assistant', content: replyText } }
```

Coach messages pattern (simplified):
```typescript
const response = await invokeLLM({
  prompt: message,
  systemPrompt: fullSystemPrompt,
  maxTokens: 2048,
  userId: user.id,
});
const replyText = typeof response.data === 'string' ? response.data : String(response.data);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tutor API routes + Zod validation schema</name>
  <files>
    mystiqor-build/src/lib/validations/tutor.ts,
    mystiqor-build/src/app/api/learn/tutor/astrology/route.ts,
    mystiqor-build/src/app/api/learn/tutor/drawing/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/coach/messages/route.ts,
    mystiqor-build/src/services/analysis/llm.ts,
    mystiqor-build/src/lib/supabase/server.ts,
    mystiqor-build/src/types/database.generated.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/lib/validations/tutor.ts`:
   - `TutorMessageSchema`: z.object with `message` (z.string().min(1, 'הודעה ריקה').max(3000, 'הודעה ארוכה מדי')).
   - Export schema and inferred type.
   - JSDoc in Hebrew.

2. Create `mystiqor-build/src/app/api/learn/tutor/astrology/route.ts`:
   - POST handler following the coach/messages pattern.
   - Auth check: createClient + getUser. Return 401 if not authenticated.
   - Validate body with TutorMessageSchema.
   - Fetch user's 3 most recent astrology analyses for context:
     ```typescript
     const { data: analyses } = await supabase
       .from('analyses')
       .select('results, summary, created_at')
       .eq('user_id', user.id)
       .eq('tool_type', 'astrology')
       .order('created_at', { ascending: false })
       .limit(3);
     ```
   - Fetch user's learning_progress for discipline='astrology':
     ```typescript
     // eslint-disable-next-line @typescript-eslint/no-explicit-any
     const { data: progress } = await (supabase as any)
       .from('learning_progress')
       .select('topic, completed, level')
       .eq('user_id', user.id)
       .eq('discipline', 'astrology');
     ```
   - Build system prompt in Hebrew:
     ```
     אתה מורה מומחה באסטרולוגיה. תפקידך ללמד את המשתמש מושגי אסטרולוגיה בצורה ברורה ומותאמת לרמתו.
     ענה בעברית בלבד. היה סבלני, השתמש בדוגמאות, והתייחס לניתוחים של המשתמש אם רלוונטי.

     ### הקשר המשתמש:
     נושאים שלמד: ${completedTopics.join(', ') || 'טרם התחיל'}
     ניתוחים קיימים: ${analysisContext || 'אין ניתוחים עדיין'}
     ```
   - Call invokeLLM with prompt=body.message, systemPrompt, maxTokens=1500, userId=user.id.
   - Return `{ data: { role: 'assistant', content: replyText } }`.
   - Note: This is a STATELESS tutor (no conversation persistence) — simpler than coach. Each request is independent.

3. Create `mystiqor-build/src/app/api/learn/tutor/drawing/route.ts`:
   - Same pattern as astrology tutor but:
   - Fetch analyses with tool_type='drawing' instead of 'astrology'.
   - Fetch learning_progress with discipline='drawing'.
   - System prompt focuses on drawing interpretation:
     ```
     אתה מורה מומחה בניתוח ציורים ופסיכולוגיה של אמנות. תפקידך ללמד את המשתמש מושגי ניתוח ציורים — HTP, Koppitz, FDM ומשמעות צבעים.
     ענה בעברית בלבד. היה סבלני, השתמש בדוגמאות ויזואליות, והתייחס לניתוחי הציורים של המשתמש אם רלוונטי.

     ### הקשר המשתמש:
     נושאים שלמד: ${completedTopics.join(', ') || 'טרם התחיל'}
     ניתוחים קיימים: ${analysisContext || 'אין ניתוחים עדיין'}
     ```
   - maxTokens: 1500.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const TutorMessageSchema" src/lib/validations/tutor.ts
    - grep -q "export async function POST" src/app/api/learn/tutor/astrology/route.ts
    - grep -q "invokeLLM" src/app/api/learn/tutor/astrology/route.ts
    - grep -q "tool_type.*astrology" src/app/api/learn/tutor/astrology/route.ts
    - grep -q "export async function POST" src/app/api/learn/tutor/drawing/route.ts
    - grep -q "invokeLLM" src/app/api/learn/tutor/drawing/route.ts
    - grep -q "tool_type.*drawing" src/app/api/learn/tutor/drawing/route.ts
  </acceptance_criteria>
  <done>Both tutor API routes accept a message, build context from user analyses and learning progress, call invokeLLM, and return the tutor response. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tutor chat components + Astrology and Drawing tutor pages</name>
  <files>
    mystiqor-build/src/components/features/learn/TutorChat.tsx,
    mystiqor-build/src/components/features/learn/QuickConceptButtons.tsx,
    mystiqor-build/src/app/(auth)/learn/astrology/page.tsx,
    mystiqor-build/src/app/(auth)/learn/drawing/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/learn/tutor/astrology/route.ts,
    mystiqor-build/src/app/api/learn/tutor/drawing/route.ts,
    mystiqor-build/src/lib/validations/tutor.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/components/features/learn/TutorChat.tsx`:
   - Props: `{ apiEndpoint: string; placeholder: string; emptyMessage: string }`.
   - State: `messages` (array of `{ role: 'user' | 'assistant'; content: string }`), `input` (string), `isLoading` (boolean).
   - useMutation: POST to apiEndpoint with `{ message: input }`. On success, push both user message and assistant reply to messages array.
   - Render:
     a. Scrollable message container (max-h-[500px] overflow-y-auto).
     b. Messages styled differently: user (bg-purple-600/20 text-end) vs assistant (bg-muted text-start).
     c. Input area at bottom: Input + Send Button (SendHorizontal icon from lucide-react). Button disabled when loading or input empty.
     d. Loading indicator: 3 bouncing dots animation when isLoading.
     e. Empty state: emptyMessage text when messages array is empty.
   - Handle Enter key to send (not Shift+Enter — that should create newline).
   - Actually, keep it simple: single-line Input, Enter sends.

2. Create `mystiqor-build/src/components/features/learn/QuickConceptButtons.tsx`:
   - Props: `{ concepts: { label: string; prompt: string }[]; onSelect: (prompt: string) => void; disabled?: boolean }`.
   - Render a flex wrap of Buttons (variant=outline, size=sm).
   - Clicking a button calls onSelect with the concept's prompt string.

3. Create `mystiqor-build/src/app/(auth)/learn/astrology/page.tsx`:
   - 'use client' page.
   - Title: "מורה אסטרולוגיה" with Stars icon.
   - Description: "שאל שאלות על אסטרולוגיה וקבל הסברים מותאמים אישית".
   - QuickConceptButtons with astrology concepts:
     - "מה זה מזל עולה?" → "הסבר לי מה זה מזל עולה ואיך הוא משפיע על האישיות"
     - "כוכבי הלכת" → "ספר לי על כוכבי הלכת ומשמעותם באסטרולוגיה"
     - "בתים אסטרולוגיים" → "הסבר לי את 12 הבתים האסטרולוגיים"
     - "אספקטים" → "מהם אספקטים באסטרולוגיה ואילו סוגים יש?"
     - "טרנזיטים" → "הסבר לי מה זה טרנזיטים ואיך הם משפיעים"
     - "Solar Return" → "מהו Solar Return ומה אפשר ללמוד ממנו?"
   - TutorChat with apiEndpoint="/api/learn/tutor/astrology".
   - onSelect from QuickConceptButtons should set the message in TutorChat and submit it. To achieve this, TutorChat should also accept an optional `externalMessage` prop (string | null) that triggers a send when set. Or simpler: pass a ref/callback. Simplest approach: QuickConceptButtons onSelect sets a state that TutorChat reads.
   - Actually simplest: TutorChat exposes a `sendMessage` callback via a forwardRef/useImperativeHandle pattern. But that's complex. Instead: have the page manage the message state and pass it to TutorChat as a controlled input, or have QuickConceptButtons call the same mutation directly. Let's go with: page manages messages array and mutation, TutorChat is purely presentational, QuickConceptButtons calls the same send function. This keeps both components simple.
   - REVISED approach: Page owns state. TutorChat displays messages and has the input. Page passes `onSend` callback. QuickConceptButtons also calls `onSend`. Both trigger the same mutation.

4. Create `mystiqor-build/src/app/(auth)/learn/drawing/page.tsx`:
   - Same pattern as astrology but with drawing-specific concepts:
     - "מה זה HTP?" → "הסבר לי את שיטת HTP — בית, עץ, אדם"
     - "ציון Koppitz" → "מהו ציון Koppitz ואיך מחשבים אותו?"
     - "מודל FDM" → "הסבר לי את מודל FDM לניתוח ציורים"
     - "משמעות צבעים" → "מה משמעות הצבעים השונים בציורי ילדים?"
     - "קומפוזיציה" → "איך מנתחים קומפוזיציה ומיקום בציור?"
   - TutorChat with apiEndpoint="/api/learn/tutor/drawing".
   - Title: "מורה ציור ואמנות" with Palette icon.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function TutorChat" src/components/features/learn/TutorChat.tsx
    - grep -q "export function QuickConceptButtons" src/components/features/learn/QuickConceptButtons.tsx
    - grep -q "api/learn/tutor/astrology" src/app/\(auth\)/learn/astrology/page.tsx
    - grep -q "TutorChat" src/app/\(auth\)/learn/astrology/page.tsx
    - grep -q "QuickConceptButtons" src/app/\(auth\)/learn/astrology/page.tsx
    - grep -q "api/learn/tutor/drawing" src/app/\(auth\)/learn/drawing/page.tsx
    - grep -q "TutorChat" src/app/\(auth\)/learn/drawing/page.tsx
    - grep -q "QuickConceptButtons" src/app/\(auth\)/learn/drawing/page.tsx
  </acceptance_criteria>
  <done>Astrology tutor page has chat interface with 6 quick concept buttons. Drawing tutor page has chat interface with 5 quick concept buttons. Both call their respective API endpoints and display responses inline. tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` exits 0
2. Astrology tutor API returns AI-generated educational responses
3. Drawing tutor API returns AI-generated educational responses
4. TutorChat component renders messages and sends via POST
5. Quick concept buttons trigger pre-defined educational queries
</verification>

<success_criteria>
- /learn/astrology shows tutor chat with quick concept buttons
- /learn/drawing shows tutor chat with quick concept buttons
- Sending a message calls the tutor API and displays the response
- Quick concept buttons populate the chat with pre-defined questions
- Tutor responses are in Hebrew and reference user's analyses when available
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-learning-history-analytics/09-03-SUMMARY.md`
</output>
