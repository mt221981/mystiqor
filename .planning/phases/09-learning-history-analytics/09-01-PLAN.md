---
phase: 09-learning-history-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/supabase/middleware.ts
  - mystiqor-build/src/components/layouts/Sidebar.tsx
  - mystiqor-build/src/lib/constants/tool-names.ts
  - mystiqor-build/src/components/features/history/HistoryFilters.tsx
  - mystiqor-build/src/components/features/history/AnalysisCard.tsx
  - mystiqor-build/src/components/features/history/ComparePanel.tsx
  - mystiqor-build/src/app/(auth)/history/page.tsx
  - mystiqor-build/src/app/(auth)/history/compare/page.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - ASTR-08

must_haves:
  truths:
    - "User sees a paginated, filterable list of all past analyses on /history"
    - "User can filter analyses by tool type using filter buttons"
    - "User can toggle between list view and timeline view on /history"
    - "User can select two analyses of the same type and navigate to /history/compare to see them side-by-side"
    - "Astrology readings appear in the history list filtered by tool_type=astrology"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/history/page.tsx"
      provides: "History page with filters, pagination, timeline toggle"
      min_lines: 80
    - path: "mystiqor-build/src/app/(auth)/history/compare/page.tsx"
      provides: "Side-by-side comparison of two analyses"
      min_lines: 60
    - path: "mystiqor-build/src/lib/constants/tool-names.ts"
      provides: "Shared TOOL_NAMES constant"
      exports: ["TOOL_NAMES"]
    - path: "mystiqor-build/src/components/features/history/HistoryFilters.tsx"
      provides: "Tool type filter buttons + date range"
      exports: ["HistoryFilters"]
    - path: "mystiqor-build/src/components/features/history/AnalysisCard.tsx"
      provides: "Single analysis card for grid/list view"
      exports: ["AnalysisCard"]
    - path: "mystiqor-build/src/components/features/history/ComparePanel.tsx"
      provides: "Side-by-side diff layout"
      exports: ["ComparePanel"]
  key_links:
    - from: "mystiqor-build/src/app/(auth)/history/page.tsx"
      to: "/api/analysis"
      via: "useQuery + fetch with tool_type, limit, offset params"
      pattern: "fetch.*api/analysis"
    - from: "mystiqor-build/src/app/(auth)/history/compare/page.tsx"
      to: "/api/analysis"
      via: "fetch with include_results=true"
      pattern: "include_results=true"
    - from: "mystiqor-build/src/lib/supabase/middleware.ts"
      to: "PROTECTED_PATHS"
      via: "array includes /history, /learn, /analytics, /blog"
      pattern: "/history|/learn|/analytics|/blog"
---

<objective>
Build the analysis history page with filterable list, timeline visualization, side-by-side comparison, and the shared infrastructure (PROTECTED_PATHS, Sidebar updates, TOOL_NAMES constant) needed by the entire Phase 9.

Purpose: Users need to browse, filter, and compare their complete analysis history across all tool types. This plan also wires all Phase 9 routes into the middleware and sidebar so Plans 02-04 can focus on their own pages without touching shared files.

Output: Working /history and /history/compare pages, updated sidebar navigation with history/learn/analytics/blog entries, PROTECTED_PATHS updated for all Phase 9 routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-history-analytics/09-RESEARCH.md

Key existing files:
- mystiqor-build/src/app/api/analysis/route.ts — GET supports tool_type, limit, offset, include_results=true
- mystiqor-build/src/components/features/shared/AnalysisHistory.tsx — has TOOL_NAMES (to be extracted)
- mystiqor-build/src/lib/validations/analysis.ts — AnalysisQuerySchema with TOOL_TYPES enum
- mystiqor-build/src/types/analysis.ts — ToolType union

<interfaces>
From src/types/analysis.ts:
```typescript
export type ToolType =
  | 'numerology' | 'astrology' | 'palmistry' | 'graphology'
  | 'tarot' | 'drawing' | 'dream' | 'career'
  | 'compatibility' | 'synastry' | 'solar_return' | 'transits'
  | 'human_design' | 'personality' | 'document' | 'question'
  | 'relationship' | 'synthesis';
```

From src/app/api/analysis/route.ts GET response:
```typescript
{ data: AnalysisRow[]; meta: { offset: number; limit: number; total: number } }
// With include_results=true: each row also has `results` and `input_data` fields
```

From src/components/features/shared/AnalysisHistory.tsx:
```typescript
const TOOL_NAMES: Record<string, string> = {
  numerology: 'נומרולוגיה', astrology: 'אסטרולוגיה', palmistry: 'כירומנטיה',
  graphology: 'גרפולוגיה', tarot: 'טארוט', drawing: 'ניתוח ציורים',
  dream: 'חלומות', career: 'קריירה', compatibility: 'תאימות',
  synastry: 'סינסטרי', solar_return: 'Solar Return', transits: 'טרנזיטים',
  human_design: 'Human Design', personality: 'אישיות', document: 'מסמכים',
  question: 'שאלה', relationship: 'יחסים', synthesis: 'סינתזה',
};
```

From src/lib/supabase/middleware.ts:
```typescript
const PROTECTED_PATHS = [
  '/dashboard', '/tools', '/onboarding', '/profile', '/settings',
  '/subscription', '/coach', '/goals', '/mood', '/journal',
];
```

Sidebar NAV_SECTIONS "למידה" section currently has:
```typescript
{ label: 'מדריכים', href: '/tutorials', icon: GraduationCap },
{ label: 'בלוג', href: '/blog', icon: Newspaper },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared infrastructure — TOOL_NAMES constant, PROTECTED_PATHS, Sidebar navigation</name>
  <files>
    mystiqor-build/src/lib/constants/tool-names.ts,
    mystiqor-build/src/components/features/shared/AnalysisHistory.tsx,
    mystiqor-build/src/lib/supabase/middleware.ts,
    mystiqor-build/src/components/layouts/Sidebar.tsx
  </files>
  <read_first>
    mystiqor-build/src/components/features/shared/AnalysisHistory.tsx,
    mystiqor-build/src/lib/supabase/middleware.ts,
    mystiqor-build/src/components/layouts/Sidebar.tsx,
    mystiqor-build/src/types/analysis.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/lib/constants/tool-names.ts`:
   - Export `TOOL_NAMES: Record<string, string>` with all 18 tool type Hebrew names (copy from AnalysisHistory.tsx).
   - Export `TOOL_ICONS: Record<string, string>` mapping each ToolType to a lucide-react icon name string (Hash, Stars, PenTool, Palette, Hand, Layers, Fingerprint, Moon, Heart, Briefcase, FileText, HelpCircle, Sparkles, Brain, FileText, HelpCircle, Heart, Sparkles).
   - Add JSDoc in Hebrew.

2. Update `mystiqor-build/src/components/features/shared/AnalysisHistory.tsx`:
   - Replace the local `TOOL_NAMES` const with `import { TOOL_NAMES } from '@/lib/constants/tool-names'`.
   - Remove the local `TOOL_NAMES` declaration (lines 26-45).
   - Keep everything else unchanged.

3. Update `mystiqor-build/src/lib/supabase/middleware.ts`:
   - Add `/history`, `/learn`, `/analytics`, `/blog` to the PROTECTED_PATHS array.
   - These four entries cover all Phase 9 routes.

4. Update `mystiqor-build/src/components/layouts/Sidebar.tsx`:
   - Add `History, BarChart3, GitCompare` to the lucide-react import.
   - In the "למידה" section, update hrefs:
     - Change `{ label: 'מדריכים', href: '/tutorials', icon: GraduationCap }` to `{ label: 'מדריכים', href: '/learn/tutorials', icon: GraduationCap }`.
     - Change `{ label: 'בלוג', href: '/blog', icon: Newspaper }` to `{ label: 'בלוג', href: '/learn/blog', icon: Newspaper }`.
     - Add `{ label: 'מורה אסטרולוגיה', href: '/learn/astrology', icon: Stars }`.
     - Add `{ label: 'מורה ציור', href: '/learn/drawing', icon: Palette }`.
   - Add a new "היסטוריה ואנליטיקה" section AFTER "למידה" and BEFORE "חשבון":
     ```typescript
     {
       title: 'היסטוריה ואנליטיקה',
       items: [
         { label: 'היסטוריית ניתוחים', href: '/history', icon: History },
         { label: 'השוואת ניתוחים', href: '/history/compare', icon: GitCompare },
         { label: 'אנליטיקה אישית', href: '/analytics', icon: BarChart3 },
       ],
     },
     ```
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const TOOL_NAMES" src/lib/constants/tool-names.ts
    - grep -q "from '@/lib/constants/tool-names'" src/components/features/shared/AnalysisHistory.tsx
    - grep -q "'/history'" src/lib/supabase/middleware.ts
    - grep -q "'/learn'" src/lib/supabase/middleware.ts
    - grep -q "'/analytics'" src/lib/supabase/middleware.ts
    - grep -q "'/blog'" src/lib/supabase/middleware.ts
    - grep -q "היסטוריה ואנליטיקה" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/tutorials" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/blog" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/astrology" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/drawing" src/components/layouts/Sidebar.tsx
  </acceptance_criteria>
  <done>TOOL_NAMES extracted to shared constant. PROTECTED_PATHS includes /history, /learn, /analytics, /blog. Sidebar has correct navigation entries for all Phase 9 pages. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: History page + Compare page with filters, pagination, timeline toggle</name>
  <files>
    mystiqor-build/src/components/features/history/HistoryFilters.tsx,
    mystiqor-build/src/components/features/history/AnalysisCard.tsx,
    mystiqor-build/src/components/features/history/ComparePanel.tsx,
    mystiqor-build/src/app/(auth)/history/page.tsx,
    mystiqor-build/src/app/(auth)/history/compare/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/analysis/route.ts,
    mystiqor-build/src/lib/validations/analysis.ts,
    mystiqor-build/src/types/analysis.ts,
    mystiqor-build/src/lib/constants/tool-names.ts,
    mystiqor-build/src/components/features/shared/AnalysisHistory.tsx,
    mystiqor-build/src/lib/utils/dates.ts
  </read_first>
  <action>
1. Create `mystiqor-build/src/components/features/history/HistoryFilters.tsx`:
   - Props: `{ selectedTool: string | null; onToolChange: (tool: string | null) => void; availableTools: string[] }`.
   - Render a row of filter Buttons: "הכל" (active when null) + one button per tool type from availableTools.
   - Use `TOOL_NAMES` from `@/lib/constants/tool-names` for Hebrew labels.
   - Tailwind: flex flex-wrap gap-2, variant=default for active, variant=outline for inactive.

2. Create `mystiqor-build/src/components/features/history/AnalysisCard.tsx`:
   - Props: `{ analysis: { id: string; tool_type: string; summary: string | null; confidence_score: number | null; created_at: string }; selected?: boolean; onSelect?: (id: string) => void }`.
   - Render a Card with: Badge (tool name), summary text (truncated), relative date (use formatRelativeDate from @/lib/utils/dates), confidence score as a subtle percentage.
   - If onSelect provided, show a checkbox-style selection indicator.
   - If selected, apply ring-2 ring-purple-500 border style.
   - Clicking card calls onSelect if provided, otherwise links to `/history?id=${analysis.id}`.

3. Create `mystiqor-build/src/components/features/history/ComparePanel.tsx`:
   - Props: `{ left: AnalysisWithResults; right: AnalysisWithResults }` where AnalysisWithResults = `{ id: string; tool_type: string; summary: string | null; results: Record<string, unknown>; input_data: Record<string, unknown>; created_at: string }`.
   - Render two columns side-by-side (grid grid-cols-1 md:grid-cols-2 gap-6).
   - Each column shows: Badge with tool name, date, summary text, and a `<pre>` or formatted JSON view of results (use JSON.stringify with indentation, wrapped in a Card).
   - Hebrew heading: "השוואת ניתוחים".

4. Create `mystiqor-build/src/app/(auth)/history/page.tsx`:
   - 'use client' page.
   - State: `page` (number, default 0), `toolFilter` (string | null), `viewMode` ('list' | 'timeline'), `selectedIds` (Set<string>).
   - useQuery: fetch `/api/analysis?limit=20&offset=${page*20}&tool_type=${toolFilter}` — queryKey: `['analyses', toolFilter, page]`.
   - Render:
     a. Page title "היסטוריית ניתוחים" with History icon.
     b. HistoryFilters component for tool filtering. Get availableTools from a separate useQuery to `/api/analysis?limit=200` (all, to extract unique tool_types — or better, use the data from the paginated query plus a Set).
     c. View toggle buttons: list icon / timeline icon to switch viewMode.
     d. In 'list' view: grid of AnalysisCard components (grid-cols-1 md:grid-cols-2 lg:grid-cols-3). Each card has onSelect to toggle selectedIds.
     e. In 'timeline' view: render analyses sorted by created_at as a vertical timeline — each item is a small card with date on the left/right alternating, connected by a vertical line (pure Tailwind, no Recharts needed for this).
     f. Pagination: "הקודם" / "הבא" buttons at bottom, disabled when at start/end. Show "מציג X-Y מתוך Z".
     g. Compare button: when selectedIds.size === 2, show a "השווה ניתוחים" button that navigates to `/history/compare?ids=${[...selectedIds].join(',')}`.
     h. ASTR-08: The filter for "אסטרולוגיה" effectively serves as the astrology readings history — no separate page needed.
   - Empty state: "אין ניתוחים עדיין. התחל להשתמש בכלים כדי לראות היסטוריה." with an icon.

5. Create `mystiqor-build/src/app/(auth)/history/compare/page.tsx`:
   - 'use client' page.
   - Read `ids` from searchParams via `useSearchParams()` wrapped in Suspense.
   - Split ids by comma, validate exactly 2 UUIDs.
   - useQuery for each ID: fetch `/api/analysis?include_results=true&limit=200` and find by id client-side (the existing API does not have a single-by-ID endpoint, so fetch with include_results and filter).
   - If both loaded, render ComparePanel with left and right.
   - If IDs missing/invalid, show error message with Link back to /history.
   - Hebrew heading: "השוואת ניתוחים".
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function HistoryFilters" src/components/features/history/HistoryFilters.tsx
    - grep -q "export function AnalysisCard" src/components/features/history/AnalysisCard.tsx
    - grep -q "export function ComparePanel" src/components/features/history/ComparePanel.tsx
    - grep -q "useQuery" src/app/\(auth\)/history/page.tsx
    - grep -q "api/analysis" src/app/\(auth\)/history/page.tsx
    - grep -q "timeline" src/app/\(auth\)/history/page.tsx
    - grep -q "useSearchParams" src/app/\(auth\)/history/compare/page.tsx
    - grep -q "include_results" src/app/\(auth\)/history/compare/page.tsx
    - grep -q "ComparePanel" src/app/\(auth\)/history/compare/page.tsx
  </acceptance_criteria>
  <done>History page shows paginated, filterable analysis list with list/timeline toggle. Compare page shows two analyses side-by-side. Astrology readings are accessible via the tool_type=astrology filter. tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` exits 0
2. PROTECTED_PATHS in middleware.ts includes /history, /learn, /analytics, /blog
3. Sidebar has entries for all Phase 9 routes with correct hrefs
4. TOOL_NAMES is shared (not duplicated in AnalysisHistory.tsx)
5. History page uses existing GET /api/analysis with pagination
6. Compare page uses include_results=true to fetch full results
</verification>

<success_criteria>
- History page renders at /history with filterable, paginated analysis cards
- Timeline view toggle shows analyses on a vertical timeline
- Selecting 2 analyses enables compare navigation
- Compare page at /history/compare?ids=X,Y shows side-by-side results
- Astrology filter serves as ASTR-08 readings history
- All Phase 9 routes protected via middleware
- Sidebar navigation updated for all Phase 9 pages
- tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-learning-history-analytics/09-01-SUMMARY.md`
</output>
