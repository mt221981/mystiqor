---
phase: 09-learning-history-analytics
plan: 05
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
files_modified: []
autonomous: false
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - ASTR-08
  - GROW-02
  - GROW-03
  - GROW-04
  - GROW-05
  - UX-09

must_haves:
  truths:
    - "tsc --noEmit exits 0 with all Phase 9 code"
    - "All Phase 9 routes are protected via PROTECTED_PATHS"
    - "Sidebar navigation links to all Phase 9 pages with correct hrefs"
    - "All Phase 9 pages render without crashes"
    - "TOOL_NAMES is imported from shared constant (not duplicated)"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 9 implementation: TypeScript compilation, TOOL_NAMES deduplication, route protection, sidebar navigation accuracy, and human verification of all pages.

Purpose: Catch integration issues between the 4 parallel Wave 1 plans, deduplicate any TOOL_NAMES copies created independently, verify all routes are protected, and get human approval on every feature.

Output: Verified Phase 9 with all 9 requirements confirmed working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-history-analytics/09-01-SUMMARY.md
@.planning/phases/09-learning-history-analytics/09-02-SUMMARY.md
@.planning/phases/09-learning-history-analytics/09-03-SUMMARY.md
@.planning/phases/09-learning-history-analytics/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration verification — TypeScript, TOOL_NAMES deduplication, route audit</name>
  <files></files>
  <read_first>
    mystiqor-build/src/lib/constants/tool-names.ts,
    mystiqor-build/src/components/features/shared/AnalysisHistory.tsx,
    mystiqor-build/src/app/(auth)/analytics/page.tsx,
    mystiqor-build/src/lib/supabase/middleware.ts,
    mystiqor-build/src/components/layouts/Sidebar.tsx
  </read_first>
  <action>
1. Run `cd mystiqor-build && npx tsc --noEmit` — fix any TypeScript errors.

2. TOOL_NAMES deduplication audit:
   - Check if `src/lib/constants/tool-names.ts` exists with the shared TOOL_NAMES export (created by Plan 09-01).
   - Check if `src/app/(auth)/analytics/page.tsx` has a local TOOL_NAMES (Plan 09-04 may have created one as a fallback).
   - If local copy exists in analytics page, replace it with `import { TOOL_NAMES } from '@/lib/constants/tool-names'`.
   - Verify `src/components/features/shared/AnalysisHistory.tsx` imports from the shared constant (done by Plan 09-01).

3. Route protection audit:
   - Verify PROTECTED_PATHS in middleware.ts includes: /history, /learn, /analytics, /blog.
   - If any are missing (possible if Plan 09-01 was implemented differently), add them.

4. Sidebar navigation audit:
   - Verify Sidebar.tsx has entries for: /history, /history/compare, /analytics, /learn/tutorials, /learn/blog, /learn/astrology, /learn/drawing.
   - Fix any mismatches between sidebar hrefs and actual page routes.

5. Re-run `cd mystiqor-build && npx tsc --noEmit` — confirm zero errors after fixes.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -rq "from '@/lib/constants/tool-names'" src/components/features/shared/AnalysisHistory.tsx
    - grep -q "'/history'" src/lib/supabase/middleware.ts
    - grep -q "'/learn'" src/lib/supabase/middleware.ts
    - grep -q "'/analytics'" src/lib/supabase/middleware.ts
    - grep -q "/learn/tutorials" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/blog" src/components/layouts/Sidebar.tsx
    - grep -q "/learn/astrology" src/components/layouts/Sidebar.tsx
  </acceptance_criteria>
  <done>TypeScript compiles with zero errors. TOOL_NAMES has a single shared source. All Phase 9 routes are protected. Sidebar navigation matches actual routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 9 — Learning + History + Analytics:
1. History page (/history) — filterable, paginated analysis list with timeline toggle
2. Compare page (/history/compare) — side-by-side analysis comparison
3. Blog (/learn/blog) — 4 educational articles with category filter and search
4. Tutorials (/learn/tutorials) — 3 learning paths with progress tracking
5. Astrology tutor (/learn/astrology) — AI chat with concept buttons
6. Drawing tutor (/learn/drawing) — AI chat with concept buttons
7. Learn hub (/learn) — navigation to all learning sections
8. Analytics (/analytics) — tool usage pie chart, activity timeline, mood trend, stat cards
9. Sidebar — updated with history, analytics, and learning navigation entries
  </what-built>
  <how-to-verify>
1. Navigate to /history — verify analysis list loads with filter buttons and pagination
2. Select 2 analyses of the same type and click compare — verify /history/compare shows side-by-side
3. Toggle timeline view on /history — verify analyses shown on vertical timeline
4. Navigate to /learn — verify hub page with 4 cards linking to sub-pages
5. Navigate to /learn/blog — verify 4 articles load with category tabs
6. Navigate to /learn/tutorials — verify 3 learning paths with progress bars
7. Mark a topic completed on tutorials — verify progress updates
8. Navigate to /learn/astrology — send a message, verify AI response
9. Click a quick concept button on astrology tutor — verify it sends the query
10. Navigate to /learn/drawing — verify drawing tutor works similarly
11. Navigate to /analytics — verify pie chart, line chart, and stat cards render
12. Switch period (week/month/3 months/all) — verify charts update
13. Check sidebar — verify all new entries link to correct pages
14. Check that /history while logged out redirects to login (middleware protection)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` exits 0
2. All 9 Phase 9 requirements verified:
   - HIST-01: History page with filterable list
   - HIST-02: Timeline visualization
   - HIST-03: Side-by-side comparison
   - ASTR-08: Astrology readings via history filter
   - GROW-02: Blog with educational content
   - GROW-03: Tutorials with progress tracking
   - GROW-04: Astrology tutor with AI chat
   - GROW-05: Drawing tutor with AI chat
   - UX-09: Self-analytics dashboard
3. Human reviewer approved all pages
</verification>

<success_criteria>
- Zero TypeScript errors
- All 9 routes render correctly
- Human reviewer approves complete Phase 9
- TOOL_NAMES deduplicated to shared constant
- All routes protected via middleware
- Sidebar navigation accurate
</success_criteria>

<output>
After completion, create `.planning/phases/09-learning-history-analytics/09-05-SUMMARY.md`
</output>
