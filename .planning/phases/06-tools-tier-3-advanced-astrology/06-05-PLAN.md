---
phase: 06-tools-tier-3-advanced-astrology
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements: [ASTR-03, ASTR-04, ASTR-05, TOOL-05, TOOL-08, TOOL-09, TOOL-10]

must_haves:
  truths:
    - "TypeScript compilation passes with zero errors across all Phase 6 work"
    - "All 7 new tool pages have SubscriptionGuard wrapping their form content"
    - "All API routes save analyses with correct tool_type values"
    - "Birth chart no longer shows isApproximate disclaimer"
    - "Ephemeris positions are accurate — Sun longitude at known reference dates matches expected values within 1-2 degrees"
    - "Human reviewer approves all tool pages as functional"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 6 work: TypeScript compilation, SubscriptionGuard audit, tool_type correctness, ephemeris accuracy spot-check, and human verification of all 7 new/updated tool pages.

Purpose: Integration gate before marking Phase 6 complete. Ensures no regressions, consistent SubscriptionGuard coverage, correct tool_type usage, and accurate ephemeris data across all routes.
Output: Phase 6 verified complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-01-SUMMARY.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-02-SUMMARY.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-03-SUMMARY.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript compilation + SubscriptionGuard audit + tool_type audit + ephemeris accuracy spot-check</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx,
    mystiqor-build/src/app/(auth)/tools/career/page.tsx,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx,
    mystiqor-build/src/app/(auth)/tools/document/page.tsx,
    mystiqor-build/src/app/api/tools/astrology/transits/route.ts,
    mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts,
    mystiqor-build/src/app/api/tools/astrology/synastry/route.ts,
    mystiqor-build/src/app/api/tools/timing/route.ts,
    mystiqor-build/src/app/api/tools/career/route.ts,
    mystiqor-build/src/app/api/tools/relationships/route.ts,
    mystiqor-build/src/app/api/tools/document/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx,
    mystiqor-build/src/app/(auth)/tools/career/page.tsx,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx,
    mystiqor-build/src/app/(auth)/tools/document/page.tsx
  </read_first>
  <action>
    1. Run `cd mystiqor-build && npx tsc --noEmit` — must exit 0. If errors, fix them.

    2. SubscriptionGuard audit — verify all 7 Phase 6 pages have SubscriptionGuard:
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/astrology/transits/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/astrology/solar-return/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/astrology/synastry/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/timing/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/career/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/relationships/page.tsx`
       - `grep -l "SubscriptionGuard" src/app/(auth)/tools/document/page.tsx`
       If any page is missing SubscriptionGuard, add it wrapping the form content.

    3. tool_type audit — verify correct tool_type in each route:
       - transits/route.ts uses 'transits'
       - solar-return/route.ts uses 'solar_return'
       - synastry/route.ts uses 'synastry'
       - timing/route.ts uses 'astrology' (with sub_type 'timing' in input_data)
       - career/route.ts uses 'career'
       - relationships/route.ts uses 'relationship'
       - document/route.ts uses 'document'

    4. Verify isApproximate is completely removed:
       - `grep -r "isApproximate" src/` should return no results from birth-chart route or astrology page.

    5. Verify no `export const runtime = 'edge'` on any route that imports ephemeris (per RESEARCH.md anti-pattern).

    6. Ephemeris accuracy spot-check — write a short inline Node script (or use the existing test file as reference) to verify:
       - Import getEphemerisPositions from the built service
       - Check Sun longitude at J2000 epoch (2000-01-01T12:00:00Z) is within 1 degree of 280.46
       - Check that all 10 planet keys are returned
       - Log results for human review. If positions are wildly off, investigate and fix ephemeris.ts.
       - This is a manual sanity check since vitest is not configured. Run via `npx tsx -e "..."` or a temporary script.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits 0
    - All 7 pages have SubscriptionGuard
    - No isApproximate in birth-chart route or astrology page
    - No edge runtime on ephemeris routes
    - Ephemeris spot-check shows Sun within 1 degree of expected value
  </acceptance_criteria>
  <done>TypeScript compilation clean, SubscriptionGuard on all 7 pages, correct tool_types, isApproximate removed, no edge runtime on ephemeris routes, ephemeris accuracy verified via manual spot-check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 6 tool pages</name>
  <what-built>
    Phase 6 adds 7 new/updated tool pages:
    1. Astrology page (updated — no more approximate disclaimer)
    2. Transits page (new — current planetary transits against natal chart)
    3. Solar Return page (new — annual forecast from real ephemeris)
    4. Synastry page (new — dual-chart relationship comparison)
    5. Timing Tools page (new — favorable day finder by activity)
    6. Career Guidance page (new — LLM career advice with birth chart context)
    7. Relationship Analysis page (new — LLM relationship analysis)
    8. Document Analyzer page (new — file upload + LLM document insights)
  </what-built>
  <how-to-verify>
    Start dev server: `cd mystiqor-build && npm run dev`

    1. Visit /tools/astrology — verify the isApproximate disclaimer banner is GONE. Submit birth data and confirm chart renders.
    2. Visit /tools/astrology/transits — verify the form loads and "Calculate Transits" button is visible. (Requires existing birth chart analysis.)
    3. Visit /tools/astrology/solar-return — verify year selector and "Calculate" button are visible.
    4. Visit /tools/astrology/synastry — verify dual-person form with two sets of birth data fields.
    5. Visit /tools/timing — verify activity type dropdown and date range pickers are visible.
    6. Visit /tools/career — verify skills, interests, and current field form fields.
    7. Visit /tools/relationships — verify dual-person name/birthdate form.
    8. Visit /tools/document — verify file upload area.
    9. For each page: confirm RTL layout, Hebrew labels, and SubscriptionGuard visible (shows upgrade prompt for free users or form for subscribed users).
    10. Ephemeris accuracy: if birth chart was submitted in step 1, compare displayed planet positions against a known astrology reference for the same date (e.g., astro.com). Sun sign and degree should match within 1 degree.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with specific pages</resume-signal>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. SubscriptionGuard audit — all 7 pages pass
3. Ephemeris accuracy spot-check — Sun position matches known reference within 1 degree
4. Human reviewer approves all pages
</verification>

<success_criteria>
- All automated checks pass (tsc --noEmit)
- All 7 new pages render correctly with RTL Hebrew UI
- All pages have SubscriptionGuard
- Ephemeris positions verified accurate against known reference dates
- Human reviewer approves Phase 6 as complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-tools-tier-3-advanced-astrology/06-05-SUMMARY.md`
</output>
