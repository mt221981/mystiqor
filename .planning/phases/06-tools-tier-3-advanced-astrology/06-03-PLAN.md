---
phase: 06-tools-tier-3-advanced-astrology
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - mystiqor-build/src/app/api/tools/astrology/synastry/route.ts
  - mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  - mystiqor-build/src/app/api/tools/timing/route.ts
  - mystiqor-build/src/app/(auth)/tools/timing/page.tsx
  - mystiqor-build/src/services/astrology/timing.ts
autonomous: true
requirements: [ASTR-05, TOOL-05]

must_haves:
  truths:
    - "User can enter birth data for two people and see inter-chart aspect analysis with compatibility interpretation"
    - "Synastry page shows compatibility score, strengths, challenges, and detailed aspect breakdown"
    - "Timing tools page returns a ranked list of favorable days within a user-selected date range"
    - "Each favorable day shows score, moon sign, favorable/unfavorable factors"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/astrology/synastry/route.ts"
      provides: "Synastry dual-chart API route"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx"
      provides: "Synastry UI page"
      min_lines: 100
    - path: "mystiqor-build/src/services/astrology/timing.ts"
      provides: "Timing scoring service"
      exports: ["scoreDayForActivity", "ACTIVITY_TYPES"]
    - path: "mystiqor-build/src/app/api/tools/timing/route.ts"
      provides: "Timing tools API route"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/timing/page.tsx"
      provides: "Timing tools UI page"
      min_lines: 100
  key_links:
    - from: "src/app/api/tools/astrology/synastry/route.ts"
      to: "src/services/astrology/ephemeris.ts"
      via: "Two getEphemerisPositions calls for two birth dates"
      pattern: "getEphemerisPositions"
    - from: "src/app/api/tools/astrology/synastry/route.ts"
      to: "src/services/astrology/aspects.ts"
      via: "calculateInterChartAspects for cross-chart aspects"
      pattern: "calculateInterChartAspects"
    - from: "src/services/astrology/timing.ts"
      to: "src/services/astrology/ephemeris.ts"
      via: "getEphemerisPositions for each day in range"
      pattern: "getEphemerisPositions"
    - from: "src/services/astrology/timing.ts"
      to: "src/services/astrology/aspects.ts"
      via: "calculateTransitAspects for daily scoring"
      pattern: "calculateTransitAspects"
---

<objective>
Build Synastry (relationship chart overlay) and Timing Tools (favorable day finder). Synastry computes two independent natal charts and calculates inter-chart aspects. Timing Tools iterates a date range, scoring each day against the user's natal chart for a specific activity type.

Purpose: Synastry is a high-demand relationship feature. Timing tools provide daily actionable guidance. Both depend on Plan 06-01's ephemeris adapter and run in parallel with Plan 06-02.
Output: Timing scoring service, two complete API routes, two full UI pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 06-01 outputs + existing code -->

From src/services/astrology/ephemeris.ts (created in 06-01):
```typescript
export function getEphemerisPositions(date: Date): PlanetPositions
export function getEphemerisPositionsWithRetrograde(date: Date): Record<string, { longitude: number; is_retrograde: boolean }>
```

From src/services/astrology/aspects.ts (extended in 06-01):
```typescript
export interface PlanetPositions { [planetName: string]: { longitude: number } }
export interface AspectResult { planet1: string; planet2: string; type: string; orb: number; strength: number }
export function calculateInterChartAspects(chart1: PlanetPositions, chart2: PlanetPositions): AspectResult[]
export function calculateTransitAspects(transiting: PlanetPositions, natal: PlanetPositions): AspectResult[]
export function getElementDistribution(planets: PlanetPositions): Record<string, number>
```

From src/services/astrology/chart.ts:
```typescript
export function assembleChart(date: Date, lat: number, lon: number, planets: PlanetPositions): ChartData
export function getSign(longitude: number): string
```

From src/app/api/tools/compatibility/route.ts (pattern for dual-person input):
```typescript
const PersonSchema = z.object({
  name: z.string().min(1, 'שם חובה'),
  birthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  birthTime: z.string().regex(/^\d{2}:\d{2}$/).optional(),
})
```

From date-fns (v4.1.0, root import per Pitfall 6):
```typescript
import { addDays, eachDayOfInterval } from 'date-fns'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synastry API route + page</name>
  <files>
    mystiqor-build/src/app/api/tools/astrology/synastry/route.ts,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/compatibility/route.ts,
    mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx,
    mystiqor-build/src/app/api/tools/astrology/birth-chart/route.ts,
    .planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md (Pattern 4, UI section for Synastry.jsx, Pitfall 3)
  </read_first>
  <action>
    1. Create `src/app/api/tools/astrology/synastry/route.ts`:
       - InputSchema: z.object with person1 and person2 — each has name, birthDate, birthTime (optional, default "12:00"), latitude, longitude. Follow the PersonSchema pattern from compatibility route but include lat/lon.
       - Auth check.
       - For each person, compute: `const datetime = new Date(\`\${birthDate}T\${birthTime || '12:00'}:00\`); const planets = getEphemerisPositions(datetime); const chart = assembleChart(datetime, lat, lon, planets);`
       - Both charts computed in parallel: `const [chart1Data, chart2Data] = await Promise.all([buildChart(person1), buildChart(person2)])` (buildChart is a local helper, synchronous so no await needed, but wrap in Promise.all pattern for clarity).
       - Call `calculateInterChartAspects(chart1Planets, chart2Planets)` for cross-chart aspects.
       - Build planet details for both people (name, sign, house, degree).
       - Build element distribution for both charts.
       - Build LLM prompt that includes: both chart summaries, inter-chart aspects, element harmony comparison. Use a synastry-specific system prompt (inline in the route — no separate prompt file needed unless > 300 lines).
       - Call invokeLLM with zodSchema for structured output: `{ compatibility_score: number (0-100), sun_sun_dynamic: string, moon_moon_dynamic: string, venus_mars_chemistry: string, strengths: string[], challenges: string[], recommendations: string[], summary: string }`.
       - Save to analyses table with `tool_type: 'synastry'` (per Pitfall 3).
       - Return full data: `{ data: { person1_chart, person2_chart, inter_aspects, interpretation, analysis_id } }`.

    2. Create `src/app/(auth)/tools/astrology/synastry/page.tsx`:
       - 'use client' directive.
       - SubscriptionGuard wraps form content.
       - Dual-person form: two sections for person 1 and person 2, each with name, birth date, birth time (optional), and location (LocationSearch component from `@/components/forms/LocationSearch`).
       - "Load from profile" button for person 1 (fetch current user profile data).
       - Results display (matching BASE44 Synastry.jsx from RESEARCH.md — text-based, NOT dual SVG):
         a. Compatibility score with visual progress ring or bar (0-100%).
         b. Dynamic cards: sun-sun, moon-moon, venus-mars.
         c. Strengths list (green badges).
         d. Challenges list (amber badges).
         e. Recommendations section.
         f. Inter-chart aspects table showing: planet1 sign, aspect type, planet2 sign, orb, strength.
       - Hebrew labels: "סינסטרי", "תאימות אסטרולוגית", "אדם 1", "אדם 2", "ציון תאימות", "חוזקות", "אתגרים".
       - RTL, Loading/Skeleton, error toast.
       - Max 300 lines — extract SynastryForm and SynastryResults if needed.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "calculateInterChartAspects" src/app/api/tools/astrology/synastry/route.ts
    - grep -q "getEphemerisPositions" src/app/api/tools/astrology/synastry/route.ts
    - grep -q "tool_type.*synastry" src/app/api/tools/astrology/synastry/route.ts
    - grep -q "SubscriptionGuard" src/app/(auth)/tools/astrology/synastry/page.tsx
    - grep -q "person1\|person2\|אדם 1\|אדם 2" src/app/(auth)/tools/astrology/synastry/page.tsx
  </acceptance_criteria>
  <done>Synastry route computes two real natal charts via ephemeris + inter-chart aspects + LLM interpretation, saves with tool_type 'synastry'. Page shows dual-person form, compatibility score, dynamic breakdowns, and aspect table. SubscriptionGuard wraps form. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Timing scoring service + API route + page</name>
  <files>
    mystiqor-build/src/services/astrology/timing.ts,
    mystiqor-build/src/app/api/tools/timing/route.ts,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/astrology/birth-chart/route.ts,
    mystiqor-build/src/app/(auth)/tools/astrology/page.tsx,
    .planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md (Pattern 5, Pitfalls 7-8, UI section for TimingTools.jsx)
  </read_first>
  <action>
    1. Create `src/services/astrology/timing.ts`:
       - Import getEphemerisPositions, getEphemerisPositionsWithRetrograde from ./ephemeris
       - Import calculateTransitAspects from ./aspects
       - Import getSign from ./chart
       - Import { normalize } from ./solar-return
       - Export ACTIVITY_TYPES constant — 8 activity types matching BASE44:
         'relationship_start', 'business_launch', 'travel', 'health_procedure', 'creative_project', 'financial_decision', 'spiritual_practice', 'important_meeting'
       - Export ACTIVITY_LABELS — Hebrew labels for each type.
       - Define ACTIVITY_ASPECT_WEIGHTS: Record<string, Record<string, number>> — per activity, per aspect planet combination, a weight score. Claude's discretion per RESEARCH.md:
         - Venus trines positive for relationships (+30), neutral for business (+5)
         - Mars sextiles positive for business/energy (+25)
         - Mercury direct positive for contracts/communication (+15)
         - Mercury retrograde penalty (-20 for business, -10 for spiritual)
         - Void of Course Moon penalty (-15)
       - Export `scoreDayForActivity(dayDate: Date, natalPlanets: PlanetPositions, activityType: string): DayScore`
         where DayScore = { date: string (ISO), score: number (0-100), moonSign: string, favorable: string[], unfavorable: string[], mercury_retrograde: boolean, near_void_moon: boolean }
         - Get transiting positions with retrograde for the day
         - Calculate transit aspects against natal chart
         - Sum scores based on ACTIVITY_ASPECT_WEIGHTS
         - Add mercury retrograde penalty if applicable
         - Add void of course check (longitude % 30 > 28)
         - Clamp score to 0-100
         - Collect favorable factors (positive aspects) and unfavorable factors (negative aspects/conditions)

    2. Create `src/app/api/tools/timing/route.ts`:
       - InputSchema: z.object({ activityType: z.enum([...ACTIVITY_TYPES]), startDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), endDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/) })
       - Validate date range: max 31 days (per Pitfall 7). If range > 31 days, return 400 with "טווח מקסימלי 31 ימים".
       - Auth check, fetch user profile for birth data, fetch natal chart from analyses.
       - If no natal chart, return 400 with "יש ליצור מפת לידה קודם".
       - Use `eachDayOfInterval({ start, end })` from date-fns (root import per Pitfall 6) to iterate days.
       - For each day, call `scoreDayForActivity(day, natalPlanets, activityType)`.
       - Sort results by score descending.
       - Slice top 5 as bestDays, bottom 3 as worstDays.
       - Build short LLM interpretation (optional — summarize top recommendation in 2-3 sentences).
       - Save to analyses table with `tool_type: 'timing'` (timing is not in ToolType union — check first. If missing, the task must also be saved with a valid tool_type. Use 'astrology' with a subtype field, or skip DB save for timing since it's a transient query).
       - Actually: check ToolType union — it does NOT have 'timing'. Save analysis with `tool_type: 'astrology'` and add a `sub_type: 'timing'` in the input_data JSON. OR skip saving since timing queries are ephemeral.
       - Return: `{ data: { bestDays, worstDays, allDays (full sorted list), activityType, interpretation } }`.

    3. Create `src/app/(auth)/tools/timing/page.tsx`:
       - 'use client' directive.
       - SubscriptionGuard wraps form content.
       - Form: activity type dropdown (8 options with Hebrew labels), start date picker, end date picker (default: today + 14 days).
       - Results display (matching BASE44 TimingTools.jsx from RESEARCH.md):
         a. Top recommendation highlight card (best day with score, moon sign, reason).
         b. "Best Days" list: each day shows score/100 progress bar, date, moon sign, favorable factors as green badges, unfavorable as amber badges, void moon/mercury retro badges if applicable.
         c. "Worst Days" list (collapsed by default, expandable).
       - Hebrew labels: "כלי תזמון", "סוג פעילות", "תאריך התחלה", "תאריך סיום", "ימים מומלצים", "ימים פחות מומלצים".
       - RTL, Loading/Skeleton, error toast.
       - Max 300 lines — extract TimingResults component if needed.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "scoreDayForActivity" src/services/astrology/timing.ts
    - grep -q "ACTIVITY_TYPES" src/services/astrology/timing.ts
    - grep -q "eachDayOfInterval" src/app/api/tools/timing/route.ts
    - grep -q "SubscriptionGuard" src/app/(auth)/tools/timing/page.tsx
    - grep -q "bestDays\|worstDays" src/app/api/tools/timing/route.ts
  </acceptance_criteria>
  <done>Timing service scores days by activity type using real ephemeris transits. Timing route validates date range (max 31 days), returns ranked best/worst days. Timing page shows activity selector, date range, and ranked results with score bars and badges. SubscriptionGuard wraps form. tsc clean.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero TypeScript errors
2. Synastry route uses calculateInterChartAspects + getEphemerisPositions for two charts
3. Timing route caps date range at 31 days and uses scoreDayForActivity
4. Both pages have SubscriptionGuard
5. Synastry saves with tool_type 'synastry'
</verification>

<success_criteria>
- Synastry page computes two real natal charts and shows inter-chart aspects + compatibility interpretation
- Timing page returns scored favorable/unfavorable days for a selected activity type within a date range
- Both routes use real ephemeris data
- Both pages wrap forms in SubscriptionGuard
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-tools-tier-3-advanced-astrology/06-03-SUMMARY.md`
</output>
