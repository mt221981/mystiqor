---
phase: 06-tools-tier-3-advanced-astrology
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/api/tools/career/route.ts
  - mystiqor-build/src/app/(auth)/tools/career/page.tsx
  - mystiqor-build/src/app/api/tools/relationships/route.ts
  - mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  - mystiqor-build/src/app/api/tools/document/route.ts
  - mystiqor-build/src/app/(auth)/tools/document/page.tsx
autonomous: true
requirements: [TOOL-08, TOOL-09, TOOL-10]

must_haves:
  truths:
    - "Career guidance page accepts skills and interests and returns actionable career insights, enriched with birth chart context when available"
    - "Relationship analysis page accepts two names and birth dates and returns compatibility analysis with structured recommendations"
    - "Document analyzer page accepts a file upload and returns AI-generated insights about the document content"
    - "All three pages wrap forms in SubscriptionGuard"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/career/route.ts"
      provides: "Career guidance LLM-only API route"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/career/page.tsx"
      provides: "Career guidance UI page"
      min_lines: 80
    - path: "mystiqor-build/src/app/api/tools/relationships/route.ts"
      provides: "Relationship analysis LLM-only API route"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/relationships/page.tsx"
      provides: "Relationship analysis UI page"
      min_lines: 80
    - path: "mystiqor-build/src/app/api/tools/document/route.ts"
      provides: "Document analyzer API route"
      exports: ["POST"]
    - path: "mystiqor-build/src/app/(auth)/tools/document/page.tsx"
      provides: "Document analyzer UI page"
      min_lines: 80
  key_links:
    - from: "src/app/api/tools/career/route.ts"
      to: "src/services/analysis/llm.ts"
      via: "invokeLLM for career interpretation"
      pattern: "invokeLLM"
    - from: "src/app/api/tools/relationships/route.ts"
      to: "src/services/analysis/llm.ts"
      via: "invokeLLM for relationship analysis"
      pattern: "invokeLLM"
    - from: "src/app/api/tools/document/route.ts"
      to: "src/services/analysis/llm.ts"
      via: "invokeLLM with imageUrls for document analysis"
      pattern: "invokeLLM"
---

<objective>
Build three LLM-only tools that have zero ephemeris dependency: Career Guidance (TOOL-08), Relationship Analysis (TOOL-09), and Document Analyzer (TOOL-10). All follow established patterns from Phase 4-5 tools.

Purpose: These tools expand MystiQor's tool suite with career/relationship/document analysis. They can run in parallel with the ephemeris work (Wave 1) since they only need invokeLLM.
Output: Three complete API routes + three full UI pages with SubscriptionGuard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md

<interfaces>
<!-- Existing patterns from Phase 4-5 tools -->

From src/services/analysis/llm.ts:
```typescript
export async function invokeLLM<T = unknown>(request: LLMRequest): Promise<LLMResponse<T>>
// LLMRequest includes: userId, systemPrompt, prompt, zodSchema?, responseSchema?, maxTokens?, imageUrls?
```

From src/app/api/tools/compatibility/route.ts (LLM-only pattern):
```typescript
// Auth check -> Zod validation -> invokeLLM with zodSchema for structured output -> save to analyses -> return
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
// Zod input + response schemas
// invokeLLM<ResponseType>({ userId, systemPrompt, prompt, responseSchema, zodSchema, maxTokens })
// Save: const row: TablesInsert<'analyses'> = { user_id, tool_type, input_data, results, summary }
```

From src/types/analysis.ts:
```typescript
export type ToolType = ... | 'career' | 'relationship' | 'document' | ...
```

From src/app/(auth)/tools/drawing/page.tsx (file upload pattern):
```typescript
// File upload uses FormData, presign route, then Supabase Storage URL
// imageUrls parameter in invokeLLM for vision analysis
```

From src/components/features/subscription/SubscriptionGuard.tsx:
```typescript
<SubscriptionGuard feature="analyses">
  {/* form content */}
</SubscriptionGuard>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Career guidance + Relationship analysis (API routes + pages)</name>
  <files>
    mystiqor-build/src/app/api/tools/career/route.ts,
    mystiqor-build/src/app/(auth)/tools/career/page.tsx,
    mystiqor-build/src/app/api/tools/relationships/route.ts,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/compatibility/route.ts,
    mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx,
    .planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md (CareerGuidance.jsx section, RelationshipAnalysis.jsx section, Pitfall 10)
  </read_first>
  <action>
    1. Create `src/app/api/tools/career/route.ts` (TOOL-08):
       - InputSchema: z.object({ currentField: z.string().max(200).optional(), skills: z.string().min(1, 'כישורים חובה'), interests: z.string().min(1, 'תחומי עניין חובה') }).
       - Auth check, fetch user profile for birth_date.
       - Optionally load user's most recent astrology analysis from analyses table to inject natal chart context into the prompt (if exists — gracefully omit when absent, do NOT fail).
       - Build system prompt: Hebrew astrological career advisor that combines skills/interests with birth chart insights.
       - Build user prompt including: skills, interests, current field (if provided), birth date, and natal chart summary (sun sign, moon sign, ascendant if available from saved analysis).
       - CareerResponseSchema = z.object({ recommended_fields: z.array(z.object({ name: z.string(), match_score: z.number().min(0).max(100), reason: z.string() })).min(1), skills_to_develop: z.array(z.string()).min(1), growth_opportunities: z.array(z.string()).min(1), challenges: z.array(z.object({ challenge: z.string(), solution: z.string() })).min(1), action_steps: z.array(z.string()).min(1), summary: z.string() }).
       - Call invokeLLM<CareerResponse> with zodSchema + responseSchema, maxTokens: 2000.
       - Save to analyses table with `tool_type: 'career'`.
       - Return: `{ data: { ...careerResult, analysis_id } }`.

    2. Create `src/app/(auth)/tools/career/page.tsx` (TOOL-08):
       - 'use client' directive.
       - SubscriptionGuard wraps form content (per Pitfall 10).
       - Form: currentField (optional text input), skills (textarea), interests (textarea). All Hebrew labels.
       - Results display (matching BASE44 CareerGuidance.jsx from RESEARCH.md):
         a. Recommended fields cards: each shows name, match_score with progress bar, reason.
         b. Skills to develop list (bulleted).
         c. Growth opportunities list.
         d. Challenges + solutions as paired cards.
         e. Action steps as numbered list.
       - Hebrew labels: "ייעוץ קריירה", "תחום נוכחי", "כישורים", "תחומי עניין", "תחומים מומלצים", "כישורים לפיתוח".
       - RTL, Loading/Skeleton, error toast.

    3. Create `src/app/api/tools/relationships/route.ts` (TOOL-09):
       - InputSchema: z.object({ person1Name: z.string().min(1), person2Name: z.string().min(1), person1BirthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), person2BirthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), relationshipType: z.enum(['romantic', 'friendship', 'business', 'family']), context: z.string().max(500).optional() }).
       - Auth check.
       - Build system prompt: Hebrew relationship advisor combining astrological and psychological insights.
       - Build user prompt with both names, birth dates, relationship type, and optional context.
       - RelationshipResponseSchema = z.object({ compatibility_score: z.number().min(0).max(100), summary: z.string(), strengths: z.array(z.string()).min(1), challenges: z.array(z.string()).min(1), recommendations: z.array(z.string()).min(1), communication_style: z.string(), emotional_dynamics: z.string() }).
       - Call invokeLLM<RelationshipResponse> with zodSchema + responseSchema, maxTokens: 2000.
       - Save to analyses table with `tool_type: 'relationship'`.
       - Return: `{ data: { ...relationshipResult, analysis_id } }`.

    4. Create `src/app/(auth)/tools/relationships/page.tsx` (TOOL-09):
       - 'use client' directive.
       - SubscriptionGuard wraps form content (per Pitfall 10 — original BASE44 was missing it, we ADD it).
       - Form: person1Name, person2Name, person1BirthDate (date picker), person2BirthDate (date picker), relationshipType dropdown (4 options with Hebrew labels), context (optional textarea).
       - Results display (matching BASE44 RelationshipAnalysis.jsx from RESEARCH.md):
         a. Compatibility score with visual percentage.
         b. Summary text.
         c. Communication style card.
         d. Emotional dynamics card.
         e. Strengths list (green).
         f. Challenges list (amber).
         g. Recommendations list.
       - Hebrew labels: "ניתוח יחסים", "שם", "תאריך לידה", "סוג הקשר", "רומנטי", "חברות", "עסקי", "משפחתי".
       - RTL, Loading/Skeleton, error toast.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tool_type.*career" src/app/api/tools/career/route.ts
    - grep -q "invokeLLM" src/app/api/tools/career/route.ts
    - grep -q "SubscriptionGuard" src/app/(auth)/tools/career/page.tsx
    - grep -q "tool_type.*relationship" src/app/api/tools/relationships/route.ts
    - grep -q "invokeLLM" src/app/api/tools/relationships/route.ts
    - grep -q "SubscriptionGuard" src/app/(auth)/tools/relationships/page.tsx
  </acceptance_criteria>
  <done>Career guidance route accepts skills/interests, enriches with natal chart context when available (gracefully omits when absent), returns structured career advice via LLM, saves with tool_type 'career'. Relationship analysis route accepts two people + relationship type, returns compatibility analysis via LLM, saves with tool_type 'relationship'. Both pages have SubscriptionGuard. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Document analyzer (API route + page)</name>
  <files>
    mystiqor-build/src/app/api/tools/document/route.ts,
    mystiqor-build/src/app/(auth)/tools/document/page.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/drawing/route.ts,
    mystiqor-build/src/app/(auth)/tools/drawing/page.tsx,
    mystiqor-build/src/app/api/tools/graphology/route.ts,
    .planning/phases/06-tools-tier-3-advanced-astrology/06-RESEARCH.md (document analyzer pattern)
  </read_first>
  <action>
    1. Create `src/app/api/tools/document/route.ts` (TOOL-10):
       - This route handles file upload + LLM analysis — same pattern as drawing and graphology routes.
       - Accept multipart/form-data with a file field (document image or PDF screenshot).
       - Use existing Phase 1 file upload infrastructure: validate file type (allow image/* and application/pdf), validate size (max 5MB), upload to Supabase Storage via presign or direct upload.
       - Auth check.
       - After upload, get the public URL from Supabase Storage.
       - Build system prompt: Hebrew document analyzer that extracts insights, key points, and actionable items from the document.
       - Optional context field: z.string().max(500).optional() — user can describe what the document is about.
       - DocumentResponseSchema = z.object({ document_type: z.string(), key_points: z.array(z.string()).min(1), insights: z.array(z.object({ title: z.string(), content: z.string(), category: z.enum(['practical', 'spiritual', 'psychological', 'informational']) })).min(1), action_items: z.array(z.string()), summary: z.string() }).
       - Call invokeLLM<DocumentResponse> with imageUrls: [storageUrl], zodSchema + responseSchema, maxTokens: 2000.
       - Save to analyses table with `tool_type: 'document'`.
       - Return: `{ data: { ...documentResult, imageUrl: storageUrl, analysis_id } }`.

    2. Create `src/app/(auth)/tools/document/page.tsx` (TOOL-10):
       - 'use client' directive.
       - SubscriptionGuard wraps form content.
       - Form: file upload area (drag & drop or click to select — reuse pattern from drawing page), optional context textarea.
       - Accept image files and PDF files. Show file preview for images.
       - Results display:
         a. Document type badge.
         b. Summary section.
         c. Key points as numbered list.
         d. Insights cards: each with title, content, category badge.
         e. Action items as checklist.
         f. Uploaded document thumbnail.
       - Hebrew labels: "ניתוח מסמך", "העלה מסמך", "הקשר (אופציונלי)", "סוג מסמך", "נקודות מפתח", "תובנות", "פעולות מומלצות".
       - RTL, Loading/Skeleton, error toast.
       - Max 300 lines.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tool_type.*document" src/app/api/tools/document/route.ts
    - grep -q "invokeLLM" src/app/api/tools/document/route.ts
    - grep -q "imageUrls\|imageUrl" src/app/api/tools/document/route.ts
    - grep -q "SubscriptionGuard" src/app/(auth)/tools/document/page.tsx
    - grep -q "FormData\|formData\|file" src/app/api/tools/document/route.ts
  </acceptance_criteria>
  <done>Document analyzer route accepts file upload (image/PDF), uploads to Supabase Storage, sends URL to LLM for analysis, returns structured insights with document type/key points/action items, saves with tool_type 'document'. Page has file upload area with drag-drop, optional context, and structured results display. SubscriptionGuard wraps form. tsc clean.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero TypeScript errors
2. All three routes use invokeLLM and save with correct tool_type
3. All three pages have SubscriptionGuard
4. Document route handles file upload via FormData
5. Career route enriches with natal chart context when available, gracefully omits when absent
</verification>

<success_criteria>
- Career guidance returns structured career advice enriched with birth chart context when available
- Relationship analysis returns compatibility score and structured recommendations for two people
- Document analyzer accepts file upload and returns AI-generated insights
- All three pages wrap forms in SubscriptionGuard
- All three routes save to analyses table with correct tool_type
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-tools-tier-3-advanced-astrology/06-04-SUMMARY.md`
</output>
