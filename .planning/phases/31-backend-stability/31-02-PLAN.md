---
phase: 31-backend-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/utils/usage-guard.ts
  - mystiqor-build/src/app/api/tools/tarot/route.ts
  - mystiqor-build/src/app/api/tools/palmistry/route.ts
  - mystiqor-build/src/app/api/tools/dream/route.ts
  - mystiqor-build/src/app/api/tools/numerology/route.ts
  - mystiqor-build/src/app/api/tools/numerology/compatibility/route.ts
  - mystiqor-build/src/app/api/tools/drawing/route.ts
  - mystiqor-build/src/app/api/tools/graphology/route.ts
  - mystiqor-build/src/app/api/tools/synthesis/route.ts
  - mystiqor-build/src/app/api/tools/career/route.ts
  - mystiqor-build/src/app/api/tools/document/route.ts
  - mystiqor-build/src/app/api/tools/relationships/route.ts
  - mystiqor-build/src/app/api/tools/compatibility/route.ts
  - mystiqor-build/src/app/api/tools/personality/route.ts
  - mystiqor-build/src/app/api/tools/timing/route.ts
  - mystiqor-build/src/app/api/tools/human-design/route.ts
  - mystiqor-build/src/app/api/tools/astrology/birth-chart/route.ts
  - mystiqor-build/src/app/api/tools/astrology/calendar/route.ts
  - mystiqor-build/src/app/api/tools/astrology/forecast/route.ts
  - mystiqor-build/src/app/api/tools/astrology/interpret/route.ts
  - mystiqor-build/src/app/api/tools/astrology/readings/route.ts
  - mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts
  - mystiqor-build/src/app/api/tools/astrology/synastry/route.ts
  - mystiqor-build/src/app/api/tools/astrology/transits/route.ts
autonomous: true
requirements:
  - STAB-01
  - STAB-05

must_haves:
  truths:
    - "משתמש שעבר מכסה מקבל 429 עברי — ללא API call ל-OpenAI"
    - "משתמש עם מנוי לא פעיל מקבל 402 עברי לפני כל קריאת LLM"
    - "כשל ב-DB insert על ניתוח מדווח למשתמש בטוסט עברי — לא נעלם בשקט"
    - "analysis_id לא יחזור null בשגיאה שקטה — המשתמש יקבל שגיאה מפורשת"
  artifacts:
    - path: "mystiqor-build/src/lib/utils/usage-guard.ts"
      provides: "checkUsageQuota() helper עם discriminated union return type"
      exports: ["checkUsageQuota", "UsageGuardResult"]
  key_links:
    - from: "every tool route POST handler"
      to: "checkUsageQuota(supabase, user.id)"
      via: "import from '@/lib/utils/usage-guard'"
      pattern: "checkUsageQuota"
    - from: "analyses .insert(row)"
      to: "error: insertError check"
      via: "destructuring"
      pattern: "error: insertError"
---

<objective>
Create the `checkUsageQuota()` server helper and wire it into all 24 tool routes immediately after auth, BEFORE any LLM call. Also fix the silent DB insert failure in all 20 routes that insert into `analyses`.

Purpose: Two categories of silent failures eliminated — (1) quota-exceeded users burning OpenAI quota silently, and (2) analyses lost without notification when DB inserts fail.
Output: New `usage-guard.ts` + 24 routes updated with quota guard + 20 routes updated with insert error check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Subscription table columns (from src/types/database.ts) -->
```typescript
// subscriptions table relevant columns:
// analyses_used: number
// analyses_limit: number  (-1 = unlimited for premium/enterprise)
// status: 'trial' | 'active' | 'cancelled' | 'expired' | 'past_due'
// plan_type: 'free' | 'basic' | 'premium' | 'enterprise'
// user_id: string
```

<!-- Existing type imports for the guard helper -->
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import { NextResponse } from 'next/server'
import type { Database } from '@/types/database'
```

<!-- Every tool route has this pattern — guard goes RIGHT AFTER the auth check -->
```typescript
export async function POST(request: NextRequest) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'לא מחובר' }, { status: 401 })
    }
    // ← INSERT GUARD HERE (before any other logic)
    // ... rest of route
  }
}
```

<!-- DB insert pattern — current (broken) vs correct -->
```typescript
// CURRENT (broken — error silently discarded):
const { data: analysis } = await supabase
  .from('analyses').insert(row).select('id').single()

// CORRECT (per STAB-05):
const { data: analysis, error: insertError } = await supabase
  .from('analyses').insert(row).select('id').single()
if (insertError) {
  console.error('[tool-name] שגיאת שמירת ניתוח:', insertError)
  return NextResponse.json(
    { error: 'הניתוח הושלם אך לא נשמר — אנא נסה שוב' },
    { status: 500 }
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create usage-guard.ts helper</name>
  <files>mystiqor-build/src/lib/utils/usage-guard.ts</files>
  <action>
Create `mystiqor-build/src/lib/utils/usage-guard.ts` as a NEW file with the following exact content:

```typescript
/**
 * usage-guard — בדיקת מכסת שימוש וסטטוס מנוי לפני קריאת LLM
 * STAB-01: חוסם גישה לפני שניגש ל-OpenAI — חוסך עלויות ומגן על המשתמש
 *
 * חשוב: קורא ישירות לטבלת subscriptions ב-Supabase — לא ל-/api/subscription/usage
 * (קריאת שרת-לשרת מיותרת כשיש גישה ישירה ל-DB)
 */

import type { SupabaseClient } from '@supabase/supabase-js'
import { NextResponse } from 'next/server'
import type { Database } from '@/types/database'

/** תוצאת בדיקת מכסת שימוש — discriminated union */
export type UsageGuardResult =
  | { allowed: true }
  | { allowed: false; response: ReturnType<typeof NextResponse.json> }

/**
 * בודק אם המשתמש רשאי לבצע ניתוח נוסף — מכסה ומנוי פעיל
 *
 * לוגיקה:
 * 1. שולף שורת מנוי מ-DB לפי user_id
 * 2. בודק שהמנוי קיים
 * 3. בודק שהסטטוס הוא 'active' או 'trial'
 * 4. בודק שלא נוצלה המכסה (analyses_used < analyses_limit; -1 = ללא הגבלה)
 *
 * @param supabase - קליינט Supabase בצד השרת (מ-createClient בכל route)
 * @param userId - מזהה המשתמש המאומת
 * @returns { allowed: true } אם מותר, או { allowed: false, response } לשליחה חזרה
 */
export async function checkUsageQuota(
  supabase: SupabaseClient<Database>,
  userId: string
): Promise<UsageGuardResult> {
  const { data: sub } = await supabase
    .from('subscriptions')
    .select('plan_type, status, analyses_used, analyses_limit')
    .eq('user_id', userId)
    .single()

  // מנוי לא נמצא — מצב חריג (משתמש ללא מנוי)
  if (!sub) {
    return {
      allowed: false,
      response: NextResponse.json(
        { error: 'לא נמצא מנוי — אנא צור קשר עם התמיכה' },
        { status: 403 }
      ),
    }
  }

  // מנוי לא פעיל — בוטל, פג, בפיגור
  const activeStatuses = ['active', 'trial'] as const
  type ActiveStatus = typeof activeStatuses[number]
  if (!activeStatuses.includes(sub.status as ActiveStatus)) {
    return {
      allowed: false,
      response: NextResponse.json(
        { error: 'המנוי שלך אינו פעיל — אנא חדש אותו כדי להמשיך' },
        { status: 402 }
      ),
    }
  }

  // מכסה נוצלה — analyses_limit === -1 פירושו ללא הגבלה (premium/enterprise)
  if (sub.analyses_limit !== -1 && sub.analyses_used >= sub.analyses_limit) {
    return {
      allowed: false,
      response: NextResponse.json(
        {
          error: `הגעת למגבלת ${sub.analyses_limit} ניתוחים לחודש זה — שדרג את המנוי כדי להמשיך`,
        },
        { status: 429 }
      ),
    }
  }

  return { allowed: true }
}
```

After creating, verify with `cd mystiqor-build && npx tsc --noEmit 2>&1 | grep usage-guard`.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- File `mystiqor-build/src/lib/utils/usage-guard.ts` exists
- Exports `UsageGuardResult` and `checkUsageQuota`
- Uses `analyses_limit !== -1` to handle unlimited plans
- Zero TypeScript errors from `tsc --noEmit`
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire usage guard into all 24 tool routes</name>
  <files>
    mystiqor-build/src/app/api/tools/tarot/route.ts,
    mystiqor-build/src/app/api/tools/palmistry/route.ts,
    mystiqor-build/src/app/api/tools/dream/route.ts,
    mystiqor-build/src/app/api/tools/numerology/route.ts,
    mystiqor-build/src/app/api/tools/numerology/compatibility/route.ts,
    mystiqor-build/src/app/api/tools/drawing/route.ts,
    mystiqor-build/src/app/api/tools/graphology/route.ts,
    mystiqor-build/src/app/api/tools/synthesis/route.ts,
    mystiqor-build/src/app/api/tools/career/route.ts,
    mystiqor-build/src/app/api/tools/document/route.ts,
    mystiqor-build/src/app/api/tools/relationships/route.ts,
    mystiqor-build/src/app/api/tools/compatibility/route.ts,
    mystiqor-build/src/app/api/tools/personality/route.ts,
    mystiqor-build/src/app/api/tools/timing/route.ts,
    mystiqor-build/src/app/api/tools/human-design/route.ts,
    mystiqor-build/src/app/api/tools/daily-insights/route.ts,
    mystiqor-build/src/app/api/tools/astrology/birth-chart/route.ts,
    mystiqor-build/src/app/api/tools/astrology/calendar/route.ts,
    mystiqor-build/src/app/api/tools/astrology/forecast/route.ts,
    mystiqor-build/src/app/api/tools/astrology/interpret/route.ts,
    mystiqor-build/src/app/api/tools/astrology/readings/route.ts,
    mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts,
    mystiqor-build/src/app/api/tools/astrology/synastry/route.ts,
    mystiqor-build/src/app/api/tools/astrology/transits/route.ts
  </files>
  <action>
For EACH of the 24 route files above, make two minimal edits:

**Edit A — add import** at the top of the file, after the last existing import line:
```typescript
import { checkUsageQuota } from '@/lib/utils/usage-guard'
```

**Edit B — add guard call** immediately after the `if (!user)` check (and before any other logic). The pattern is:
```typescript
// בדיקת מכסת שימוש — STAB-01
const guard = await checkUsageQuota(supabase, user.id)
if (!guard.allowed) return guard.response
```

**Special handling for dream/route.ts:**
The dream route is fire-and-forget. The guard MUST run in the main handler (before `backgroundWork`), NOT inside `backgroundWork`. The correct insertion point is after `const { data: { user } } = await supabase.auth.getUser()` and `if (!user)` check — which is in the main handler. This is already the case structurally — just add the guard before `getPersonalContext`.

**Notes:**
- Read each file before editing — do not assume positions
- Do NOT add the guard inside try blocks that wrap backgroundWork closures
- Do NOT modify any logic other than adding the import and the 3-line guard call
- The `supabase` variable is always available at the insertion point (created just above)
- Process all 24 files; do not skip any

After all edits, run `cd mystiqor-build && npx tsc --noEmit 2>&1 | head -40`.
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -rl "checkUsageQuota" src/app/api/tools/ | wc -l</automated>
  </verify>
  <done>
- `grep -rl "checkUsageQuota" mystiqor-build/src/app/api/tools/ | wc -l` outputs 24
- Every route file has `import { checkUsageQuota } from '@/lib/utils/usage-guard'`
- Every route has `const guard = await checkUsageQuota(supabase, user.id)` immediately after `if (!user)` check
- TypeScript build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix silent DB insert errors in all 20 analyses routes</name>
  <files>
    mystiqor-build/src/app/api/tools/tarot/route.ts,
    mystiqor-build/src/app/api/tools/palmistry/route.ts,
    mystiqor-build/src/app/api/tools/dream/route.ts,
    mystiqor-build/src/app/api/tools/numerology/route.ts,
    mystiqor-build/src/app/api/tools/numerology/compatibility/route.ts,
    mystiqor-build/src/app/api/tools/drawing/route.ts,
    mystiqor-build/src/app/api/tools/graphology/route.ts,
    mystiqor-build/src/app/api/tools/synthesis/route.ts,
    mystiqor-build/src/app/api/tools/career/route.ts,
    mystiqor-build/src/app/api/tools/document/route.ts,
    mystiqor-build/src/app/api/tools/relationships/route.ts,
    mystiqor-build/src/app/api/tools/compatibility/route.ts,
    mystiqor-build/src/app/api/tools/personality/route.ts,
    mystiqor-build/src/app/api/tools/timing/route.ts,
    mystiqor-build/src/app/api/tools/human-design/route.ts,
    mystiqor-build/src/app/api/tools/astrology/birth-chart/route.ts,
    mystiqor-build/src/app/api/tools/astrology/calendar/route.ts,
    mystiqor-build/src/app/api/tools/astrology/forecast/route.ts,
    mystiqor-build/src/app/api/tools/astrology/readings/route.ts,
    mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts,
    mystiqor-build/src/app/api/tools/astrology/synastry/route.ts,
    mystiqor-build/src/app/api/tools/astrology/transits/route.ts
  </files>
  <action>
These are the 20 routes (plus 2 already excluded: daily-insights and dream's initial insert already check errors) that insert into `analyses` without checking the `error` field.

Note: `dream/route.ts` inserts into `analyses` INSIDE `backgroundWork` — this background insert also needs the fix. The initial `dreams` insert already checks errors. For the background work, if `insertError` on the `analyses` insert fails, update `ai_interpretation` to a Hebrew fallback string so the polling UI can report something:
```typescript
if (insertError) {
  console.error('[dream] שגיאת שמירת ניתוח:', insertError)
  await updateSupabase.from('dreams').update({
    ai_interpretation: 'לא ניתן לשמור את הניתוח — נסה שוב'
  }).eq('id', dreamId)
  return
}
```

For all other routes, apply this exact pattern at the `analyses` insert site:

**Before (broken pattern):**
```typescript
const { data: analysis } = await supabase
  .from('analyses')
  .insert(row)
  .select('id')
  .single()
```

**After (STAB-05 pattern):**
```typescript
const { data: analysis, error: insertError } = await supabase
  .from('analyses')
  .insert(row)
  .select('id')
  .single()

if (insertError) {
  console.error('[TOOL_NAME] שגיאת שמירת ניתוח:', insertError)
  return NextResponse.json(
    { error: 'הניתוח הושלם אך לא נשמר — אנא נסה שוב' },
    { status: 500 }
  )
}
```

Replace `[TOOL_NAME]` with the actual tool folder name (e.g., `[tarot]`, `[numerology]`, `[astrology/synastry]`).

**Skip:** `daily-insights/route.ts` (already checks `insertError` correctly — do not modify).
**Note on astrology/interpret:** This route was confirmed to have 0 inserts — skip.

Read each file before editing to confirm the exact insert pattern. Do NOT change any other logic.

After all edits, run:
```
cd mystiqor-build && grep -rn "error: insertError" src/app/api/tools/ --include="route.ts" | wc -l
```
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -rn "error: insertError" src/app/api/tools/ --include="route.ts" | wc -l</automated>
  </verify>
  <done>
- All 20 routes now destructure `error: insertError` from the `analyses` insert
- Each `insertError` check includes `console.error` with a Hebrew tag and a Hebrew `NextResponse.json` error message
- The `dream` backgroundWork handles insert failure by setting `ai_interpretation` to Hebrew fallback
- TypeScript build passes with zero errors: `cd mystiqor-build && npx tsc --noEmit`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tool route | Unauthenticated or over-quota requests arrive here |
| tool route → Supabase DB | Insert failures must be surfaced, not silently discarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Elevation of Privilege | tool routes — missing quota check | mitigate | `checkUsageQuota()` runs before `invokeLLM` on every tool route; over-quota users receive 429 without OpenAI call |
| T-31-05 | Elevation of Privilege | cancelled/expired subscriptions | mitigate | `checkUsageQuota()` checks `status` — `cancelled`, `expired`, `past_due` all return 402 |
| T-31-06 | Spoofing | subscription row not found for user | mitigate | Guard returns 403 when subscription row is missing — not a default-allow |
| T-31-07 | Tampering | silent DB insert failure | mitigate | `insertError` check returns 500 with Hebrew message — analysis never silently vanishes (STAB-05) |
| T-31-08 | Denial of Service | `checkUsageQuota` Supabase read overhead | accept | Single `.select()` query on indexed `user_id`; negligible latency (~1-2ms) vs LLM call cost |
</threat_model>

<verification>
1. `grep -rl "checkUsageQuota" mystiqor-build/src/app/api/tools/ | wc -l` — must output 24
2. `grep -rn "error: insertError" mystiqor-build/src/app/api/tools/ --include="route.ts" | wc -l` — must be ≥ 22 (daily-insights + dream already had some)
3. `cat mystiqor-build/src/lib/utils/usage-guard.ts` — file exists with `UsageGuardResult` export
4. `cd mystiqor-build && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- All 24 tool routes call `checkUsageQuota` before `invokeLLM`
- Over-quota users receive 429 with Hebrew message — no OpenAI call made
- Inactive subscription users receive 402 with Hebrew message
- All 20 routes with `analyses` insert now check and report `insertError` with Hebrew toast-ready message
- Dream background work reports insert failure via `ai_interpretation` field update
- TypeScript build is clean
</success_criteria>

<output>
After completion, create `.planning/phases/31-backend-stability/31-02-SUMMARY.md`
</output>
