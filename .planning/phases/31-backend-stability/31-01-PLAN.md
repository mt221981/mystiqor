---
phase: 31-backend-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/services/analysis/llm.ts
autonomous: true
requirements:
  - STAB-02
  - STAB-03

must_haves:
  truths:
    - "קריאת LLM מנסה שוב אוטומטית עד שתי פעמים לפני שמחזירה שגיאה"
    - "שגיאות OpenAI (timeout, rate limit, חיבור, auth) מתורגמות להודעות עבריות ידידותיות"
    - "לא נחשפים קודי שגיאה גולמיים באנגלית בתגובה למשתמש"
  artifacts:
    - path: "mystiqor-build/src/services/analysis/llm.ts"
      provides: "invokeLLM עם timeout 9s, maxRetries 2, ומיפוי שגיאות עברי"
      contains: "timeout: 9_000"
  key_links:
    - from: "new OpenAI({ apiKey })"
      to: "timeout: 9_000, maxRetries: 2"
      via: "constructor options"
      pattern: "timeout.*9"
    - from: "catch block"
      to: "Hebrew error messages"
      via: "instanceof checks on OpenAI error classes"
      pattern: "APIConnectionTimeoutError|RateLimitError|InternalServerError"
---

<objective>
Harden the single `invokeLLM` function so every OpenAI call has a 9-second timeout, up to 2 automatic retries with exponential backoff, and every error class maps to a clear Hebrew user message.

Purpose: All 24 tool routes share a single LLM entry point — one change here protects every tool simultaneously. No retry or timeout logic should live in individual routes.
Output: Modified `llm.ts` with timeout/retry constructor options and a typed Hebrew error-mapping catch block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current invokeLLM signature — do NOT change the public interface, only internals -->

From mystiqor-build/src/services/analysis/llm.ts (current — read before editing):

```typescript
// Line 66: current client construction — ADD timeout and maxRetries here
const openai = new OpenAI({ apiKey })

// Line 143-151: current catch block — REPLACE with typed error mapping below
} catch (error) {
  if (error instanceof SyntaxError) {
    throw new Error('שגיאה בפרסור תשובת LLM — התשובה אינה JSON תקין')
  }
  const message = error instanceof Error ? error.message : 'שגיאה לא צפויה'
  throw new Error(`שגיאה בקריאה ל-LLM: ${message}`)
}
```

OpenAI SDK error classes (verified from node_modules/openai/error.d.ts):
```typescript
import {
  APIConnectionTimeoutError,
  APIConnectionError,
  RateLimitError,
  InternalServerError as OpenAIInternalServerError,
  AuthenticationError,
} from 'openai'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout/retry and Hebrew error mapping to invokeLLM</name>
  <files>mystiqor-build/src/services/analysis/llm.ts</files>
  <action>
Read the current file first. Make exactly two targeted changes:

**Change 1 — OpenAI constructor (per STAB-02):**

Replace line 66:
```typescript
const openai = new OpenAI({ apiKey })
```
With:
```typescript
const openai = new OpenAI({
  apiKey,
  timeout: 9_000,   // 9 שניות לפי דרישת STAB-02
  maxRetries: 2,    // 2 ניסיונות חוזרים עם backoff — סה"כ 3 ניסיונות
})
```

**Change 2 — imports + catch block (per STAB-03):**

Add to the top-level imports (after `import OpenAI from 'openai'`):
```typescript
import {
  APIConnectionTimeoutError,
  APIConnectionError,
  RateLimitError,
  InternalServerError as OpenAIInternalServerError,
  AuthenticationError,
} from 'openai'
```

Replace the entire catch block (currently lines 143-151) with:
```typescript
  } catch (error) {
    // SyntaxError — JSON parsing failed
    if (error instanceof SyntaxError) {
      throw new Error('שגיאה בפרסור תשובת AI — תגובה לא תקינה התקבלה')
    }
    // שגיאות OpenAI מטיפוסים ידועים — מיפוי לעברית (STAB-03)
    if (error instanceof APIConnectionTimeoutError) {
      throw new Error('שרת ה-AI לא הגיב בזמן — נסה שוב בעוד מספר שניות')
    }
    if (error instanceof APIConnectionError) {
      throw new Error('בעיית חיבור לשרת AI — בדוק את החיבור לאינטרנט ונסה שוב')
    }
    if (error instanceof RateLimitError) {
      throw new Error('שרת ה-AI עמוס כרגע — נסה שוב בעוד מספר דקות')
    }
    if (error instanceof OpenAIInternalServerError) {
      throw new Error('שגיאה זמנית בשרת AI — נסה שוב בעוד מספר דקות')
    }
    if (error instanceof AuthenticationError) {
      throw new Error('שגיאת הגדרות שרת — צור קשר עם התמיכה')
    }
    // שגיאה לא ידועה — שולחים הודעה כללית
    const message = error instanceof Error ? error.message : 'שגיאה לא צפויה'
    throw new Error(`שגיאה בקריאה ל-AI: ${message}`)
  }
```

Do NOT change: the function signature, the `LLMRequest` interface, the `LLMResponse` interface, validation logic, or any other part of the file. Max 300 lines — the final file must stay within that budget.

After editing, run `cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30` to verify zero TypeScript errors.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- `mystiqor-build/src/services/analysis/llm.ts` contains `timeout: 9_000` and `maxRetries: 2` on the OpenAI constructor
- All 5 OpenAI error classes are imported from `'openai'`
- The catch block has `instanceof APIConnectionTimeoutError`, `instanceof RateLimitError`, `instanceof OpenAIInternalServerError`, `instanceof APIConnectionError`, `instanceof AuthenticationError` checks with Hebrew messages
- `npx tsc --noEmit` exits with 0 errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invokeLLM → OpenAI API | Server-to-external-service; error responses cross back into app code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | catch block in llm.ts | mitigate | Hebrew error mapping strips raw OpenAI error codes/messages before they reach the client response; no raw `error.message` forwarded |
| T-31-02 | Denial of Service | invokeLLM timeout | mitigate | `timeout: 9_000` per attempt; `maxRetries: 2` caps total retries; prevents infinite hang and runaway costs |
| T-31-03 | Tampering | error message content | accept | Error messages are server-controlled strings — not echoing LLM content or user input, so no injection surface |
</threat_model>

<verification>
1. `grep -n "timeout.*9_000" mystiqor-build/src/services/analysis/llm.ts` — must find match
2. `grep -n "maxRetries.*2" mystiqor-build/src/services/analysis/llm.ts` — must find match
3. `grep -n "APIConnectionTimeoutError\|RateLimitError\|OpenAIInternalServerError" mystiqor-build/src/services/analysis/llm.ts` — must find all 3 imports and usage in catch
4. `cd mystiqor-build && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- timeout: 9_000 and maxRetries: 2 are set on the OpenAI constructor inside invokeLLM
- Every OpenAI-typed error class produces a Hebrew message — no English error codes reach the client
- TypeScript build passes with zero errors
- No other logic in llm.ts is changed (interfaces, validation, model selection remain intact)
</success_criteria>

<output>
After completion, create `.planning/phases/31-backend-stability/31-01-SUMMARY.md`
</output>
