---
phase: 07-ai-coach-mystic-synthesis
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - mystiqor-build/src/app/(auth)/coach/page.tsx
  - mystiqor-build/src/components/layouts/Sidebar.tsx
autonomous: false
requirements: [COCH-01, COCH-03, COCH-04, SYNT-01]

must_haves:
  truths:
    - "Coach page has a Journeys tab that shows journey cards with progress"
    - "Sidebar link to AI Coach navigates to /coach (not /ai-coach)"
    - "Sidebar link to Synthesis navigates to /tools/synthesis (not /synthesis)"
    - "TypeScript compiles with zero errors across all Phase 7 files"
    - "All Phase 7 pages have SubscriptionGuard wrapping"
    - "Human reviewer confirms chat and synthesis features work end-to-end"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/coach/page.tsx"
      provides: "Updated coach page with journeys tab integration"
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Fixed navigation links for coach and synthesis"
  key_links:
    - from: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      to: "/coach"
      via: "href property in nav items"
      pattern: "href.*coach"
    - from: "mystiqor-build/src/app/(auth)/coach/page.tsx"
      to: "JourneyCard component"
      via: "import from @/components/features/coach/JourneyCard"
      pattern: "JourneyCard"
---

<objective>
Integrate journeys into the coach page, fix sidebar navigation links, run TypeScript compilation check, and verify all Phase 7 features end-to-end with human approval.

Purpose: Wires together all Phase 7 work (Plans 01-03): adds journeys tab to coach page, fixes sidebar navigation from old BASE44 paths to correct Next.js routes, and verifies everything compiles and works.
Output: Complete Phase 7 with all features integrated, sidebar navigating correctly, and human approval of the live experience.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-coach-mystic-synthesis/07-RESEARCH.md
@.planning/phases/07-ai-coach-mystic-synthesis/07-01-SUMMARY.md
@.planning/phases/07-ai-coach-mystic-synthesis/07-02-SUMMARY.md
@.planning/phases/07-ai-coach-mystic-synthesis/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate journeys tab into coach page + Fix sidebar links + TypeScript check</name>
  <files>
    mystiqor-build/src/app/(auth)/coach/page.tsx,
    mystiqor-build/src/components/layouts/Sidebar.tsx
  </files>
  <read_first>
    mystiqor-build/src/app/(auth)/coach/page.tsx,
    mystiqor-build/src/components/layouts/Sidebar.tsx,
    mystiqor-build/src/components/features/coach/JourneyCard.tsx,
    mystiqor-build/src/app/api/coach/journeys/route.ts,
    mystiqor-build/src/app/api/coach/journeys/[id]/route.ts
  </read_first>
  <action>
    **1. Update `mystiqor-build/src/app/(auth)/coach/page.tsx` — Add journeys tab:**

    The coach page (created in Plan 07-01) currently has a conversation sidebar + chat area. Add a tab system at the top of the page:

    - Two tabs: `שיחות` (Conversations/Chat, default) and `מסעות` (Journeys)
    - Use `useState<'chat' | 'journeys'>('chat')` for active tab
    - Tab buttons: styled similar to existing Tabs pattern in the project (simple button group with active indicator)

    **When `journeys` tab is active, render:**
    - "צור מסע חדש" button that opens a focus area selector:
      - Dropdown or button group with 8 focus areas: `מטרת חיים`, `יחסים`, `קריירה`, `צמיחה אישית`, `מסע רוחני`, `גילוי עצמי`, `בריאות`, `יצירתיות`
      - On select, POST to `/api/coach/journeys` with `{ focus_area: selectedArea }`
      - Use `useMutation` with `onSuccess` invalidating `['coach-journeys']` query + `toast.success('המסע נוצר בהצלחה!')`
    - Journey list:
      - `useQuery({ queryKey: ['coach-journeys'], queryFn: async () => { const res = await fetch('/api/coach/journeys'); const json = await res.json(); return json.data ?? []; } })`
      - Map journeys to `<JourneyCard>` components
      - Pass `onStepComplete` handler that calls `PATCH /api/coach/journeys/[id]` with `{ step_number }`:
        ```typescript
        const completeStepMutation = useMutation({
          mutationFn: async ({ journeyId, stepNumber }: { journeyId: string; stepNumber: number }) => {
            const res = await fetch(`/api/coach/journeys/${journeyId}`, {
              method: 'PATCH',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ step_number: stepNumber }),
            })
            if (!res.ok) throw new Error('שגיאה בעדכון הצעד')
            return res.json()
          },
          onSuccess: () => queryClient.invalidateQueries({ queryKey: ['coach-journeys'] }),
          onError: () => toast.error('שגיאה בסימון הצעד כהושלם'),
        })
        ```
      - Pass `isUpdating={completeStepMutation.isPending}` to JourneyCard
    - Empty state: if no journeys, show encouraging message `עדיין לא התחלת מסע. בחר נושא ליצירת מסע אימון אישי!` with the focus area selector prominently

    **2. Fix `mystiqor-build/src/components/layouts/Sidebar.tsx` — Navigation links:**

    Change two sidebar nav items:
    - Line ~103: Change `href: '/ai-coach'` to `href: '/coach'`
    - Line ~96: Change `href: '/synthesis'` to `href: '/tools/synthesis'`

    These are the only two changes needed. All other sidebar links are unrelated to Phase 7.

    **3. TypeScript compilation check:**
    After both changes, run `cd mystiqor-build && npx tsc --noEmit` to verify zero errors across all Phase 7 files.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `mystiqor-build/src/app/(auth)/coach/page.tsx` contains import of `JourneyCard` from `@/components/features/coach/JourneyCard`
    - Coach page contains tab state: `useState<'chat' | 'journeys'>`
    - Coach page contains `useQuery` with queryKey `coach-journeys`
    - Coach page contains `useMutation` for step completion with `PATCH` method
    - Coach page contains Hebrew tab labels `שיחות` and `מסעות`
    - File `mystiqor-build/src/components/layouts/Sidebar.tsx` contains `href: '/coach'` (not `/ai-coach`)
    - File `mystiqor-build/src/components/layouts/Sidebar.tsx` contains `href: '/tools/synthesis'` (not `/synthesis`)
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Coach page has journeys tab with journey list, creation, and step completion. Sidebar links point to correct routes. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 7 features</name>
  <what-built>
    Complete AI Coach + Mystic Synthesis feature set:
    1. AI coaching chat with personalized responses
    2. Conversation persistence across sessions
    3. Coaching journeys with step-by-step progress
    4. Mystic Synthesis (on-demand + weekly reports)
    5. Sidebar navigation fixes
  </what-built>
  <how-to-verify>
    Start the dev server: `cd mystiqor-build && npm run dev`

    **1. Coach Chat (COCH-01, COCH-02, COCH-05):**
    - Navigate to `/coach` from sidebar ("מאמן AI")
    - Click "שיחה חדשה" to create a conversation
    - Send a message (e.g., "מה אתה יודע עלי?")
    - Verify: AI responds with references to your actual data (natal chart, numerology, etc.)
    - Verify: Messages appear in RTL layout — your message on left, AI on right
    - Verify: Quick actions show 6 Hebrew buttons when no messages
    - Refresh page — verify conversation and messages persist

    **2. Coaching Journeys (COCH-03, COCH-04):**
    - Switch to "מסעות" tab
    - Click "צור מסע חדש" and select a focus area (e.g., "צמיחה אישית")
    - Verify: Journey card appears with 7-12 steps, progress at 0%
    - Expand the journey card
    - Click "סמן כהושלם" on a step
    - Verify: Progress bar updates, step shows completion checkmark

    **3. Mystic Synthesis (SYNT-01, SYNT-02, SYNT-03):**
    - Navigate to `/tools/synthesis` from sidebar ("סינתזה")
    - Click "צור סינתזה" button
    - Verify: Synthesis result appears with personality profile, predictions, recommendations
    - Verify: Result cross-references at least 2 different analysis types
    - Try "צור דוח שבועי" for weekly synthesis (may show error if < 2 analyses in last 7 days — that is OK)

    **4. Sidebar Navigation:**
    - Verify: "מאמן AI" link goes to `/coach` (not 404)
    - Verify: "סינתזה" link goes to `/tools/synthesis` (not 404)

    **5. SubscriptionGuard:**
    - Verify: Both coach and synthesis pages are wrapped in SubscriptionGuard
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` exits 0
- `grep -rn "SubscriptionGuard" src/app/(auth)/coach/page.tsx src/app/(auth)/tools/synthesis/page.tsx` returns matches for both files
- `grep -rn "href.*'/coach'" src/components/layouts/Sidebar.tsx` returns a match
- `grep -rn "href.*'/tools/synthesis'" src/components/layouts/Sidebar.tsx` returns a match
- `grep -rn "JourneyCard" src/app/(auth)/coach/page.tsx` returns a match
- Human verifier approves all 5 verification areas
</verification>

<success_criteria>
- Coach page has two tabs (chat + journeys) with full functionality
- Sidebar navigates to correct routes for coach and synthesis
- TypeScript compilation passes with zero errors
- Human verifier confirms chat responses reference actual user data
- Human verifier confirms journey step completion updates progress
- Human verifier confirms synthesis cross-references multiple tool types
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-coach-mystic-synthesis/07-04-SUMMARY.md`
</output>
