---
phase: 26-icon-system-overhaul
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
files_modified:
  - src/app/(auth)/tools/astrology/forecast/page.tsx
  - src/app/(auth)/tools/astrology/transits/page.tsx
  - src/app/(auth)/tools/astrology/calendar/page.tsx
  - src/app/(auth)/tools/astrology/synastry/page.tsx
  - src/app/(auth)/tools/astrology/solar-return/page.tsx
  - src/app/(auth)/tools/astrology/readings/page.tsx
  - src/app/(auth)/learn/astrology/page.tsx
  - src/app/(auth)/learn/drawing/page.tsx
  - src/app/(auth)/learn/astrology/dictionary/page.tsx
  - src/app/(auth)/learn/blog/[slug]/page.tsx
  - src/components/features/dashboard/DailyInsightCard.tsx
  - src/components/features/daily-insights/InsightHeroCard.tsx
  - package.json
autonomous: true
requirements:
  - ICON-01
  - ICON-02
  - ICON-03

must_haves:
  truths:
    - "Zero files in src/ import from react-icons/gi or react-icons"
    - "react-icons package fully removed from package.json and node_modules"
    - "All astrology sub-pages use Lucide icons"
    - "All learn pages use Lucide icons"
    - "DailyInsightCard and InsightHeroCard use Lucide icons"
    - "npm run build succeeds with zero errors"
  artifacts:
    - path: "package.json"
      provides: "No react-icons dependency"
    - path: "src/app/(auth)/tools/astrology/forecast/page.tsx"
      provides: "Search icon for forecast header"
      contains: "from 'lucide-react'"
    - path: "src/components/features/daily-insights/InsightHeroCard.tsx"
      provides: "Sparkles, Asterisk, Sun icons replacing Gi equivalents"
      contains: "from 'lucide-react'"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm uninstall react-icons removes dependency"
      pattern: "react-icons.*ABSENT"
---

<objective>
Migrate the final 12 files (astrology sub-pages, learn pages, dashboard/insight components), then remove the react-icons package entirely.

Purpose: Completes the migration across all 36 files and removes the unused dependency, achieving 100% Lucide icon coverage (ICON-01, ICON-02, ICON-03).

Output: All 36 files migrated, react-icons removed from package.json, clean build passes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-icon-system-overhaul/26-CONTEXT.md
@.planning/phases/26-icon-system-overhaul/26-RESEARCH.md
@.planning/phases/26-icon-system-overhaul/26-01-SUMMARY.md
@.planning/phases/26-icon-system-overhaul/26-02-SUMMARY.md

@mystiqor-build/src/app/(auth)/tools/astrology/forecast/page.tsx
@mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx
@mystiqor-build/src/app/(auth)/tools/astrology/calendar/page.tsx
@mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
@mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx
@mystiqor-build/src/app/(auth)/tools/astrology/readings/page.tsx
@mystiqor-build/src/app/(auth)/learn/astrology/page.tsx
@mystiqor-build/src/app/(auth)/learn/drawing/page.tsx
@mystiqor-build/src/app/(auth)/learn/astrology/dictionary/page.tsx
@mystiqor-build/src/app/(auth)/learn/blog/[slug]/page.tsx
@mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
@mystiqor-build/src/components/features/daily-insights/InsightHeroCard.tsx

<interfaces>
<!-- From Plan 01: centralized mapping -->
From src/lib/constants/tool-icons.ts:
```typescript
export const TOOL_ICONS: Record<string, LucideIcon>;
export function getToolIcon(toolId: string): LucideIcon;
```
These files use direct Lucide imports (not the centralized mapping) since they need JSX elements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 astrology sub-pages</name>
  <files>src/app/(auth)/tools/astrology/forecast/page.tsx, src/app/(auth)/tools/astrology/transits/page.tsx, src/app/(auth)/tools/astrology/calendar/page.tsx, src/app/(auth)/tools/astrology/synastry/page.tsx, src/app/(auth)/tools/astrology/solar-return/page.tsx, src/app/(auth)/tools/astrology/readings/page.tsx</files>
  <action>
Migrate each astrology sub-page. Each file has a single Gi import.

**File-by-file mapping:**

**1. forecast/page.tsx:**
- Remove: `import { GiMagnifyingGlass } from 'react-icons/gi'`
- Add `Search` to lucide-react import
- Replace: `GiMagnifyingGlass` -> `Search` in all JSX usages

**2. transits/page.tsx:**
- Remove: `import { GiOrbital } from 'react-icons/gi'`
- Add `Orbit` to lucide-react import
- Replace: `GiOrbital` -> `Orbit` in all JSX usages

**3. calendar/page.tsx:**
- Remove: `import { GiCalendar } from 'react-icons/gi'`
- Add `CalendarDays` to lucide-react import
- Replace: `GiCalendar` -> `CalendarDays` in all JSX usages

**4. synastry/page.tsx:**
- Remove: `import { GiHearts } from 'react-icons/gi'`
- Add `HeartHandshake` to lucide-react import
- Replace: `GiHearts` -> `HeartHandshake` in all JSX usages

**5. solar-return/page.tsx:**
- Remove: `import { GiSunrise } from 'react-icons/gi'`
- Add `Sunrise` to lucide-react import
- Replace: `GiSunrise` -> `Sunrise` in all JSX usages

**6. readings/page.tsx:**
- Remove: `import { GiSpellBook } from 'react-icons/gi'`
- Add `BookMarked` to lucide-react import
- Replace: `GiSpellBook` -> `BookMarked` in all JSX usages

For ALL files: preserve everything else. Only touch icon imports and JSX usages.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -r "react-icons/gi" src/app/\(auth\)/tools/astrology/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>6 astrology sub-pages migrated. Zero react-icons/gi imports in astrology/ tree. TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 learn pages + 2 dashboard/insight components</name>
  <files>src/app/(auth)/learn/astrology/page.tsx, src/app/(auth)/learn/drawing/page.tsx, src/app/(auth)/learn/astrology/dictionary/page.tsx, src/app/(auth)/learn/blog/[slug]/page.tsx, src/components/features/dashboard/DailyInsightCard.tsx, src/components/features/daily-insights/InsightHeroCard.tsx</files>
  <action>
Migrate the final 6 source files that still import from react-icons/gi.

**Learn pages:**

**1. learn/astrology/page.tsx:**
- Remove: `import { GiAstrolabe } from 'react-icons/gi'`
- Add `Orbit` to lucide-react import
- Replace: `GiAstrolabe` -> `Orbit`

**2. learn/drawing/page.tsx:**
- Remove: `import { GiPaintBrush } from 'react-icons/gi'`
- Add `Palette` to lucide-react import
- Replace: `GiPaintBrush` -> `Palette`

**3. learn/astrology/dictionary/page.tsx:**
- Remove: `import { GiAstrolabe } from 'react-icons/gi'`
- Add `Orbit` to lucide-react import
- Replace: `GiAstrolabe` -> `Orbit`

**4. learn/blog/[slug]/page.tsx:**
- Remove: `import { GiSpellBook } from 'react-icons/gi'`
- Add `BookMarked` to lucide-react import
- Replace: `GiSpellBook` -> `BookMarked`

**Dashboard/Insight components:**

**5. DailyInsightCard.tsx:**
- Remove: `import { GiSparkles } from 'react-icons/gi'`
- Add: `import { Sparkles } from 'lucide-react'`
- Replace: `GiSparkles` -> `Sparkles` in all JSX usages

**6. InsightHeroCard.tsx:**
- Remove: `import { GiSparkles, GiStarShuriken, GiLightBulb } from 'react-icons/gi'`
- Add `Sparkles, Asterisk, Sun` to lucide-react import
- Replace: `GiSparkles` -> `Sparkles`, `GiStarShuriken` -> `Asterisk`, `GiLightBulb` -> `Sun` in all JSX usages

For ALL files: preserve everything else. Only touch icon imports and JSX usages.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -r "react-icons" src/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 6 files migrated. Zero react-icons imports in entire src/ tree. TypeScript passes. grep count is 0.</done>
</task>

<task type="auto">
  <name>Task 3: Remove react-icons dependency and run final verification</name>
  <files>package.json</files>
  <action>
**Step A: Final pre-uninstall sweep**
Run `grep -r "react-icons" src/` to confirm zero results. If ANY file still imports from react-icons, fix it before proceeding.

**Step B: Uninstall react-icons**
```bash
cd mystiqor-build && npm uninstall react-icons
```
This removes `react-icons` from package.json dependencies and from node_modules.

**Step C: Full build verification**
```bash
cd mystiqor-build && npm run build
```
This runs the full Next.js build including TypeScript compilation, route generation, and bundling. Must pass with zero errors.

**Step D: Final grep verification**
```bash
grep -r "react-icons" src/ 2>/dev/null | wc -l   # Must be 0
grep -r "IconType" src/ 2>/dev/null | wc -l       # Must be 0 (all changed to LucideIcon)
grep "react-icons" package.json 2>/dev/null | wc -l  # Must be 0
```

If build fails: the error will indicate which file still references react-icons. Fix that file's import, then re-run build.
  </action>
  <verify>
    <automated>cd mystiqor-build && npm run build 2>&1 | tail -10 && grep -r "react-icons" src/ package.json 2>/dev/null | wc -l</automated>
  </verify>
  <done>react-icons fully removed from package.json. npm run build passes. grep for "react-icons" in src/ and package.json returns 0. grep for "IconType" in src/ returns 0. Migration complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. Final cleanup of icon imports and dependency removal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Denial of Service | package.json | accept | Removing unused dependency reduces attack surface. npm uninstall is a standard safe operation. |
</threat_model>

<verification>
```bash
cd mystiqor-build

# PHASE GATE: Zero react-icons anywhere
grep -r "react-icons" src/ 2>/dev/null | wc -l
# Expected: 0

grep -r "IconType" src/ 2>/dev/null | wc -l
# Expected: 0

grep "react-icons" package.json 2>/dev/null | wc -l
# Expected: 0

# Full build passes
npm run build
# Expected: 0 errors, successful build

# tool-icons.ts has 17 unique mappings
node -e "const m = require('./src/lib/constants/tool-icons'); console.log(Object.keys(m.TOOL_ICONS).length, new Set(Object.values(m.TOOL_ICONS).map(v=>v.displayName||v.name)).size)"
# Expected: 17 17 (17 keys, 17 unique values -- though some sub-pages intentionally share)
```
</verification>

<success_criteria>
1. All 36 files migrated -- zero files import from react-icons/gi or react-icons
2. react-icons removed from package.json dependencies
3. `npm run build` passes with zero errors
4. `grep -r "react-icons" src/ package.json` returns empty
5. `grep -r "IconType" src/` returns empty (all replaced with LucideIcon)
6. Phase 26 complete: every tool has a unique, consistent Lucide icon across sidebar and tool pages
</success_criteria>

<output>
After completion, create `.planning/phases/26-icon-system-overhaul/26-03-SUMMARY.md`
</output>
