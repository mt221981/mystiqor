---
phase: 26-icon-system-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants/tool-icons.ts
  - src/lib/constants/tools.ts
  - src/components/layouts/Sidebar.tsx
  - src/components/layouts/BottomTabBar.tsx
  - src/components/layouts/Header.tsx
  - src/components/common/LoadingSpinner.tsx
  - src/components/common/EmptyState.tsx
  - src/components/features/dashboard/StatCards.tsx
autonomous: true
requirements:
  - ICON-01
  - ICON-03

must_haves:
  truths:
    - "Centralized tool-icons.ts exists with all 17 unique tool icons mapped"
    - "Sidebar shows Lucide icons for every nav item -- no Gi icons remain"
    - "BottomTabBar coach tab uses Sparkles instead of GiCrystalBall"
    - "Header, LoadingSpinner, EmptyState, StatCards use Lucide icons"
    - "tools.ts uses LucideIcon type instead of IconType from react-icons"
    - "TypeScript compilation passes with zero errors for all 8 files"
  artifacts:
    - path: "src/lib/constants/tool-icons.ts"
      provides: "Centralized tool-to-icon mapping with getToolIcon() helper"
      exports: ["TOOL_ICONS", "getToolIcon"]
    - path: "src/lib/constants/tools.ts"
      provides: "PrimaryTool interface with LucideIcon type"
      contains: "LucideIcon"
    - path: "src/components/layouts/Sidebar.tsx"
      provides: "Sidebar with all Lucide icons, zero Gi imports"
  key_links:
    - from: "src/lib/constants/tool-icons.ts"
      to: "lucide-react"
      via: "named imports of 17 Lucide icon components"
      pattern: "import.*from 'lucide-react'"
    - from: "src/lib/constants/tools.ts"
      to: "src/lib/constants/tool-icons.ts"
      via: "imports tool icons from centralized mapping"
      pattern: "import.*from.*tool-icons"
---

<objective>
Create the centralized icon mapping and migrate all shared infrastructure files (7 files) from react-icons/gi to lucide-react.

Purpose: Establishes the single source of truth for tool icons and migrates the most-imported shared components first, so that Wave 2 (tool pages) and Wave 3 (remaining files) can reference the centralized mapping.

Output: tool-icons.ts created, 7 infrastructure files migrated, zero Gi imports in shared layer.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-icon-system-overhaul/26-CONTEXT.md
@.planning/phases/26-icon-system-overhaul/26-RESEARCH.md

@mystiqor-build/src/lib/constants/tools.ts
@mystiqor-build/src/components/layouts/Sidebar.tsx
@mystiqor-build/src/components/layouts/BottomTabBar.tsx
@mystiqor-build/src/components/layouts/Header.tsx
@mystiqor-build/src/components/common/LoadingSpinner.tsx
@mystiqor-build/src/components/common/EmptyState.tsx
@mystiqor-build/src/components/features/dashboard/StatCards.tsx

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/lib/constants/tools.ts (CURRENT -- will be changed):
```typescript
import type { IconType } from 'react-icons';
export interface PrimaryTool {
  readonly Icon: IconType;
}
```

From src/components/layouts/Sidebar.tsx:
```typescript
interface NavItem {
  readonly icon: React.ComponentType<{ className?: string }>;
}
```
Both react-icons and Lucide components satisfy `React.ComponentType<{ className?: string }>`, so NavItem interface does NOT need changing.

From src/components/layouts/BottomTabBar.tsx:
```typescript
interface TabDefinition {
  readonly Icon: React.ComponentType<{ className?: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized tool-icons.ts and migrate tools.ts type</name>
  <files>src/lib/constants/tool-icons.ts, src/lib/constants/tools.ts</files>
  <action>
**Step A: Create `src/lib/constants/tool-icons.ts`** (new file) with:

1. Import `LucideIcon` type and all 17 tool icons from `lucide-react`:
   - Orbit, Layers, Hash, Moon, Hand, PenTool, Palette, Dna, Heart, Sparkles, Brain, Compass, Users, FileSearch, Clock, Merge, Sun
2. Export `TOOL_ICONS: Record<string, LucideIcon>` mapping tool IDs to icons:
   - astrology: Orbit, tarot: Layers, numerology: Hash, dream: Moon, palmistry: Hand, graphology: PenTool, drawing: Palette, 'human-design': Dna, compatibility: Heart, coach: Sparkles, personality: Brain, career: Compass, relationships: Users, document: FileSearch, timing: Clock, synthesis: Merge, 'daily-insights': Sun
3. Export `getToolIcon(toolId: string): LucideIcon` function that returns `TOOL_ICONS[toolId] ?? Sparkles` as fallback.
4. JSDoc in Hebrew for the file, the constant, and the function.

**Step B: Migrate `src/lib/constants/tools.ts`:**

1. Remove `import { GiAstrolabe, GiCardRandom, GiAbacus, GiHand, GiQuillInk, GiPaintBrush } from 'react-icons/gi'`
2. Remove `import type { IconType } from 'react-icons'`
3. Add `import { Orbit, Layers, Hash, Hand, PenTool, Palette } from 'lucide-react'`
4. Add `import type { LucideIcon } from 'lucide-react'`
5. Change `readonly Icon: IconType` to `readonly Icon: LucideIcon` in the `PrimaryTool` interface
6. Update each tool's `Icon` field: GiAbacus -> Hash, GiAstrolabe -> Orbit, GiHand -> Hand, GiCardRandom -> Layers, GiQuillInk -> PenTool, GiPaintBrush -> Palette
7. Do NOT change anything else in the file -- preserve all other fields, comments, structure.

CRITICAL: Pitfall from RESEARCH -- change the type BEFORE changing icon imports. `LucideIcon` and `IconType` have different signatures.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>tool-icons.ts exists with 17 unique icons exported. tools.ts uses LucideIcon type and Lucide imports. Zero react-icons imports remain in either file. TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Sidebar.tsx -- replace 25 Gi imports with Lucide</name>
  <files>src/components/layouts/Sidebar.tsx</files>
  <action>
**Replace the entire `react-icons/gi` import block** (lines 13-38 approximately) with Lucide imports. The existing `lucide-react` import block (lines 40-55) should be MERGED with the new icons.

**Gi-to-Lucide mapping for Sidebar NAV_SECTIONS:**

| Gi Import | Lucide Replacement | Used For |
|-----------|-------------------|----------|
| GiAbacus | Hash | Numerology |
| GiAstrolabe | Orbit | Astrology + Astro Teacher + Astro Dictionary |
| GiQuillInk | PenTool | Graphology |
| GiPaintBrush | Palette | Drawing + Drawing Teacher |
| GiHandOfGod | Hand | Palmistry |
| GiCardRandom | Layers | Tarot |
| GiBodyBalance | Dna | Human Design |
| GiDreamCatcher | Moon | Dreams |
| GiSunRadiations | SunDim | Daily Forecast |
| GiCalendar | CalendarDays | Astro Calendar |
| GiCompass | Orbit | Transits (shares with Astrology -- acceptable per RESEARCH) |
| GiLovers (synastry) | HeartHandshake | Synastry |
| GiLovers (relationships) | Users | Relationships |
| GiHearts | Heart | Compatibility |
| GiBriefcase | Compass | Career |
| GiScrollUnfurled | FileSearch | Document |
| GiAllSeeingEye | Merge | Synthesis |
| GiMirrorMirror | Brain | Personality |
| GiCrystalBall | Sparkles | Coach |
| GiTargetArrows | Target | Goals |
| GiNotebook | NotebookPen | Journal |
| GiLightBulb | Sun | Daily Insights |
| GiGraduateCap | GraduationCap | Tutorials |
| GiNewspaper | Newspaper | Blog |

**DEAD IMPORTS -- REMOVE entirely (do NOT replace):**
- GiSparkles -- imported but never used in NAV_SECTIONS
- GiMountainRoad -- imported but never used in NAV_SECTIONS

**After replacing imports:** Walk through every item in NAV_SECTIONS and update the `icon` field to use the new Lucide component name. The existing Lucide imports (LayoutDashboard, Home, MessageCircle, Smile, User, Settings, CreditCard, Tag, Gift, Bell, ChevronDown, ChevronUp, History, BarChart3, GitCompare) remain untouched.

**Merge into a single lucide-react import statement** containing both the existing and new Lucide icons. Remove the react-icons/gi import entirely.

Do NOT change the NavItem or NavSection interfaces -- they use `React.ComponentType<{ className?: string }>` which is compatible with Lucide.
Do NOT change file structure, comments, or any non-icon-related code.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar.tsx has zero react-icons imports. All 25 Gi icons replaced with Lucide equivalents. 2 dead imports removed. Single lucide-react import block. TypeScript passes. NavItem interface unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate BottomTabBar, Header, LoadingSpinner, EmptyState, StatCards</name>
  <files>src/components/layouts/BottomTabBar.tsx, src/components/layouts/Header.tsx, src/components/common/LoadingSpinner.tsx, src/components/common/EmptyState.tsx, src/components/features/dashboard/StatCards.tsx</files>
  <action>
**Migrate 5 remaining infrastructure files.** Each file has 1-4 Gi imports. Simple 1:1 replacements.

**BottomTabBar.tsx:**
- Remove: `import { GiCrystalBall } from 'react-icons/gi'`
- Add `Sparkles` to existing lucide-react import: `import { LayoutDashboard, Lightbulb, Grid2x2, User, Sparkles } from 'lucide-react'`
- In TABS array, change `Icon: GiCrystalBall` to `Icon: Sparkles` for the coach tab

**Header.tsx:**
- Remove: `import { GiSparkles } from 'react-icons/gi'`
- Add `Sparkles` to existing lucide-react import (which already has Sun, Moon, Menu, User, LogOut, Settings, ArrowRight)
- Replace every `GiSparkles` usage in JSX with `Sparkles`

**LoadingSpinner.tsx:**
- Remove: `import { GiSparkles } from 'react-icons/gi'`
- Add: `import { Sparkles } from 'lucide-react'`
- Replace every `GiSparkles` usage in JSX with `Sparkles`

**EmptyState.tsx:**
- Remove: `import { GiCrystalBall } from 'react-icons/gi'`
- Add: `import { Gem } from 'lucide-react'` (Gem, not Sparkles -- distinct from coach per RESEARCH)
- Replace every `GiCrystalBall` usage in JSX with `Gem`

**StatCards.tsx:**
- Remove: `import { GiTargetArrows, GiFaceToFace, GiStarMedal, GiAlarmClock } from 'react-icons/gi'`
- Add: `import { Target, Smile, Award, AlarmClock } from 'lucide-react'`
- Replace in STAT_CARD_DEFINITIONS: GiTargetArrows -> Target, GiFaceToFace -> Smile, GiStarMedal -> Award, GiAlarmClock -> AlarmClock

For all 5 files: do NOT change anything else -- preserve structure, comments, interfaces, logic. Only touch import lines and icon references.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -r "react-icons/gi" src/components/layouts/ src/components/common/ src/components/features/dashboard/StatCards.tsx src/lib/constants/tools.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 5 files migrated. Zero react-icons/gi imports in layouts/, common/, StatCards.tsx, or tools.ts. TypeScript passes. grep count is 0 for migrated files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This phase is purely a visual presentation layer change (icon component swaps). No user input, no data flow, no API changes, no authentication changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | Icon imports | accept | Icons are bundled at build time from npm package already in lockfile. No new attack surface. |
</threat_model>

<verification>
```bash
# All Wave 1 files migrated -- zero Gi imports in shared infrastructure
cd mystiqor-build
grep -r "react-icons/gi" src/lib/constants/tools.ts src/components/layouts/ src/components/common/ src/components/features/dashboard/StatCards.tsx 2>/dev/null | wc -l
# Expected: 0

# TypeScript compiles cleanly
npx tsc --noEmit
# Expected: 0 errors

# tool-icons.ts exists with correct exports
grep -c "export" src/lib/constants/tool-icons.ts
# Expected: 2+ (TOOL_ICONS + getToolIcon)
```
</verification>

<success_criteria>
1. tool-icons.ts exists at src/lib/constants/tool-icons.ts with 17 unique tool icon mappings
2. tools.ts uses LucideIcon type (not IconType), imports from lucide-react (not react-icons)
3. Sidebar.tsx has zero react-icons imports, all NAV_SECTIONS use Lucide icons
4. BottomTabBar, Header, LoadingSpinner, EmptyState, StatCards -- all use Lucide icons
5. `npx tsc --noEmit` passes with zero errors
6. `grep -r "react-icons" src/lib/constants/ src/components/layouts/ src/components/common/ src/components/features/dashboard/StatCards.tsx` returns empty
</success_criteria>

<output>
After completion, create `.planning/phases/26-icon-system-overhaul/26-01-SUMMARY.md`
</output>
