---
phase: 20-dream-blog-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/constants/blog-data.ts
  - mystiqor-build/scripts/seed-blog-posts.ts
  - mystiqor-build/src/app/api/blog/[slug]/route.ts
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02

must_haves:
  truths:
    - "3 rich Hebrew blog articles exist as seed data with 800-1200 words each"
    - "Seed script can upsert articles to Supabase blog_posts table idempotently"
    - "GET /api/blog/[slug] returns a single blog post by slug with auth check"
  artifacts:
    - path: "mystiqor-build/src/lib/constants/blog-data.ts"
      provides: "BLOG_POSTS_SEED constant with 3 articles"
      exports: ["BLOG_POSTS_SEED"]
      contains: "numerology-beginners-guide"
    - path: "mystiqor-build/scripts/seed-blog-posts.ts"
      provides: "Supabase seed script for blog posts"
      contains: "upsert"
    - path: "mystiqor-build/src/app/api/blog/[slug]/route.ts"
      provides: "GET single blog post API route"
      exports: ["GET"]
      contains: "auth.getUser"
  key_links:
    - from: "mystiqor-build/scripts/seed-blog-posts.ts"
      to: "mystiqor-build/src/lib/constants/blog-data.ts"
      via: "import BLOG_POSTS_SEED"
      pattern: "import.*BLOG_POSTS_SEED.*blog-data"
    - from: "mystiqor-build/src/app/api/blog/[slug]/route.ts"
      to: "supabase.from('blog_posts')"
      via: "Supabase query with slug filter"
      pattern: "eq.*slug.*params"
---

<objective>
Create 3 rich Hebrew blog articles as seed data, a Supabase seed script, and a GET /api/blog/[slug] route for fetching individual posts.

Purpose: BLOG-01 requires 3+ rich articles (numerology, zodiac signs, palmistry). BLOG-02 requires content in DB via seed script (not hardcoded). The [slug] API route is needed for the detail page in Plan 03.

Output: blog-data.ts constant with 3 articles, seed-blog-posts.ts script, and /api/blog/[slug]/route.ts API endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dream-blog-content/20-CONTEXT.md
@.planning/phases/20-dream-blog-content/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From mystiqor-build/src/types/database.ts (blog_posts Insert type):
```typescript
blog_posts: {
  Insert: {
    id?: string;
    title: string;
    slug: string;
    content: string;
    excerpt?: string | null;
    author: string;
    category: string;
    tags?: string[];
    read_time_minutes?: number;
    is_published?: boolean;
    published_at?: string;
  };
};
```

From mystiqor-build/src/app/api/blog/route.ts (auth + query pattern):
```typescript
import { NextResponse, type NextRequest } from 'next/server';
import { createClient } from '@/lib/supabase/server';

export async function GET(request: NextRequest) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) {
    return NextResponse.json({ error: 'לא מחובר' }, { status: 401 });
  }
  // ... query blog_posts
}
```

From mystiqor-build/scripts/sync-tarot-meta.ts (seed script pattern):
```typescript
import { readFileSync } from 'fs'
import { resolve } from 'path'
import { createClient } from '@supabase/supabase-js'

function loadEnv(): void {
  try {
    const envContent = readFileSync(resolve(__dirname, '..', '.env.local'), 'utf-8')
    for (const line of envContent.split('\n')) {
      const m = line.match(/^([^#=\s][^=]*)=(.*)$/)
      if (m?.[1] && m[2] !== undefined) process.env[m[1].trim()] = m[2].trim().replace(/^["']|["']$/g, '')
    }
  } catch {
    console.warn('...')
  }
}
```

From mystiqor-build/src/lib/validations/learning.ts:
```typescript
export const BlogQuerySchema = z.object({
  category: z.string().optional(),
  search: z.string().optional(),
  limit: z.coerce.number().int().min(1).max(50).default(20),
  offset: z.coerce.number().int().min(0).default(0),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog-data.ts with 3 rich Hebrew articles + seed script</name>
  <files>
    mystiqor-build/src/lib/constants/blog-data.ts,
    mystiqor-build/scripts/seed-blog-posts.ts
  </files>
  <read_first>
    mystiqor-build/scripts/sync-tarot-meta.ts,
    mystiqor-build/src/types/database.ts,
    mystiqor-build/src/lib/constants/dream-data.ts
  </read_first>
  <action>
    **Create mystiqor-build/src/lib/constants/blog-data.ts (per D-04, D-05, D-06, D-07):**

    1. Define a `BlogPostSeed` interface matching the `blog_posts.Insert` type from database.ts:
       ```typescript
       /** נתוני מאמר בלוג לזריעה */
       export interface BlogPostSeed {
         readonly slug: string;
         readonly title: string;
         readonly content: string;
         readonly excerpt: string;
         readonly author: string;
         readonly category: string;
         readonly tags: string[];
         readonly read_time_minutes: number;
         readonly is_published: boolean;
         readonly published_at: string;
       }
       ```

    2. Export `BLOG_POSTS_SEED` as `readonly BlogPostSeed[]` with exactly 3 articles:

    **Article 1 — `numerology-beginners-guide`:**
    - title: "נומרולוגיה למתחילים — המדריך המלא"
    - category: "נומרולוגיה"
    - tags: ['נומרולוגיה', 'מספר חיים', 'למתחילים']
    - read_time_minutes: 8
    - content: Rich Markdown, 800-1200 Hebrew words. Structure:
      - `# נומרולוגיה למתחילים — המדריך המלא`
      - `## מה היא נומרולוגיה?` — Brief history (Pythagoras, Kabbalah connection)
      - `## מספר החיים — המפתח המרכזי` — How to calculate life path number (with example: birthdate -> single digit)
      - `## המשמעות של כל מספר` — Numbers 1-9, each with 2-3 line description of personality traits
      - `## מספרי מאסטר: 11, 22, 33` — Brief on master numbers
      - `## איך להתחיל?` — Practical tips for beginners
    - excerpt: "גלה כיצד מספרים חושפים את מסלול חייך — מדריך מקיף לנומרולוגיה עם דוגמאות מעשיות"

    **Article 2 — `zodiac-signs-complete-guide`:**
    - title: "12 המזלות — מדריך מקיף לכל מזל"
    - category: "אסטרולוגיה"
    - tags: ['אסטרולוגיה', 'מזלות', 'גלגל המזלות']
    - read_time_minutes: 10
    - content: Rich Markdown, 800-1200 Hebrew words. Structure:
      - `# 12 המזלות — מדריך מקיף`
      - `## מהו גלגל המזלות?` — Brief intro to zodiac
      - `## מזלות האש: טלה, אריה, קשת` — Each with personality, element connection, ruling planet
      - `## מזלות האדמה: שור, בתולה, גדי` — Same structure
      - `## מזלות האוויר: תאומים, מאזניים, דלי` — Same structure
      - `## מזלות המים: סרטן, עקרב, דגים` — Same structure
      - `## מעבר למזל השמש` — Mention rising sign and moon sign
    - excerpt: "הכר את 12 המזלות — תכונות, אלמנטים, כוכבי שולטים והקשרים ביניהם"

    **Article 3 — `palmistry-reading-guide`:**
    - title: "קריאה בכף היד — סימנים, קווים ומשמעויות"
    - category: "קריאה בכף יד"
    - tags: ['קריאה בכף יד', 'כירומנטיה', 'קווי כף היד']
    - read_time_minutes: 7
    - content: Rich Markdown, 800-1200 Hebrew words. Structure:
      - `# קריאה בכף היד — סימנים, קווים ומשמעויות`
      - `## מהי כירומנטיה?` — Brief history
      - `## שלושת הקווים העיקריים` — Life line, head line, heart line with descriptions
      - `## קווים נוספים` — Fate line, sun line
      - `## צורת כף היד` — Earth, air, water, fire hand shapes
      - `## טיפים למתחילים` — How to read your own palm
    - excerpt: "למד לקרוא את כף ידך — מדריך מפורט לקווים, סימנים ומשמעויות בכירומנטיה"

    All articles: `author: 'MystiQor'`, `is_published: true`, `published_at: '2026-03-01T00:00:00.000Z'` (fixed date, not dynamic).

    The content must be genuine, informative, well-written Hebrew. Each article MUST be 800-1200 words. Use Markdown headings (`##`, `###`), lists (`-`), bold (`**`), and practical examples. Write as an expert teaching beginners — warm, accessible, mystical but grounded.

    3. All JSDoc comments in Hebrew. Export both `BlogPostSeed` interface and `BLOG_POSTS_SEED` constant.

    **Create mystiqor-build/scripts/seed-blog-posts.ts (per D-06):**

    1. Follow the exact pattern from `sync-tarot-meta.ts`:
       - Import `readFileSync`, `resolve` from Node built-ins
       - Import `createClient` from `@supabase/supabase-js`
       - Import `BLOG_POSTS_SEED` from `../src/lib/constants/blog-data`
       - `loadEnv()` function — copy exactly from sync-tarot-meta.ts
       - Create Supabase client with `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`
       - Validate env vars exist, exit with error if not
       - Use `supabase.from('blog_posts').upsert(BLOG_POSTS_SEED as any[], { onConflict: 'slug' })` for idempotent seeding
       - Report results: number of articles seeded, any errors
       - Use `console.log` with Hebrew messages
    2. Run command: `npx tsx mystiqor-build/scripts/seed-blog-posts.ts`
    3. All JSDoc in Hebrew
  </action>
  <verify>
    <automated>cd mystiqor-build && npx vitest run tests/services/blog.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File mystiqor-build/src/lib/constants/blog-data.ts exists
    - grep -c "BLOG_POSTS_SEED" mystiqor-build/src/lib/constants/blog-data.ts returns >= 1
    - grep -c "numerology-beginners-guide" mystiqor-build/src/lib/constants/blog-data.ts returns >= 1
    - grep -c "zodiac-signs-complete-guide" mystiqor-build/src/lib/constants/blog-data.ts returns >= 1
    - grep -c "palmistry-reading-guide" mystiqor-build/src/lib/constants/blog-data.ts returns >= 1
    - grep -c "BlogPostSeed" mystiqor-build/src/lib/constants/blog-data.ts returns >= 2
    - File mystiqor-build/scripts/seed-blog-posts.ts exists
    - grep -c "upsert" mystiqor-build/scripts/seed-blog-posts.ts returns >= 1
    - grep -c "onConflict.*slug" mystiqor-build/scripts/seed-blog-posts.ts returns >= 1
    - grep -c "loadEnv" mystiqor-build/scripts/seed-blog-posts.ts returns >= 2
    - Blog tests pass: vitest run tests/services/blog.test.ts exits 0
    - Each article content length >= 800 characters (verified by test)
  </acceptance_criteria>
  <done>3 rich Hebrew blog articles exist in blog-data.ts. Seed script exists and uses upsert with onConflict:'slug'. Blog tests pass validating article shape, count, uniqueness, and content length.</done>
</task>

<task type="auto">
  <name>Task 2: Create GET /api/blog/[slug] API route</name>
  <files>
    mystiqor-build/src/app/api/blog/[slug]/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/blog/route.ts,
    mystiqor-build/src/lib/supabase/server.ts,
    mystiqor-build/src/lib/validations/learning.ts
  </read_first>
  <action>
    **Create mystiqor-build/src/app/api/blog/[slug]/route.ts:**

    Follow the exact pattern from the existing `/api/blog/route.ts` for auth, error handling, and response format.

    1. Import `NextResponse` and `NextRequest` from `next/server`
    2. Import `createClient` from `@/lib/supabase/server`
    3. Import `z` from `zod` for slug validation
    4. Define a slug validation schema:
       ```typescript
       /** סכמת Zod לאימות slug */
       const SlugSchema = z.string().min(1).max(100).regex(/^[a-z0-9-]+$/, 'slug לא תקין');
       ```

    5. Export async function `GET`:
       ```typescript
       /**
        * GET /api/blog/[slug] — שליפת מאמר בלוג בודד לפי slug
        * מחזיר מאמר מפורסם בלבד. דורש משתמש מחובר.
        */
       export async function GET(
         _request: NextRequest,
         { params }: { params: Promise<{ slug: string }> }
       ) {
       ```
       NOTE: In Next.js 15+ App Router, `params` is a Promise and must be awaited: `const { slug } = await params;`

    6. Auth check — copy exactly from /api/blog/route.ts:
       ```typescript
       const supabase = await createClient();
       const { data: { user } } = await supabase.auth.getUser();
       if (!user) {
         return NextResponse.json({ error: 'לא מחובר' }, { status: 401 });
       }
       ```

    7. Validate slug with Zod:
       ```typescript
       const parseResult = SlugSchema.safeParse(slug);
       if (!parseResult.success) {
         return NextResponse.json({ error: 'slug לא תקין' }, { status: 400 });
       }
       ```

    8. Query single post:
       ```typescript
       const { data: post, error } = await supabase
         .from('blog_posts')
         .select('*')
         .eq('slug', parseResult.data)
         .eq('is_published', true)
         .single();
       ```

    9. Handle not found:
       ```typescript
       if (error || !post) {
         return NextResponse.json({ error: 'מאמר לא נמצא' }, { status: 404 });
       }
       ```

    10. Return success:
        ```typescript
        return NextResponse.json({ data: post });
        ```

    11. Wrap entire function body in try/catch with 500 error response (matching /api/blog/route.ts pattern)

    12. All JSDoc in Hebrew. File should be ~50 lines.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File mystiqor-build/src/app/api/blog/[slug]/route.ts exists
    - grep -c "export async function GET" mystiqor-build/src/app/api/blog/[slug]/route.ts returns 1
    - grep -c "auth.getUser" mystiqor-build/src/app/api/blog/[slug]/route.ts returns 1
    - grep -c "slug" mystiqor-build/src/app/api/blog/[slug]/route.ts returns >= 3
    - grep -c "is_published.*true" mystiqor-build/src/app/api/blog/[slug]/route.ts returns 1
    - grep -c "single()" mystiqor-build/src/app/api/blog/[slug]/route.ts returns 1
    - grep -c "SlugSchema" mystiqor-build/src/app/api/blog/[slug]/route.ts returns >= 2
    - grep "status: 401" mystiqor-build/src/app/api/blog/[slug]/route.ts matches (auth check present)
    - grep "status: 404" mystiqor-build/src/app/api/blog/[slug]/route.ts matches (not found handling)
    - TypeScript compiles: npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GET /api/blog/[slug] route returns a single blog post by slug. Auth required. Zod validates slug. Returns 401 if not logged in, 400 if invalid slug, 404 if not found, 200 with post data if found. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx vitest run tests/services/blog.test.ts` — passes (validates 3 articles, correct shape, 800+ chars each)
2. `cd mystiqor-build && npx tsc --noEmit` — TypeScript compiles cleanly
3. `cd mystiqor-build && npx vitest run` — full suite passes
4. blog-data.ts contains exactly 3 articles with correct slugs
5. seed script uses upsert with onConflict: 'slug'
6. API route has auth check, Zod validation, and proper error handling
</verification>

<success_criteria>
- 3 Hebrew blog articles (800-1200 words each) exist in blog-data.ts
- Seed script is re-runnable (upsert with onConflict)
- /api/blog/[slug] route works with auth, validation, and error handling
- All blog tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-dream-blog-content/20-02-SUMMARY.md`
</output>
