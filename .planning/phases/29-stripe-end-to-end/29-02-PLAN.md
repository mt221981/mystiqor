---
phase: 29-stripe-end-to-end
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified: []
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]

must_haves:
  truths:
    - "Checkout route creates Stripe session with correct metadata (user_id, plan_id)"
    - "Webhook handler uses request.text() for raw body before signature verification"
    - "Webhook handler inserts to processed_webhook_events BEFORE business logic"
    - "Webhook handler returns 400 on invalid signature"
    - "Webhook handles all 4 event types: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed"
    - "Cancel route sets cancel_at_period_end=true via Stripe API"
    - "No duplicate subscription rows on webhook retry (idempotency via processed_webhook_events)"
    - "SubscriptionGuard functional (no early return bypass)"
    - "Success page invalidates subscription cache"
  artifacts:
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Stripe webhook handler with signature verification and idempotency"
      contains: "request.text()"
    - path: "src/app/api/subscription/checkout/route.ts"
      provides: "Stripe Checkout session creation with metadata"
      contains: "metadata"
    - path: "src/app/api/subscription/cancel/route.ts"
      provides: "Subscription cancellation via Stripe API"
      contains: "cancel_at_period_end"
    - path: "src/components/features/subscription/SubscriptionGuard.tsx"
      provides: "Functional subscription gate"
      contains: "canUseFeature"
  key_links:
    - from: "src/app/api/subscription/checkout/route.ts"
      to: "src/app/api/webhooks/stripe/route.ts"
      via: "Stripe sends webhook after checkout completes"
      pattern: "metadata.*user_id.*plan_id"
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "processed_webhook_events"
      via: "idempotency INSERT before business logic"
      pattern: "processed_webhook_events.*insert"
    - from: "src/app/api/subscription/cancel/route.ts"
      to: "src/app/api/webhooks/stripe/route.ts"
      via: "Stripe sends customer.subscription.updated webhook after cancel"
      pattern: "cancel_at_period_end"
---

<objective>
Verify the complete Stripe end-to-end flow by running automated checks on all PAY requirements. No code changes — purely verification that all existing code (checkout, webhook, cancel, guard) is wired correctly.

Purpose: The webhook handler, checkout route, cancel route, and guard are all built. This plan confirms they are wired correctly and that the five success criteria from the roadmap are satisfied.

Output: Verification report confirming all PAY-01 through PAY-04 requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-stripe-end-to-end/29-01-SUMMARY.md

@src/app/api/webhooks/stripe/route.ts
@src/app/api/subscription/checkout/route.ts
@src/app/api/subscription/cancel/route.ts
@src/app/api/subscription/portal/route.ts
@src/app/api/subscription/route.ts
@src/components/features/subscription/SubscriptionGuard.tsx
@src/components/features/subscription/SubscriptionManagement.tsx
@src/app/(auth)/subscription/success/page.tsx
@src/hooks/useSubscription.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all Stripe routes and subscription wiring (PAY-01 through PAY-04)</name>
  <files></files>
  <read_first>
    - src/app/api/webhooks/stripe/route.ts
    - src/app/api/subscription/checkout/route.ts
    - src/app/api/subscription/cancel/route.ts
    - src/components/features/subscription/SubscriptionGuard.tsx
    - src/components/features/subscription/SubscriptionManagement.tsx
    - src/app/(auth)/subscription/success/page.tsx
    - src/app/(auth)/pricing/page.tsx
  </read_first>
  <action>
    Run a comprehensive automated verification of all 5 roadmap success criteria. This is a READ-ONLY verification task — do NOT modify any files.

    **Verification Script — run each check as a grep/command and record pass/fail:**

    **SC-1: Checkout creates subscription with metadata (PAY-01)**
    - Verify: `grep -n "metadata" src/app/api/subscription/checkout/route.ts` — must contain `user_id` and `plan_id` in metadata
    - Verify: `grep -n "STRIPE_PRICE" src/app/api/subscription/checkout/route.ts` — must use env vars, not hardcoded price IDs
    - Verify: `grep -n "success_url" src/app/api/subscription/checkout/route.ts` — must redirect to success page
    - Verify: Pricing page calls `/api/subscription/checkout` with planId
    - Verify: Success page has `invalidateQueries` for subscription cache

    **SC-2: Webhook handles 4 event types (PAY-02)**
    - Verify: `grep -n "checkout.session.completed\|customer.subscription.updated\|customer.subscription.deleted\|invoice.payment_failed" src/app/api/webhooks/stripe/route.ts` — all 4 must appear
    - Verify: `handleCheckoutSessionCompleted` function exists and does upsert to subscriptions table
    - Verify: `handleSubscriptionUpdated` function exists and updates status
    - Verify: `handleSubscriptionDeleted` function exists and resets to free plan
    - Verify: `handleInvoicePaymentFailed` function exists and calls sendPaymentFailedEmail

    **SC-3: Webhook rejects invalid signatures (PAY-03)**
    - Verify: `grep -n "request.text()" src/app/api/webhooks/stripe/route.ts` — must use text() not json()
    - Verify: `grep -n "constructEvent" src/app/api/webhooks/stripe/route.ts` — Stripe signature verification
    - Verify: `grep -n "status: 400" src/app/api/webhooks/stripe/route.ts` — returns 400 on invalid signature
    - Verify: `grep -n "processed_webhook_events" src/app/api/webhooks/stripe/route.ts` — idempotency table used
    - Verify: The idempotency INSERT appears BEFORE the switch statement (business logic)

    **SC-4: Cancel flow works (PAY-04)**
    - Verify: `grep -n "cancel_at_period_end" src/app/api/subscription/cancel/route.ts` — sets flag to true
    - Verify: SubscriptionManagement has cancel button that calls `/api/subscription/cancel`
    - Verify: SubscriptionGuard does NOT have early return bypass before useSubscription()
    - Verify: SubscriptionManagement shows cancel_at_period_end warning when set

    **SC-5: No duplicate rows on retry (PAY-03)**
    - Verify: The idempotency INSERT to `processed_webhook_events` uses the event ID
    - Verify: The INSERT happens before the switch/case business logic
    - Verify: Duplicate INSERT (dupError) returns 200 immediately without processing

    **Build check:**
    - Run `npx tsc --noEmit` to confirm no TypeScript errors
    - Run `npm run build` to confirm the application builds

    After running all checks, produce a verification matrix:

    ```
    | SC | Requirement | Check | Result |
    |----|------------|-------|--------|
    | SC-1 | PAY-01 | Checkout metadata | PASS/FAIL |
    | SC-2 | PAY-02 | 4 event types | PASS/FAIL |
    | SC-3 | PAY-03 | Signature + idempotency | PASS/FAIL |
    | SC-4 | PAY-04 | Cancel flow | PASS/FAIL |
    | SC-5 | PAY-03 | No duplicate rows | PASS/FAIL |
    | BUILD | All | tsc + build | PASS/FAIL |
    ```

    If any check FAILS: document what failed and what file/line needs fixing. Then fix it (small targeted fix only — do not refactor). If the webhook handler needs a fix, STOP and document instead — it must not be refactored per project rules.
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Verification matrix produced with all 5 success criteria checked
    - All 5 success criteria PASS
    - `npx tsc --noEmit` exits with 0
    - `npm run build` exits with 0
    - Webhook handler file was NOT modified (read only)
    - Checkout route has metadata with user_id and plan_id
    - SubscriptionGuard has no early return bypass
  </acceptance_criteria>
  <done>All 5 roadmap success criteria for Phase 29 verified as PASS. Build completes. Webhook handler preserved untouched.</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. All PAY-01 through PAY-04 requirements addressed
2. Webhook handler NOT refactored (file content preserved from Phase 28)
3. Build passes with zero errors
4. Cancel route from Phase 28 acknowledged and wired into UI (not duplicated)
5. Checkout route with correct metadata verified
6. SubscriptionGuard functional (blocks free users from premium features)
</verification>

<success_criteria>
- Verification matrix shows PASS for all 5 success criteria
- TypeScript compilation succeeds
- Application builds successfully
- No files modified in this plan (verification only)
- Webhook handler hash unchanged from before this phase
</success_criteria>

<output>
After completion, create `.planning/phases/29-stripe-end-to-end/29-02-SUMMARY.md`
</output>
