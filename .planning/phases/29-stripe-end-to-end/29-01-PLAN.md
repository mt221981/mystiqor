---
phase: 29-stripe-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/subscription/SubscriptionGuard.tsx
  - src/components/features/subscription/SubscriptionManagement.tsx
  - src/app/(auth)/subscription/success/page.tsx
autonomous: true
requirements: [PAY-01, PAY-04]

must_haves:
  truths:
    - "SubscriptionGuard blocks premium features for free users and shows upgrade prompt"
    - "Success page refreshes subscription data after Stripe checkout so UI reflects new plan"
    - "User can cancel subscription from the subscription management page"
    - "Cancel confirmation shows cancel_at_period_end warning before executing"
  artifacts:
    - path: "src/components/features/subscription/SubscriptionGuard.tsx"
      provides: "Functional subscription gate (no early return bypass)"
      min_lines: 40
    - path: "src/components/features/subscription/SubscriptionManagement.tsx"
      provides: "Cancel button with confirmation and API call"
      min_lines: 100
    - path: "src/app/(auth)/subscription/success/page.tsx"
      provides: "Cache invalidation after checkout"
      min_lines: 60
  key_links:
    - from: "src/app/(auth)/subscription/success/page.tsx"
      to: "useSubscription query"
      via: "queryClient.invalidateQueries(['subscription'])"
      pattern: "invalidateQueries.*subscription"
    - from: "src/components/features/subscription/SubscriptionManagement.tsx"
      to: "/api/subscription/cancel"
      via: "fetch POST call"
      pattern: "fetch.*subscription/cancel"
    - from: "src/components/features/subscription/SubscriptionGuard.tsx"
      to: "useSubscription"
      via: "canUseFeature check without early return bypass"
      pattern: "canUseFeature\\(feature\\)"
---

<objective>
Wire the three missing connections in the Stripe subscription flow so users can subscribe, see their updated plan, cancel, and be gated by SubscriptionGuard.

Purpose: The checkout route, webhook handler, cancel route, and all subscription components already exist. What is missing is (a) SubscriptionGuard is bypassed by an early return, (b) the success page does not refresh subscription state after checkout, and (c) there is no cancel button in the subscription management UI.

Output: Three updated files that complete the Stripe end-to-end flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/features/subscription/SubscriptionGuard.tsx
@src/components/features/subscription/SubscriptionManagement.tsx
@src/app/(auth)/subscription/success/page.tsx
@src/hooks/useSubscription.ts
@src/app/api/subscription/cancel/route.ts
@src/types/subscription.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/subscription.ts:
```typescript
export type PlanType = 'free' | 'basic' | 'premium' | 'enterprise';
export type SubscriptionStatus = 'trial' | 'active' | 'cancelled' | 'expired' | 'past_due';
export interface PlanInfo {
  name: string;
  analyses: number;
  guestProfiles: number;
  features: string[];
}
```

From src/hooks/useSubscription.ts:
```typescript
export function useSubscription(): {
  subscription: SubscriptionRow;
  planInfo: PlanInfo;
  hasUsageLeft: boolean;
  canUseFeature: (featureName: string) => boolean;
  incrementUsage: () => Promise<{ new_count: number }>;
  isLoading: boolean;
}
```

From src/app/api/subscription/cancel/route.ts:
```typescript
// POST /api/subscription/cancel
// Returns: { success: true, cancel_at_period_end: true }
// Errors: 401 (not logged in), 400 (no active subscription), 500 (Stripe error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate SubscriptionGuard + add cancel button to SubscriptionManagement</name>
  <files>src/components/features/subscription/SubscriptionGuard.tsx, src/components/features/subscription/SubscriptionManagement.tsx</files>
  <read_first>
    - src/components/features/subscription/SubscriptionGuard.tsx
    - src/components/features/subscription/SubscriptionManagement.tsx
    - src/hooks/useSubscription.ts
    - src/app/api/subscription/cancel/route.ts
  </read_first>
  <action>
    **SubscriptionGuard.tsx — Remove early return bypass (PAY-04 prerequisite):**

    The file currently has an early return on line 28 (`return <>{children}</>;`) that bypasses all subscription checks. This was placed intentionally during development to unlock all tools, but now the Stripe flow is complete and the guard must function.

    1. Remove the line `return <>{children}</>;` (line 28) so the component continues to the `useSubscription()` call and the `canUseFeature(feature)` check.
    2. Keep all other code exactly as-is — the loading state, the canUseFeature check, the fallback rendering, and the upgrade card are all correct.
    3. The component should flow: isLoading check -> canUseFeature check -> show children or fallback/upgrade card.
    4. Do NOT change the Props interface, do NOT add new props.
    5. Hebrew JSDoc comment must be preserved.

    **SubscriptionManagement.tsx — Add cancel button (PAY-04):**

    The component currently has "ניהול חשבון" button for paid users that opens Stripe portal. Add a dedicated cancel button below it with confirmation.

    1. Add state: `const [isCancelLoading, setIsCancelLoading] = useState(false);`
    2. Add state: `const [showCancelConfirm, setShowCancelConfirm] = useState(false);`
    3. Add `handleCancelSubscription` async function that:
       - Sets `isCancelLoading(true)`
       - Calls `fetch('/api/subscription/cancel', { method: 'POST' })`
       - On success: uses `queryClient.invalidateQueries({ queryKey: ['subscription'] })` to refresh subscription data
       - On success: shows `toast.success('המנוי יבוטל בסוף התקופה הנוכחית')`
       - On error: shows `toast.error(errorMessage)` with the error from the API response
       - Finally sets `isCancelLoading(false)` and `setShowCancelConfirm(false)`
    4. Import `useQueryClient` from `@tanstack/react-query` and `toast` from `sonner`
    5. In the action buttons section, below the Stripe portal button (for non-free users only), add:
       - If `showCancelConfirm` is false: A text button "בטל מנוי" (outline variant, error color styling `text-error border-error/30 hover:bg-error/10`)
       - If `showCancelConfirm` is true: A confirmation panel with text "האם אתה בטוח? המנוי ימשיך עד סוף התקופה הנוכחית." and two buttons: "אשר ביטול" (error variant, calls handleCancelSubscription) and "חזור" (outline, sets showCancelConfirm to false)
       - Do NOT show cancel button if `subscription.cancel_at_period_end` is already true (cancellation already pending)
    6. Keep file under 300 lines per CLAUDE.md standards.
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - SubscriptionGuard.tsx does NOT contain `return <>{children}</>` before the useSubscription() call
    - SubscriptionGuard.tsx contains `canUseFeature(feature)` conditional check
    - SubscriptionManagement.tsx contains `fetch('/api/subscription/cancel'`
    - SubscriptionManagement.tsx contains `invalidateQueries` call for subscription cache refresh
    - SubscriptionManagement.tsx contains cancel confirmation UI text "האם אתה בטוח"
    - SubscriptionManagement.tsx contains `toast.success` and `toast.error` for cancel feedback
    - No `any` types introduced
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>SubscriptionGuard gates premium features (no bypass), SubscriptionManagement has working cancel button with confirmation and cache invalidation</done>
</task>

<task type="auto">
  <name>Task 2: Add subscription cache invalidation to success page</name>
  <files>src/app/(auth)/subscription/success/page.tsx</files>
  <read_first>
    - src/app/(auth)/subscription/success/page.tsx
    - src/hooks/useSubscription.ts
  </read_first>
  <action>
    **Success page — Invalidate subscription cache after Stripe Checkout completes (PAY-01 completion):**

    The success page renders after Stripe redirects the user back. Currently it shows a static success message but does NOT tell React Query to refetch subscription data. This means the useSubscription hook still returns the old plan until the cache expires naturally (staleTime). The user sees "success" but the app still shows the old plan.

    1. Add imports: `useQueryClient` from `@tanstack/react-query`, `useEffect` from `react`
    2. Inside `SuccessPageContent`, add:
       ```
       const queryClient = useQueryClient();
       useEffect(() => {
         queryClient.invalidateQueries({ queryKey: ['subscription'] });
       }, [queryClient]);
       ```
       This ensures that when the success page mounts (after redirect from Stripe), the subscription data is refetched immediately. The webhook may have already updated the DB by this point, so the fresh query will return the new plan.
    3. Keep all existing UI code unchanged — the CheckCircle icon, the session ID display, the two buttons.
    4. The Suspense wrapper must remain as-is (required for useSearchParams).
    5. Hebrew JSDoc comments preserved.
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - success/page.tsx contains `invalidateQueries` call with `['subscription']` query key
    - success/page.tsx contains `useEffect` that fires on mount
    - success/page.tsx contains `useQueryClient` import from `@tanstack/react-query`
    - The Suspense wrapper is preserved
    - No `any` types introduced
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Success page invalidates subscription cache on mount so user sees updated plan immediately after checkout</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (no build-time errors)
3. Grep verification:
   - `grep -n "return.*children" src/components/features/subscription/SubscriptionGuard.tsx` — should NOT appear before the useSubscription() call
   - `grep -n "canUseFeature" src/components/features/subscription/SubscriptionGuard.tsx` — must appear
   - `grep -n "subscription/cancel" src/components/features/subscription/SubscriptionManagement.tsx` — must find the cancel API call
   - `grep -n "invalidateQueries" src/app/(auth)/subscription/success/page.tsx` — must find cache invalidation
</verification>

<success_criteria>
- SubscriptionGuard actually blocks premium features for free users
- Success page refreshes subscription data after Stripe checkout
- Cancel button exists in subscription management with confirmation dialog
- Cancel calls POST /api/subscription/cancel and refreshes cache
- All TypeScript compiles, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/29-stripe-end-to-end/29-01-SUMMARY.md`
</output>
