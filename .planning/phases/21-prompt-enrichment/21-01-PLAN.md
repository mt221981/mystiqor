---
phase: 21-prompt-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/services/analysis/personal-context.ts
  - mystiqor-build/src/app/api/tools/daily-insights/route.ts
autonomous: true
requirements: [PROMPT-01, PROMPT-02]
must_haves:
  truths:
    - "getPersonalContext returns firstName, zodiacSign, lifePathNumber for any userId"
    - "daily-insights systemPrompt includes user first name, zodiac sign, and life path number"
    - "daily-insights buildPrompt includes lifePathNumber and prior insight summaries"
    - "Two users with different profiles receive different daily insights content"
  artifacts:
    - path: "mystiqor-build/src/services/analysis/personal-context.ts"
      provides: "Shared personal context helper for all 21 routes"
      exports: ["getPersonalContext", "PersonalContext"]
    - path: "mystiqor-build/src/app/api/tools/daily-insights/route.ts"
      provides: "Enriched daily-insights with life path + prior analyses"
      contains: "getPersonalContext"
  key_links:
    - from: "mystiqor-build/src/services/analysis/personal-context.ts"
      to: "@/services/numerology/calculations"
      via: "import reduceToSingleDigit"
      pattern: "reduceToSingleDigit"
    - from: "mystiqor-build/src/app/api/tools/daily-insights/route.ts"
      to: "mystiqor-build/src/services/analysis/personal-context.ts"
      via: "import getPersonalContext"
      pattern: "getPersonalContext"
---

<objective>
Create the shared `getPersonalContext` helper and enrich the daily-insights route as the gold standard for all other routes.

Purpose: Establishes the foundational helper (PROMPT-01) and the deepest enrichment pattern (PROMPT-02) that all subsequent plans reference.
Output: `personal-context.ts` helper + enriched daily-insights route with life path number and prior analyses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-prompt-enrichment/21-CONTEXT.md
@.planning/phases/21-prompt-enrichment/21-RESEARCH.md

@mystiqor-build/src/services/analysis/llm.ts
@mystiqor-build/src/services/numerology/calculations.ts
@mystiqor-build/src/services/coach/context-builder.ts
@mystiqor-build/src/app/api/tools/daily-insights/route.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/services/analysis/llm.ts:
```typescript
export interface LLMRequest {
  prompt: string
  systemPrompt?: string
  responseSchema?: Record<string, unknown>
  zodSchema?: z.ZodSchema
  imageUrls?: string[]
  maxTokens?: number
  userId: string
}
export async function invokeLLM<T = unknown>(request: LLMRequest): Promise<LLMResponse<T>>
```

From src/services/numerology/calculations.ts:
```typescript
export function reduceToSingleDigit(num: number): number
```

From src/services/coach/context-builder.ts:
```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export async function buildCoachingContext(userId: string, supabase: SupabaseClient<any>): Promise<string>
// Pattern: supabase.from('profiles').select('full_name, birth_date').eq('id', userId).maybeSingle()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared getPersonalContext helper</name>
  <files>mystiqor-build/src/services/analysis/personal-context.ts</files>
  <read_first>
    mystiqor-build/src/services/analysis/llm.ts
    mystiqor-build/src/services/numerology/calculations.ts
    mystiqor-build/src/services/coach/context-builder.ts
    mystiqor-build/src/app/api/tools/daily-insights/route.ts
  </read_first>
  <action>
Create NEW file `mystiqor-build/src/services/analysis/personal-context.ts` with:

1. **PersonalContext interface** (exported):
   ```typescript
   export interface PersonalContext {
     /** שם פרטי — ריק אם לא הוגדר בפרופיל */
     firstName: string
     /** מזל בעברית — 'טלה' וכו', ריק אם אין תאריך לידה */
     zodiacSign: string
     /** מספר חיים (life path) — מחושב מתאריך הלידה, 0 אם חסר */
     lifePathNumber: number
   }
   ```

2. **ZODIAC_RANGES constant** — copy EXACTLY from daily-insights/route.ts lines 19-32 (the `as const` array with Hebrew zodiac names, startMonth/startDay/endMonth/endDay).

3. **getZodiacSign(birthDate: string): string** — copy EXACTLY from daily-insights/route.ts lines 39-53 (uses ZODIAC_RANGES, UTC-based, returns Hebrew name, fallback 'גדי'). Add JSDoc in Hebrew.

4. **computeLifePath(birthDate: string): number** — private function:
   ```typescript
   function computeLifePath(birthDate: string): number {
     const parts = birthDate.split('-').map(Number)
     const year = parts[0] ?? 0
     const month = parts[1] ?? 0
     const day = parts[2] ?? 0
     if (!year || !month || !day) return 0
     return reduceToSingleDigit(day + month + year)
   }
   ```
   Import `reduceToSingleDigit` from `@/services/numerology/calculations`.

5. **getPersonalContext(supabase, userId): Promise&lt;PersonalContext&gt;** (exported):
   - Param `supabase: SupabaseClient<any>` with eslint-disable comment (matches context-builder.ts pattern)
   - Param `userId: string`
   - Query: `supabase.from('profiles').select('full_name, birth_date').eq('id', userId).maybeSingle()`
   - Extract firstName: `fullName.trim().split(/\s+/)[0] ?? ''`
   - If no birthDate: return `{ firstName, zodiacSign: '', lifePathNumber: 0 }`
   - Otherwise: compute and return all three fields
   - Wrap in try/catch — on error return `{ firstName: '', zodiacSign: '', lifePathNumber: 0 }`
   - Add JSDoc in Hebrew

Per D-02: This helper ensures every route can address the user by first name.
Per D-03: This helper provides zodiac + life path number for personal data references.

Import: `import type { SupabaseClient } from '@supabase/supabase-js'`
Import: `import { reduceToSingleDigit } from '@/services/numerology/calculations'`

File should be ~90 lines. All comments in Hebrew. No `any` without eslint-disable.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mystiqor-build/src/services/analysis/personal-context.ts
    - grep "export interface PersonalContext" finds the interface
    - grep "export async function getPersonalContext" finds the function
    - grep "reduceToSingleDigit" confirms import from numerology
    - grep "ZODIAC_RANGES" confirms zodiac data is present
    - grep "maybeSingle" confirms profile query pattern
    - grep "eslint-disable-next-line" confirms any-suppression for SupabaseClient
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>personal-context.ts exports PersonalContext interface and getPersonalContext function. TypeScript compiles. Helper returns firstName, zodiacSign, lifePathNumber with safe fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich daily-insights route (PROMPT-02 gold standard)</name>
  <files>mystiqor-build/src/app/api/tools/daily-insights/route.ts</files>
  <read_first>
    mystiqor-build/src/app/api/tools/daily-insights/route.ts
    mystiqor-build/src/services/analysis/personal-context.ts
  </read_first>
  <action>
Modify `mystiqor-build/src/app/api/tools/daily-insights/route.ts`:

**1. Add import at top:**
```typescript
import { getPersonalContext } from '@/services/analysis/personal-context'
```

**2. Remove the local ZODIAC_RANGES array and getZodiacSign function** (lines 19-53) — these are now in personal-context.ts. Keep getDayNumber and extractTipFromContent and buildPrompt as they are.

**3. Update buildPrompt signature** — add two new parameters:
```typescript
function buildPrompt(
  zodiacSign: string,
  dayNumber: number,
  tarotCardName: string,
  modules: DailyInsightModules,
  lifePathNumber: number,
  priorSummary: string
): string {
```
In the return template, add after the existing zodiac line:
```
מספר החיים שלו הוא ${lifePathNumber} — התייחס למשמעותו הרוחנית העמוקה ולהשפעתו על יומו.
```
And before the instructions section, if priorSummary is non-empty:
```
${priorSummary ? `\nנושאים מניתוחים אחרונים שלו: ${priorSummary}\nשלב אזכור עדין של נושאים אלו בתובנה.` : ''}
```

**4. In the GET handler**, replace the profile fetch + zodiac calc section (lines 192-201) with:
```typescript
const ctx = await getPersonalContext(supabase, user.id)
const birthDate = ctx.zodiacSign ? '' : '1990-01-01' // not needed — ctx has zodiac
const zodiacSign = ctx.zodiacSign || 'טלה'
const dayNumber = getDayNumber(today)
```
Remove the old `userName`, `birthDate`, `zodiacSign` variables that used manual profile fetch.

**5. Fetch prior insight summaries** (new block, after the getPersonalContext call, before tarot card fetch):
```typescript
let priorSummary = ''
try {
  const { data: recentInsights } = await supabase
    .from('daily_insights')
    .select('title')
    .eq('user_id', user.id)
    .eq('mood_type', 'daily')
    .neq('insight_date', today)
    .order('insight_date', { ascending: false })
    .limit(3)
  if (recentInsights && recentInsights.length > 0) {
    priorSummary = recentInsights.map(i => i.title).join(' | ')
  }
} catch { /* graceful — no prior context */ }
```

**6. Enrich the systemPrompt** (per D-01 Kabbalistic + warm-intimate):
```typescript
const systemPrompt = `אתה מייעץ רוחני-מיסטי עמוק עם ידע בקבלה, אסטרולוגיה ונומרולוגיה.
אתה מדבר ישירות לנשמה — חם, אינטימי, חודר. כל מילה שלך מחוברת לאור אין-סוף.
${ctx.firstName ? `אתה פונה אל ${ctx.firstName} — ממזל ${zodiacSign}, מספר חיים ${ctx.lifePathNumber}. דבר אליו כאילו אתה מכיר אותו שנים.` : ''}
שלב רפרנסים לספירות עץ החיים ונתיבות החכמה כשרלוונטי — בשפה חמה, לא אקדמית.
פסקאות קצרות וברורות. ללא כוכביות (asterisks) מיותרות.`
```

**7. Update the buildPrompt call:**
```typescript
const prompt = buildPrompt(zodiacSign, dayNumber, tarotCardName, modules, ctx.lifePathNumber, priorSummary)
```

Per D-04: Deepening the existing prompt, not changing architecture.
Per D-05: Two different users get different content (different zodiac, life path, prior insights).

Keep file under 300 lines. All comments in Hebrew.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getPersonalContext" in daily-insights/route.ts confirms import and usage
    - grep "lifePathNumber" in daily-insights/route.ts confirms life path in prompt (PROMPT-02)
    - grep "priorSummary" in daily-insights/route.ts confirms prior analyses reference
    - grep "אור אין-סוף" in daily-insights/route.ts confirms Kabbalistic language (D-01)
    - grep "ctx.firstName" in daily-insights/route.ts confirms personal address (D-02)
    - grep "ZODIAC_RANGES" does NOT appear in daily-insights/route.ts (removed — now in personal-context.ts)
    - tsc --noEmit passes with zero errors
    - File is under 300 lines
  </acceptance_criteria>
  <done>Daily-insights route uses getPersonalContext helper. SystemPrompt includes first name + zodiac + life path + Kabbalistic language. buildPrompt includes lifePathNumber and prior insight summaries. ZODIAC_RANGES removed (DRY). Build passes.</done>
</task>

</tasks>

<verification>
```bash
cd mystiqor-build && npx tsc --noEmit
```
Zero TypeScript errors. Both files compile cleanly.

```bash
grep -r "getPersonalContext" mystiqor-build/src/services/analysis/personal-context.ts mystiqor-build/src/app/api/tools/daily-insights/route.ts
```
Helper defined in personal-context.ts, imported and used in daily-insights/route.ts.
</verification>

<success_criteria>
- personal-context.ts exists with exported getPersonalContext and PersonalContext
- daily-insights route enriched with life path number, prior analyses, Kabbalistic language
- No ZODIAC_RANGES duplication (removed from daily-insights, lives in personal-context.ts)
- TypeScript build passes with zero errors
- daily-insights satisfies PROMPT-02 (zodiac + life path + personal tarot card — all present)
</success_criteria>

<output>
After completion, create `.planning/phases/21-prompt-enrichment/21-01-SUMMARY.md`
</output>
