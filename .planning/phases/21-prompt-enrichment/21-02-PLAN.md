---
phase: 21-prompt-enrichment
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - mystiqor-build/src/app/api/tools/tarot/route.ts
  - mystiqor-build/src/app/api/coach/journeys/route.ts
  - mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts
  - mystiqor-build/src/app/api/tools/astrology/transits/route.ts
  - mystiqor-build/src/app/api/tools/dream/route.ts
autonomous: true
requirements: [PROMPT-01]
must_haves:
  truths:
    - "tarot route systemPrompt addresses user by name with zodiac and life path"
    - "coach/journeys route has a systemPrompt with personal context (was missing)"
    - "solar-return route has a systemPrompt with personal context (was missing)"
    - "transits route has a systemPrompt with personal context (was missing)"
    - "dream route systemPrompt addresses user by name with spiritual language"
  artifacts:
    - path: "mystiqor-build/src/app/api/tools/tarot/route.ts"
      provides: "Enriched tarot prompt with personal context"
      contains: "getPersonalContext"
    - path: "mystiqor-build/src/app/api/coach/journeys/route.ts"
      provides: "New systemPrompt for journeys with personal context"
      contains: "getPersonalContext"
    - path: "mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts"
      provides: "New systemPrompt for solar-return"
      contains: "getPersonalContext"
    - path: "mystiqor-build/src/app/api/tools/astrology/transits/route.ts"
      provides: "New systemPrompt for transits"
      contains: "getPersonalContext"
    - path: "mystiqor-build/src/app/api/tools/dream/route.ts"
      provides: "Enriched dream prompt with personal context"
      contains: "getPersonalContext"
  key_links:
    - from: "all 5 routes"
      to: "mystiqor-build/src/services/analysis/personal-context.ts"
      via: "import getPersonalContext"
      pattern: "getPersonalContext"
---

<objective>
Enrich the 5 DEEP-tier routes (tarot, coach/journeys, solar-return, transits) plus dream (top MEDIUM) with personal context and Kabbalistic-spiritual language.

Purpose: These routes have the most significant gaps — 3 lack systemPrompt entirely, 2 have generic prompts. Enriching them delivers the most visible PROMPT-01 impact.
Output: All 5 routes use getPersonalContext and have enriched systemPrompts per D-01/D-02/D-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-prompt-enrichment/21-CONTEXT.md
@.planning/phases/21-prompt-enrichment/21-RESEARCH.md
@.planning/phases/21-prompt-enrichment/21-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the shared helper -->
From src/services/analysis/personal-context.ts:
```typescript
export interface PersonalContext {
  firstName: string      // '' if no profile
  zodiacSign: string     // Hebrew name or '' if unknown
  lifePathNumber: number // 1-9 or master 11/22/33, or 0 if unknown
}
export async function getPersonalContext(
  supabase: SupabaseClient<any>,
  userId: string
): Promise<PersonalContext>
```

From src/services/analysis/llm.ts:
```typescript
export interface LLMRequest {
  prompt: string
  systemPrompt?: string
  responseSchema?: Record<string, unknown>
  zodSchema?: z.ZodSchema
  imageUrls?: string[]
  maxTokens?: number
  userId: string
}
```

CRITICAL — Pitfall 2 (from research):
Routes that use `responseSchema` (coach/journeys) have the LLM service append
`\n\nענה בפורמט JSON בלבד` to systemContent. Adding a systemPrompt for identity
and personal context is SAFE, but do NOT add instructions that conflict with
JSON mode (like "write poetically without JSON").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich tarot + coach/journeys + dream routes (DEEP/MEDIUM)</name>
  <files>
    mystiqor-build/src/app/api/tools/tarot/route.ts
    mystiqor-build/src/app/api/coach/journeys/route.ts
    mystiqor-build/src/app/api/tools/dream/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/tarot/route.ts
    mystiqor-build/src/app/api/coach/journeys/route.ts
    mystiqor-build/src/app/api/tools/dream/route.ts
    mystiqor-build/src/services/analysis/personal-context.ts
  </read_first>
  <action>
**A. tools/tarot/route.ts** — DEEP enrichment:

1. Add import: `import { getPersonalContext } from '@/services/analysis/personal-context'`
2. In POST handler, after auth check and before input validation, add:
   ```typescript
   const ctx = await getPersonalContext(supabase, user.id)
   ```
3. Find the existing systemPrompt string (starts with "אתה קורא טארוט חכם ואינטואיטיבי"). Replace it with:
   ```typescript
   const personalLine = ctx.firstName
     ? `אתה פונה אל ${ctx.firstName} — ממזל ${ctx.zodiacSign}, מספר חיים ${ctx.lifePathNumber}.`
     : ''

   const systemPrompt = `אתה קורא טארוט חכם ואינטואיטיבי עם ידע עמוק בקבלה ובסמליות הקלפים.
   ${personalLine}
   דבר ישירות לנשמה — חם, אינטימי, כאילו אתה רואה אותה דרך הקלפים.
   שלב בין סמל, מספר, אלמנט וארכיטיפ של כל קלף — חדור לעומק, חשוף את הנסתר.
   רפרנסים לקבלה: ספירות, נתיבות עץ החיים, אותיות עבריות — רק כשרלוונטי לקלף.
   ענה בעברית. פסקאות קצרות וברורות.`
   ```
4. DO NOT change the invokeLLM call signature or response handling. Only the systemPrompt content changes.

**B. coach/journeys/route.ts** — DEEP enrichment (currently has NO systemPrompt):

1. Add import: `import { getPersonalContext } from '@/services/analysis/personal-context'`
2. In POST handler, after auth check, add:
   ```typescript
   const ctx = await getPersonalContext(supabase, user.id)
   ```
3. **Add a systemPrompt** to the invokeLLM call. Currently the call is:
   ```typescript
   const llmResponse = await invokeLLM<CoachingJourneyResponse>({
     userId: user.id,
     prompt,
     responseSchema: JOURNEY_RESPONSE_SCHEMA,
     zodSchema: CoachingJourneyResponseSchema,
     maxTokens: 6000,
   })
   ```
   Change to:
   ```typescript
   const journeySystemPrompt = ctx.firstName
     ? `אתה מאמן אישי מיסטי-קבלי שמלווה את ${ctx.firstName} (מזל ${ctx.zodiacSign}, מספר חיים ${ctx.lifePathNumber}) במסע צמיחה. כל צעד מותאם לנשמתו.`
     : 'אתה מאמן אישי מיסטי-קבלי שמלווה את הפונה במסע צמיחה רוחנית ואישית.'

   const llmResponse = await invokeLLM<CoachingJourneyResponse>({
     userId: user.id,
     systemPrompt: journeySystemPrompt,
     prompt,
     responseSchema: JOURNEY_RESPONSE_SCHEMA,
     zodSchema: CoachingJourneyResponseSchema,
     maxTokens: 6000,
   })
   ```
   IMPORTANT (Pitfall 2): Keep the systemPrompt short and identity-focused. Do NOT add "write poetically" — the responseSchema + JSON mode must not be disrupted.

4. Also enrich the `userContext` string (currently only birth_date). After the existing profile fetch block, add personal context info:
   ```typescript
   if (ctx.firstName) {
     userContext = `שם: ${ctx.firstName}, מזל: ${ctx.zodiacSign}, מספר חיים: ${ctx.lifePathNumber}\n` + userContext
   }
   ```

**C. tools/dream/route.ts** — MEDIUM enrichment:

1. Add import: `import { getPersonalContext } from '@/services/analysis/personal-context'`
2. In POST handler, after auth check (BEFORE any background work — Pitfall 3), add:
   ```typescript
   const ctx = await getPersonalContext(supabase, user.id)
   ```
   CRITICAL (Pitfall 3): The dream route uses fire-and-forget backgroundWork. Fetch personal context in the main handler, NOT inside the background function. Close over `ctx` in the background closure.
3. Find the existing systemPrompt string (starts with "אתה פסיכולוג חלומות"). Replace with:
   ```typescript
   const personalLine = ctx.firstName
     ? `אתה פונה אל ${ctx.firstName} — ממזל ${ctx.zodiacSign}, מספר חיים ${ctx.lifePathNumber}.`
     : ''

   const systemPrompt = `אתה מפרש חלומות מיסטי-קבלי שרואה את השפה הסמלית של הנשמה.
   ${personalLine}
   שלב בין פסיכולוגיה עמוקה (יונג, פרויד) לבין חכמה קבלית — חלומות כמסרים מעולם היצירה.
   כל סמל בחלום הוא נתיב בעץ החיים. ענה בעברית, בשפה חמה ואינטימית.`
   ```
4. DO NOT change the invokeLLM call structure — only the systemPrompt text.

All comments in Hebrew. Keep each file under 300 lines.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getPersonalContext" in tarot/route.ts confirms import and call
    - grep "ctx.firstName" in tarot/route.ts confirms personal address
    - grep "ספירות" in tarot/route.ts confirms Kabbalistic language (D-01)
    - grep "getPersonalContext" in coach/journeys/route.ts confirms import and call
    - grep "systemPrompt: journeySystemPrompt" in coach/journeys/route.ts confirms new systemPrompt added
    - grep "getPersonalContext" in dream/route.ts confirms import and call
    - grep "עולם היצירה" in dream/route.ts confirms Kabbalistic language (D-01)
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Tarot route has enriched systemPrompt with name + zodiac + life path + Kabbalistic references. Coach/journeys has a NEW systemPrompt (was missing) with personal context. Dream route has enriched systemPrompt with mystical-Kabbalistic tone. All compile.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich solar-return + transits routes (no-systemPrompt DEEP)</name>
  <files>
    mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts
    mystiqor-build/src/app/api/tools/astrology/transits/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts
    mystiqor-build/src/app/api/tools/astrology/transits/route.ts
    mystiqor-build/src/services/analysis/personal-context.ts
    mystiqor-build/src/services/analysis/llm.ts
  </read_first>
  <action>
**A. tools/astrology/solar-return/route.ts** — DEEP (currently NO systemPrompt):

1. Add import: `import { getPersonalContext } from '@/services/analysis/personal-context'`
2. In POST handler, after auth check, add:
   ```typescript
   const ctx = await getPersonalContext(supabase, user.id)
   ```
3. Find the invokeLLM call — it currently has NO systemPrompt key (only `prompt: srPrompt`). Add a systemPrompt:
   ```typescript
   const solarReturnSystemPrompt = `אתה אסטרולוג קבלי עמוק שמפרש מפות חזרת שמש.
   ${ctx.firstName ? `אתה פונה אל ${ctx.firstName} — ממזל ${ctx.zodiacSign}, מספר חיים ${ctx.lifePathNumber}.` : ''}
   חזרת השמש היא רגע של התחדשות נשמתית — התייחס אליו כאל נתיב חדש בעץ החיים.
   ענה בעברית. שפה חמה, אינטימית, חודרת.`

   const llmResponse = await invokeLLM<string>({
     userId: user.id,
     systemPrompt: solarReturnSystemPrompt,
     prompt: srPrompt,    // keep existing prompt variable name
     maxTokens: 2000,     // keep existing maxTokens
   })
   ```
   Match the EXISTING invokeLLM call's other parameters (userId, prompt variable name, maxTokens). Only ADD systemPrompt.

**B. tools/astrology/transits/route.ts** — DEEP (currently NO systemPrompt):

1. Add import: `import { getPersonalContext } from '@/services/analysis/personal-context'`
2. In POST handler, after auth check, add:
   ```typescript
   const ctx = await getPersonalContext(supabase, user.id)
   ```
3. Find the invokeLLM call — it currently has NO systemPrompt key. Add a systemPrompt:
   ```typescript
   const transitsSystemPrompt = `אתה אסטרולוג קבלי שמפרש מעברים פלנטריים.
   ${ctx.firstName ? `אתה פונה אל ${ctx.firstName} — ממזל ${ctx.zodiacSign}, מספר חיים ${ctx.lifePathNumber}.` : ''}
   כל טרנזיט הוא הזדמנות לעבודה פנימית — קשר בין תנועת הכוכבים לספירות הנשמה.
   ענה בעברית. שפה חמה, עמוקה, מעוררת תובנה.`

   // Add systemPrompt to the existing invokeLLM call
   ```
   Find the exact invokeLLM call and add `systemPrompt: transitsSystemPrompt,` to the request object. Keep all other parameters unchanged.

Pitfall 6 (from research): If either route imports INTERPRETATION_SYSTEM_PROMPT, do NOT modify that constant. Instead, build an enriched systemPrompt at the route level and pass it to invokeLLM. The original constant stays unchanged.

All comments in Hebrew. Keep each file under 300 lines.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getPersonalContext" in solar-return/route.ts confirms import and call
    - grep "systemPrompt" in solar-return/route.ts confirms NEW systemPrompt added (was missing)
    - grep "חזרת השמש" in solar-return/route.ts confirms solar return context
    - grep "getPersonalContext" in transits/route.ts confirms import and call
    - grep "systemPrompt" in transits/route.ts confirms NEW systemPrompt added (was missing)
    - grep "טרנזיט" in transits/route.ts confirms transits context
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Solar-return and transits routes now have systemPrompts (were missing). Both include personal context (name, zodiac, life path) and Kabbalistic-spiritual language. Build passes.</done>
</task>

</tasks>

<verification>
```bash
cd mystiqor-build && npx tsc --noEmit
```
Zero TypeScript errors across all 5 modified routes.

```bash
grep -l "getPersonalContext" mystiqor-build/src/app/api/tools/tarot/route.ts mystiqor-build/src/app/api/coach/journeys/route.ts mystiqor-build/src/app/api/tools/dream/route.ts mystiqor-build/src/app/api/tools/astrology/solar-return/route.ts mystiqor-build/src/app/api/tools/astrology/transits/route.ts
```
All 5 route files import and use getPersonalContext.
</verification>

<success_criteria>
- All 5 DEEP-tier routes enriched with personal context from getPersonalContext
- 3 routes that had NO systemPrompt (coach/journeys, solar-return, transits) now have one
- 2 routes with existing systemPrompt (tarot, dream) have enriched versions with Kabbalistic language
- All systemPrompts include conditional personal address (firstName ? ... : '')
- TypeScript build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-prompt-enrichment/21-02-SUMMARY.md`
</output>
