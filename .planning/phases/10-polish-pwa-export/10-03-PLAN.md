---
phase: 10-polish-pwa-export
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified: []
autonomous: false
requirements: [EXPO-01, EXPO-02, UX-04]

must_haves:
  truths:
    - "tsc --noEmit passes with zero errors across all Phase 10 work"
    - "ExportButton renders and triggers PDF download with Hebrew content"
    - "SharePanel generates a share link and displays social sharing buttons"
    - "Public share page renders analysis content without authentication"
    - "PWA manifest is valid and browser recognizes installability criteria"
    - "Service worker registers without breaking existing auth or navigation"
    - "Install prompt appears on Android or shows iOS instructions"
  artifacts:
    - path: "mystiqor-build/src/components/features/export/ExportButton.tsx"
      provides: "PDF export button"
    - path: "mystiqor-build/src/components/features/sharing/SharePanel.tsx"
      provides: "Social sharing panel"
    - path: "mystiqor-build/src/app/(public)/share/[token]/page.tsx"
      provides: "Public share page"
    - path: "mystiqor-build/src/app/manifest.ts"
      provides: "PWA manifest"
    - path: "mystiqor-build/public/sw.js"
      provides: "Service worker"
    - path: "mystiqor-build/src/components/features/pwa/InstallPrompt.tsx"
      provides: "Install prompt component"
  key_links:
    - from: "all Phase 10 files"
      to: "tsc --noEmit"
      via: "TypeScript compilation"
      pattern: "exit code 0"
---

<objective>
Integration verification for Phase 10 — confirm all PDF export, social sharing, and PWA features compile and function correctly.

Purpose: Final quality gate before marking Phase 10 complete. Ensures TypeScript compilation, verifies all components exist with correct exports, and presents a human verification checkpoint.

Output: Verified Phase 10 with human approval.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-pwa-export/10-01-SUMMARY.md
@.planning/phases/10-polish-pwa-export/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript compilation + component audit</name>
  <files></files>
  <read_first>
    mystiqor-build/src/components/features/export/ExportButton.tsx,
    mystiqor-build/src/components/features/export/AnalysisPDF.tsx,
    mystiqor-build/src/components/features/export/pdf-styles.ts,
    mystiqor-build/src/components/features/sharing/SharePanel.tsx,
    mystiqor-build/src/app/api/analysis/share/route.ts,
    mystiqor-build/src/app/(public)/share/[token]/page.tsx,
    mystiqor-build/src/app/manifest.ts,
    mystiqor-build/public/sw.js,
    mystiqor-build/src/components/features/pwa/InstallPrompt.tsx,
    mystiqor-build/src/app/layout.tsx,
    mystiqor-build/src/app/(auth)/layout.tsx,
    mystiqor-build/next.config.ts
  </read_first>
  <action>
    **Step 1 — TypeScript compilation:**
    ```bash
    cd mystiqor-build && npx tsc --noEmit
    ```
    Must exit 0. If errors exist, fix them before proceeding.

    **Step 2 — Component existence audit:**
    Verify all Phase 10 files exist:
    ```bash
    test -f src/components/features/export/ExportButton.tsx && echo "OK: ExportButton" || echo "MISSING: ExportButton"
    test -f src/components/features/export/AnalysisPDF.tsx && echo "OK: AnalysisPDF" || echo "MISSING: AnalysisPDF"
    test -f src/components/features/export/pdf-styles.ts && echo "OK: pdf-styles" || echo "MISSING: pdf-styles"
    test -f src/components/features/sharing/SharePanel.tsx && echo "OK: SharePanel" || echo "MISSING: SharePanel"
    test -f src/app/api/analysis/share/route.ts && echo "OK: share route" || echo "MISSING: share route"
    test -f src/app/\(public\)/share/\[token\]/page.tsx && echo "OK: share page" || echo "MISSING: share page"
    test -f src/app/manifest.ts && echo "OK: manifest" || echo "MISSING: manifest"
    test -f public/sw.js && echo "OK: sw.js" || echo "MISSING: sw.js"
    test -f public/icon-192.png && echo "OK: icon-192" || echo "MISSING: icon-192"
    test -f public/icon-512.png && echo "OK: icon-512" || echo "MISSING: icon-512"
    test -f public/fonts/Heebo-Regular.ttf && echo "OK: Heebo font" || echo "MISSING: Heebo font"
    test -f src/components/features/pwa/InstallPrompt.tsx && echo "OK: InstallPrompt" || echo "MISSING: InstallPrompt"
    test -f supabase/migrations/005_schema_fixes.sql && echo "OK: migration" || echo "MISSING: migration"
    ```
    If any file is MISSING, investigate and fix.

    **Step 3 — Verify key patterns:**
    ```bash
    grep -q "Font.register" src/components/features/export/pdf-styles.ts && echo "OK: Font registered" || echo "MISSING: Font.register"
    grep -q "ssr.*false" src/components/features/export/ExportButton.tsx && echo "OK: SSR disabled" || echo "MISSING: ssr:false"
    grep -q "share_token" src/types/database.ts && echo "OK: share_token in types" || echo "MISSING: share_token type"
    grep -q "is_public" src/types/database.ts && echo "OK: is_public in types" || echo "MISSING: is_public type"
    grep -q "standalone" src/app/manifest.ts && echo "OK: standalone in manifest" || echo "MISSING: standalone"
    grep -q "serviceWorker" src/app/layout.tsx && echo "OK: SW registered" || echo "MISSING: SW registration"
    grep -q "InstallPrompt" src/app/\(auth\)/layout.tsx && echo "OK: InstallPrompt in layout" || echo "MISSING: InstallPrompt in layout"
    grep -q "transpilePackages" next.config.ts && echo "OK: transpilePackages" || echo "MISSING: transpilePackages"
    ```
    Fix any MISSING items.

    **Step 4 — Requirement coverage check:**
    - EXPO-01 (PDF export): ExportButton.tsx + AnalysisPDF.tsx + pdf-styles.ts + Heebo font
    - EXPO-02 (Share via link/social): SharePanel.tsx + share route + share page + 005_schema_fixes.sql
    - UX-04 (PWA): manifest.ts + sw.js + icons + InstallPrompt.tsx + SW registration

    All 3 requirements must be covered. Log coverage status.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cd mystiqor-build && npx tsc --noEmit exits 0
    - All 13 Phase 10 files exist (no MISSING in audit)
    - All 8 key patterns present (no MISSING in pattern check)
    - EXPO-01, EXPO-02, UX-04 all covered
  </acceptance_criteria>
  <done>
    TypeScript compilation passes with zero errors.
    All Phase 10 files exist and contain expected patterns.
    All 3 requirements (EXPO-01, EXPO-02, UX-04) have file coverage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of PDF export + sharing + PWA</name>
  <what-built>
    Phase 10 delivers three capabilities:
    1. **PDF Export (EXPO-01):** ExportButton component renders on analysis results, clicking generates a downloadable PDF with Hebrew RTL text using Heebo font
    2. **Social Sharing (EXPO-02):** SharePanel component generates a unique share link via API, shows copy button + WhatsApp/Telegram/Facebook share buttons; public share page at /share/[token] renders analysis without auth
    3. **PWA (UX-04):** manifest.ts provides PWA manifest with Hebrew RTL config, minimal service worker enables install prompt, InstallPrompt component handles Android (native prompt) and iOS (manual instructions)
  </what-built>
  <how-to-verify>
    **PDF Export:**
    1. Navigate to any completed analysis (e.g., /tools/numerology, run an analysis)
    2. Look for "Export PDF" or equivalent button (note: ExportButton and SharePanel are created but need to be wired into analysis result pages — this may require adding them manually to a result view for testing)
    3. Click export — verify PDF downloads with Hebrew text visible (not blank/tofu boxes)
    4. Open PDF — verify RTL alignment, Heebo font, MystiQor branding

    **Social Sharing:**
    5. Visit an analysis result page with SharePanel integrated
    6. Click "Share" — verify share link is generated
    7. Copy the share link and open in an incognito/private window (no auth)
    8. Verify the public share page renders the analysis content
    9. Verify WhatsApp/Telegram/Facebook buttons are visible

    **PWA:**
    10. Open Chrome DevTools > Application > Manifest — verify manifest loads with correct name, icons, display:standalone
    11. Check Application > Service Workers — verify sw.js is registered and active
    12. On Android/Chrome: look for install prompt banner at bottom of page
    13. Verify Supabase auth still works after service worker registration (login, navigate, refresh)

    **Note:** ExportButton and SharePanel are standalone components. If they are not yet wired into specific analysis pages, test by temporarily importing them into any analysis result view. The wiring into every tool page is optional for Phase 10 — the infrastructure is in place.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 10, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero TypeScript errors
2. All Phase 10 artifacts exist and contain expected patterns
3. Human reviewer confirms PDF export, sharing, and PWA features work
4. EXPO-01, EXPO-02, UX-04 requirements satisfied
</verification>

<success_criteria>
- Zero TypeScript errors
- Human reviewer approves all three Phase 10 capabilities
- Phase 10 can be marked COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-pwa-export/10-03-SUMMARY.md`
</output>
