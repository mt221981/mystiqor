---
phase: 14-typography-hebrew-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/features/astrology/HumanDesignCenters.tsx
  - mystiqor-build/src/app/(auth)/tools/drawing/page.tsx
  - mystiqor-build/src/app/(auth)/learn/drawing/page.tsx
  - mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx
  - mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx
  - mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx
  - mystiqor-build/src/components/features/drawing/DrawingCompare.tsx
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02

must_haves:
  truths:
    - "All Hebrew body text renders in Heebo font (no font-sans overrides)"
    - "Human Design appears as 'עיצוב אנושי' in every visible UI location"
    - "HTP appears as 'בית-עץ-אדם' in every visible UI location"
    - "Koppitz appears as 'קופיץ' in every visible UI location (except English academic subtitle)"
    - "No code identifiers, URL slugs, prompt strings, or JSDoc comments were modified"
  artifacts:
    - path: "mystiqor-build/src/components/features/astrology/HumanDesignCenters.tsx"
      provides: "Hebrew aria-label + Heebo SVG font"
      contains: "עיצוב אנושי"
    - path: "mystiqor-build/src/app/(auth)/tools/drawing/page.tsx"
      provides: "Hebrew drawing page header"
      contains: "בית-עץ-אדם"
    - path: "mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx"
      provides: "Hebrew concept card titles and descriptions"
      contains: "בית-עץ-אדם"
    - path: "mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx"
      provides: "Hebrew Koppitz label"
      contains: "קופיץ"
    - path: "mystiqor-build/src/components/features/drawing/DrawingCompare.tsx"
      provides: "Hebrew Koppitz comparison labels"
      contains: "קופיץ"
    - path: "mystiqor-build/src/app/(auth)/learn/drawing/page.tsx"
      provides: "Hebrew learn drawing labels"
      contains: "בית-עץ-אדם"
    - path: "mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx"
      provides: "Hebrew tutorial topic names"
      contains: "בית-עץ-אדם"
  key_links:
    - from: "mystiqor-build/src/app/layout.tsx"
      to: "mystiqor-build/tailwind.config.ts"
      via: "CSS variable --font-hebrew maps to font-body utility"
      pattern: "font-body"
    - from: "mystiqor-build/src/lib/constants/tool-names.ts"
      to: "all tool pages"
      via: "TOOL_NAMES constant provides Hebrew names"
      pattern: "human_design.*עיצוב אנושי"
---

<objective>
Verify Heebo font propagation and replace all English display terms with Hebrew across the MystiQor UI.

Purpose: Complete Phase 14 (Typography & Hebrew Localization) by confirming TYPO-01 (Heebo font active on all Hebrew text) and executing TYPO-02 (replace "Human Design", "HTP", "Koppitz" with Hebrew equivalents in all visible UI strings).

Output: 7 modified source files with Hebrew display strings; zero English terms remaining in user-facing UI text; Heebo font confirmed active including SVG text elements.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OWNER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-typography-hebrew-localization/14-RESEARCH.md
@.planning/phases/14-typography-hebrew-localization/14-UI-SPEC.md
@.planning/phases/14-typography-hebrew-localization/14-VALIDATION.md

<interfaces>
<!-- Source of truth for term mapping — DO NOT TOUCH this file -->
From mystiqor-build/src/lib/constants/tool-names.ts:
  - TOOL_NAMES maps: human_design -> 'עיצוב אנושי', solar_return -> 'חזרת שמש'
  - These are ALREADY CORRECT. This file must NOT be modified.

<!-- Font infrastructure — DO NOT TOUCH these files -->
From mystiqor-build/src/app/layout.tsx:
  - Heebo imported from 'next/font/google' with variable: '--font-hebrew'
  - <body className="font-body"> on line 112
  - ALREADY CORRECT. Do not modify.

From mystiqor-build/tailwind.config.ts:
  - fontFamily.body: ['var(--font-hebrew)', 'Heebo', 'var(--font-body)', 'Inter', 'sans-serif']
  - ALREADY CORRECT. Do not modify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Heebo font propagation and fix SVG font attribute</name>
  <files>mystiqor-build/src/components/features/astrology/HumanDesignCenters.tsx</files>
  <read_first>
    - mystiqor-build/src/app/layout.tsx (confirm font-body on body tag, Heebo variable setup)
    - mystiqor-build/tailwind.config.ts (confirm font-body resolves to Heebo)
    - mystiqor-build/src/components/features/astrology/HumanDesignCenters.tsx (the file being modified — see current fontFamily and aria-label)
  </read_first>
  <action>
TYPO-01 verification steps:

1. Confirm `font-body` class is on `<body>` in layout.tsx (read-only, do not modify layout.tsx).
2. Confirm `tailwind.config.ts` fontFamily.body array starts with `'var(--font-hebrew)'` (read-only, do not modify).
3. Run grep across `mystiqor-build/src/` for any `font-sans` class usage. Expected result: zero matches. If any found in Hebrew text contexts, replace with `font-body`.

TYPO-01 + TYPO-02 code change in HumanDesignCenters.tsx:

4. In `HumanDesignCenters.tsx`, find the SVG `<text>` elements (around line 107) that have `fontFamily="sans-serif"`. These render Hebrew center names ("לב", "ראש", etc.). Change the attribute to `fontFamily="Heebo, sans-serif"` so Hebrew text in the SVG renders in Heebo, consistent with the rest of the app.

5. In the same file, find the `aria-label` on the `<svg>` element (around line 52). Current value: `aria-label="מפת מרכזי Human Design"`. Replace with: `aria-label="מפת מרכזי עיצוב אנושי"`.

IMPORTANT DO-NOT-TOUCH rules:
- Do NOT modify `layout.tsx`
- Do NOT modify `tailwind.config.ts`
- Do NOT modify any JSDoc comments in HumanDesignCenters.tsx
- Do NOT modify function names, variable names, or import paths
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && grep -rn "font-sans" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next" ; echo "---" ; grep "עיצוב אנושי" src/components/features/astrology/HumanDesignCenters.tsx ; echo "---" ; grep "Heebo" src/components/features/astrology/HumanDesignCenters.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for `font-sans` across `mystiqor-build/src/` returns zero results (or only non-Hebrew contexts)
    - HumanDesignCenters.tsx contains `aria-label="מפת מרכזי עיצוב אנושי"` (no "Human Design" in aria-label)
    - HumanDesignCenters.tsx contains `fontFamily="Heebo, sans-serif"` on SVG text elements (replacing bare `sans-serif`)
    - layout.tsx is UNCHANGED (no modifications)
    - tailwind.config.ts is UNCHANGED (no modifications)
  </acceptance_criteria>
  <done>Heebo font propagation verified across app; SVG text elements updated to use Heebo; aria-label localized to Hebrew.</done>
</task>

<task type="auto">
  <name>Task 2: Replace English display terms with Hebrew across 6 files</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/drawing/page.tsx,
    mystiqor-build/src/app/(auth)/learn/drawing/page.tsx,
    mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx,
    mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx,
    mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx,
    mystiqor-build/src/components/features/drawing/DrawingCompare.tsx
  </files>
  <read_first>
    - mystiqor-build/src/app/(auth)/tools/drawing/page.tsx (current PageHeader description with HTP/Koppitz)
    - mystiqor-build/src/app/(auth)/learn/drawing/page.tsx (current button labels with HTP/Koppitz)
    - mystiqor-build/src/app/(auth)/learn/tutorials/page.tsx (current topic names with HTP/Koppitz)
    - mystiqor-build/src/components/features/drawing/DrawingConceptCards.tsx (current card titles with HTP/Koppitz)
    - mystiqor-build/src/components/features/drawing/KoppitzVisualization.tsx (current span text with Koppitz)
    - mystiqor-build/src/components/features/drawing/DrawingCompare.tsx (current labels with Koppitz)
    - .planning/phases/14-typography-hebrew-localization/14-RESEARCH.md (exact replacement table, lines 100-148)
  </read_first>
  <action>
Execute all TYPO-02 term replacements. Every replacement below is a specific JSX display string change. Apply each one exactly as specified.

**File 1: `src/app/(auth)/tools/drawing/page.tsx`**
- Line ~50: Change `description="ניתוח פסיכולוגי של ציורים HTP — בית, עץ, אדם — עם מדדי Koppitz ו-FDM"`
  TO: `description="ניתוח פסיכולוגי של ציורים (בית-עץ-אדם) — עם מדדי קופיץ ו-FDM"`

**File 2: `src/app/(auth)/learn/drawing/page.tsx`**
- Line ~23: Change `label: 'מה זה HTP?'` TO: `label: 'מה זה בית-עץ-אדם?'`
- Line ~24: Change `label: 'ציון Koppitz'` TO: `label: 'ציון קופיץ'`

**File 3: `src/app/(auth)/learn/tutorials/page.tsx`**
- Line ~61: Change `description: 'יסודות HTP, ניקוד Koppitz, מודל FDM וצבעים'`
  TO: `description: 'יסודות בית-עץ-אדם, ניקוד קופיץ, מודל FDM וצבעים'`
- Line ~64: Change `name: 'יסודות HTP'` TO: `name: 'יסודות בית-עץ-אדם'`
- Line ~65: Change `name: 'ניקוד Koppitz'` TO: `name: 'ניקוד קופיץ'`

**File 4: `src/components/features/drawing/DrawingConceptCards.tsx`**
- Line ~34: Change `title: 'מהו מבחן HTP?'` TO: `title: 'מהו מבחן בית-עץ-אדם?'`
- Line ~37: In the description string containing `'מבחן HTP (בית-עץ-אדם) פותח...'`, replace so "HTP" is removed and the full Hebrew name stands alone. Result should read: `'מבחן בית-עץ-אדם פותח...'`
- Line ~43: Change `title: 'מדדי Koppitz'` TO: `title: 'מדדי קופיץ'`
- Line ~44: KEEP `subtitle: 'Koppitz Emotional Indicators'` AS-IS (official English academic name, acceptable)
- Line ~46: In the description containing `'ציון Koppitz גבוה...'`, change to `'ציון קופיץ גבוה...'`

**File 5: `src/components/features/drawing/KoppitzVisualization.tsx`**
- Line ~59: Change `<span>מדדי Koppitz</span>` TO: `<span>מדדי קופיץ</span>`

**File 6: `src/components/features/drawing/DrawingCompare.tsx`**
- Line ~147: Change `ציון Koppitz` TO: `ציון קופיץ`
- Line ~300: Change `שינוי ציון Koppitz:` TO: `שינוי ציון קופיץ:`

CRITICAL DO-NOT-REPLACE RULES:
- Do NOT change `subtitle: 'Koppitz Emotional Indicators'` in DrawingConceptCards.tsx line ~44 (academic English name)
- Do NOT change any `id:` property values (e.g., `id: 'htp_basics'`, `id: 'koppitz_scoring'`)
- Do NOT change any `prompt:` property values (AI instructions, not user-facing)
- Do NOT change any function/variable names (`KoppitzDelta`, `koppitz_score`, `koppitzScore`)
- Do NOT change any JSDoc comments or code comments
- Do NOT change any import paths or TypeScript type names
- Do NOT touch `src/lib/constants/tool-names.ts` (already correct)
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && echo "=== Remaining English HTP in display strings ===" && grep -rn "title:\|label:\|description:\|<span>" src/ --include="*.tsx" | grep -i "HTP\b" | grep -v "id:" | grep -v "prompt:" | grep -v "//" | grep -v "htp_" ; echo "=== Remaining English Koppitz in display strings ===" && grep -rn "title:\|label:\|description:\|<span>" src/ --include="*.tsx" | grep "Koppitz" | grep -v "Emotional Indicators" | grep -v "id:" | grep -v "prompt:" | grep -v "//" | grep -v "koppitz_" | grep -v "koppitzScore" ; echo "=== Hebrew terms present ===" && grep -rn "בית-עץ-אדם" src/ --include="*.tsx" | head -10 ; echo "---" ; grep -rn "קופיץ" src/ --include="*.tsx" | head -10 ; echo "=== Build check ===" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/tools/drawing/page.tsx` contains `בית-עץ-אדם` and `קופיץ` in the description string
    - `src/app/(auth)/tools/drawing/page.tsx` does NOT contain `HTP` or `Koppitz` in the description string
    - `src/app/(auth)/learn/drawing/page.tsx` contains `label: 'מה זה בית-עץ-אדם?'` and `label: 'ציון קופיץ'`
    - `src/app/(auth)/learn/tutorials/page.tsx` contains `name: 'יסודות בית-עץ-אדם'` and `name: 'ניקוד קופיץ'`
    - `src/components/features/drawing/DrawingConceptCards.tsx` contains `title: 'מהו מבחן בית-עץ-אדם?'`
    - `src/components/features/drawing/DrawingConceptCards.tsx` contains `title: 'מדדי קופיץ'`
    - `src/components/features/drawing/DrawingConceptCards.tsx` STILL contains `subtitle: 'Koppitz Emotional Indicators'` (unchanged)
    - `src/components/features/drawing/KoppitzVisualization.tsx` contains `<span>מדדי קופיץ</span>`
    - `src/components/features/drawing/DrawingCompare.tsx` contains `ציון קופיץ` (two occurrences)
    - No `id:` values were changed (grep for `id: 'htp_basics'` and `id: 'koppitz_scoring'` still present)
    - `npx next build` exits with code 0 (no TypeScript errors introduced)
    - `src/lib/constants/tool-names.ts` is UNCHANGED
  </acceptance_criteria>
  <done>All 15 English display string occurrences replaced with Hebrew equivalents. No code identifiers, prompts, comments, or academic subtitles were modified. Build passes cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **TYPO-01 font verification:**
   - `grep -q 'font-body' mystiqor-build/src/app/layout.tsx` exits 0 (font-body on body tag)
   - `grep -c 'font-sans' mystiqor-build/src/**/*.tsx` returns 0 matches in Hebrew text contexts
   - `grep 'Heebo' mystiqor-build/src/components/features/astrology/HumanDesignCenters.tsx` returns the SVG fontFamily line

2. **TYPO-02 term verification:**
   - `grep -rn 'Human Design' mystiqor-build/src/ --include="*.tsx"` returns ONLY JSDoc comment lines (starting with * or //)
   - `grep -rn '".*HTP.*"' mystiqor-build/src/ --include="*.tsx"` returns ONLY id properties and prompt strings (no display text)
   - `grep -rn 'Koppitz' mystiqor-build/src/ --include="*.tsx"` returns ONLY: academic subtitle, code identifiers (KoppitzDelta, koppitz_score, koppitzScore), and import/component names
   - `grep -rn 'עיצוב אנושי' mystiqor-build/src/ --include="*.tsx"` returns at least 1 result (aria-label)
   - `grep -rn 'בית-עץ-אדם' mystiqor-build/src/ --include="*.tsx"` returns at least 7 results
   - `grep -rn 'קופיץ' mystiqor-build/src/ --include="*.tsx"` returns at least 8 results

3. **No regression:**
   - `cd mystiqor-build && npx next build` exits 0
   - `cd mystiqor-build && npx vitest run` exits 0 (existing tests still pass)
</verification>

<success_criteria>
1. Heebo font confirmed active on all Hebrew text (body tag has font-body, no font-sans overrides, SVG uses Heebo)
2. Zero remaining English display strings for "Human Design", "HTP", or "Koppitz" in user-facing UI
3. Hebrew terms present: "עיצוב אנושי", "בית-עץ-אדם", "קופיץ" in all locations specified by the UI-SPEC
4. No code breakage: build passes, existing tests pass, no TypeScript errors
5. No collateral damage: code identifiers, prompt strings, academic subtitles, and JSDoc comments all untouched
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-hebrew-localization/14-01-SUMMARY.md`
</output>
