---
phase: 14-typography-hebrew-localization
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/layouts/StandardSectionHeader.tsx
  - src/components/layouts/PageHeader.tsx
  - src/components/layouts/Sidebar.tsx
  - src/components/features/dashboard/DailyInsightCard.tsx
  - src/components/features/shared/ToolPageHero.tsx
  - src/app/(auth)/dashboard/page.tsx
  - src/components/features/daily-insights/InsightHeroCard.tsx
  - src/app/(auth)/tools/graphology/page.tsx
  - src/components/features/astrology/ZodiacGrid.tsx
  - src/components/features/astrology/ReadingCard.tsx
  - src/components/features/astrology/PlanetGrid.tsx
  - src/components/features/astrology/AstroCalendar/index.tsx
  - src/components/features/tarot/TarotCardDetailModal.tsx
  - src/components/features/astrology/AspectDictionary.tsx
  - src/components/features/astrology/HouseList.tsx
  - src/components/features/astrology/DailyForecast/index.tsx
  - src/components/features/synthesis/SynthesisResult.tsx
  - src/components/features/journal/JournalEntryCard.tsx
  - src/components/features/daily-insights/InsightHistoryList.tsx
  - src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx
  - src/components/features/insights/ExplainableInsight.tsx
  - src/components/features/graphology/GraphologyTimeline.tsx
  - src/components/features/graphology/GraphologyQuickStats.tsx
  - src/components/features/history/ComparePanel.tsx
  - src/components/features/floating-coach/FloatingCoachPanel.tsx
  - src/components/features/personality/BigFiveQuestionnaire.tsx
  - src/components/features/numerology/NumberCard.tsx
  - src/components/features/mood/MoodEntryCard.tsx
  - src/components/features/numerology/CompatibilityCard.tsx
  - src/components/features/onboarding/BarnumEthicsStep.tsx
  - src/components/features/coach/JourneyCard.tsx
  - src/components/features/coach/ChatMessage.tsx
  - src/components/features/drawing/KoppitzVisualization.tsx
  - src/components/features/drawing/DrawingAnalysisForm.tsx
  - src/components/features/drawing/KoppitzIndicators.tsx
  - src/components/features/drawing/AnnotatedDrawingViewer.tsx
  - src/app/(public)/share/[token]/page.tsx
  - src/components/features/drawing/DrawingCompare.tsx
  - src/components/features/drawing/DrawingConceptCards.tsx
  - src/app/(auth)/tools/timing/page.tsx
  - src/app/(auth)/tools/tarot/page.tsx
  - src/app/(auth)/tools/synthesis/page.tsx
  - src/app/(auth)/tools/personality/page.tsx
  - src/app/(auth)/tools/palmistry/page.tsx
  - src/app/(auth)/tools/astrology/transits/page.tsx
  - src/app/(auth)/tools/numerology/page.tsx
  - src/app/(auth)/tools/astrology/synastry/page.tsx
  - src/app/(auth)/tools/document/page.tsx
  - src/app/(auth)/tools/compatibility/page.tsx
  - src/app/(auth)/tools/career/page.tsx
  - src/components/features/drawing/FDMVisualization.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "No Hebrew text element has tracking-tight, tracking-wider, tracking-wide, or tracking-widest classes"
    - "No Hebrew body text element has redundant leading-relaxed that overrides the global 1.7 line-height"
    - "Latin headline elements (font-headline with Latin content like 404) retain their tracking"
    - "All existing component functionality is preserved — only class removals, no logic changes"
  artifacts:
    - path: "src/components/layouts/StandardSectionHeader.tsx"
      provides: "Hebrew page title without tracking"
      contains: "font-headline font-bold text-gradient-gold"
    - path: "src/components/layouts/Sidebar.tsx"
      provides: "Hebrew section labels without tracking"
      contains: "font-bold uppercase"
    - path: "src/components/features/dashboard/DailyInsightCard.tsx"
      provides: "Hebrew label without tracking, Hebrew body without leading-relaxed"
      contains: "font-label font-medium uppercase"
  key_links:
    - from: "src/app/globals.css (.font-body rule)"
      to: "all components with font-body class"
      via: "CSS cascade — global line-height: 1.7 applies, no leading-relaxed override"
      pattern: "font-body"
---

<objective>
Remove tracking utilities from Hebrew text elements and remove redundant leading-relaxed classes superseded by the global .font-body line-height: 1.7 rule.

Purpose: D-06 requires zero letter-spacing on Hebrew text (tracking makes Hebrew unreadable). D-05 requires removing per-component leading-relaxed (1.625) that would override the correct global 1.7 line-height downward. These are mechanical class removals with no logic changes.

Output: 8 files with tracking classes removed from Hebrew elements. ~40 files with leading-relaxed removed from font-body Hebrew text elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-typography-hebrew-localization/14-CONTEXT.md
@.planning/phases/14-typography-hebrew-localization/14-01-SUMMARY.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracking utilities from Hebrew text elements (D-06)</name>
  <files>
    src/components/layouts/StandardSectionHeader.tsx
    src/components/layouts/PageHeader.tsx
    src/components/layouts/Sidebar.tsx
    src/components/features/dashboard/DailyInsightCard.tsx
    src/components/features/shared/ToolPageHero.tsx
    src/app/(auth)/dashboard/page.tsx
    src/components/features/daily-insights/InsightHeroCard.tsx
    src/app/(auth)/tools/graphology/page.tsx
  </files>
  <read_first>
    - src/components/layouts/StandardSectionHeader.tsx (line 105 — tracking-tight on h1)
    - src/components/layouts/PageHeader.tsx (line 80 — tracking-tight on h1)
    - src/components/layouts/Sidebar.tsx (line 185 — tracking-wider on section button)
    - src/components/features/dashboard/DailyInsightCard.tsx (line 153 — tracking-wider on label)
    - src/components/features/shared/ToolPageHero.tsx (line 99 — tracking-tight on h1)
    - src/app/(auth)/dashboard/page.tsx (line 310 — tracking-tight on h1)
    - src/components/features/daily-insights/InsightHeroCard.tsx (line 159 — tracking-wide on label)
    - src/app/(auth)/tools/graphology/page.tsx (line 315 — tracking-wide on label)
  </read_first>
  <action>
    Per D-06: Remove tracking utilities from elements that render Hebrew text. Keep tracking on elements rendering Latin content (like "404" in not-found.tsx, or shortcut keys in dropdown-menu.tsx — those are NOT in this list).

    For each file below, remove the specified tracking class from the className string. Do NOT change any other classes, props, or logic.

    1. **StandardSectionHeader.tsx line 105:**
       BEFORE: `"text-2xl font-headline font-bold tracking-tight text-gradient-gold"`
       AFTER:  `"text-2xl font-headline font-bold text-gradient-gold"`
       (Remove `tracking-tight` — this h1 renders Hebrew page titles like "נומרולוגיה")

    2. **PageHeader.tsx line 80:**
       BEFORE: `"text-2xl font-headline font-bold tracking-tight text-gradient-gold"`
       AFTER:  `"text-2xl font-headline font-bold text-gradient-gold"`
       (Remove `tracking-tight` — same pattern, Hebrew page titles)

    3. **Sidebar.tsx line 185:**
       BEFORE: `'text-lg font-bold uppercase tracking-wider',`
       AFTER:  `'text-lg font-bold uppercase',`
       (Remove `tracking-wider` — Hebrew section labels like "כלים מיסטיים")

    4. **DailyInsightCard.tsx line 153:**
       BEFORE: `"text-sm font-label font-medium uppercase tracking-wider text-white/70 mb-2"`
       AFTER:  `"text-sm font-label font-medium uppercase text-white/70 mb-2"`
       (Remove `tracking-wider` — Hebrew label "תובנה יומית")

    5. **ToolPageHero.tsx line 99:**
       BEFORE: `"text-4xl md:text-5xl font-headline font-black text-gradient-gold tracking-tight"`
       AFTER:  `"text-4xl md:text-5xl font-headline font-black text-gradient-gold"`
       (Remove `tracking-tight` — Hebrew tool names like "נומרולוגיה")

    6. **dashboard/page.tsx line 310:**
       BEFORE: `"text-4xl sm:text-5xl md:text-6xl font-thin tracking-tight text-foreground font-headline"`
       AFTER:  `"text-4xl sm:text-5xl md:text-6xl font-thin text-foreground font-headline"`
       (Remove `tracking-tight` — Hebrew greeting "שלום,")

    7. **InsightHeroCard.tsx line 159:**
       BEFORE: `"text-xs font-label font-semibold uppercase tracking-wide text-tertiary"`
       AFTER:  `"text-xs font-label font-semibold uppercase text-tertiary"`
       (Remove `tracking-wide` — Hebrew label "טיפ ליום")

    8. **graphology/page.tsx line 315:**
       BEFORE: `"text-xs font-semibold text-primary font-label uppercase tracking-wide"`
       AFTER:  `"text-xs font-semibold text-primary font-label uppercase"`
       (Remove `tracking-wide` — Hebrew category labels)

    IMPORTANT: Do NOT touch tracking classes in these files (they are Latin content):
    - src/app/not-found.tsx — "404" is a number, tracking-tight is correct
    - src/components/ui/dropdown-menu.tsx — shortcut text is Latin, tracking-widest is correct
  </action>
  <verify>
    <automated>cd "D:/AI_projects/MystiQor/mystiqor-build" && echo "=== Hebrew tracking violations remaining ===" && grep -rn "tracking-\(tight\|wider\|wide\|widest\)" src/components/layouts/StandardSectionHeader.tsx src/components/layouts/PageHeader.tsx src/components/layouts/Sidebar.tsx src/components/features/dashboard/DailyInsightCard.tsx src/components/features/shared/ToolPageHero.tsx src/app/\(auth\)/dashboard/page.tsx src/components/features/daily-insights/InsightHeroCard.tsx src/app/\(auth\)/tools/graphology/page.tsx 2>/dev/null; echo "Exit: $?" && echo "=== Latin tracking preserved ===" && grep -c "tracking-tight" src/app/not-found.tsx && grep -c "tracking-widest" src/components/ui/dropdown-menu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "tracking-" src/components/layouts/StandardSectionHeader.tsx` returns no results
    - `grep -rn "tracking-" src/components/layouts/PageHeader.tsx` returns no results
    - `grep -rn "tracking-" src/components/layouts/Sidebar.tsx` returns no results
    - `grep -rn "tracking-" src/components/features/dashboard/DailyInsightCard.tsx` returns no results
    - `grep -rn "tracking-" src/components/features/shared/ToolPageHero.tsx` returns no results
    - `grep -rn "tracking-" src/app/(auth)/dashboard/page.tsx` returns no results
    - `grep -rn "tracking-" src/components/features/daily-insights/InsightHeroCard.tsx` returns no results
    - `grep -rn "tracking-" src/app/(auth)/tools/graphology/page.tsx` returns no results
    - `grep -c "tracking-tight" src/app/not-found.tsx` returns 1 (Latin "404" preserved)
    - `grep -c "tracking-widest" src/components/ui/dropdown-menu.tsx` returns 1 (Latin shortcut preserved)
    - No other changes to any file — same line count, same logic, only class string changed
  </acceptance_criteria>
  <done>All 8 Hebrew tracking violations removed. Latin tracking instances preserved. Zero logic changes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove redundant leading-relaxed from Hebrew body text elements (D-05)</name>
  <files>
    src/components/features/astrology/ZodiacGrid.tsx
    src/components/features/astrology/ReadingCard.tsx
    src/components/features/astrology/PlanetGrid.tsx
    src/components/features/astrology/AstroCalendar/index.tsx
    src/components/features/tarot/TarotCardDetailModal.tsx
    src/components/features/astrology/AspectDictionary.tsx
    src/components/features/astrology/HouseList.tsx
    src/components/features/astrology/DailyForecast/index.tsx
    src/components/features/synthesis/SynthesisResult.tsx
    src/components/features/journal/JournalEntryCard.tsx
    src/components/features/daily-insights/InsightHistoryList.tsx
    src/components/features/astrology/ChartInfoPanels/AIInterpretation.tsx
    src/components/features/insights/ExplainableInsight.tsx
    src/components/features/graphology/GraphologyTimeline.tsx
    src/components/features/graphology/GraphologyQuickStats.tsx
    src/components/features/history/ComparePanel.tsx
    src/components/features/floating-coach/FloatingCoachPanel.tsx
    src/components/features/personality/BigFiveQuestionnaire.tsx
    src/components/features/numerology/NumberCard.tsx
    src/components/features/mood/MoodEntryCard.tsx
    src/components/features/numerology/CompatibilityCard.tsx
    src/components/features/onboarding/BarnumEthicsStep.tsx
    src/components/features/coach/JourneyCard.tsx
    src/components/features/coach/ChatMessage.tsx
    src/components/features/drawing/KoppitzVisualization.tsx
    src/components/features/drawing/DrawingAnalysisForm.tsx
    src/components/features/drawing/KoppitzIndicators.tsx
    src/components/features/drawing/AnnotatedDrawingViewer.tsx
    src/app/(public)/share/[token]/page.tsx
    src/components/features/dashboard/DailyInsightCard.tsx
    src/components/features/drawing/DrawingCompare.tsx
    src/components/features/drawing/DrawingConceptCards.tsx
    src/app/(auth)/tools/timing/page.tsx
    src/app/(auth)/tools/tarot/page.tsx
    src/app/(auth)/tools/synthesis/page.tsx
    src/app/(auth)/tools/personality/page.tsx
    src/app/(auth)/tools/palmistry/page.tsx
    src/app/(auth)/tools/astrology/transits/page.tsx
    src/app/(auth)/tools/numerology/page.tsx
    src/app/(auth)/tools/astrology/synastry/page.tsx
    src/app/(auth)/tools/document/page.tsx
    src/app/(auth)/tools/compatibility/page.tsx
    src/app/(auth)/tools/career/page.tsx
    src/components/features/drawing/FDMVisualization.tsx
    src/components/features/daily-insights/InsightHeroCard.tsx
  </files>
  <read_first>
    - src/app/globals.css (confirm .font-body { line-height: 1.7 } is in place from Plan 01)
    - Each file listed above — read before editing to see current leading-relaxed usage
  </read_first>
  <action>
    Per D-05: Remove `leading-relaxed` (line-height: 1.625) from Hebrew body text elements. The global `.font-body { line-height: 1.7 }` rule from Plan 01 now handles line-height for all elements with `font-body` class or inheriting from body. Keeping `leading-relaxed` would OVERRIDE the global 1.7 DOWN to 1.625 — the opposite of what we want.

    **Rule for removal:** Remove `leading-relaxed` from any className string where:
    - The element has `font-body` class in the same string, OR
    - The element renders Hebrew text and inherits font-body from body (no explicit font class)

    **Rule for keeping (DO NOT remove):**
    - `leading-snug` on `font-headline` elements — these are Latin headings with intentionally tighter line-height
    - `leading-snug` on any element — snug (1.375) serves a different design purpose for compact headings
    - `leading-relaxed` inside prose modifier syntax like `prose-p:leading-relaxed` — replace this with `prose-p:leading-[1.7]` to match the global value
    - `[&>p]:leading-relaxed` and `[&>li]:leading-relaxed` in ChatMessage.tsx prose blocks — replace with `[&>p]:leading-[1.7]` and `[&>li]:leading-[1.7]`

    **Mechanical operation for each file:**
    Open the file, find every instance of `leading-relaxed` in a className string, and:
    - If the element has `font-body` or renders Hebrew: DELETE `leading-relaxed` from the className (leave a single space, don't create double spaces)
    - If inside a prose variant selector (`prose-p:leading-relaxed`, `[&>p]:leading-relaxed`, `[&>li]:leading-relaxed`): REPLACE with the `leading-[1.7]` variant (`prose-p:leading-[1.7]`, `[&>p]:leading-[1.7]`, `[&>li]:leading-[1.7]`)

    **Specific files with prose variant replacements (not just removal):**
    - `InsightHeroCard.tsx` line 139: `prose-p:leading-relaxed` -> `prose-p:leading-[1.7]`
    - `ChatMessage.tsx` line 70: `[&>p]:leading-relaxed` -> `[&>p]:leading-[1.7]`
    - `ChatMessage.tsx` line 73: `[&>li]:leading-relaxed` -> `[&>li]:leading-[1.7]`

    **All other files:** Simple removal of `leading-relaxed` from the className string. No logic changes, no other class modifications.

    IMPORTANT: Do NOT remove `leading-snug` from any file. It appears in card.tsx, InsightHistoryList.tsx, InsightHeroCard.tsx, GoalCard.tsx, and AnnotatedDrawingViewer.tsx on heading/compact elements — these are intentional and correct.
  </action>
  <verify>
    <automated>cd "D:/AI_projects/MystiQor/mystiqor-build" && echo "=== leading-relaxed remaining in src/ ===" && grep -rn "leading-relaxed" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v ".next" | wc -l && echo "=== leading-snug preserved ===" && grep -rn "leading-snug" src/ --include="*.tsx" | grep -v "node_modules" | wc -l && echo "=== leading-[1.7] replacements ===" && grep -rn "leading-\[1\.7\]" src/ --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "leading-relaxed" src/ --include="*.tsx" | wc -l` returns 0 (all instances removed or replaced)
    - `grep -rn "leading-snug" src/ --include="*.tsx" | wc -l` returns same count as before (all preserved)
    - `grep -rn "leading-\[1\.7\]" src/ --include="*.tsx" | wc -l` returns 3 (the prose variant replacements in InsightHeroCard and ChatMessage)
    - No double spaces in any modified className string
    - No logic changes in any file — same component behavior, same props, same rendering
    - Build passes with zero errors: `npx next build` succeeds
  </acceptance_criteria>
  <done>All leading-relaxed removed from Hebrew body text elements across ~45 files. Prose variant selectors replaced with leading-[1.7]. leading-snug on headline/compact elements preserved. Global .font-body line-height 1.7 now cascades without override.</done>
</task>

</tasks>

<verification>
1. `grep -rn "tracking-\(tight\|wider\|wide\)" src/ --include="*.tsx" | grep -v not-found | grep -v dropdown-menu` — should return 0 Hebrew tracking violations
2. `grep -rn "leading-relaxed" src/ --include="*.tsx" | wc -l` — should return 0
3. `grep -rn "leading-snug" src/ --include="*.tsx" | wc -l` — same count as before task execution
4. `npx next build 2>&1 | tail -5` — build succeeds
5. Spot-check: `grep "font-headline font-bold text-gradient-gold" src/components/layouts/StandardSectionHeader.tsx` — no tracking class present
</verification>

<success_criteria>
- Zero tracking-tight/tracking-wider/tracking-wide on Hebrew text elements across entire src/
- Zero leading-relaxed remaining in any .tsx file
- All leading-snug instances preserved (headline/compact elements)
- 3 prose variant selectors use leading-[1.7] instead of leading-relaxed
- Build passes with zero errors
- No functional changes — components render identically except for corrected typography
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-hebrew-localization/14-02-SUMMARY.md`
</output>
