---
phase: 25-coach-intelligence-sidebar-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/api/coach/messages/route.ts
autonomous: true
requirements:
  - COACH-04

must_haves:
  truths:
    - "הודעה למאמן AI אחרי ביצוע ניתוח כוללת את הניתוח בהקשר — המאמן יודע מה נעשה"
    - "שאלה על 'מה עשיתי היום' מחזירה תשובה ספציפית מבוססת על ניתוחים אחרונים"
  artifacts:
    - path: "mystiqor-build/src/app/api/coach/messages/route.ts"
      provides: "Per-message analysis context injection into system prompt"
      contains: "ניתוחים אחרונים בשיחה"
  key_links:
    - from: "mystiqor-build/src/app/api/coach/messages/route.ts"
      to: "analyses table"
      via: "supabase.from('analyses').select().eq('user_id').order().limit(5)"
      pattern: "from\\('analyses'\\)"
    - from: "mystiqor-build/src/app/api/coach/messages/route.ts"
      to: "mystiqor-build/src/lib/constants/tool-names.ts"
      via: "TOOL_NAMES import for Hebrew tool labels"
      pattern: "TOOL_NAMES"
---

<objective>
Inject the user's 5 most recent analyses into every coach message's system prompt so the AI coach responds with awareness of what the user has done recently.

Purpose: Per D-01, every POST to /api/coach/messages fetches the 5 most recent analyses and appends them to the system prompt. The coach always knows what the user has done, enabling contextual answers like "you did a birth chart analysis 2 hours ago."

Output: Modified messages/route.ts with per-message analysis context injection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mystiqor-build/src/app/api/coach/messages/route.ts
@mystiqor-build/src/services/coach/context-builder.ts
@mystiqor-build/src/lib/constants/tool-names.ts
@mystiqor-build/src/services/analysis/personal-context.ts

<interfaces>
<!-- TOOL_NAMES constant for Hebrew tool type labels -->
From mystiqor-build/src/lib/constants/tool-names.ts:
```typescript
export const TOOL_NAMES: Record<string, string> = {
  numerology: 'נומרולוגיה',
  astrology: 'אסטרולוגיה',
  palmistry: 'קריאה בכף יד',
  graphology: 'גרפולוגיה',
  tarot: 'טארוט',
  drawing: 'ניתוח ציורים',
  dream: 'חלומות',
  career: 'קריירה',
  compatibility: 'תאימות',
  synastry: 'סינסטרי',
  solar_return: 'חזרת שמש',
  transits: 'טרנזיטים',
  human_design: 'עיצוב אנושי',
  personality: 'אישיות',
  document: 'מסמכים',
  question: 'שאלה',
  relationship: 'יחסים',
  synthesis: 'סינתזה',
} as const;
```

From mystiqor-build/src/app/api/coach/messages/route.ts (current system prompt construction, lines 183-198):
```typescript
let fullSystemPrompt = COACH_PERSONA
if (contextText) {
  fullSystemPrompt += `\n\n### הקשר המשתמש:\n${contextText}`
}
if (ctx.firstName) {
  fullSystemPrompt += `\n\n### זהות הפונה:\nשם: ${ctx.firstName}, מזל: ${ctx.zodiacSign}, מספר חיים: ${ctx.lifePathNumber}.\nפנה אליו בשמו. התייחס למזלו ולמספר חייו כשרלוונטי.`
}
if (priorList.length > 0) {
  const historyLines = priorList
    .map((msg) => `${msg.role === 'user' ? 'משתמש' : 'מאמן'}: ${msg.content}`)
    .join('\n')
  fullSystemPrompt += `\n\n### היסטוריית שיחה:\n${historyLines}`
}
```

From mystiqor-build/src/services/coach/context-builder.ts (reference query pattern, line 29-33):
```typescript
supabase
  .from('analyses')
  .select('tool_type, results, summary, created_at')
  .eq('user_id', userId)
  .order('created_at', { ascending: false })
  .limit(20)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-message analysis context injection to coach messages route</name>
  <files>mystiqor-build/src/app/api/coach/messages/route.ts</files>

  <read_first>
    mystiqor-build/src/app/api/coach/messages/route.ts
    mystiqor-build/src/services/coach/context-builder.ts
    mystiqor-build/src/lib/constants/tool-names.ts
  </read_first>

  <action>
  Modify the POST handler in messages/route.ts to inject per-message analysis context (per D-01, D-02, D-03).

  **Step 1: Add import at top of file:**
  ```typescript
  import { TOOL_NAMES } from '@/lib/constants/tool-names'
  ```

  **Step 2: Add a helper function (above the POST export) for relative time in Hebrew:**
  ```typescript
  /** חישוב זמן יחסי בעברית — "לפני X דקות/שעות/ימים" */
  function relativeTimeHebrew(dateStr: string): string {
    const now = Date.now()
    const then = new Date(dateStr).getTime()
    const diffMs = now - then
    const diffMin = Math.floor(diffMs / 60000)
    if (diffMin < 1) return 'הרגע'
    if (diffMin < 60) return `לפני ${diffMin} דקות`
    const diffHours = Math.floor(diffMin / 60)
    if (diffHours < 24) return `לפני ${diffHours} שעות`
    const diffDays = Math.floor(diffHours / 24)
    if (diffDays === 1) return 'אתמול'
    return `לפני ${diffDays} ימים`
  }
  ```

  **Step 3: Add an interface for the light analysis row:**
  ```typescript
  /** שורת ניתוח קלה לזריקת הקשר לכל הודעה */
  interface RecentAnalysisRow {
    tool_type: string
    summary: string | null
    created_at: string | null
  }
  ```

  **Step 4: In the POST handler, AFTER the line `const convRow = conversation as ConversationRow` (around line 151) and BEFORE the "שמירת הודעת המשתמש" block, add the per-message analysis fetch:**
  ```typescript
  // שליפת 5 ניתוחים אחרונים להזרקת הקשר דינמי בכל הודעה (per D-01/D-03)
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: recentAnalyses } = await (supabase as any)
    .from('analyses')
    .select('tool_type, summary, created_at')
    .eq('user_id', user.id)
    .order('created_at', { ascending: false })
    .limit(5)
  ```

  **Step 5: In the system prompt construction section (after the `### זהות הפונה` block, BEFORE the history block), add the analysis context injection:**
  ```typescript
  // הזרקת ניתוחים אחרונים להקשר — בין זהות הפונה להיסטוריית שיחה (per D-02)
  const analysisList = recentAnalyses as RecentAnalysisRow[] | null
  if (analysisList && analysisList.length > 0) {
    const analysisLines = analysisList.map((a) => {
      const toolHebrew = TOOL_NAMES[a.tool_type] ?? a.tool_type
      const summaryText = a.summary ? a.summary.slice(0, 80) : 'ללא סיכום'
      const timeText = a.created_at ? relativeTimeHebrew(a.created_at) : ''
      return `- ${toolHebrew}: ${summaryText}${timeText ? ` (${timeText})` : ''}`
    }).join('\n')
    fullSystemPrompt += `\n\n### ניתוחים אחרונים בשיחה:\n${analysisLines}\nהשתמש במידע זה כדי לתת ייעוץ מותאם. אם המשתמש שואל "מה עשיתי" — התבסס על הרשימה הזו.`
  }
  ```

  **IMPORTANT:** Do NOT modify the GET handler, COACH_PERSONA, or any existing prompt sections. The injection goes between the identity block and the history block — this is the only change to the prompt construction flow.

  **IMPORTANT:** The query fetches only `tool_type, summary, created_at` (3 fields, no `results`) with `.limit(5)` — this is intentionally lighter than context-builder.ts which fetches 20 with `results` (per D-03).
  </action>

  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit src/app/api/coach/messages/route.ts 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
  - grep -c "from('analyses')" mystiqor-build/src/app/api/coach/messages/route.ts returns 1 (the new query)
  - grep -c "TOOL_NAMES" mystiqor-build/src/app/api/coach/messages/route.ts returns at least 1
  - grep -c "ניתוחים אחרונים בשיחה" mystiqor-build/src/app/api/coach/messages/route.ts returns 1
  - grep -c "relativeTimeHebrew" mystiqor-build/src/app/api/coach/messages/route.ts returns at least 2 (definition + usage)
  - grep -c "limit(5)" mystiqor-build/src/app/api/coach/messages/route.ts returns 1
  - File compiles with no TypeScript errors
  - COACH_PERSONA constant is unchanged
  - GET handler is unchanged
  </acceptance_criteria>

  <done>
  POST handler fetches 5 most recent analyses and injects them as a Hebrew markdown list into the system prompt between identity and history sections. Each entry shows Hebrew tool name, summary (80 char max), and relative time. The coach can now answer "what did I do today" with specific data.
  </done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. `grep "ניתוחים אחרונים בשיחה" src/app/api/coach/messages/route.ts` — section header present
3. `grep "TOOL_NAMES" src/app/api/coach/messages/route.ts` — Hebrew tool names imported and used
4. `grep "limit(5)" src/app/api/coach/messages/route.ts` — light query (5, not 20)
5. The GET handler is unchanged (no regression)
</verification>

<success_criteria>
- Coach messages route injects 5 most recent analyses into every system prompt
- Analysis context uses Hebrew tool names from TOOL_NAMES constant
- Each analysis entry shows: tool type (Hebrew), summary (80 chars), relative time (Hebrew)
- Context appears between identity and history sections in the prompt
- No changes to COACH_PERSONA, GET handler, or conversation context flow
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-coach-intelligence-sidebar-polish/25-01-SUMMARY.md`
</output>
