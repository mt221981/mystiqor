---
phase: 34-rich-numerology-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/features/numerology/NumberCard.tsx
  - mystiqor-build/src/components/ui/mystic-scroll.tsx
  - mystiqor-build/src/app/(auth)/tools/numerology/page.tsx
  - mystiqor-build/src/app/api/tools/numerology/route.ts
autonomous: true
requirements: [NUM-01, NUM-02]

must_haves:
  truths:
    - "כרטיס נומרולוגי מציג אות עברית גדולה ודקורטיבית, שם ספירה, כותרת מיסטית, 5 תגיות מילות מפתח וקשר כוכבי — גלוי ישירות על פני הכרטיס ללא לחיצה"
    - "סקציית הסינתזה של AI מוצגת עטופה בקומפוננטת MysticScroll עם בורדרים זהובים ותאורת זוהר, לא כ-Card רגיל"
    - "ה-API מחשב עם 3000 tokens — AI synthesis מורחבת ועמוקה יותר"
    - "הדיאלוג המפורט של הכרטיס (לחיצה) עדיין עובד בדיוק כמקודם"
  artifacts:
    - path: "mystiqor-build/src/components/features/numerology/NumberCard.tsx"
      provides: "כרטיס נומרולוגי עשיר — אות עברית + ספירה + keywords + association על פני הכרטיס"
      contains: "hebrewLetter, sephira, keywords, association"
    - path: "mystiqor-build/src/components/ui/mystic-scroll.tsx"
      provides: "קומפוננטת MysticScroll — מגילה מיסטית עם עיצוב זהוב"
      exports: ["MysticScroll"]
    - path: "mystiqor-build/src/app/(auth)/tools/numerology/page.tsx"
      provides: "שימוש ב-MysticScroll לעטוף את סקציית הפרשנות"
      contains: "MysticScroll"
    - path: "mystiqor-build/src/app/api/tools/numerology/route.ts"
      provides: "maxTokens מוגדל ל-3000"
      contains: "maxTokens: 3000"
  key_links:
    - from: "NumberCard.tsx"
      to: "numerology-meanings.ts"
      via: "getNumberMeaning(value)"
      pattern: "getNumberMeaning"
    - from: "page.tsx"
      to: "mystic-scroll.tsx"
      via: "import MysticScroll"
      pattern: "MysticScroll"
    - from: "route.ts"
      to: "invokeLLM"
      via: "maxTokens: 3000"
      pattern: "maxTokens.*3000"
---

<objective>
הצגה ויזואלית עשירה של תוצאות נומרולוגיה: כרטיסים מיסטיים עם כל המידע הקבלי גלוי ישירות + מגילה מסכמת לסינתזת AI עמוקה יותר.

Purpose: כרגע כרטיס הנומרולוגיה מציג רק מספר + תווית + כותרת + רמז ללחיצה. הנתונים העשירים (אות עברית, ספירה, מילות מפתח, כוכב) קבורים בדיאלוג. NUM-01 מוציא אותם לפני הכרטיס. NUM-02 הופך את סקציית הסינתזה מ-Card רגיל למגילה מיסטית ומגדיל את עומק ה-AI.

Output: NumberCard.tsx מורחב + קובץ חדש mystic-scroll.tsx + עדכונים ב-page.tsx ו-route.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mystiqor-build/src/components/features/numerology/NumberCard.tsx
@mystiqor-build/src/lib/constants/numerology-meanings.ts
@mystiqor-build/src/app/(auth)/tools/numerology/page.tsx
@mystiqor-build/src/app/api/tools/numerology/route.ts
@mystiqor-build/src/app/globals.css
</context>

<interfaces>
<!-- מ-numerology-meanings.ts — הממשקים שהאקזקיוטור צריך -->

```typescript
export interface NumberMeaning {
  readonly number: number
  readonly title: string
  readonly shortDescription: string
  readonly deepDescription: string
  readonly sephira: string
  readonly hebrewLetter: string
  readonly association: string
  readonly keywords: readonly string[]
}

export function getNumberMeaning(num: number): NumberMeaning
```

<!-- CSS utilities קיימים ב-globals.css שרלוונטיים -->
<!--
  .mystic-card-gold  — גרדיאנט כהה + בורדר זהוב rgba(212,168,83,0.2) + glow
  .mystic-card       — גרדיאנט כהה + בורדר סגול-לבן
  --accent: 42 65% 58%  → #d4a853 gold
  text-primary       → #ddb8ff lavender
  bg-surface-container → כרטיסי הרקע הנוכחיים
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich NumberCard face — Hebrew letter, sephira, keywords, association (NUM-01)</name>
  <files>mystiqor-build/src/components/features/numerology/NumberCard.tsx</files>
  <behavior>
    - כרטיס שולח getNumberMeaning(value) כבר עכשיו — הנתונים זמינים
    - פני הכרטיס (CardContent) מציגים: typeInfo.icon + מספר גדול + תווית + אות עברית גדולה + כותרת + ספירה/כוכב + 3 מילות מפתח ראשונות כ-pills
    - הדיאלוג לא משתנה — כל הלוגיקה הקיימת נשמרת
    - build עובר ללא TypeScript errors
  </behavior>
  <action>
    ערוך את mystiqor-build/src/components/features/numerology/NumberCard.tsx — עדכן את ה-CardContent בלבד (שורות 64-94 בגרסה הנוכחית). הדיאלוג (שורות 98-176) לא נגעים בו כלל.

    מבנה CardContent החדש — מלמעלה למטה:
    1. שורה עליונה: typeInfo.icon (text-base opacity-70) + אות עברית (text-3xl font-headline font-black text-accent leading-none) בצד ימין — flex justify-between items-start
    2. המספר הגדול: text-5xl font-headline font-black text-primary — נשמר בדיוק כמקודם
    3. תווית עברית: text-sm font-headline font-semibold text-on-surface — נשמרת
    4. כותרת (meaning.title): text-xs font-body text-on-surface-variant/70 — נשמרת
    5. שורת ספירה + כוכב: flex gap-2 justify-center
       - ספירה: text-[10px] font-body text-primary/60 — "ספירת {meaning.sephira}"
       - מפריד: text-[10px] text-accent/40 — "·"
       - כוכב: text-[10px] font-body text-accent/70 — meaning.association
    6. שורת keywords: flex flex-wrap gap-1 justify-center mt-1 — הצג 3 keywords ראשונות בלבד (slice(0,3)):
       כל keyword: text-[9px] font-body px-1.5 py-0.5 rounded-full bg-primary/8 text-primary/70 border border-primary/15
    7. רמז ללחיצה: text-[10px] font-body text-primary/40 group-hover:text-primary/70 — "▾"

    אנא שים לב: `text-accent` בפרויקט = gold (#d4a853). `bg-primary/8` = bg-primary/[0.08] ב-Tailwind.

    אין לשנות את ה-Props interface, את ה-Dialog, ולא את כלום מחוץ ל-CardContent.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | grep -i "NumberCard" || echo "No TypeScript errors in NumberCard"</automated>
  </verify>
  <done>
    - פני כרטיס נומרולוגיה מציגים אות עברית גדולה + ספירה + כוכב + 3 keywords כ-pills
    - הדיאלוג המפורט עדיין נפתח בלחיצה ומציג את כל המידע המלא
    - build עובר ללא errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MysticScroll component + AI synthesis rewrap + maxTokens 3000 (NUM-02)</name>
  <files>
    mystiqor-build/src/components/ui/mystic-scroll.tsx
    mystiqor-build/src/app/(auth)/tools/numerology/page.tsx
    mystiqor-build/src/app/api/tools/numerology/route.ts
  </files>
  <behavior>
    - MysticScroll מקבל children + title prop ומציג אותם בעיצוב מגילה
    - page.tsx עוטף את הCard של "פרשנות AI" ב-MysticScroll במקום Card רגיל
    - route.ts: maxTokens: 2000 → maxTokens: 3000
    - build עובר ללא TypeScript errors
  </behavior>
  <action>
    **שלב א — צור mystiqor-build/src/components/ui/mystic-scroll.tsx:**

    קומפוננטה חדשה MysticScroll. Props:
    ```typescript
    interface MysticScrollProps {
      /** כותרת המגילה — מוצגת בראש */
      readonly title: string
      /** תוכן — ReactNode */
      readonly children: React.ReactNode
      /** מחלקות נוספות — אופציונלי */
      readonly className?: string
    }
    ```

    עיצוב המגילה (div עטיפה):
    - className: `relative rounded-2xl overflow-hidden mystic-card-gold`
    - אין להשתמש ב-Card מ-shadcn — זהו div מעוצב ידנית

    פס זהוב עליון (div):
    - `h-px w-full bg-gradient-to-r from-transparent via-accent to-transparent opacity-60`

    כותרת (div פנימי — p-5 pb-3 text-center):
    - אייקון מגילה: `<span className="text-2xl block mb-1">📜</span>`
    - כותרת: `<h3 className="text-base font-headline font-bold text-accent">{title}</h3>`
    - קו הפרדה: `<div className="h-px w-16 mx-auto bg-accent/30 mt-2" />`

    גוף תוכן (div — px-6 pb-6 pt-2):
    - `<div className="font-body text-on-surface-variant text-sm leading-relaxed">{children}</div>`

    פס זהוב תחתון: `h-px w-full bg-gradient-to-r from-transparent via-accent to-transparent opacity-60`

    זוהר רקע עדין (pseudo — div מוחלט):
    - `<div className="absolute inset-0 pointer-events-none" style={{ background: 'radial-gradient(ellipse at 50% 0%, rgba(212,168,83,0.04) 0%, transparent 60%)' }} />`

    הכל עטוף ב-`dir="rtl"`.

    **שלב ב — ערוך mystiqor-build/src/app/(auth)/tools/numerology/page.tsx:**

    1. הוסף import: `import { MysticScroll } from '@/components/ui/mystic-scroll'`
    2. מצא את ה-RevealItem שמכיל את "פרשנות AI מותאמת אישית" (שורות ~354-371 בגרסה הנוכחית). מחק את ה-Card הרגיל עם CardHeader/CardTitle/CardContent.
    3. החלף ב:
    ```tsx
    <RevealItem>
      <MysticScroll title="סינתזה מיסטית">
        <div className="prose prose-invert prose-sm max-w-none result-heading-glow">
          <ReactMarkdown>{result.interpretation}</ReactMarkdown>
        </div>
      </MysticScroll>
    </RevealItem>
    ```
    4. הסר את ה-imports שכבר לא בשימוש מהסרת ה-Card (אם Card/CardHeader/CardContent/CardTitle כבר לא בשימוש בשאר הדף — השאר אותם אם עדיין בשימוש בטופס הקלט).

    **שלב ג — ערוך mystiqor-build/src/app/api/tools/numerology/route.ts:**

    שורה 76: שנה `maxTokens: 2000` → `maxTokens: 3000`
    זהו שינוי שורה בודד. אין לגעת בשום דבר אחר בקובץ.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | grep -E "mystic-scroll|numerology/page|numerology/route" || echo "No TypeScript errors in modified files"</automated>
  </verify>
  <done>
    - mystiqor-build/src/components/ui/mystic-scroll.tsx קיים ומייצא MysticScroll
    - סקציית "פרשנות AI" בדף הנומרולוגיה עטופה ב-MysticScroll ולא ב-Card רגיל
    - route.ts: maxTokens: 3000 (וגרסה: grep "3000" mystiqor-build/src/app/api/tools/numerology/route.ts מחזיר תוצאה)
    - build עובר: cd mystiqor-build && npm run build
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - NumberCard: אות עברית גדולה + ספירה + כוכב + 3 keywords pills על פני הכרטיס
    - MysticScroll: מגילה זהובה שעוטפת את סינתזת ה-AI
    - maxTokens 3000 ב-API
  </what-built>
  <how-to-verify>
    1. הפעל: cd mystiqor-build && npm run dev
    2. עבור אל http://localhost:3000/tools/numerology
    3. הזן שם + תאריך לידה ולחץ חשב
    4. בדוק כרטיסי המספרים: כל כרטיס מציג אות עברית גדולה (כמו "א", "ב"), ספירה ("ספירת כתר"), כוכב ("שמש"), ו-3 keywords כ-pills קטנים
    5. בדוק שלחיצה על כרטיס עדיין פותחת את הדיאלוג המפורט
    6. גלול למטה לסקציית "סינתזה מיסטית" — אמורה להופיע כמגילה עם בורדרים זהובים ולא כ-Card אפור רגיל
    7. אמת שהסינתזה מורחבת ועמוקה יותר (3000 tokens)
  </how-to-verify>
  <resume-signal>הקלד "approved" אם הכל נראה טוב, או תאר בעיות</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/tools/numerology | קלט fullName + birthDate — לא מהימן |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | maxTokens in route.ts | accept | ערך hardcoded בקוד שרת — לא נחשף ל-client, אין וקטור תקיפה |
| T-34-02 | Information Disclosure | AI synthesis content | accept | תוכן ה-AI מוצג למשתמש שיצר את הניתוח בלבד — RLS קיים מפאזות קודמות |
| T-34-03 | Denial of Service | maxTokens: 3000 | mitigate | checkUsageQuota קיים ב-route.ts מ-STAB-01 — מגביל מספר בקשות למשתמש |
</threat_model>

<verification>
בסיום Phase 34, הדרישות הבאות צריכות להיות מוגשמות:

NUM-01: כל אחד מ-5 כרטיסי הנומרולוגיה מציג על פניו — אות עברית גדולה, שם ספירה, כותרת מיסטית, לפחות 3 מילות מפתח כ-pills, וקשר כוכבי. הדיאלוג המפורט עדיין פועל.

NUM-02: סקציית הסינתזה עטופה ב-MysticScroll עם עיצוב מגילה מיסטי (גבולות זהובים, זוהר, כותרת עברית). maxTokens = 3000 ב-route.ts. ה-build עובר ללא שגיאות.

בדיקות build:
```bash
cd mystiqor-build && npm run build
```
</verification>

<success_criteria>
- [ ] npm run build עובר ללא TypeScript errors
- [ ] כרטיס נומרולוגיה מציג: אות עברית (text-3xl, text-accent), ספירה, כוכב, 3 keywords pills
- [ ] לחיצה על כרטיס פותחת את הדיאלוג המפורט הקיים — ללא שינוי
- [ ] סקציית AI synthesis עטופה ב-MysticScroll (לא Card רגיל) — גבולות זהובים גלויים
- [ ] route.ts: maxTokens: 3000
- [ ] אין regressions — יתר הכלים עובדים, CompatibilityCard לא נגעים בה
</success_criteria>

<output>
לאחר סיום הביצוע, צור `.planning/phases/34-rich-numerology-display/34-01-SUMMARY.md`
</output>
