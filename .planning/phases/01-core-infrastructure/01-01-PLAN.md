---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/setup.ts
  - tests/services/numerology.test.ts
  - tests/services/astrology.test.ts
  - tests/services/rule-engine.test.ts
  - supabase/migrations/001_schema.sql
  - supabase/migrations/002_functions.sql
autonomous: true
requirements: [INFRA-01, INFRA-04]
must_haves:
  truths:
    - "npx vitest run tests/services/ --reporter=verbose completes in < 20s with test files present"
    - "npx tsc --noEmit passes 0 errors after package installs"
    - "Supabase migration files contain all 20 tables and all 3 DB functions (increment_usage, reset_monthly_usage, calculate_profile_completion)"
    - "vitest.config.ts resolves @/ path aliases matching tsconfig.json paths"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test framework config with path aliases"
      contains: "resolve: { alias: { '@/': resolve(__dirname, './src/') } }"
    - path: "tests/setup.ts"
      provides: "Shared test setup"
      contains: "vi.mock"
    - path: "supabase/migrations/001_schema.sql"
      provides: "All 20 DB tables with RLS"
      contains: "CREATE TABLE profiles"
    - path: "supabase/migrations/002_functions.sql"
      provides: "3 PostgreSQL functions"
      contains: "increment_usage"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias resolution"
      pattern: "alias.*@/"
    - from: "supabase/migrations/002_functions.sql"
      to: "subscriptions table"
      via: "SQL function body"
      pattern: "FROM subscriptions WHERE user_id"
---

<objective>
Bootstrap the test infrastructure and database migration layer that every subsequent plan depends on.

Purpose: Wave 1 must establish the DB schema (so Wave 4+ hooks can actually query Supabase) and the vitest config (so all subsequent plans can write `npx vitest run` as their verify command). Without these, no other plan can verify its work.

Output: vitest.config.ts, tests/setup.ts, 3 test scaffold files, 2 SQL migration files, and openai+resend packages installed.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OWNER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-infrastructure/01-VALIDATION.md
@src/types/database.ts
@03_ARCHITECTURE.md
</context>

<interfaces>
<!-- DB types needed by migration validator -->
From src/types/database.ts (lines 1-120):
All 20 tables are manually defined as TypeScript types. The SQL migrations must produce tables whose columns EXACTLY match these types.

Key table structures (from 03_ARCHITECTURE.md §2.2):
- profiles: id UUID PK REFERENCES auth.users, full_name TEXT NOT NULL, birth_date DATE NOT NULL, ...
- subscriptions: id UUID PK, user_id UUID UNIQUE, plan_type TEXT DEFAULT 'free', analyses_limit INTEGER DEFAULT 3, analyses_used INTEGER DEFAULT 0
- analyses: id UUID PK, user_id UUID, tool_type TEXT (18 values), input_data JSONB, results JSONB

DB functions (from 03_ARCHITECTURE.md §2.3):
- increment_usage(p_user_id UUID) RETURNS JSONB — atomically increments analyses_used, raises exception on limit
- reset_monthly_usage() RETURNS void — resets used counters for subscriptions past their reset date
- calculate_profile_completion(p_user_id UUID) RETURNS INTEGER — returns 0-100 score
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install missing packages and configure vitest</name>
  <read_first>
    - D:/AI_projects/mystiqor-build/package.json (see current deps to add to, not replace)
    - D:/AI_projects/mystiqor-build/tsconfig.json (need paths section for alias config)
  </read_first>
  <files>package.json, vitest.config.ts, tests/setup.ts</files>
  <action>
Run these installs from the project root:
```bash
npm install openai@4
npm install resend@4
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/user-event jsdom
```

Create `vitest.config.ts` at project root:
```typescript
/**
 * הגדרת vitest — מסגרת בדיקות עם תמיכה ב-path aliases ו-JSX
 */
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import { resolve } from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
    include: ['tests/**/*.test.{ts,tsx}'],
    exclude: ['node_modules', '.next'],
  },
  resolve: {
    alias: {
      '@/': resolve(__dirname, './src/'),
    },
  },
})
```

Create `tests/setup.ts`:
```typescript
/**
 * הגדרת סביבת בדיקות — mocks גלובליים ו-cleanup
 */
import '@testing-library/jest-dom'
import { vi } from 'vitest'

// Mock Supabase browser client — בדיקות שירותים לא צריכות DB אמיתי
vi.mock('@/lib/supabase/client', () => ({
  createBrowserClient: vi.fn(() => ({
    from: vi.fn(() => ({
      select: vi.fn().mockResolvedValue({ data: [], error: null }),
      insert: vi.fn().mockResolvedValue({ data: null, error: null }),
      update: vi.fn().mockResolvedValue({ data: null, error: null }),
    })),
    auth: {
      getUser: vi.fn().mockResolvedValue({ data: { user: null }, error: null }),
    },
  })),
}))

// Mock Next.js router
vi.mock('next/navigation', () => ({
  useRouter: vi.fn(() => ({ push: vi.fn(), replace: vi.fn() })),
  usePathname: vi.fn(() => '/'),
  useSearchParams: vi.fn(() => new URLSearchParams()),
}))
```

Also create the `tests/services/` and `tests/hooks/` and `tests/components/` directories — create a `.gitkeep` or the actual test files below will create them.
  </action>
  <verify>
    <automated>cd D:/AI_projects/mystiqor-build && node -e "require('./node_modules/openai')" && node -e "require('./node_modules/resend')" && echo "packages OK"</automated>
  </verify>
  <done>openai and resend in node_modules; vitest.config.ts exists with @/ alias; tests/setup.ts exists with vi.mock calls; npx tsc --noEmit still passes 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds for services</name>
  <read_first>
    - D:/AI_projects/mystiqor-build/02b_GEMS.md (GEM 2 gematria values, GEM 3 rule engine operators, GEM 14 aspect definitions)
    - D:/AI_projects/mystiqor-build/.planning/phases/01-core-infrastructure/01-VALIDATION.md (exact test IDs and commands)
  </read_first>
  <files>
    tests/services/numerology.test.ts,
    tests/services/astrology.test.ts,
    tests/services/rule-engine.test.ts
  </files>
  <action>
Create `tests/services/numerology.test.ts`:
```typescript
/**
 * בדיקות שירות נומרולוגיה — גימטריה, צמצום, ומסלול חיים
 * מכסה: INFRA-01 (gematria + master numbers)
 */
import { describe, it, expect } from 'vitest'
import { calculateGematria, cleanHebrewText } from '@/services/numerology/gematria'
import { reduceToSingleDigit, calculateLifePath } from '@/services/numerology/calculations'

describe('cleanHebrewText', () => {
  it('מסיר ניקוד מטקסט עברי', () => {
    // ניקוד unicode range u0591-u05C7
    expect(cleanHebrewText('שָׁלוֹם')).toBe('שלום')
  })
  it('מסיר רווחים', () => {
    expect(cleanHebrewText('דוד  לוי')).toBe('דודלוי')
  })
  it('מחזיר מחרוזת ריקה עבור קלט ריק', () => {
    expect(cleanHebrewText('')).toBe('')
  })
})

describe('calculateGematria', () => {
  it('מחשב גימטריה נכונה — שלום = 376', () => {
    // ש=300, ל=30, ו=6, ם=40 = 376
    expect(calculateGematria('שלום')).toBe(376)
  })
  it('מתעלם מתווים שאינם עבריים', () => {
    expect(calculateGematria('abc')).toBe(0)
  })
})

describe('reduceToSingleDigit', () => {
  it('מצמצם מספר רב-ספרתי לספרה בודדת', () => {
    expect(reduceToSingleDigit(29)).toBe(2) // 2+9=11, 1+1=2
  })
  it('שומר על master numbers — 11', () => {
    expect(reduceToSingleDigit(11)).toBe(11)
  })
  it('שומר על master numbers — 22', () => {
    expect(reduceToSingleDigit(22)).toBe(22)
  })
  it('שומר על master numbers — 33', () => {
    expect(reduceToSingleDigit(33)).toBe(33)
  })
  it('מחזיר ספרה בודדת כפי שהיא', () => {
    expect(reduceToSingleDigit(7)).toBe(7)
  })
})

describe('calculateLifePath', () => {
  it('מחשב מסלול חיים נכון', () => {
    // תאריך 1990-05-15: d=15→6, m=5, y=1990→1+9+9+0=19→10→1 → 6+5+1=12→3
    expect(calculateLifePath('1990-05-15')).toBe(3)
  })
})
```

Create `tests/services/astrology.test.ts`:
```typescript
/**
 * בדיקות שירות אסטרולוגיה — חישוב אספקטים
 * מכסה: INFRA-01 (aspect calculation)
 */
import { describe, it, expect } from 'vitest'
import { calculateAspects } from '@/services/astrology/aspects'

describe('calculateAspects', () => {
  it('מזהה conjunction בין כוכבי לכת סמוכים', () => {
    const planets = { sun: { longitude: 10 }, moon: { longitude: 12 } }
    const aspects = calculateAspects(planets)
    expect(aspects.some(a => a.type === 'Conjunction')).toBe(true)
  })

  it('מזהה opposition בזווית 180°', () => {
    const planets = { sun: { longitude: 0 }, moon: { longitude: 180 } }
    const aspects = calculateAspects(planets)
    expect(aspects.some(a => a.type === 'Opposition')).toBe(true)
  })

  it('מחשב strength כאחוז הפוך של orb', () => {
    // orb=0 → strength=1.0 (conjunction מושלם)
    const planets = { sun: { longitude: 0 }, moon: { longitude: 0 } }
    const aspects = calculateAspects(planets)
    const conj = aspects.find(a => a.type === 'Conjunction')
    expect(conj?.strength).toBe(1)
  })

  it('לא מחזיר aspects מחוץ לטווח ה-orb', () => {
    // 45° — לא אחד מ-5 האספקטים המוגדרים
    const planets = { sun: { longitude: 0 }, moon: { longitude: 45 } }
    const aspects = calculateAspects(planets)
    expect(aspects).toHaveLength(0)
  })
})
```

Create `tests/services/rule-engine.test.ts`:
```typescript
/**
 * בדיקות מנוע כללים — כל אופרטורים
 * מכסה: INFRA-01 (evaluateCondition operators)
 */
import { describe, it, expect } from 'vitest'
import { evaluateCondition } from '@/services/analysis/rule-engine'

describe('evaluateCondition', () => {
  it('equals — מספרים שווים', () => {
    expect(evaluateCondition(5, 'equals', 5)).toBe(true)
  })
  it('equals — מחרוזות שוות', () => {
    expect(evaluateCondition('Aries', 'equals', 'Aries')).toBe(true)
  })
  it('not_equals — ערכים שונים', () => {
    expect(evaluateCondition(5, 'not_equals', 3)).toBe(true)
  })
  it('greater_than', () => {
    expect(evaluateCondition(10, 'greater_than', 5)).toBe(true)
  })
  it('less_than', () => {
    expect(evaluateCondition(3, 'less_than', 5)).toBe(true)
  })
  it('greater_or_equal — שווה', () => {
    expect(evaluateCondition(5, 'greater_or_equal', 5)).toBe(true)
  })
  it('contains', () => {
    expect(evaluateCondition('Aries Sun', 'contains', 'Aries')).toBe(true)
  })
  it('in — ערך ברשימה', () => {
    expect(evaluateCondition('Aries', 'in', ['Aries', 'Leo', 'Sagittarius'])).toBe(true)
  })
  it('in — ערך לא ברשימה', () => {
    expect(evaluateCondition('Taurus', 'in', ['Aries', 'Leo'])).toBe(false)
  })
  it('אופרטור לא מוכר מחזיר false', () => {
    // @ts-expect-error testing invalid operator
    expect(evaluateCondition(5, 'unknown_op', 5)).toBe(false)
  })
})
```
  </action>
  <verify>
    <automated>cd D:/AI_projects/mystiqor-build && npx vitest run tests/services/ --reporter=verbose 2>&1 | grep -E "(FAIL|ERROR|cannot find|Cannot find)" || echo "Test files parse OK (services not built yet — SKIP errors expected)"</automated>
  </verify>
  <done>3 test scaffold files exist in tests/services/; each file has describe/it blocks matching the behaviors above; files are valid TypeScript (no syntax errors)</done>
</task>

<task type="auto">
  <name>Task 3: Create Supabase DB migration files</name>
  <read_first>
    - D:/AI_projects/mystiqor-build/03_ARCHITECTURE.md (§2.2 table definitions, §2.3 functions — exact SQL)
    - D:/AI_projects/mystiqor-build/src/types/database.ts (column names must match)
  </read_first>
  <files>
    supabase/migrations/001_schema.sql,
    supabase/migrations/002_functions.sql
  </files>
  <action>
Create `supabase/migrations/001_schema.sql` containing ALL table CREATE statements and RLS policies from `03_ARCHITECTURE.md §2.2`.

Include tables in dependency order (no FK violations):
1. profiles (references auth.users)
2. subscriptions (references auth.users)
3. analyses (references auth.users)
4. analysis_features (references analyses)
5. goals (references auth.users)
6. mood_entries (references auth.users)
7. journal_entries (references auth.users)
8. daily_insights (references auth.users)
9. dreams (references auth.users)
10. coaching_journeys (references auth.users, goals)
11. coaching_messages (references auth.users)
12. reminders (references auth.users)
13. payment_history (references auth.users)
14. guest_profiles (references auth.users)
15. learning_progress (references auth.users)
16. referrals (references auth.users)
17. analytics_events (references auth.users)
18. rulebook (no FK — system table)
19. tarot_cards (no FK — system table)
20. blog_posts (no FK — system table)

For analytics_events table (referenced in types but not in architecture doc), use:
```sql
CREATE TABLE analytics_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  event_type TEXT NOT NULL,
  event_data JSONB DEFAULT '{}',
  page_path TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE analytics_events ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users insert own events" ON analytics_events FOR INSERT WITH CHECK (auth.uid() = user_id);
```

Each table from §2.2 must be copied EXACTLY — same column names, same CHECK constraints, same DEFAULT values. Do NOT change snake_case column names.

Add a comment header at top:
```sql
-- MystiQor — Initial Schema
-- Generated: 2026-03-20
-- Tables: 20
-- Apply with: supabase db push OR psql -f this file
```

Create `supabase/migrations/002_functions.sql` containing the 3 PostgreSQL functions from `03_ARCHITECTURE.md §2.3`:
1. `reset_monthly_usage()` — exact copy from §2.3
2. `increment_usage(p_user_id UUID) RETURNS JSONB` — exact copy from §2.3
3. `calculate_profile_completion(p_user_id UUID) RETURNS INTEGER` — exact copy from §2.3

Add comment header:
```sql
-- MystiQor — DB Helper Functions
-- increment_usage: atomic usage counter with limit enforcement
-- reset_monthly_usage: cron-triggered monthly reset
-- calculate_profile_completion: score calculator (0-100)
```
  </action>
  <verify>
    <automated>cd D:/AI_projects/mystiqor-build && grep -c "CREATE TABLE" supabase/migrations/001_schema.sql && grep "increment_usage" supabase/migrations/002_functions.sql && echo "migrations OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE" supabase/migrations/001_schema.sql` returns 20
    - `grep "ROW LEVEL SECURITY" supabase/migrations/001_schema.sql | wc -l` returns >= 16 (system tables exempt)
    - `grep "increment_usage" supabase/migrations/002_functions.sql` finds the function definition
    - `grep "reset_monthly_usage" supabase/migrations/002_functions.sql` finds the function definition
    - `grep "calculate_profile_completion" supabase/migrations/002_functions.sql` finds the function definition
    - File supabase/migrations/001_schema.sql exists and is > 200 lines
    - File supabase/migrations/002_functions.sql exists and is > 40 lines
  </acceptance_criteria>
  <done>Two SQL migration files in supabase/migrations/ with all 20 tables, RLS policies, and 3 DB functions matching 03_ARCHITECTURE.md §2.2 and §2.3 exactly</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `cd D:/AI_projects/mystiqor-build && npx tsc --noEmit` — must be 0 errors
2. `ls node_modules/openai && ls node_modules/resend` — both must exist
3. `cat vitest.config.ts | grep "@/"` — alias must be present
4. `grep -c "CREATE TABLE" supabase/migrations/001_schema.sql` — must output 20
5. `grep "increment_usage" supabase/migrations/002_functions.sql` — must find the function
</verification>

<success_criteria>
- vitest is installed and configured with @/ path aliases
- openai@4 and resend@4 are in node_modules
- 3 test scaffold files exist in tests/services/ with valid TypeScript
- supabase/migrations/001_schema.sql has 20 CREATE TABLE statements with RLS
- supabase/migrations/002_functions.sql has all 3 DB functions
- tsc --noEmit passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
