---
phase: 01-infrastructure-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/services/geocode.ts
  - mystiqor-build/src/app/api/geocode/route.ts
autonomous: true
requirements: [INFRA-06]

must_haves:
  truths:
    - "Geocoding results include IANA timezone name resolved from coordinates"
    - "Repeated geocode queries for the same city hit the cache instead of Nominatim"
    - "A Nominatim request that hangs for >5 seconds is aborted with a timeout error"
  artifacts:
    - path: "mystiqor-build/src/services/geocode.ts"
      provides: "Geocoding service with cache, timeout, and timezone lookup"
      contains: "timezone_name"
    - path: "mystiqor-build/src/app/api/geocode/route.ts"
      provides: "Geocoding API route returning timezone_name in results"
      contains: "timezone_name"
  key_links:
    - from: "mystiqor-build/src/services/geocode.ts"
      to: "@photostructure/tz-lookup"
      via: "import tzlookup"
      pattern: "tzlookup"
    - from: "mystiqor-build/src/services/geocode.ts"
      to: "Nominatim API"
      via: "fetch with AbortController"
      pattern: "AbortController"
---

<objective>
Add timezone resolution, in-memory caching, and request timeout handling to the geocoding service. After this plan, every geocoded birth place returns an IANA timezone name (e.g., 'Asia/Jerusalem') alongside coordinates, cached results reduce Nominatim API load, and slow requests are aborted after 5 seconds.

Purpose: Astrology calculations require accurate timezone data for birth charts. Nominatim does NOT return timezone natively — it must be resolved from coordinates using tz-lookup. Caching prevents hitting Nominatim's rate limits. Timeout prevents indefinite hangs.
Output: Enhanced geocode service and API route with timezone, cache, and timeout.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OWNER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mystiqor-build/src/services/geocode.ts
@mystiqor-build/src/app/api/geocode/route.ts

<interfaces>
<!-- Current GeocodingResult — timezone_name will be added -->
From mystiqor-build/src/services/geocode.ts:
```typescript
export interface GeocodingResult {
  display_name: string
  lat: number
  lon: number
  country_code: string
}
```

<!-- Current API route — simple pass-through -->
From mystiqor-build/src/app/api/geocode/route.ts:
```typescript
export async function GET(request: NextRequest) {
  const q = request.nextUrl.searchParams.get('q');
  const results = await geocodeCity(q);
  return NextResponse.json({ results });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance geocode service with timezone, cache, and timeout</name>
  <files>mystiqor-build/src/services/geocode.ts</files>
  <read_first>
    mystiqor-build/src/services/geocode.ts
  </read_first>
  <action>
Rewrite `mystiqor-build/src/services/geocode.ts` preserving the existing interface contract but adding 3 features:

```typescript
/**
 * שירות גיאוקודינג — ממיר שם עיר לקואורדינטות + אזור זמן
 * משתמש ב-Nominatim (OpenStreetMap) — חינמי, תומך בעברית
 * מוסיף: tz-lookup לאזור זמן, cache בזיכרון, timeout של 5 שניות
 * שירות זה הוא server-side only — לא לייבא בקומפוננטות לקוח
 */

import tzlookup from '@photostructure/tz-lookup'

/** תוצאת גיאוקודינג לעיר אחת — כולל אזור זמן IANA */
export interface GeocodingResult {
  /** שם מקום מלא כפי שמוחזר מ-Nominatim */
  display_name: string
  /** קו רוחב — ערך עשרוני */
  lat: number
  /** קו אורך — ערך עשרוני */
  lon: number
  /** קוד מדינה ISO דו-ספרתי (il, us, gb וכו') */
  country_code: string
  /** אזור זמן IANA — למשל 'Asia/Jerusalem', 'America/New_York' */
  timezone_name: string
}

/** מבנה תשובה גולמי מ-Nominatim API */
interface NominatimResult {
  display_name: string
  lat: string
  lon: string
  address: {
    country_code?: string
  }
}

// =========================================
// Cache בזיכרון — Map עם TTL של שעה
// =========================================

/** ערך מאוחסן ב-cache עם זמן תפוגה */
interface CacheEntry {
  results: GeocodingResult[]
  expiresAt: number
}

/** cache פנימי — מפתח הוא שם עיר מנורמל */
const geocodeCache = new Map<string, CacheEntry>()

/** זמן חיים של ערך ב-cache: שעה אחת */
const CACHE_TTL_MS = 60 * 60 * 1000

/** timeout לבקשת Nominatim: 5 שניות */
const REQUEST_TIMEOUT_MS = 5000

/**
 * מנקה ערכים שפג תוקפם מה-cache
 * נקרא באופן עצלני — רק כשמתבצע חיפוש חדש
 */
function pruneExpiredEntries(): void {
  const now = Date.now()
  for (const [key, entry] of geocodeCache) {
    if (entry.expiresAt <= now) {
      geocodeCache.delete(key)
    }
  }
}

/**
 * מחפש עיר בשמה ומחזיר עד 5 תוצאות עם קואורדינטות ואזור זמן
 * תומך בחיפוש בעברית ובאנגלית
 * כולל: cache בזיכרון, timeout של 5 שניות, lookup אזור זמן מקואורדינטות
 *
 * @param cityName - שם העיר לחיפוש (עברית או אנגלית)
 * @returns מערך של עד 5 תוצאות גיאוקודינג כולל timezone_name
 * @throws Error אם הבקשה לנומינטים נכשלה או עברה timeout
 */
export async function geocodeCity(cityName: string): Promise<GeocodingResult[]> {
  const trimmed = cityName.trim()
  if (!trimmed) {
    return []
  }

  // נרמול מפתח cache — lowercase ללא רווחים כפולים
  const cacheKey = trimmed.toLowerCase().replace(/\s+/g, ' ')

  // בדיקת cache — מחזיר ישירות אם יש ערך בתוקף
  const cached = geocodeCache.get(cacheKey)
  if (cached && cached.expiresAt > Date.now()) {
    return cached.results
  }

  // ניקוי עצלני של ערכים שפגו
  if (geocodeCache.size > 100) {
    pruneExpiredEntries()
  }

  // שליחת בקשה ל-Nominatim עם timeout של 5 שניות
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), REQUEST_TIMEOUT_MS)

  try {
    const encoded = encodeURIComponent(trimmed)
    const url = `https://nominatim.openstreetmap.org/search?q=${encoded}&format=json&limit=5&addressdetails=1`

    const response = await fetch(url, {
      headers: {
        'User-Agent': 'MystiQor/1.0 (contact@mystiqor.com)',
        'Accept-Language': 'he,en',
      },
      signal: controller.signal,
    })

    if (!response.ok) {
      throw new Error(`שגיאת גיאוקודינג: ${response.status} ${response.statusText}`)
    }

    const data = (await response.json()) as NominatimResult[]

    const results: GeocodingResult[] = data.map((item) => {
      const lat = parseFloat(item.lat)
      const lon = parseFloat(item.lon)

      // tz-lookup — ממיר קואורדינטות לאזור זמן IANA
      let timezone_name = 'Asia/Jerusalem' // ברירת מחדל לישראל
      try {
        timezone_name = tzlookup(lat, lon)
      } catch {
        // אם tz-lookup נכשל (נקודה בים וכו') — משאיר ברירת מחדל
        console.warn(`[Geocode] tz-lookup failed for ${lat},${lon} — defaulting to Asia/Jerusalem`)
      }

      return {
        display_name: item.display_name,
        lat,
        lon,
        country_code: item.address?.country_code ?? 'il',
        timezone_name,
      }
    })

    // שמירה ב-cache עם TTL של שעה
    geocodeCache.set(cacheKey, {
      results,
      expiresAt: Date.now() + CACHE_TTL_MS,
    })

    return results
  } catch (error) {
    if (error instanceof Error && error.name === 'AbortError') {
      throw new Error('שגיאת גיאוקודינג: הבקשה חרגה מזמן המתנה (5 שניות)')
    }
    throw error
  } finally {
    clearTimeout(timeoutId)
  }
}
```

Key changes from original:
1. Added `timezone_name: string` to `GeocodingResult` interface
2. Added `@photostructure/tz-lookup` import and call for each result
3. Added in-memory Map cache with 1-hour TTL and lazy pruning
4. Added 5-second AbortController timeout
5. Changed User-Agent to 'MystiQor/1.0'
6. Added try/catch around tzlookup call (fails for ocean coordinates)
7. Preserved existing function signature and return type contract
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -c "timezone_name" src/services/geocode.ts && grep -c "tzlookup" src/services/geocode.ts && grep -c "AbortController" src/services/geocode.ts && grep -c "geocodeCache" src/services/geocode.ts && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains "timezone_name: string" in GeocodingResult interface
    - File contains "import tzlookup from '@photostructure/tz-lookup'"
    - File contains "tzlookup(lat, lon)" (timezone resolution call)
    - File contains "const geocodeCache = new Map" (cache implementation)
    - File contains "CACHE_TTL_MS = 60 * 60 * 1000" (1-hour TTL)
    - File contains "new AbortController()" (timeout mechanism)
    - File contains "REQUEST_TIMEOUT_MS = 5000" (5-second timeout)
    - File contains "controller.abort()" (abort on timeout)
    - File contains "'AbortError'" (timeout error handling)
    - File contains "pruneExpiredEntries" (cache cleanup)
    - npx tsc --noEmit passes with no errors
  </acceptance_criteria>
  <done>Geocode service returns IANA timezone, caches results for 1 hour, and aborts slow requests after 5 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Update geocode API route for timezone_name in response</name>
  <files>mystiqor-build/src/app/api/geocode/route.ts</files>
  <read_first>
    mystiqor-build/src/app/api/geocode/route.ts,
    mystiqor-build/src/services/geocode.ts
  </read_first>
  <action>
Update `mystiqor-build/src/app/api/geocode/route.ts` to handle the timeout error specifically and add a minimum query length of 2 characters. The route itself is minimal — the service does the heavy lifting. But ensure the timeout error returns a user-friendly message.

```typescript
/**
 * API Route: Geocoding — חיפוש מיקום דרך Nominatim
 * GET /api/geocode?q=תל אביב
 * מחזיר עד 5 תוצאות כולל אזור זמן IANA
 */
import { NextResponse, type NextRequest } from 'next/server'
import { geocodeCity } from '@/services/geocode'

export async function GET(request: NextRequest) {
  const q = request.nextUrl.searchParams.get('q')
  if (!q || q.trim().length < 2) {
    return NextResponse.json({ results: [] })
  }

  try {
    const results = await geocodeCity(q)
    return NextResponse.json({ results })
  } catch (error) {
    const message = error instanceof Error ? error.message : 'שגיאה בחיפוש מיקום'
    const isTimeout = message.includes('זמן המתנה')
    return NextResponse.json(
      { results: [], error: isTimeout ? 'חיפוש המיקום ארך זמן רב מדי. נסה שוב.' : 'שגיאה בחיפוש מיקום' },
      { status: isTimeout ? 504 : 500 }
    )
  }
}
```

Changes from original:
1. Added `.trim()` before length check
2. Improved error handling — detects timeout vs generic error
3. Returns 504 Gateway Timeout for timeout errors (specific)
4. Returns 500 for other errors (generic)
5. Hebrew error messages preserved
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -c "timezone_name\|זמן המתנה\|504" src/app/api/geocode/route.ts && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains "q.trim().length < 2" (trimmed length check)
    - File contains "504" (timeout-specific status code)
    - File contains "זמן המתנה" (timeout error detection in Hebrew)
    - File contains "import { geocodeCity } from '@/services/geocode'" (correct import)
    - npx tsc --noEmit passes with no errors
  </acceptance_criteria>
  <done>Geocode API route handles timeout errors with 504 status and Hebrew message.</done>
</task>

</tasks>

<verification>
- geocode.ts returns GeocodingResult with timezone_name field
- geocode.ts uses tz-lookup to resolve timezone from coordinates
- geocode.ts caches results with 1-hour TTL
- geocode.ts aborts requests after 5 seconds
- geocode/route.ts returns 504 on timeout
- TypeScript compilation passes
</verification>

<success_criteria>
1. geocodeCity("תל אביב") returns results with timezone_name = "Asia/Jerusalem"
2. Calling geocodeCity twice with same input uses cache on second call
3. A hanging Nominatim request is aborted after 5 seconds with a Hebrew error message
4. API route returns 504 for timeouts, 500 for other errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-04-SUMMARY.md`
</output>
