---
phase: 01-infrastructure-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/api/upload/route.ts
  - mystiqor-build/src/app/api/upload/presign/route.ts
  - mystiqor-build/src/lib/utils/file-validation.ts
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "A 5MB image uploaded via presigned URL reaches Supabase Storage without hitting Vercel body limit"
    - "Upload route validates file type by magic bytes — not just MIME header"
    - "Uploaded images have EXIF metadata stripped before storage"
    - "Presign route requires authentication and validates file metadata"
  artifacts:
    - path: "mystiqor-build/src/app/api/upload/presign/route.ts"
      provides: "Presigned URL endpoint for client-direct uploads"
      contains: "createSignedUploadUrl"
    - path: "mystiqor-build/src/lib/utils/file-validation.ts"
      provides: "Magic-byte validation utility"
      contains: "MAGIC_BYTES"
    - path: "mystiqor-build/src/app/api/upload/route.ts"
      provides: "Hardened upload route with magic-byte + EXIF strip"
      contains: "validateMagicBytes"
  key_links:
    - from: "mystiqor-build/src/app/api/upload/route.ts"
      to: "mystiqor-build/src/lib/utils/file-validation.ts"
      via: "import { validateMagicBytes }"
      pattern: "validateMagicBytes"
    - from: "mystiqor-build/src/app/api/upload/presign/route.ts"
      to: "Supabase Storage"
      via: "supabase.storage.from('uploads').createSignedUploadUrl"
      pattern: "createSignedUploadUrl"
---

<objective>
Create a presigned upload URL endpoint that bypasses Vercel's 4.5MB body limit for client-direct uploads to Supabase Storage, and harden the existing upload route with magic-byte file validation and EXIF metadata stripping.

Purpose: The current upload route buffers the entire file body through Vercel's route handler, which will fail for images > 4.5MB in production. The presign flow lets the client upload directly to Supabase Storage. Magic-byte validation prevents file type spoofing. EXIF stripping protects user privacy (removes GPS coordinates from photos).
Output: Presign endpoint, file validation utility, hardened upload route.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OWNER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mystiqor-build/src/app/api/upload/route.ts
@mystiqor-build/src/lib/supabase/server.ts

<interfaces>
<!-- Current upload route — to be modified (score 44/50) -->
From mystiqor-build/src/app/api/upload/route.ts:
```typescript
const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'application/pdf'];
const MAX_SIZE = 10 * 1024 * 1024;

export async function POST(request: NextRequest) {
  // ... auth check, formData, file validation, upload to Supabase Storage
}
```

<!-- Supabase server client — DO NOT MODIFY -->
From mystiqor-build/src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file-validation utility with magic-byte checking</name>
  <files>mystiqor-build/src/lib/utils/file-validation.ts</files>
  <read_first>
    mystiqor-build/src/app/api/upload/route.ts
  </read_first>
  <action>
Create `mystiqor-build/src/lib/utils/file-validation.ts`:

```typescript
/**
 * ולידציית קבצים — בדיקת Magic Bytes ואימות סוג קובץ
 * Magic Bytes הם הבתים הראשונים של קובץ שמזהים את הפורמט האמיתי
 * אמין יותר מכותרת MIME שניתנת לזיוף
 */

/** חתימות Magic Bytes לפורמטים נתמכים — null = any byte */
const MAGIC_BYTES: Record<string, (number | null)[][]> = {
  'image/jpeg': [[0xFF, 0xD8, 0xFF]],
  'image/png': [[0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A]],
  'image/webp': [[0x52, 0x49, 0x46, 0x46, null, null, null, null, 0x57, 0x45, 0x42, 0x50]],
  'application/pdf': [[0x25, 0x50, 0x44, 0x46]],
}

/** סוגי קבצים מותרים להעלאה */
export const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'application/pdf'] as const
export type AllowedFileType = typeof ALLOWED_TYPES[number]

/** גודל מקסימלי: 10MB */
export const MAX_FILE_SIZE = 10 * 1024 * 1024

/**
 * מוודא שהקובץ תואם לסוג MIME המוצהר על פי Magic Bytes ראשוניים
 * קורא את 12 הבתים הראשונים ומשווה לחתימות ידועות
 *
 * @param buffer - ArrayBuffer של הקובץ (לפחות 12 בתים ראשונים)
 * @param declaredType - סוג ה-MIME שהמשתמש הצהיר עליו
 * @returns true אם ה-Magic Bytes תואמים לסוג המוצהר
 */
export function validateMagicBytes(buffer: ArrayBuffer, declaredType: string): boolean {
  const signatures = MAGIC_BYTES[declaredType]
  if (!signatures) return false

  const bytes = new Uint8Array(buffer.slice(0, 12))
  return signatures.some(sig =>
    sig.every((byte, i) => byte === null || bytes[i] === byte)
  )
}

/**
 * בודק אם סוג הקובץ נמצא ברשימת הסוגים המותרים
 *
 * @param type - סוג MIME של הקובץ
 * @returns true אם הסוג מותר
 */
export function isAllowedType(type: string): type is AllowedFileType {
  return (ALLOWED_TYPES as readonly string[]).includes(type)
}
```

All JSDoc in Hebrew. Export constants and functions for use by both upload routes.
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -c "MAGIC_BYTES" src/lib/utils/file-validation.ts && grep -c "validateMagicBytes" src/lib/utils/file-validation.ts && grep -c "0xFF, 0xD8" src/lib/utils/file-validation.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mystiqor-build/src/lib/utils/file-validation.ts
    - File contains "const MAGIC_BYTES" with signatures for jpeg, png, webp, pdf
    - File exports "validateMagicBytes" function
    - File exports "isAllowedType" function
    - File exports "ALLOWED_TYPES" constant
    - File exports "MAX_FILE_SIZE" constant set to 10 * 1024 * 1024
    - JPEG signature includes 0xFF, 0xD8, 0xFF
    - PNG signature includes 0x89, 0x50, 0x4E, 0x47
  </acceptance_criteria>
  <done>Magic-byte validation utility exists with signatures for all 4 allowed file types.</done>
</task>

<task type="auto">
  <name>Task 2: Create presign route + harden existing upload route</name>
  <files>
    mystiqor-build/src/app/api/upload/presign/route.ts,
    mystiqor-build/src/app/api/upload/route.ts
  </files>
  <read_first>
    mystiqor-build/src/app/api/upload/route.ts,
    mystiqor-build/src/lib/utils/file-validation.ts,
    mystiqor-build/src/lib/supabase/server.ts
  </read_first>
  <action>
**Part A: Create `mystiqor-build/src/app/api/upload/presign/route.ts`:**

```typescript
/**
 * API Route: יצירת URL חתום להעלאה ישירה ל-Supabase Storage
 * POST /api/upload/presign
 * עוקף את מגבלת 4.5MB של Vercel — הלקוח מעלה ישירות ל-Storage
 */
import { NextResponse, type NextRequest } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { z } from 'zod'
import { ALLOWED_TYPES, MAX_FILE_SIZE } from '@/lib/utils/file-validation'

/** סכמת ולידציה לבקשת presign */
const PresignRequestSchema = z.object({
  filename: z.string().min(1).max(255),
  contentType: z.enum(ALLOWED_TYPES),
  size: z.number().int().positive().max(MAX_FILE_SIZE),
})

export async function POST(request: NextRequest) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'לא מחובר' }, { status: 401 })
    }

    const body: unknown = await request.json()
    const parsed = PresignRequestSchema.safeParse(body)
    if (!parsed.success) {
      return NextResponse.json(
        { error: 'קלט לא תקין', details: parsed.error.flatten() },
        { status: 400 }
      )
    }

    const ext = parsed.data.filename.split('.').pop() ?? 'bin'
    const path = `${user.id}/${Date.now()}.${ext}`

    const { data, error } = await supabase.storage
      .from('uploads')
      .createSignedUploadUrl(path)

    if (error) {
      console.error('[Upload Presign] Storage error:', error.message)
      return NextResponse.json({ error: 'שגיאה ביצירת URL להעלאה' }, { status: 500 })
    }

    return NextResponse.json({
      signedUrl: data.signedUrl,
      token: data.token,
      path,
    })
  } catch {
    return NextResponse.json({ error: 'שגיאה פנימית' }, { status: 500 })
  }
}
```

**Part B: Update `mystiqor-build/src/app/api/upload/route.ts`** to add magic-byte validation and EXIF stripping:

```typescript
/**
 * API Route: העלאת קבצים — שומר ב-Supabase Storage
 * POST /api/upload (FormData with file field)
 * מבצע: אימות auth, בדיקת magic bytes, הסרת EXIF, העלאה ל-Storage
 * לקבצים גדולים (>4.5MB) — השתמש ב-/api/upload/presign במקום
 */
import { NextResponse, type NextRequest } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import sharp from 'sharp'
import { validateMagicBytes, isAllowedType, MAX_FILE_SIZE } from '@/lib/utils/file-validation'

export async function POST(request: NextRequest) {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: 'לא מחובר' }, { status: 401 })
    }

    const formData = await request.formData()
    const file = formData.get('file') as File | null

    if (!file) {
      return NextResponse.json({ error: 'לא נבחר קובץ' }, { status: 400 })
    }

    if (!isAllowedType(file.type)) {
      return NextResponse.json(
        { error: 'סוג קובץ לא נתמך. נתמכים: JPEG, PNG, WebP, PDF' },
        { status: 400 }
      )
    }

    if (file.size > MAX_FILE_SIZE) {
      return NextResponse.json({ error: 'קובץ גדול מדי. מקסימום: 10MB' }, { status: 400 })
    }

    // בדיקת Magic Bytes — מוודא שהקובץ באמת מהסוג שהוצהר
    const fileBuffer = await file.arrayBuffer()
    if (!validateMagicBytes(fileBuffer, file.type)) {
      return NextResponse.json(
        { error: 'תוכן הקובץ לא תואם לסוג שהוצהר' },
        { status: 400 }
      )
    }

    const ext = file.name.split('.').pop() ?? 'bin'
    const path = `${user.id}/${Date.now()}.${ext}`

    // הסרת EXIF מתמונות — מגן על פרטיות (מסיר קואורדינטות GPS)
    let uploadBuffer: Buffer | ArrayBuffer = fileBuffer
    if (file.type.startsWith('image/')) {
      uploadBuffer = await sharp(Buffer.from(fileBuffer)).toBuffer()
    }

    const { data, error } = await supabase.storage
      .from('uploads')
      .upload(path, uploadBuffer, { contentType: file.type, upsert: false })

    if (error) {
      console.error('[Upload] Storage error:', error.message)
      return NextResponse.json({ error: 'שגיאה בהעלאת הקובץ' }, { status: 500 })
    }

    const { data: urlData } = supabase.storage.from('uploads').getPublicUrl(data.path)

    return NextResponse.json({ url: urlData.publicUrl, path: data.path })
  } catch {
    return NextResponse.json({ error: 'שגיאה פנימית' }, { status: 500 })
  }
}
```

Key changes to upload/route.ts:
1. Import `sharp` for EXIF stripping
2. Import `validateMagicBytes`, `isAllowedType`, `MAX_FILE_SIZE` from file-validation utility
3. Remove inline `ALLOWED_TYPES` and `MAX_SIZE` constants (use shared utility)
4. Add magic-byte validation after reading arrayBuffer
5. Add EXIF stripping with `sharp(Buffer.from(fileBuffer)).toBuffer()` for image types
6. Upload the stripped buffer instead of the raw file
  </action>
  <verify>
    <automated>cd mystiqor-build && grep -c "validateMagicBytes" src/app/api/upload/route.ts && grep -c "createSignedUploadUrl" src/app/api/upload/presign/route.ts && grep -c "sharp" src/app/api/upload/route.ts && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - mystiqor-build/src/app/api/upload/presign/route.ts exists
    - Presign route contains "PresignRequestSchema" (Zod input validation)
    - Presign route contains "createSignedUploadUrl" (Supabase Storage call)
    - Presign route contains "auth.getUser()" (authentication check)
    - Presign route returns "signedUrl", "token", "path" in response
    - Upload route.ts contains "validateMagicBytes" (magic-byte check)
    - Upload route.ts contains "import sharp from 'sharp'" (EXIF stripping)
    - Upload route.ts contains "sharp(Buffer.from(fileBuffer)).toBuffer()" (EXIF strip call)
    - Upload route.ts does NOT contain "const ALLOWED_TYPES" (uses shared utility)
    - Upload route.ts does NOT contain "const MAX_SIZE" (uses shared MAX_FILE_SIZE)
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Presign endpoint created for client-direct upload. Existing upload route hardened with magic-byte validation and EXIF stripping.</done>
</task>

</tasks>

<verification>
- file-validation.ts exports validateMagicBytes with 4 file type signatures
- presign/route.ts validates auth + Zod input, returns signed URL
- upload/route.ts validates magic bytes before accepting file
- upload/route.ts strips EXIF from images using sharp before storage
- TypeScript compilation passes
</verification>

<success_criteria>
1. Presign endpoint returns signed URL for authenticated users with valid file metadata
2. Upload route rejects files whose magic bytes don't match declared MIME type
3. Uploaded images have EXIF stripped (no GPS coordinates in stored images)
4. Both routes return Hebrew error messages and correct HTTP status codes
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-03-SUMMARY.md`
</output>
