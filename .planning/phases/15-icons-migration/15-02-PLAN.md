---
phase: 15-icons-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/features/dashboard/StatCards.tsx
  - mystiqor-build/src/components/layouts/Header.tsx
autonomous: true
requirements:
  - ICON-02
  - ICON-03

must_haves:
  truths:
    - "StatCards מציג GiFaceToFace (מצב רוח), GiStarMedal (יעדים שהושלמו), GiAlarmClock (תזכורות)"
    - "Header מציג GiSparkles כלוגו מובייל (לא Sparkles של lucide)"
    - "npx tsc --noEmit עובר עם אפס שגיאות"
    - "אייקוני UI בהדר (Sun, Moon, Menu, User, LogOut, Settings) נשמרים כ-lucide-react"
  artifacts:
    - path: "mystiqor-build/src/components/features/dashboard/StatCards.tsx"
      provides: "3 GI icons replacing lucide in STAT_CARD_DEFINITIONS"
      contains: "GiFaceToFace"
    - path: "mystiqor-build/src/components/layouts/Header.tsx"
      provides: "GiSparkles logo icon replacing lucide Sparkles"
      contains: "GiSparkles"
  key_links:
    - from: "mystiqor-build/src/components/features/dashboard/StatCards.tsx"
      to: "react-icons/gi"
      via: "named import"
      pattern: "GiFaceToFace.*GiStarMedal.*GiAlarmClock"
---

<objective>
Migrate StatCards dashboard icons and the Header mobile logo icon from lucide-react to react-icons/gi, completing ICON-02 and ICON-03.

Purpose: Dashboard stat cards show generic lucide icons for mood, completed goals, and reminders. The Header mobile logo uses lucide Sparkles instead of GiSparkles. Both break visual consistency with the mystical theme.
Output: StatCards uses 4 GI icons (GiTargetArrows already done + 3 new), Header uses GiSparkles logo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- CONFIRMED AVAILABLE react-icons/gi icons (verified via node import check): -->
<!-- GiFaceToFace: true  → use for mood/SmilePlus replacement -->
<!-- GiStarMedal: true   → use for completed goals/CheckCircle replacement -->
<!-- GiAlarmClock: true  → use for reminders/Bell replacement -->
<!-- GiSparkles: already in project (LoadingSpinner, DailyInsightCard) -->

<!-- Current StatCards.tsx state (from codebase): -->
```typescript
// Line 8: import { SmilePlus, CheckCircle, Bell } from 'lucide-react';
// Line 9: import { GiTargetArrows } from 'react-icons/gi';
// Line 49: Icon: SmilePlus,   // currentMoodScore card
// Line 56: Icon: CheckCircle, // completedGoals card
// Line 63: Icon: Bell,        // pendingReminders card
// The STAT_CARD_DEFINITIONS array is typed `as const` — Icon type is inferred,
// NOT explicitly typed as LucideIcon. GI components are drop-in replacements.
```

<!-- Current Header.tsx state (from codebase): -->
```typescript
// Line 11: import { Sun, Moon, Menu, Sparkles, User, LogOut, Settings } from 'lucide-react';
// Line 89: <Sparkles className="h-5 w-5 text-primary" />  ← this is the mobile logo
// Sun, Moon, Menu, User, LogOut, Settings are UI controls — KEEP as lucide-react
```

<!-- Icon prop usage pattern (same as Sidebar — both accept className?: string): -->
```typescript
// react-icons/gi and lucide-react are drop-in replacements at JSX level:
<GiFaceToFace className="h-5 w-5 text-tertiary" />
// Renders identically to:
<SmilePlus className="h-5 w-5 text-tertiary" />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate StatCards — 3 lucide icons to GI equivalents</name>
  <files>
    mystiqor-build/src/components/features/dashboard/StatCards.tsx
  </files>
  <action>
    In `StatCards.tsx`, replace the 3 lucide thematic icons in STAT_CARD_DEFINITIONS:

    1. Change import line 8:
       - BEFORE: `import { SmilePlus, CheckCircle, Bell } from 'lucide-react';`
       - AFTER:  remove this line entirely (all three are being replaced)

    2. Change import line 9:
       - BEFORE: `import { GiTargetArrows } from 'react-icons/gi';`
       - AFTER:  `import { GiTargetArrows, GiFaceToFace, GiStarMedal, GiAlarmClock } from 'react-icons/gi';`

    3. In STAT_CARD_DEFINITIONS array:
       - `Icon: SmilePlus`   → `Icon: GiFaceToFace`   (ציון מצב רוח — פנים מול פנים)
       - `Icon: CheckCircle` → `Icon: GiStarMedal`     (יעדים שהושלמו — מדלית כוכב)
       - `Icon: Bell`        → `Icon: GiAlarmClock`    (תזכורות ממתינות — שעון אזעקה)

    Do NOT change the `as const` assertion, any formatting, or any other code.
    The Icon type is inferred in the array — no explicit type annotation needed.

    After change: `cd mystiqor-build && npx tsc --noEmit` must pass.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <done>
    - StatCards.tsx imports GiFaceToFace, GiStarMedal, GiAlarmClock from react-icons/gi
    - SmilePlus, CheckCircle, Bell lucide imports removed
    - All 4 STAT_CARD_DEFINITIONS entries use react-icons/gi icons
    - TypeScript compiles with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Header — Sparkles logo to GiSparkles</name>
  <files>
    mystiqor-build/src/components/layouts/Header.tsx
  </files>
  <action>
    In `Header.tsx`, replace only the logo Sparkles icon. All other icons (Sun, Moon, Menu, User, LogOut, Settings) are UI controls — keep them as lucide-react.

    1. Change import line 11:
       - BEFORE: `import { Sun, Moon, Menu, Sparkles, User, LogOut, Settings } from 'lucide-react';`
       - AFTER:  `import { Sun, Moon, Menu, User, LogOut, Settings } from 'lucide-react';`

    2. Add new import after the lucide line:
       - ADD: `import { GiSparkles } from 'react-icons/gi';`

    3. In the JSX (line ~89):
       - BEFORE: `<Sparkles className="h-5 w-5 text-primary" />`
       - AFTER:  `<GiSparkles className="h-5 w-5 text-primary" />`

    There is exactly ONE Sparkles usage in Header.tsx — the mobile logo. Confirm with a quick read before editing.
    Do NOT change Sun/Moon (theme toggle), Menu (mobile nav trigger), User/LogOut/Settings (user menu).

    After change: `cd mystiqor-build && npx tsc --noEmit` must pass.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Header.tsx imports GiSparkles from react-icons/gi
    - Sparkles removed from lucide import; Sun/Moon/Menu/User/LogOut/Settings kept as lucide
    - Mobile logo renders GiSparkles matching Sidebar logo
    - TypeScript compiles with zero errors
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd mystiqor-build && npx tsc --noEmit` — zero errors
2. `grep "GiFaceToFace\|GiStarMedal\|GiAlarmClock" mystiqor-build/src/components/features/dashboard/StatCards.tsx` — all 3 found
3. `grep "GiSparkles" mystiqor-build/src/components/layouts/Header.tsx` — found
4. `grep "SmilePlus\|CheckCircle.*lucide\|Bell.*lucide" mystiqor-build/src/components/features/dashboard/StatCards.tsx` — empty (no lucide thematic icons remain)
</verification>

<success_criteria>
Dashboard StatCards shows 4 react-icons/gi mystical icons across all stat cards. Header mobile logo shows GiSparkles matching the Sidebar logo. No regressions — all functional Header icons (Sun, Moon, Menu, User, LogOut, Settings) remain as lucide-react. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15-icons-migration/15-02-SUMMARY.md`
</output>
