---
phase: 02-auth-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/lib/validations/profile.ts
  - mystiqor-build/src/app/api/onboarding/complete/route.ts
  - mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03]

must_haves:
  truths:
    - "Onboarding completion validates all required fields server-side including Barnum consent"
    - "Onboarding completion creates a free subscription row alongside the profile upsert"
    - "OnboardingWizard calls the API route instead of writing directly to Supabase"
    - "Birth data from onboarding is stored in profiles table and available for all analysis tools"
    - "Completed onboarding redirects to /tools"
  artifacts:
    - path: "mystiqor-build/src/lib/validations/profile.ts"
      provides: "onboardingCompleteSchema with Barnum consent validation"
      exports: ["onboardingCompleteSchema", "OnboardingCompleteData"]
    - path: "mystiqor-build/src/app/api/onboarding/complete/route.ts"
      provides: "POST endpoint for onboarding completion (profile upsert + subscription creation)"
      exports: ["POST"]
    - path: "mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx"
      provides: "Wizard calling API route instead of direct Supabase write"
      contains: "api/onboarding/complete"
  key_links:
    - from: "mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx"
      to: "mystiqor-build/src/app/api/onboarding/complete/route.ts"
      via: "fetch POST with onboarding data"
      pattern: "fetch.*api/onboarding/complete"
    - from: "mystiqor-build/src/app/api/onboarding/complete/route.ts"
      to: "mystiqor-build/src/lib/validations/profile.ts"
      via: "onboardingCompleteSchema.safeParse"
      pattern: "onboardingCompleteSchema\\.safeParse"
    - from: "mystiqor-build/src/app/api/onboarding/complete/route.ts"
      to: "supabase.from('subscriptions').insert"
      via: "free subscription creation"
      pattern: "from\\('subscriptions'\\)\\.insert"
---

<objective>
Create the onboarding completion API route with server-side validation and free subscription creation, and update the OnboardingWizard to call it.

Purpose: The OnboardingWizard currently writes directly to Supabase from the browser, skipping server-side validation and not creating the required subscription row. Without a subscription row, the `increment_usage` DB function and SubscriptionGuard will fail on first tool use. This plan adds the Zod schema, API route, and wizard update.

Output: Server-validated onboarding completion that creates both the profile row and free subscription row atomically.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-onboarding/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mystiqor-build/src/types/database.generated.ts — profiles Insert:
```typescript
{
  id: string;                          // = user.id, required
  full_name: string;                   // required
  birth_date: string;                  // required, YYYY-MM-DD
  birth_time?: string | null;
  birth_place?: string | null;
  latitude?: number | null;
  longitude?: number | null;
  gender?: string | null;
  disciplines?: string[] | null;
  focus_areas?: string[] | null;
  ai_suggestions_enabled?: boolean | null;
  onboarding_completed?: boolean | null;
  timezone_name?: string | null;
  personal_goals?: string[] | null;
  profile_completion_score?: number | null;
}
```

From mystiqor-build/src/types/database.generated.ts — subscriptions Insert:
```typescript
{
  user_id: string;                     // required, = user.id
  id?: string;                         // optional, auto-generated
  plan_type?: string;                  // default exists, use 'free'
  status?: string;                     // default exists, use 'trial'
  analyses_limit?: number;             // use 3
  analyses_used?: number;              // use 0
  guest_profiles_limit?: number;       // use 1
  guest_profiles_used?: number;        // use 0
}
```

From mystiqor-build/src/lib/validations/profile.ts:
```typescript
const GENDER_VALUES = ['male', 'female', 'other', 'prefer_not_to_say'] as const;
const TIME_REGEX = /^([01]\d|2[0-3]):([0-5]\d)$/;
const DATE_REGEX = /^\d{4}-\d{2}-\d{2}$/;
export const profileSchema = z.object({ ... });
export type ProfileFormData = z.infer<typeof profileSchema>;
```

From mystiqor-build/src/stores/onboarding.ts — OnboardingData interface:
```typescript
interface OnboardingData {
  fullName: string;       // maps to full_name
  birthDate: string;      // maps to birth_date
  birthTime: string;      // maps to birth_time
  birthPlace: string;     // maps to birth_place
  latitude: number | null;
  longitude: number | null;
  gender: 'male' | 'female' | 'other' | 'prefer_not_to_say';
  disciplines: string[];
  focusAreas: string[];   // maps to focus_areas
  aiSuggestionsEnabled: boolean;  // maps to ai_suggestions_enabled
  acceptedBarnum: boolean;
  acceptedTerms: boolean;
}
```

From mystiqor-build/src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onboardingCompleteSchema to profile validations</name>
  <files>mystiqor-build/src/lib/validations/profile.ts</files>
  <read_first>
    - mystiqor-build/src/lib/validations/profile.ts
    - mystiqor-build/src/stores/onboarding.ts
  </read_first>
  <action>
Add a new Zod schema to the END of `mystiqor-build/src/lib/validations/profile.ts` (after the existing `onboardingStep4Schema` and its type export). Do NOT modify any existing schemas or types.

Add the following:

```typescript
// ===== סכמת השלמת Onboarding (API route) =====

/**
 * סכמת אימות להשלמת onboarding — נשלחת ל-API route
 * כוללת אימות הסכמה ל-Barnum ותנאי שימוש בצד השרת
 */
export const onboardingCompleteSchema = z.object({
  /** שם מלא - חובה, מינימום 2 תווים */
  full_name: z
    .string()
    .min(2, 'שם חייב להכיל לפחות 2 תווים')
    .max(100, 'שם לא יכול להכיל יותר מ-100 תווים'),

  /** תאריך לידה בפורמט YYYY-MM-DD - חובה */
  birth_date: z
    .string()
    .min(1, 'נא להזין תאריך לידה')
    .regex(DATE_REGEX, 'פורמט תאריך לא תקין (YYYY-MM-DD)')
    .refine(
      (date) => {
        const parsed = new Date(date);
        return !isNaN(parsed.getTime()) && parsed < new Date();
      },
      'תאריך לידה חייב להיות בעבר',
    ),

  /** שעת לידה בפורמט HH:mm - אופציונלי */
  birth_time: z
    .string()
    .regex(TIME_REGEX, 'פורמט שעה לא תקין (HH:mm)')
    .optional()
    .or(z.literal('')),

  /** מקום לידה - אופציונלי */
  birth_place: z
    .string()
    .max(200, 'מקום לידה לא יכול להכיל יותר מ-200 תווים')
    .optional()
    .or(z.literal('')),

  /** קו רוחב */
  latitude: z.number().nullable(),

  /** קו אורך */
  longitude: z.number().nullable(),

  /** מגדר - אופציונלי */
  gender: z
    .enum(GENDER_VALUES, { error: 'ערך מגדר לא תקין' })
    .optional(),

  /** דיסציפלינות מועדפות */
  disciplines: z.array(z.string()).default([]),

  /** תחומי עניין */
  focus_areas: z.array(z.string()).default([]),

  /** הצעות AI */
  ai_suggestions_enabled: z.boolean().default(true),

  /** אישור הבנת אפקט Barnum — חובה */
  accepted_barnum: z.literal(true, { error: 'יש לאשר הבנת הניתוחים' }),

  /** אישור תנאי שימוש — חובה */
  accepted_terms: z.literal(true, { error: 'יש לאשר תנאי השימוש' }),
});

/** טיפוס נתוני השלמת onboarding */
export type OnboardingCompleteData = z.infer<typeof onboardingCompleteSchema>;
```

Note: `DATE_REGEX`, `TIME_REGEX`, and `GENDER_VALUES` are already defined at the top of the file — reuse them. Do NOT redefine.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "onboardingCompleteSchema" mystiqor-build/src/lib/validations/profile.ts returns at least 2 matches (definition + type export)
    - grep "OnboardingCompleteData" mystiqor-build/src/lib/validations/profile.ts returns at least 1 match
    - grep "accepted_barnum" mystiqor-build/src/lib/validations/profile.ts returns at least 1 match
    - grep "accepted_terms" mystiqor-build/src/lib/validations/profile.ts returns at least 1 match
    - grep "z.literal(true" mystiqor-build/src/lib/validations/profile.ts returns at least 2 matches (barnum + terms)
    - Existing schemas (profileSchema, onboardingStep1Schema, onboardingStep2Schema, onboardingStep4Schema) are unchanged — grep "export const profileSchema" still returns 1 match
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>onboardingCompleteSchema exported from profile.ts with server-side Barnum consent validation; all existing schemas untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create onboarding completion API route + update OnboardingWizard to call it</name>
  <files>mystiqor-build/src/app/api/onboarding/complete/route.ts, mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx</files>
  <read_first>
    - mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx
    - mystiqor-build/src/lib/validations/profile.ts
    - mystiqor-build/src/lib/supabase/server.ts
    - mystiqor-build/src/stores/onboarding.ts
    - mystiqor-build/src/types/database.generated.ts (lines 665-723 for profiles, lines 836-898 for subscriptions)
  </read_first>
  <action>
**A. Create `mystiqor-build/src/app/api/onboarding/complete/route.ts`** — new file.

Create the directory `mystiqor-build/src/app/api/onboarding/complete/` if it does not exist.

```typescript
/**
 * API route להשלמת onboarding
 * מבצע: (1) אימות קלט עם Zod, (2) upsert לטבלת profiles, (3) יצירת שורת subscription חינמית
 */

import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
import { onboardingCompleteSchema } from '@/lib/validations/profile';

import type { NextRequest } from 'next/server';

/** ערכי ברירת מחדל למנוי חינמי */
const FREE_SUBSCRIPTION_DEFAULTS = {
  plan_type: 'free',
  status: 'trial',
  analyses_limit: 3,
  analyses_used: 0,
  guest_profiles_limit: 1,
  guest_profiles_used: 0,
} as const;

/**
 * POST /api/onboarding/complete
 * מקבל נתוני onboarding, מאמת עם Zod, שומר פרופיל ויוצר מנוי חינמי
 */
export async function POST(request: NextRequest) {
  try {
    const supabase = await createClient();

    // בדיקת אימות — חובה
    const {
      data: { user },
    } = await supabase.auth.getUser();

    if (!user) {
      return NextResponse.json(
        { error: 'לא מחובר למערכת' },
        { status: 401 }
      );
    }

    // אימות קלט עם Zod
    const body: unknown = await request.json();
    const parsed = onboardingCompleteSchema.safeParse(body);

    if (!parsed.success) {
      return NextResponse.json(
        { error: 'קלט לא תקין', details: parsed.error.flatten().fieldErrors },
        { status: 400 }
      );
    }

    const {
      accepted_barnum: _acceptedBarnum,
      accepted_terms: _acceptedTerms,
      ...profileData
    } = parsed.data;

    // 1. Upsert profile
    const { error: profileError } = await supabase.from('profiles').upsert({
      id: user.id,
      ...profileData,
      birth_time: profileData.birth_time || null,
      birth_place: profileData.birth_place || null,
      onboarding_completed: true,
    });

    if (profileError) {
      console.error('[onboarding/complete] Profile upsert failed:', profileError);
      return NextResponse.json(
        { error: 'שגיאה בשמירת הפרופיל' },
        { status: 500 }
      );
    }

    // 2. Create free subscription row (if not exists)
    const { data: existingSub } = await supabase
      .from('subscriptions')
      .select('id')
      .eq('user_id', user.id)
      .maybeSingle();

    if (!existingSub) {
      const { error: subError } = await supabase
        .from('subscriptions')
        .insert({
          user_id: user.id,
          ...FREE_SUBSCRIPTION_DEFAULTS,
        });

      if (subError) {
        console.error('[onboarding/complete] Subscription creation failed:', subError);
        // Profile was saved — subscription failure is non-fatal
        // Log but don't fail the request
      }
    }

    return NextResponse.json({ success: true });
  } catch (error) {
    console.error('[onboarding/complete] Unexpected error:', error);
    return NextResponse.json(
      { error: 'שגיאה בלתי צפויה' },
      { status: 500 }
    );
  }
}
```

Note: The subscriptions table uses `user_id` (not `id`) as the required foreign key to auth.users. The `id` column is auto-generated.

**B. Update `mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx`:**

Replace the `handleComplete` function body (lines 100-136). The function signature stays the same. The new implementation:

1. Remove `import { createClient } from '@/lib/supabase/client';` — no longer needed (the import may be used elsewhere in the file; check first. If only used in handleComplete, remove it).

2. Replace the body of `handleComplete`:

```typescript
const handleComplete = async () => {
  setIsLoading(true);
  try {
    const response = await fetch('/api/onboarding/complete', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        full_name: data.fullName,
        birth_date: data.birthDate,
        birth_time: data.birthTime || '',
        birth_place: data.birthPlace || '',
        latitude: data.latitude,
        longitude: data.longitude,
        gender: data.gender,
        disciplines: data.disciplines,
        focus_areas: data.focusAreas,
        ai_suggestions_enabled: data.aiSuggestionsEnabled,
        accepted_barnum: data.acceptedBarnum,
        accepted_terms: data.acceptedTerms,
      }),
    });

    if (!response.ok) {
      const errorData = await response.json().catch(() => null);
      throw new Error(errorData?.error ?? 'שגיאה בשמירת הפרופיל');
    }

    toast.success('ברוך הבא! הפרופיל נשמר בהצלחה');
    reset();
    router.push('/tools');
  } catch (error) {
    const message = error instanceof Error ? error.message : 'שגיאה בשמירת הפרופיל. נסה שוב';
    toast.error(message);
  } finally {
    setIsLoading(false);
  }
};
```

3. If `createClient` from `@/lib/supabase/client` is no longer used anywhere in the file after removing it from handleComplete, delete the import line. The wizard no longer talks to Supabase directly — only through the API route.

Preserve all existing imports, exports, re-exports (`BarnumEthicsStep`, `BarnumEthicsStepProps`), component structure, StepIndicator, and step rendering. Only the handleComplete function body and the unused import change.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File mystiqor-build/src/app/api/onboarding/complete/route.ts exists
    - grep "export async function POST" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "onboardingCompleteSchema" mystiqor-build/src/app/api/onboarding/complete/route.ts returns at least 1 match
    - grep "from('subscriptions').insert" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "from('profiles').upsert" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "user_id: user.id" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "plan_type: 'free'" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "analyses_limit: 3" mystiqor-build/src/app/api/onboarding/complete/route.ts returns 1 match
    - grep "api/onboarding/complete" mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx returns 1 match
    - grep "fetch('/api/onboarding/complete'" mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx returns 1 match OR grep "fetch.*api/onboarding/complete" returns 1 match
    - grep "supabase.from('profiles').upsert" mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx returns NO matches (direct Supabase write removed)
    - grep "accepted_barnum" mystiqor-build/src/components/features/onboarding/OnboardingWizard.tsx returns at least 1 match (sent in fetch body)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>OnboardingWizard calls /api/onboarding/complete instead of direct Supabase write; API route validates with Zod (including Barnum consent), upserts profile, and creates free subscription row; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd mystiqor-build && npx tsc --noEmit` — zero TypeScript errors
2. `grep "onboardingCompleteSchema" src/lib/validations/profile.ts` — exists
3. `grep "accepted_barnum.*z.literal(true" src/lib/validations/profile.ts` — server-side consent validation
4. File `src/app/api/onboarding/complete/route.ts` exists with POST handler
5. `grep "subscriptions" src/app/api/onboarding/complete/route.ts` — subscription creation
6. `grep "api/onboarding/complete" src/components/features/onboarding/OnboardingWizard.tsx` — wizard calls API
7. `grep "supabase.from('profiles')" src/components/features/onboarding/OnboardingWizard.tsx` — returns empty (direct write removed)
</verification>

<success_criteria>
- onboardingCompleteSchema validates full_name, birth_date, accepted_barnum (literal true), accepted_terms (literal true)
- POST /api/onboarding/complete upserts profile + creates free subscription row with analyses_limit=3
- OnboardingWizard.handleComplete calls fetch('/api/onboarding/complete') instead of direct Supabase write
- Subscription row uses user_id field (not id) matching the auth.users foreign key
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-02-SUMMARY.md`
</output>
