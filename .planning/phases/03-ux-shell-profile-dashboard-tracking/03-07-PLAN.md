---
phase: 03-ux-shell-profile-dashboard-tracking
plan: 07
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "TypeScript compilation passes with zero errors across all new and modified files"
    - "Every new page renders inside the Sidebar + Header shell"
    - "Every new page has RTL layout, Hebrew labels, ErrorBoundary, and Breadcrumbs"
    - "Dashboard charts render with data when mood entries and goals exist"
    - "Mood entry appears in dashboard mood trend chart after creation"
    - "Theme toggle persists across page refresh"
    - "Mobile navigation works on narrow viewports"
  artifacts: []
  key_links: []
---

<objective>
Integration verification checkpoint — confirm all Phase 3 pages work together: shell navigation, CRUD operations, dashboard data flow, theme persistence, and mobile responsiveness.

Purpose: Final quality gate before Phase 3 is marked complete. Catches integration issues between the 6 preceding plans.
Output: Human-verified confirmation that all Phase 3 success criteria are met.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-shell-profile-dashboard-tracking/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript compilation and fix any errors</name>
  <files></files>
  <read_first>
    - mystiqor-build/tsconfig.json (TypeScript config)
  </read_first>
  <action>
Run `cd mystiqor-build && npx tsc --noEmit` and fix any compilation errors found across all Phase 3 files. Common issues to check:
- Import path correctness (all @/ paths resolve)
- Type mismatches between components and their props
- Missing exports or incorrect export names
- Zod v4 schema usage (ensure no v3 patterns)
- date-fns v4 import paths (use `date-fns/locale/he` not `date-fns/locale`)
- Next.js 15 params pattern (params is a Promise)

If errors exist, fix them. Then run again to confirm zero errors.

Also run `cd mystiqor-build && npm run build` as the full build gate.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0
    - npm run build exits with code 0
    - No TypeScript errors in any Phase 3 files
  </acceptance_criteria>
  <done>Full TypeScript compilation and Next.js build pass with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all Phase 3 pages</name>
  <what-built>Complete Phase 3: app shell (Sidebar, Header, MobileNav), mood tracker, personal journal, goals system, profile/guest profiles/settings, and dashboard with charts</what-built>
  <how-to-verify>
Start the dev server: `cd mystiqor-build && npm run dev`

**1. App Shell (UX-01, UX-02, UX-03)**
- Visit http://localhost:3000/dashboard
- Verify: Sidebar visible on desktop with navigation categories
- Verify: Header at top with theme toggle and user menu
- Verify: RTL layout — text flows right-to-left, sidebar on right side
- Click theme toggle — verify dark/light switch
- Refresh page — verify theme persists (UX-02)
- Resize to mobile width — verify sidebar hides, hamburger menu appears
- Tap hamburger — verify MobileNav overlay opens (UX-03)

**2. Dashboard (DASH-01 through DASH-06)**
- On /dashboard: verify daily insight card with zodiac sign and day number (D-01)
- Verify 4 stat cards: active goals, mood score, completed goals, reminders (D-04)
- Verify biorhythm chart shows 3 colored lines (DASH-01)
- Click period selector tabs (daily/weekly/monthly) — verify charts update (DASH-06)
- If mood data exists: verify mood trend line chart (DASH-03)
- If goals exist: verify goals progress bar chart (DASH-04)

**3. Mood Tracker (TRCK-01)**
- Navigate to /mood via sidebar
- Select a mood emoji — verify selection highlight
- Adjust energy/stress/sleep sliders
- Add notes text
- Submit — verify toast success message
- Verify new entry appears in list below
- Delete an entry — verify it disappears

**4. Journal (TRCK-02)**
- Navigate to /journal via sidebar
- Click "new entry" — verify form opens
- Fill title, content, select mood emoji, add gratitude items
- Submit — verify entry appears in list
- Edit an entry — verify form pre-fills
- Delete an entry

**5. Goals (TRCK-03, TRCK-04)**
- Navigate to /goals via sidebar
- Create a new goal with category and target date
- Verify it appears in the list with progress bar at 0%
- Update progress slider to 50% — verify bar updates
- Switch status tabs (active/in progress/completed)

**6. Profile + Guest Profiles (PROF-01, PROF-02)**
- Navigate to /profile via sidebar
- Verify profile data pre-filled from onboarding
- Edit name — save — verify change reflected
- Switch to "guest profiles" tab
- Add a guest profile with birth data
- Verify limit message shows (e.g., "1/1 profiles" for free plan)

**7. Settings (PROF-03)**
- Navigate to /settings via sidebar
- Verify theme toggle works
- Verify notification preferences are shown as placeholder

**8. Error Boundary (UX-07)**
- Verify all pages are wrapped in ErrorBoundary (no blank white screens on error)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation: `cd mystiqor-build && npx tsc --noEmit` passes
- Full build: `cd mystiqor-build && npm run build` passes
- Human verification of all 8 functional areas passes
</verification>

<success_criteria>
- All 6 Phase 3 success criteria from ROADMAP verified:
  1. Every page renders with correct RTL layout, Hebrew labels, Hebrew error messages
  2. Theme toggle persists across sessions
  3. Dashboard displays all charts with real data (not placeholder content)
  4. Mood entry logged and appears in dashboard trend chart
  5. Profile edit reflects immediately
  6. Error boundary shows recovery UI (not blank screen)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-shell-profile-dashboard-tracking/03-07-SUMMARY.md`
</output>
