---
phase: 03-ux-shell-profile-dashboard-tracking
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - mystiqor-build/src/app/(auth)/dashboard/page.tsx
  - mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
  - mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
  - mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
  - mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
  - mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
  - mystiqor-build/src/components/features/dashboard/StatCards.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Dashboard hero shows a daily insight card with user's zodiac sign and numerology day number (D-01)"
    - "4 stat cards show: active goals count, current mood score (7-day avg), completed goals count, pending reminders count (D-04)"
    - "Biorhythm chart shows 3 cyclic sine waves (physical/emotional/intellectual) based on user's birth date (D-02, DASH-01)"
    - "Mood trend chart shows line graph of mood_score over selected period (D-02, DASH-03)"
    - "Goals progress chart shows bar chart of goal progress by category (D-02, DASH-04)"
    - "Analysis distribution chart shows bar chart of analyses by tool type (DASH-05)"
    - "Period selector (daily/weekly/monthly) changes mood trend and analysis distribution data (D-03, DASH-06)"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      provides: "Full dashboard page with all charts"
      contains: "DailyInsightCard"
    - path: "mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx"
      provides: "Hero card with zodiac + numerology day"
      contains: "getZodiacSign"
    - path: "mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx"
      provides: "3-cycle biorhythm Recharts LineChart"
      contains: "physical.*emotional.*intellectual"
    - path: "mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx"
      provides: "Mood score line chart with period awareness"
      contains: "LineChart"
    - path: "mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx"
      provides: "Goal progress bar chart by category"
      contains: "BarChart"
    - path: "mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx"
      provides: "Daily/weekly/monthly tabs"
      contains: "daily.*weekly.*monthly"
    - path: "mystiqor-build/src/components/features/dashboard/StatCards.tsx"
      provides: "4 stat cards component"
      contains: "יעדים פעילים"
  key_links:
    - from: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      to: "mood_entries table"
      via: "useQuery for mood data"
      pattern: "from.*mood_entries"
    - from: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      to: "goals table"
      via: "useQuery for goals data"
      pattern: "from.*goals"
    - from: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      to: "profiles table"
      via: "useQuery for birth_date (biorhythm calc)"
      pattern: "from.*profiles"
---

<objective>
Rebuild the dashboard page with all locked chart decisions — daily insight hero card, 4 stat cards, biorhythm chart, mood trend, goals progress, analysis distribution, and period selector.

Purpose: DASH-01 through DASH-06 — the dashboard is the user's home page, showing meaningful data-driven charts and stats. Replaces the existing scaffold dashboard.
Output: Complete dashboard with 7 chart/stat components and period-aware data fetching.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-shell-profile-dashboard-tracking/03-CONTEXT.md
@.planning/phases/03-ux-shell-profile-dashboard-tracking/03-RESEARCH.md
@mystiqor-build/src/app/(auth)/dashboard/page.tsx
@mystiqor-build/src/components/features/shared/AnalysesChart.tsx
@mystiqor-build/src/lib/query/cache-config.ts

<interfaces>
<!-- Existing AnalysesChart from shared/ — KEEP and reuse for DASH-05 -->
```typescript
export interface ChartDataPoint { name: string; [key: string]: string | number; }
export function AnalysesChart({ data }: { data: ChartDataPoint[] }): JSX.Element
```

<!-- TOOL_NAME_HE from existing dashboard page — reuse for DASH-05 -->
```typescript
const TOOL_NAME_HE: Record<string, string> = {
  numerology: 'נומרולוגיה', astrology: 'אסטרולוגיה', palmistry: 'כירומנטיה',
  graphology: 'גרפולוגיה', tarot: 'טארוט', drawing: 'ציורים',
  dream: 'חלומות', career: 'קריירה', compatibility: 'התאמה',
  synastry: 'סינסטרי', solar_return: 'חזרה סולרית', transits: 'טרנזיטים',
  human_design: 'הדיזיין האנושי', personality: 'אישיות',
  document: 'מסמך', question: 'שאלה', relationship: 'יחסים', synthesis: 'סינתזה',
};
```

<!-- Biorhythm algorithm from RESEARCH.md Pattern 4 -->
```typescript
interface BiorhythmDataPoint {
  date: string; physical: number; emotional: number; intellectual: number;
}
function generateBiorhythmData(birthDate: string, days = 14): BiorhythmDataPoint[] {
  // Pure sine wave calculation: physical=23 days, emotional=28 days, intellectual=33 days
}
```

<!-- Daily Insight from RESEARCH.md Pattern 5 -->
```typescript
function getZodiacSign(birthDate: string): string // → Hebrew zodiac name
function getNumerologyDayNumber(today: Date): number // → 1-9 or 11/22
```

<!-- queryKeys from cache-config.ts -->
```typescript
moods: { all, list, recent },
goals: { all, list, active, completed },
analyses: { all, list, byType },
profile: { all, current },
reminders: { all, list, upcoming },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard chart components (DailyInsightCard, BiorhythmChart, MoodTrendChart, GoalsProgressChart, PeriodSelector, StatCards)</name>
  <files>mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx, mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx, mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx, mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx, mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx, mystiqor-build/src/components/features/dashboard/StatCards.tsx</files>
  <read_first>
    - mystiqor-build/src/app/(auth)/dashboard/page.tsx (existing dashboard — understand current structure, keep TOOL_NAME_HE and buildChartData)
    - mystiqor-build/src/components/features/shared/AnalysesChart.tsx (existing chart component — reuse for DASH-05)
    - github-source/src/components/dashboard/BiorhythmChart.jsx (original biorhythm algorithm to port)
    - mystiqor-build/src/lib/query/cache-config.ts (queryKeys)
    - .planning/phases/03-ux-shell-profile-dashboard-tracking/03-RESEARCH.md (Patterns 4 and 5 — biorhythm and daily insight algorithms)
    - .planning/phases/03-ux-shell-profile-dashboard-tracking/03-CONTEXT.md (D-01 through D-04 decisions)
  </read_first>
  <action>
Create 6 component files in `mystiqor-build/src/components/features/dashboard/`:

**1. `DailyInsightCard.tsx`** — Per D-01 hero element.

Props: `{ birthDate: string }` (from user profile).

Deterministic calculations (no LLM):
- `getZodiacSign(birthDate)`: Map month/day to Hebrew zodiac sign. Standard boundaries:
  ```
  טלה (3/21-4/19), שור (4/20-5/20), תאומים (5/21-6/20), סרטן (6/21-7/22),
  אריה (7/23-8/22), בתולה (8/23-9/22), מאזניים (9/23-10/22), עקרב (10/23-11/21),
  קשת (11/22-12/21), גדי (12/22-1/19), דלי (1/20-2/18), דגים (2/19-3/20)
  ```
- `getNumerologyDayNumber(today)`: Sum digits of today's date (YYYYMMDD), reduce to 1-9 or master 11/22.
- Display: gradient background (purple-to-indigo in dark, lighter in light theme), zodiac icon placeholder (Stars from lucide), zodiac sign name, day number, and a static insight text selected from a pool using `(dayOfYear + numerologyNumber) % poolLength`. Pool of ~9 Hebrew insight sentences.

Per user's specific idea: "should feel mystical — gradient background, zodiac icon, brief personalized message."

**2. `BiorhythmChart.tsx`** — Per D-02 and DASH-01.

Props: `{ birthDate: string }`.

Port from `github-source/src/components/dashboard/BiorhythmChart.jsx`:
- Generate 14-day data using sine wave calculation (physical=23, emotional=28, intellectual=33 day cycles)
- Render with Recharts `<LineChart>` — 3 lines:
  - Physical: red/rose (`stroke="#f43f5e"`)
  - Emotional: blue (`stroke="#3b82f6"`)
  - Intellectual: yellow/amber (`stroke="#eab308"`)
- Hebrew date labels on XAxis (use `he-IL` locale formatting)
- `<XAxis reversed={true}>` for RTL (per Pitfall 2)
- `<ResponsiveContainer width="100%" height={250}>`
- Legend with Hebrew labels: פיזי / רגשי / אינטלקטואלי

**3. `MoodTrendChart.tsx`** — Per D-02 and DASH-03.

Props: `{ data: Array<{ date: string; mood_score: number }>; period: string }`.

- Recharts `<LineChart>` with single line for mood_score
- XAxis with Hebrew date labels, reversed for RTL
- YAxis 1-10 range
- Gradient fill under the line (like an area chart)
- Show data points as dots
- Empty state if no data

**4. `GoalsProgressChart.tsx`** — Per D-02 and DASH-04.

Props: `{ data: Array<{ category: string; progress: number; count: number }> }`.

- Recharts `<BarChart>` showing average progress per goal category
- Hebrew category labels (reuse CATEGORY_LABELS from GoalForm)
- Color bars by category
- Show count label on each bar
- Horizontal layout for RTL readability

**5. `PeriodSelector.tsx`** — Per D-03 and DASH-06.

Props: `{ value: 'daily' | 'weekly' | 'monthly'; onChange: (period: 'daily' | 'weekly' | 'monthly') => void }`.

- Three tab buttons: "יומי" / "שבועי" / "חודשי"
- Selected tab has active styling (bg-primary text-primary-foreground)
- Compact, inline with chart section header

**6. `StatCards.tsx`** — Per D-04 and DASH-02.

Props:
```typescript
interface StatCardsProps {
  readonly stats: {
    activeGoals: number;
    currentMoodScore: number; // 7-day average
    completedGoals: number;
    pendingReminders: number;
  };
  readonly isLoading: boolean;
}
```
- 4 cards in a grid (grid-cols-2 lg:grid-cols-4)
- Per D-04: "יעדים פעילים" (Target icon, emerald), "ציון מצב רוח" (SmilePlus icon, amber — show as X.X/10), "יעדים שהושלמו" (CheckCircle icon, purple), "תזכורות ממתינות" (Bell icon, blue)
- Skeleton state when isLoading
- Each card: label, large number, icon with gradient background

Per Pitfall 5: Replace existing STAT_CARDS constant (analysesCount) with the new D-04 stat cards.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists: mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx
    - File exists: mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
    - File exists: mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
    - File exists: mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
    - File exists: mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
    - File exists: mystiqor-build/src/components/features/dashboard/StatCards.tsx
    - grep -q "getZodiacSign" mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx returns 0
    - grep -q "getNumerologyDayNumber" mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx returns 0
    - grep -q "physical.*emotional.*intellectual\|generateBiorhythmData" mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx returns 0
    - grep -q "reversed" mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx returns 0 (RTL XAxis)
    - grep -q "LineChart" mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx returns 0
    - grep -q "BarChart" mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx returns 0
    - grep -q "יומי.*שבועי.*חודשי\|daily.*weekly.*monthly" mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx returns 0
    - grep -q "יעדים פעילים" mystiqor-build/src/components/features/dashboard/StatCards.tsx returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All 6 dashboard chart components created: DailyInsightCard (zodiac + numerology), BiorhythmChart (3 sine waves), MoodTrendChart (line chart), GoalsProgressChart (bar chart), PeriodSelector (3 tabs), StatCards (4 cards per D-04). All use Recharts with RTL-aware XAxis.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild dashboard page.tsx with all chart components and period-aware data fetching</name>
  <files>mystiqor-build/src/app/(auth)/dashboard/page.tsx</files>
  <read_first>
    - mystiqor-build/src/app/(auth)/dashboard/page.tsx (current scaffold — will be replaced)
    - mystiqor-build/src/components/features/dashboard/DailyInsightCard.tsx (just created in Task 1)
    - mystiqor-build/src/components/features/dashboard/BiorhythmChart.tsx
    - mystiqor-build/src/components/features/dashboard/MoodTrendChart.tsx
    - mystiqor-build/src/components/features/dashboard/GoalsProgressChart.tsx
    - mystiqor-build/src/components/features/dashboard/PeriodSelector.tsx
    - mystiqor-build/src/components/features/dashboard/StatCards.tsx
    - mystiqor-build/src/components/features/shared/AnalysesChart.tsx (keep for DASH-05)
    - mystiqor-build/src/lib/query/cache-config.ts (queryKeys with period in key — per Pitfall 7)
    - mystiqor-build/src/components/common/ErrorBoundary.tsx
    - mystiqor-build/src/components/common/Breadcrumbs.tsx
  </read_first>
  <action>
Rewrite `mystiqor-build/src/app/(auth)/dashboard/page.tsx` completely. The current file is a scaffold with wrong stat cards (Pitfall 5). Replace with the full D-01 through D-06 implementation.

`'use client'` directive. Structure:

1. State: `const [period, setPeriod] = useState<'daily' | 'weekly' | 'monthly'>('weekly');`

2. Data fetching with `useQuery` + `Promise.allSettled` pattern (keep from existing):
   - **Profile** (for biorhythm birth_date + daily insight): `supabase.from('profiles').select('birth_date').eq('id', user.id).single()`
   - **Active goals count**: `supabase.from('goals').select('id', { count: 'exact' }).eq('user_id', user.id).in('status', ['active', 'in_progress'])`
   - **Completed goals count**: `supabase.from('goals').select('id', { count: 'exact' }).eq('user_id', user.id).eq('status', 'completed')`
   - **Goals by category** (for GoalsProgressChart): `supabase.from('goals').select('category, progress, status').eq('user_id', user.id).in('status', ['active', 'in_progress'])`
   - **Mood entries** (period-aware — per Pitfall 7, include period in queryKey): date range based on `period` state. Use `subDays`/`subMonths` from date-fns.
   - **Average mood score** (7-day): compute from mood entries data
   - **Pending reminders**: `supabase.from('reminders').select('id', { count: 'exact' }).eq('user_id', user.id).eq('status', 'pending')`
   - **Recent analyses** (for AnalysesChart — DASH-05, period-aware): same period filter

   Per Pitfall 7: queryKey MUST include period — `queryKeys.moods.list({ period })` so cache varies by period.

3. Layout (top to bottom):
   ```
   <ErrorBoundary>
     <Breadcrumbs items={[{ label: 'לוח בקרה' }]} />
     <h1>לוח הבקרה</h1>

     {/* D-01: Hero daily insight card */}
     <DailyInsightCard birthDate={profile.birth_date} />

     {/* D-04: 4 stat cards */}
     <StatCards stats={{ activeGoals, currentMoodScore, completedGoals, pendingReminders }} isLoading={isLoading} />

     {/* D-03: Period selector */}
     <PeriodSelector value={period} onChange={setPeriod} />

     {/* Charts grid: 2 columns on desktop */}
     <div className="grid gap-6 lg:grid-cols-2">
       {/* D-02/DASH-01: Biorhythm */}
       <div className="rounded-xl border border-border bg-card p-5">
         <h2>ביוריתם</h2>
         <BiorhythmChart birthDate={profile.birth_date} />
       </div>

       {/* D-02/DASH-03: Mood trend */}
       <div className="rounded-xl border border-border bg-card p-5">
         <h2>מגמת מצב רוח</h2>
         <MoodTrendChart data={moodTrendData} period={period} />
       </div>

       {/* D-02/DASH-04: Goals progress */}
       <div className="rounded-xl border border-border bg-card p-5">
         <h2>התקדמות יעדים</h2>
         <GoalsProgressChart data={goalsChartData} />
       </div>

       {/* DASH-05: Analysis distribution */}
       <div className="rounded-xl border border-border bg-card p-5">
         <h2>ניתוחים לפי כלי</h2>
         <AnalysesChart data={analysesChartData} />
       </div>
     </div>
   </ErrorBoundary>
   ```

4. Keep TOOL_NAME_HE constant and buildChartData helper from current file (they work for DASH-05).

5. Skeleton loading: StatCards has built-in skeleton, charts show `<Skeleton className="h-64 w-full" />` when loading.

6. All Hebrew labels. dir="rtl" inherited from layout (set in 03-01).
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DailyInsightCard" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "BiorhythmChart" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "MoodTrendChart" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "GoalsProgressChart" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "PeriodSelector" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "StatCards" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "AnalysesChart" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0 (DASH-05 retained)
    - grep -q "ErrorBoundary" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0
    - grep -q "period" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 0 (DASH-06 period state)
    - grep -q "analysesCount" mystiqor-build/src/app/(auth)/dashboard/page.tsx returns 1 (old stat card GONE per Pitfall 5)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Dashboard page renders: DailyInsightCard hero (D-01), 4 correct stat cards (D-04), period selector (D-03/DASH-06), biorhythm chart (DASH-01), mood trend (DASH-03), goals progress (DASH-04), analysis distribution (DASH-05). Period selector changes data for mood and analysis charts. ErrorBoundary wraps page.</done>
</task>

</tasks>

<verification>
- `cd mystiqor-build && npx tsc --noEmit` passes with zero errors
- All 7 files exist and compile
- Dashboard renders all 6 DASH requirements
- Period selector affects mood trend and analysis distribution queries (Pitfall 7 addressed)
- Old analysesCount stat card is replaced with D-04 stat cards (Pitfall 5 addressed)
- XAxis reversed for RTL on charts (Pitfall 2 addressed)
</verification>

<success_criteria>
- Dashboard shows daily insight hero card with zodiac sign and numerology day number
- 4 stat cards show active goals, mood score (7-day avg), completed goals, pending reminders
- Biorhythm chart shows 3 sine wave lines
- Mood trend shows line chart that changes with period selector
- Goals progress shows bar chart by category
- Analysis distribution shows bar chart by tool type
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-shell-profile-dashboard-tracking/03-06-SUMMARY.md`
</output>
