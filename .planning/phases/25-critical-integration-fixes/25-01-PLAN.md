---
phase: 25-critical-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layouts/BottomTabBar.tsx
  - src/lib/supabase/middleware.ts
autonomous: true
requirements: [FIX-01, FIX-02]
gap_closure: true

must_haves:
  truths:
    - "Mobile user tapping Insights tab navigates to /tools/daily-insights (no 404)"
    - "Unauthenticated user visiting /notifications is redirected to /login?next=/notifications"
    - "Unauthenticated user visiting /referrals is redirected to /login?next=/referrals"
    - "Unauthenticated user visiting /pricing is redirected to /login?next=/pricing"
    - "Build remains clean — 0 TypeScript errors, all tests pass"
  artifacts:
    - path: "src/components/layouts/BottomTabBar.tsx"
      provides: "Corrected mobile bottom tab navigation"
      contains: "/tools/daily-insights"
    - path: "src/lib/supabase/middleware.ts"
      provides: "Complete PROTECTED_PATHS with notifications, referrals, pricing"
      contains: "/notifications"
  key_links:
    - from: "src/components/layouts/BottomTabBar.tsx"
      to: "src/app/(auth)/tools/daily-insights/page.tsx"
      via: "Next.js Link href"
      pattern: "href.*'/tools/daily-insights'"
    - from: "src/lib/supabase/middleware.ts"
      to: "src/app/(auth)/notifications/ | referrals/ | pricing/"
      via: "PROTECTED_PATHS prefix match in updateSession"
      pattern: "'/notifications'|'/referrals'|'/pricing'"
---

<objective>
Fix two integration gaps identified by the v1.2 milestone audit:

1. **INT-01-BOTTOMTAB-404 / FLOW-MOBILE-INSIGHTS-404:** BottomTabBar.tsx links to `/daily-insights` (404) instead of `/tools/daily-insights` where the page actually lives. Mobile users tapping the Insights tab get a broken page.

2. **INT-02-MIDDLEWARE-PATHS:** Three paths (`/notifications`, `/referrals`, `/pricing`) are missing from the PROTECTED_PATHS array in middleware.ts. Auth still works via layout guard, but the `?next=` redirect-back UX is degraded — unauthenticated users don't get redirected back to their intended page after login.

Purpose: Close critical and moderate integration gaps so all navigation works and auth redirect UX is complete.
Output: Two patched files, zero new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BottomTabBar insights href (FIX-01)</name>
  <files>src/components/layouts/BottomTabBar.tsx</files>
  <read_first>
    - src/components/layouts/BottomTabBar.tsx (current state — line 31 has wrong href)
  </read_first>
  <action>
In `src/components/layouts/BottomTabBar.tsx`, change line 31 in the TABS array from:

```typescript
{ label: 'תובנות', href: '/daily-insights', Icon: Lightbulb },
```

to:

```typescript
{ label: 'תובנות', href: '/tools/daily-insights', Icon: Lightbulb },
```

This is the ONLY change in this file. Do not modify anything else. The Sidebar already uses the correct `/tools/daily-insights` path — this aligns BottomTabBar to match.

Closes: INT-01-BOTTOMTAB-404, FLOW-MOBILE-INSIGHTS-404 (per FIX-01).
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && grep -n "daily-insights" src/components/layouts/BottomTabBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for `/daily-insights` in BottomTabBar.tsx returns ONLY `/tools/daily-insights` (no bare `/daily-insights`)
    - Exact match: `grep "href: '/tools/daily-insights'" src/components/layouts/BottomTabBar.tsx` returns 1 result
    - File still has exactly 93 lines (no accidental additions/deletions)
    - `npx tsc --noEmit` passes with 0 errors
  </acceptance_criteria>
  <done>BottomTabBar Insights tab links to /tools/daily-insights — mobile 404 eliminated</done>
</task>

<task type="auto">
  <name>Task 2: Add missing paths to PROTECTED_PATHS (FIX-02)</name>
  <files>src/lib/supabase/middleware.ts</files>
  <read_first>
    - src/lib/supabase/middleware.ts (current state — PROTECTED_PATHS array lines 13-29)
  </read_first>
  <action>
In `src/lib/supabase/middleware.ts`, add three entries to the PROTECTED_PATHS array. Insert them after the existing `/blog` entry (line 28), before the closing `]`:

```typescript
  '/blog',
  // Phase 25 — missing protected paths (middleware redirect UX)
  '/notifications',
  '/referrals',
  '/pricing',
];
```

This ensures unauthenticated users hitting these paths get redirected to `/login?next=<path>` instead of falling through to the layout-level guard (which works but loses the redirect-back URL).

Do NOT modify any other part of the file. The updateSession function, cookie handling, and x-pathname header logic are all correct.

Closes: INT-02-MIDDLEWARE-PATHS (per FIX-02).
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && grep -c "'/notifications'\|'/referrals'\|'/pricing'" src/lib/supabase/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'/notifications'" src/lib/supabase/middleware.ts` returns exactly 1 match
    - `grep "'/referrals'" src/lib/supabase/middleware.ts` returns exactly 1 match
    - `grep "'/pricing'" src/lib/supabase/middleware.ts` returns exactly 1 match
    - PROTECTED_PATHS array now has 16 entries (was 13)
    - `npx tsc --noEmit` passes with 0 errors
  </acceptance_criteria>
  <done>PROTECTED_PATHS includes /notifications, /referrals, /pricing — auth redirect UX complete for all protected routes</done>
</task>

</tasks>

<verification>
After both tasks complete, run:

1. `npx tsc --noEmit` — must pass with 0 errors
2. `npx vitest run` — all 103 tests must pass (no regressions)
3. `grep "daily-insights" src/components/layouts/BottomTabBar.tsx` — must show only `/tools/daily-insights`
4. `grep -c "PROTECTED_PATHS" src/lib/supabase/middleware.ts` — confirms array exists
5. `grep -E "'/notifications'|'/referrals'|'/pricing'" src/lib/supabase/middleware.ts` — all 3 present

All 3 audit gaps closed: INT-01-BOTTOMTAB-404, INT-02-MIDDLEWARE-PATHS, FLOW-MOBILE-INSIGHTS-404
</verification>

<success_criteria>
- BottomTabBar Insights tab href is `/tools/daily-insights` (not `/daily-insights`)
- PROTECTED_PATHS contains `/notifications`, `/referrals`, `/pricing`
- TypeScript compiles cleanly (0 errors)
- All existing tests pass (no regressions)
- Requirements FIX-01 and FIX-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/25-critical-integration-fixes/25-01-SUMMARY.md`
</output>
