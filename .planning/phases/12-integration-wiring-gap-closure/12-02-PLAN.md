---
phase: 12-integration-wiring-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/tools/astrology/page.tsx
  - mystiqor-build/src/app/(auth)/tools/numerology/page.tsx
  - mystiqor-build/src/app/(auth)/tools/tarot/page.tsx
  - mystiqor-build/src/app/(auth)/tools/personality/page.tsx
  - mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx
  - mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx
  - mystiqor-build/src/app/(auth)/tools/graphology/page.tsx
  - mystiqor-build/src/components/features/drawing/DrawingAnalysisForm.tsx
  - mystiqor-build/src/app/(auth)/tools/human-design/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  - mystiqor-build/src/app/(auth)/tools/career/page.tsx
  - mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  - mystiqor-build/src/app/(auth)/tools/timing/page.tsx
  - mystiqor-build/src/app/(auth)/tools/document/page.tsx
autonomous: true
requirements: [INFRA-03, SUBS-04]
gap_closure: true

must_haves:
  truths:
    - "Running any tool analysis increments the user's analyses_used counter in the subscriptions table"
    - "Free users who have used 3 analyses see the SubscriptionGuard upgrade prompt on the 4th attempt (counter is incremented, limit is enforced)"
    - "The usage bar in the Sidebar updates after each tool use (React Query cache invalidated)"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/tools/astrology/page.tsx"
      provides: "Birth chart page calls incrementUsage() on mutation onSuccess"
      contains: "incrementUsage"
    - path: "mystiqor-build/src/app/(auth)/tools/numerology/page.tsx"
      provides: "Numerology page calls incrementUsage() on mutation onSuccess"
      contains: "incrementUsage"
    - path: "mystiqor-build/src/app/(auth)/tools/tarot/page.tsx"
      provides: "Tarot page calls incrementUsage() on mutation onSuccess"
      contains: "incrementUsage"
  key_links:
    - from: "Tool page mutation onSuccess"
      to: "/api/subscription/usage"
      via: "incrementUsage() from useSubscription hook"
      pattern: "incrementUsage"
    - from: "incrementMutation.mutateAsync in useSubscription"
      to: "subscriptions.analyses_used DB column"
      via: "POST /api/subscription/usage → increment_usage DB function"
      pattern: "increment_usage"
---

<objective>
Wire `incrementUsage()` from `useSubscription` into every tool page's success handler so that each completed analysis increments the user's usage counter. This closes the subscription enforcement gap — free users were running unlimited analyses because the counter was never incremented.

Purpose: SUBS-04 and INFRA-03 both require usage enforcement. The SubscriptionGuard blocks entry when limits are reached, but limits can never be reached if no tool ever increments the counter.
Output: 16 files updated. Every tool's onSuccess handler calls `incrementUsage()` after saving the result.
</objective>

<execution_context>
@D:/AI_projects/MystiQor/.planning/phases/workflows/execute-plan.md
</execution_context>

<context>
@D:/AI_projects/MystiQor/.planning/ROADMAP.md
@D:/AI_projects/MystiQor/.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire incrementUsage into Tier-1 and Tier-2 tool pages (useMutation pattern)</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/astrology/page.tsx,
    mystiqor-build/src/app/(auth)/tools/numerology/page.tsx,
    mystiqor-build/src/app/(auth)/tools/tarot/page.tsx,
    mystiqor-build/src/app/(auth)/tools/personality/page.tsx,
    mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx,
    mystiqor-build/src/app/(auth)/tools/graphology/page.tsx,
    mystiqor-build/src/components/features/drawing/DrawingAnalysisForm.tsx
  </files>

  <read_first>
  Read these files before modifying:
  - mystiqor-build/src/hooks/useSubscription.ts (understand incrementUsage signature: () => Promise&lt;{new_count: number}&gt;)
  - Each file listed in &lt;files&gt; above — read each one to understand its exact onSuccess handler before editing
  </read_first>

  <action>
  All 8 files use `useMutation` from `@tanstack/react-query`. Apply the same pattern to each:

  **Step 1 — Add import at the top of each file:**
  ```typescript
  import { useSubscription } from '@/hooks/useSubscription';
  ```

  **Step 2 — Call the hook inside the component function (before the mutation):**
  ```typescript
  const { incrementUsage } = useSubscription();
  ```

  **Step 3 — Update the mutation's `onSuccess` handler to call `incrementUsage()` after setting state:**
  The call is fire-and-forget — errors must NOT break the UI, use `.catch(() => {})`:
  ```typescript
  onSuccess: (data) => {
    setResult(data);
    toast.success('...');
    // עדכן שימוש — non-blocking, non-fatal
    void incrementUsage().catch(() => {});
  },
  ```

  **Specific notes per file:**

  `astrology/page.tsx`: mutation uses `mutationFn: fetchBirthChart`. Add `void incrementUsage().catch(() => {})` in `onSuccess` after `setResult(data)` and `toast.success(...)`.

  `numerology/page.tsx`: has TWO mutations — `mutation` (main numerology) and `compatMutation` (compatibility). Wire `incrementUsage` only into the MAIN `mutation.onSuccess` (not compatibility — it's the same subscription action). The compatibility sub-form does not generate a separate analysis_id (shares the existing analysis).

  `tarot/page.tsx`: Add after `setResult(data)` and `toast.success(...)`.

  `personality/page.tsx`: Read the file first. Find the mutation's onSuccess, add after existing state update + toast.

  `palmistry/page.tsx`: Has `useMutation`. Find the `onSuccess` where `setResult(data)` and `toast.success` appear. Add `void incrementUsage().catch(() => {})`.

  `compatibility/page.tsx`: Read the file first. Find the mutation's onSuccess, add after existing state update + toast.

  `graphology/page.tsx`: Read the file first. Find the primary analysis mutation (not the comparison or reminder ones). Add incrementUsage only to the primary analysis onSuccess.

  `DrawingAnalysisForm.tsx`: Find the `useMutation` for the drawing analysis API call. Add `void incrementUsage().catch(() => {})` in `onSuccess` after `setResult(data)`.

  **TypeScript safety:** `incrementUsage` returns `Promise<{new_count: number}>`. The `void` prefix discards the promise return value (avoids unhandled promise warning). The `.catch(() => {})` silences errors so usage counter failure never blocks the analysis result UI.
  </action>

  <verify>
    <automated>grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/page.tsx mystiqor-build/src/app/(auth)/tools/numerology/page.tsx mystiqor-build/src/app/(auth)/tools/tarot/page.tsx mystiqor-build/src/components/features/drawing/DrawingAnalysisForm.tsx</automated>
  </verify>

  <acceptance_criteria>
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/page.tsx` → 2 matches (import + call)
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/numerology/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/tarot/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/personality/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/palmistry/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/compatibility/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/graphology/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/components/features/drawing/DrawingAnalysisForm.tsx` → 2 matches
  - `cd mystiqor-build && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
  </acceptance_criteria>

  <done>All 8 Tier-1/Tier-2 tool mutations fire incrementUsage() on success. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire incrementUsage into Tier-3 and async-pattern tool pages</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx,
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx,
    mystiqor-build/src/app/(auth)/tools/career/page.tsx,
    mystiqor-build/src/app/(auth)/tools/relationships/page.tsx,
    mystiqor-build/src/app/(auth)/tools/timing/page.tsx,
    mystiqor-build/src/app/(auth)/tools/human-design/page.tsx,
    mystiqor-build/src/app/(auth)/tools/document/page.tsx
  </files>

  <read_first>
  Read each file before modifying:
  - mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  - mystiqor-build/src/app/(auth)/tools/career/page.tsx
  - mystiqor-build/src/app/(auth)/tools/relationships/page.tsx
  - mystiqor-build/src/app/(auth)/tools/timing/page.tsx
  - mystiqor-build/src/app/(auth)/tools/human-design/page.tsx
  - mystiqor-build/src/app/(auth)/tools/document/page.tsx
  - mystiqor-build/src/hooks/useSubscription.ts
  </read_first>

  <action>
  **Group A — Tier-3 pages using `useMutation` (transits, solar-return, synastry, career, relationships, timing):**

  Apply the same pattern as Task 1 to each:
  1. Add `import { useSubscription } from '@/hooks/useSubscription';`
  2. Add `const { incrementUsage } = useSubscription();` inside the component
  3. Add `void incrementUsage().catch(() => {});` in each mutation's `onSuccess` after existing state/toast calls

  **Group B — Async/await pattern pages (human-design, document):**

  These pages use `async function onSubmit(values)` with manual `try/catch`. The correct insertion point is right after `toast.success(...)` inside the `try` block, before the `finally`:

  For `human-design/page.tsx`, the current pattern is:
  ```typescript
  if (json.data) {
    setResult(json.data);
    toast.success('ניתוח Human Design מוכן!');
  }
  ```
  Update to:
  ```typescript
  if (json.data) {
    setResult(json.data);
    toast.success('ניתוח Human Design מוכן!');
    void incrementUsage().catch(() => {});
  }
  ```
  The hook must be called at component top level (not inside the callback). Add `const { incrementUsage } = useSubscription();` inside `HumanDesignPage()` before `onSubmit`.

  For `document/page.tsx`, find the `toast.success('ניתוח המסמך הושלם')` line. Add `void incrementUsage().catch(() => {});` immediately after it. Add hook call and import at top of file/component similarly.

  **Important TypeScript note for all files:** `useSubscription` returns `incrementUsage: () => Promise<{new_count: number}>`. Using `void incrementUsage().catch(() => {})` is correct — `void` converts the unhandled Promise to `void` type, `.catch(() => {})` prevents unhandled rejection.
  </action>

  <verify>
    <automated>grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx mystiqor-build/src/app/(auth)/tools/human-design/page.tsx mystiqor-build/src/app/(auth)/tools/document/page.tsx</automated>
  </verify>

  <acceptance_criteria>
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/transits/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/solar-return/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/career/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/relationships/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/timing/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/human-design/page.tsx` → 2 matches
  - `grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/document/page.tsx` → 2 matches
  - Full TypeScript check: `cd mystiqor-build && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
  </acceptance_criteria>

  <done>All 16 tool pages (Tier-1, Tier-2, Tier-3) call incrementUsage() on successful analysis. Usage counter now enforces the 3/month free tier limit.</done>
</task>

</tasks>

<verification>
Full TypeScript compilation after both tasks:
```bash
cd mystiqor-build && npx tsc --noEmit 2>&1 | head -30
```

Count how many files now call incrementUsage:
```bash
grep -rn "incrementUsage" mystiqor-build/src/app/(auth)/tools/ mystiqor-build/src/components/features/drawing/ --include="*.tsx" -l | wc -l
```
Expected: 16 files.
</verification>

<success_criteria>
1. All 16 tool pages import and call `incrementUsage()` in their success handlers
2. `void incrementUsage().catch(() => {})` pattern used consistently — never throws, never blocks UI
3. TypeScript build passes with zero errors
4. Usage counter increments on every tool result (free users reach 3/month limit)
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-wiring-gap-closure/12-02-SUMMARY.md`
</output>
