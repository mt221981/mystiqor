---
phase: 32-frontend-accessibility-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/app/(auth)/coach/page.tsx
  - mystiqor-build/src/app/(auth)/journal/page.tsx
  - mystiqor-build/src/app/(auth)/referrals/page.tsx
  - mystiqor-build/src/app/(auth)/settings/page.tsx
  - mystiqor-build/src/app/(auth)/tools/document/page.tsx
  - mystiqor-build/src/components/features/coach/JourneysPanel.tsx
  - mystiqor-build/src/components/features/journal/JournalEntryForm.tsx
  - mystiqor-build/src/components/features/history/AnalysisCard.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/readings/page.tsx
  - mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
  - mystiqor-build/src/app/(auth)/history/page.tsx
  - mystiqor-build/src/app/api/coach/messages/route.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - UX-01
  - UX-02

must_haves:
  truths:
    - "אין ml-N / mr-N / pl-N / pr-N בקמפוננטות feature — רק ms/me/ps/pe"
    - "טקסט משני (confidence label, character counter, notifications header) נקרא ב-WCAG AA 4.5:1"
    - "כפתורי tab במאמן מוכרזים נכון עם role=tab + aria-selected על ידי screen reader"
    - "כפתורי אייקון-בלבד (mobile toggle, remove-file) כוללים aria-label בעברית"
    - "דפי מאמן והיסטוריה מציגים EmptyState עם כפתור CTA כשאין נתונים"
    - "טפסים אסטרולוגיה עם שדות מרובים מוצגים בעמודה אחת במובייל (<640px)"
    - "coach POST context מוגבל ל-10 הודעות אחרונות"
  artifacts:
    - path: "mystiqor-build/src/app/(auth)/coach/page.tsx"
      provides: "RTL margins fixed, tab ARIA roles, mobile toggle aria-label, EmptyState zero-state"
      contains: "role=\"tablist\""
    - path: "mystiqor-build/src/app/(auth)/history/page.tsx"
      provides: "EmptyState with router.push('/tools') CTA"
      contains: "EmptyState"
    - path: "mystiqor-build/src/app/api/coach/messages/route.ts"
      provides: "context window capped at 10 messages"
      contains: ".limit(10)"
    - path: "mystiqor-build/src/components/features/history/AnalysisCard.tsx"
      provides: "opacity-60 removed from confidence label"
  key_links:
    - from: "coach/page.tsx tab buttons"
      to: "role=tab + aria-selected"
      via: "direct attribute on <button>"
      pattern: "role=\"tab\""
    - from: "coach/page.tsx zero-state branch"
      to: "EmptyState component"
      via: "conditional render when conversations.length === 0 && !activeConversationId"
      pattern: "EmptyState"
    - from: "messages/route.ts POST handler"
      to: ".limit(10)"
      via: "supabase query chain — line ~200 only (NOT the GET handler)"
      pattern: "\\.limit\\(10\\)"
---

<objective>
RTL accessibility sweep, WCAG contrast fixes, ARIA labels, empty states, and mobile form responsiveness across the MystiQor frontend.

Purpose: Close all five Phase 32 accessibility and UX requirements (A11Y-01 through UX-02) in a single execution pass. Every change is mechanical and isolated — no shared state, no new dependencies, no architectural decisions.

Output:
- 12 RTL margin/padding violations fixed across 6 files (A11Y-01)
- 3 WCAG AA contrast violations resolved + coach tab ARIA roles + icon aria-labels (A11Y-02, A11Y-03)
- Coach and history pages upgraded to EmptyState with CTA (UX-01)
- 4 form grids made responsive + coach context capped at 10 messages (UX-02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-accessibility-ux/32-RESEARCH.md

<interfaces>
<!-- EmptyState component API — from mystiqor-build/src/components/common/EmptyState.tsx -->
```typescript
interface EmptyStateAction {
  readonly label: string;
  readonly onClick: () => void;
}

interface EmptyStateProps {
  readonly icon?: ReactNode;
  readonly title: string;
  readonly description?: string;
  readonly action?: EmptyStateAction;
}

// Usage (from goals/page.tsx — already working reference):
<EmptyState
  icon={<Target className="h-8 w-8" />}
  title="אין מטרות עדיין"
  description="הגדר מטרה חדשה ועקוב אחר ההתקדמות שלך"
  action={{ label: 'מטרה חדשה', onClick: () => setIsCreateOpen(true) }}
/>
```

<!-- Key state variables in coach/page.tsx (verified by research): -->
<!-- - activeConversationId: string | null -->
<!-- - conversations: array (from useQuery) -->
<!-- - createMutation: useMutation({ mutationFn: createConversation }) -->
<!-- - setActiveConversationId: (id: string | null) => void -->
<!-- - showSidebar: boolean -->
<!-- - setShowSidebar: Dispatch<SetStateAction<boolean>> -->

<!-- Key variables in history/page.tsx (verified by research): -->
<!-- - isEmpty: boolean (derived from analyses array) -->
<!-- - isError: boolean -->
<!-- - router: from useRouter() — already imported -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RTL margin/padding sweep (A11Y-01)</name>
  <files>
    mystiqor-build/src/app/(auth)/coach/page.tsx
    mystiqor-build/src/app/(auth)/journal/page.tsx
    mystiqor-build/src/app/(auth)/referrals/page.tsx
    mystiqor-build/src/app/(auth)/settings/page.tsx
    mystiqor-build/src/app/(auth)/tools/document/page.tsx
    mystiqor-build/src/components/features/coach/JourneysPanel.tsx
    mystiqor-build/src/components/features/journal/JournalEntryForm.tsx
  </files>
  <action>
Apply the following exact substitutions (read each file first, make targeted edits, do NOT touch anything else):

**coach/page.tsx** — lines 212 and 214:
- `ml-2` on `<Loader2>` icon → `ms-2`
- `ml-2` on `<Plus>` icon → `ms-2`

**journal/page.tsx** — lines 224 and 232:
- `ml-1` on `<Plus>` icon inside "יומן מהיר" button → `ms-1`
- `ml-1` on `<Plus>` icon inside "יומן מלא" button → `ms-1`

**referrals/page.tsx** — line 118:
- `pr-1` on `<ol>` → `pe-1`

**settings/page.tsx** — lines 126 and 139:
- `ml-1.5` on `<Moon>` icon → `ms-1.5`
- `ml-1.5` on `<Sun>` icon → `ms-1.5`

**tools/document/page.tsx** — line 262:
- `ml-2` on `<Loader2>` inside submit button → `ms-2`

**JourneysPanel.tsx** — lines 154 and 156:
- `ml-2` on `<Loader2>` icon → `ms-2`
- `ml-2` on `<Plus>` icon → `ms-2`

**JournalEntryForm.tsx** — lines 143 and 183:
- `mr-1` on required asterisk `*` span → `me-1`
- `mr-2` on energy score badge → `me-2`

CRITICAL — DO NOT TOUCH:
- Any file under `src/components/ui/` (shadcn primitives — `pl-7`, `pr-8` there are fixed layout geometry)
- `ml-auto` or `mr-auto` anywhere (symmetric flex utilities, RTL-safe)
- `px-N` or `py-N` (symmetric, RTL-safe)
- `disabled:opacity-*` classes (correct, WCAG exempts disabled controls)
  </action>
  <verify>
    <automated>cd mystiqor-build && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes with 0 TypeScript errors. Running `grep -rn "ml-[0-9]\|mr-[0-9]\|pl-[0-9]\|pr-[0-9]" mystiqor-build/src/app/(auth) mystiqor-build/src/components/features` returns only `ml-auto`, `mr-auto`, `pl-N`/`pr-N` inside `src/components/ui/`, and the notifications/goals button-pair `grid-cols-2` (intentionally left). No directional margin/padding on feature components.
  </done>
</task>

<task type="auto">
  <name>Task 2: WCAG AA contrast fixes + ARIA labels (A11Y-02, A11Y-03)</name>
  <files>
    mystiqor-build/src/components/features/history/AnalysisCard.tsx
    mystiqor-build/src/app/(auth)/settings/page.tsx
    mystiqor-build/src/app/(auth)/tools/astrology/readings/page.tsx
    mystiqor-build/src/app/(auth)/coach/page.tsx
    mystiqor-build/src/app/(auth)/tools/document/page.tsx
  </files>
  <action>
**CONTRAST FIXES (A11Y-02):**

**AnalysisCard.tsx — line 74:**
Change:
```tsx
<span className="font-label text-xs text-on-surface-variant opacity-60">
```
To:
```tsx
<span className="font-label text-xs text-on-surface-variant">
```
Remove only `opacity-60`. The `text-on-surface-variant` token already provides sufficient contrast on `surface-container` backgrounds per MD3 spec.

**settings/page.tsx — line 155:**
The entire notifications section is wrapped in a `<div className="... opacity-60">`. Replace the opacity-60 treatment with a visual "coming soon" badge. Change:
```tsx
<div className="... opacity-60">
  {/* notifications content */}
</div>
```
To:
```tsx
<div className="...">
  {/* add a "בקרוב" badge on the section heading using:
      <span className="me-2 rounded-full bg-surface-variant px-2 py-0.5 text-xs text-on-surface-variant">בקרוב</span>
      placed inline after the section title */}
  {/* rest of notifications content unchanged */}
</div>
```
Remove `opacity-60` from the wrapper. Add the badge inline next to the notifications heading text. Do NOT restructure the section.

**astrology/readings/page.tsx — line 109:**
Change `text-on-surface-variant/60` → `text-on-surface-variant` on the character counter span.

**ARIA FIXES (A11Y-03):**

**coach/page.tsx — lines 165-188 (tab container and buttons):**
Add `role="tablist"` and `aria-label="ניווט מאמן"` to the container `<div>`:
```tsx
<div role="tablist" aria-label="ניווט מאמן" className="mt-4 flex gap-2 border-b border-outline-variant/20 pb-0">
```
Add `role="tab"` and `aria-selected={activeTab === 'chat'}` to the first tab button (שיחות).
Add `role="tab"` and `aria-selected={activeTab === 'journeys'}` to the second tab button (מסעות).

**coach/page.tsx — lines 262-269 (mobile sidebar toggle):**
Add `aria-label` to the icon-only toggle button:
```tsx
<Button
  variant="ghost"
  size="sm"
  aria-label={showSidebar ? 'סגור רשימת שיחות' : 'פתח רשימת שיחות'}
  onClick={() => setShowSidebar((prev) => !prev)}
>
  {showSidebar ? <X className="w-4 h-4" /> : <Menu className="w-4 h-4" />}
</Button>
```

**tools/document/page.tsx — line 240 (icon-only remove-file button):**
Add `aria-label="הסר קובץ"` to the button element.

DO NOT add `aria-label` to any `<TabsTrigger>` elements (shadcn Radix handles those automatically — adding aria-label would cause double-announcement).
  </action>
  <verify>
    <automated>cd mystiqor-build && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes with 0 TypeScript errors. `grep -n "opacity-60\|opacity-40" mystiqor-build/src/components/features/history/AnalysisCard.tsx` returns nothing (removed). `grep -n "role=\"tablist\"" mystiqor-build/src/app/(auth)/coach/page.tsx` returns the tablist container. `grep -n "aria-label" mystiqor-build/src/app/(auth)/coach/page.tsx` returns both the tablist aria-label and the mobile toggle aria-label.
  </done>
</task>

<task type="auto">
  <name>Task 3: Empty states with CTA for coach and history (UX-01)</name>
  <files>
    mystiqor-build/src/app/(auth)/coach/page.tsx
    mystiqor-build/src/app/(auth)/history/page.tsx
  </files>
  <action>
**coach/page.tsx — add zero-conversations empty state:**

The coach page has two distinct states to handle:
1. `conversations.length === 0` AND `!activeConversationId` → true zero state (first time user)
2. `conversations.length > 0` AND `!activeConversationId` → user has conversations but none selected

For state 1, replace or wrap the existing empty area (lines 278-288) with a conditional:
```tsx
{conversations.length === 0 && !activeConversationId ? (
  <EmptyState
    icon={<MessageCircle className="h-8 w-8" />}
    title="ברוכים הבאים למאמן"
    description="התחל שיחה ראשונה עם המאמן המיסטי שלך"
    action={{ label: 'התחל שיחה ראשונה', onClick: () => createMutation.mutate() }}
  />
) : !activeConversationId ? (
  /* existing "select conversation" state with QuickActions — keep as-is */
  <div className="flex flex-col items-center justify-center h-full gap-6 py-12">
    <MessageCircle className="w-16 h-16 text-primary/20" />
    <p className="text-on-surface-variant text-center font-body">
      בחר שיחה קיימת או לחץ על &quot;שיחה חדשה&quot; להתחיל
    </p>
    <QuickActions ... />
  </div>
) : (
  /* existing conversation view */
)}
```

Add `import { EmptyState } from '@/components/common/EmptyState'` if not already present.

**history/page.tsx — replace inline empty state div:**

Find the existing empty state block (lines 302-309):
```tsx
{isEmpty && !isError && (
  <div className="flex flex-col items-center justify-center py-16 text-center">
    <History className="h-12 w-12 text-on-surface-variant/40 mb-4" />
    <p className="font-body text-on-surface-variant">
      אין ניתוחים עדיין. התחל להשתמש בכלים כדי לראות היסטוריה.
    </p>
  </div>
)}
```

Replace with:
```tsx
{isEmpty && !isError && (
  <EmptyState
    icon={<History className="h-8 w-8" />}
    title="אין ניתוחים עדיין"
    description="השתמש בכלים כדי לייצר ניתוחים — הם יופיעו כאן"
    action={{ label: 'עבור לכלים', onClick: () => router.push('/tools') }}
  />
)}
```

Add `import { EmptyState } from '@/components/common/EmptyState'` if not already present. The `History` icon and `router` are already imported in this file — do not re-import them.

Note: Goals page already passes UX-01 — do NOT touch it.
  </action>
  <verify>
    <automated>cd mystiqor-build && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes with 0 TypeScript errors. `grep -n "EmptyState" mystiqor-build/src/app/(auth)/history/page.tsx` shows EmptyState import and usage. `grep -n "EmptyState" mystiqor-build/src/app/(auth)/coach/page.tsx` shows EmptyState import and usage with `conversations.length === 0` condition.
  </done>
</task>

<task type="auto">
  <name>Task 4: Mobile-responsive form grids + coach context limit (UX-02)</name>
  <files>
    mystiqor-build/src/app/(auth)/tools/astrology/page.tsx
    mystiqor-build/src/app/(auth)/tools/astrology/readings/page.tsx
    mystiqor-build/src/app/(auth)/tools/astrology/synastry/page.tsx
    mystiqor-build/src/app/api/coach/messages/route.ts
  </files>
  <action>
**FORM GRIDS — add `grid-cols-1` base with `sm:grid-cols-2` breakpoint:**

**astrology/page.tsx — line 235:**
`grid grid-cols-2 gap-3` → `grid grid-cols-1 sm:grid-cols-2 gap-3`
(affects: תאריך לידה + שעת לידה fields)

**astrology/page.tsx — line 253:**
`grid grid-cols-2 gap-3` → `grid grid-cols-1 sm:grid-cols-2 gap-3`
(affects: קו רוחב + קו אורך fields)

**astrology/readings/page.tsx — line 76:**
`grid grid-cols-2 gap-3` → `grid grid-cols-1 sm:grid-cols-2 gap-3`
(affects: חודש + שנה inputs)

**astrology/synastry/page.tsx — line 292:**
`grid grid-cols-2 gap-2` → `grid grid-cols-1 sm:grid-cols-2 gap-2`
(affects: קו רוחב + קו אורך in person form)

DO NOT change:
- `notifications/page.tsx` line 202 — that is a two-button action row, not a data form
- `GoalForm.tsx` line 269 — שמור/ביטול button pair, acceptable

**COACH CONTEXT LIMIT — POST handler only:**

**messages/route.ts — line ~200:**
In the POST handler (the LLM context assembly query), change:
`.limit(20)` → `.limit(10)`

This is the query that fetches `priorMessages` for injection into the OpenAI prompt. It is NOT the GET handler at line ~113 which fetches all messages for display — do not change that one. Read the file to confirm the exact line before editing.
  </action>
  <verify>
    <automated>cd mystiqor-build && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes with 0 TypeScript errors. `grep -n "grid-cols-2" mystiqor-build/src/app/(auth)/tools/astrology/page.tsx` shows only `sm:grid-cols-2` (not bare `grid-cols-2`). `grep -n "\.limit(" mystiqor-build/src/app/api/coach/messages/route.ts` shows `.limit(10)` in the POST context query and the GET display query is unchanged (or returns more than 10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (messages route) | POST body with conversationId crosses to LLM context builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | coach/messages POST — context limit | accept | Reducing limit(20)→limit(10) tightens the LLM context window; cannot expose prior messages beyond what the authenticated user owns (RLS enforced on coaching_messages) |
| T-32-02 | Spoofing | aria-label values | accept | aria-label is client-only markup, not a trust boundary; no auth or data concern |
| T-32-03| Tampering | EmptyState CTA onClick | accept | All onClick handlers route to existing mutations/router.push — no new surfaces introduced |
</threat_model>

<verification>
After all four tasks complete, run the full build and perform the manual checklist:

**Automated gate (must pass before manual):**
```bash
cd mystiqor-build && npm run build
```
Zero TypeScript errors required.

**Manual browser checklist (Chrome, RTL mode, Hebrew locale):**

1. RTL check — open /coach, /journal, /settings, /tools/document in Chrome DevTools. Switch to RTL mode. Verify icon spacing next to button labels looks correct (no squished or over-spaced icons).

2. Contrast check — open /history, click on any AnalysisCard. The confidence label at card footer must be clearly readable (not faded). Open /tools/astrology/readings — character counter must be clearly readable.

3. Screen reader check — open /coach, use VoiceOver or NVDA. Tab to the שיחות/מסעות tab row. Confirm announcement includes "tab, 1 of 2, selected" or equivalent. Tab to mobile menu icon — confirm it announces "פתח רשימת שיחות".

4. Empty state check — create a fresh test user (or clear history). Open /history — must see "אין ניתוחים עדיין" with "עבור לכלים" button. Open /coach with no prior conversations — must see "ברוכים הבאים למאמן" with "התחל שיחה ראשונה" button.

5. Mobile form check — open Chrome DevTools, set viewport to 375px width. Open /tools/astrology — date/time fields must stack vertically. Open /tools/astrology/synastry — lat/lng fields must stack vertically.
</verification>

<success_criteria>
1. `npm run build` passes with 0 TypeScript errors
2. Zero `ml-[0-9]`, `mr-[0-9]`, `pl-[0-9]`, `pr-[0-9]` in feature components (excluding ui/ and auto/symmetric utilities)
3. `opacity-60` removed from AnalysisCard confidence label and settings notifications wrapper
4. Coach tab buttons have `role="tab"` and `aria-selected`; mobile toggle has `aria-label`
5. History page uses `<EmptyState>` component with "עבור לכלים" CTA
6. Coach page uses `<EmptyState>` component for zero-conversation state
7. All four astrology form grids use `grid-cols-1 sm:grid-cols-2`
8. Coach messages POST handler uses `.limit(10)` for LLM context injection
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-accessibility-ux/32-01-SUMMARY.md` using the summary template.
</output>
