---
phase: 29-ui-rebuild-faithful-base44-design-recreation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
  - mystiqor-build/src/app/(auth)/dashboard/page.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "כל כרטיס כלי ב-HeroToolGrid מציג שם הכלי בעברית מתחת לתמונה"
    - "שם הכלי מופיע עם tool-card-fade overlay — כמו ב-ToolGrid"
    - "הברכה בדשבורד מציגה גרדיאנט סגול-ורוד על שם המשתמש בנאמנות ל-BASE44"
    - "כל הלוגיקה הפונקציונלית (Supabase queries, React Query, auth) לא השתנתה"
  artifacts:
    - path: "mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx"
      provides: "Tool card with name overlay using tool-card-fade pattern"
      contains: "tool-card-fade"
    - path: "mystiqor-build/src/app/(auth)/dashboard/page.tsx"
      provides: "Dashboard hero with BASE44-faithful purple-pink name gradient"
      contains: "from-purple-400 to-pink-400"
  key_links:
    - from: "mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx"
      to: "@/lib/constants/tools"
      via: "PrimaryTool interface — tool.name used for overlay text"
      pattern: "tool\\.name"
    - from: "mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx"
      to: "globals.css"
      via: "tool-card-fade utility class for gradient overlay"
      pattern: "tool-card-fade"
---

<objective>
Add tool name overlays to HeroToolCard (satisfying DASH-01/DASH-02) and align the dashboard hero greeting gradient to match BASE44's purple-to-pink palette.

Purpose: The HeroToolCards currently show only images with no tool name — users must guess which tool is which. Adding the name overlay makes each card self-explanatory. The hero gradient change aligns the greeting with the original BASE44 mystical purple-pink identity.
Output: HeroToolCard.tsx with name+description overlay, dashboard/page.tsx with BASE44-faithful hero gradient.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-rebuild-faithful-base44-design-recreation/29-RESEARCH.md

@mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
@mystiqor-build/src/components/features/dashboard/HeroToolGrid.tsx
@mystiqor-build/src/components/features/shared/ToolGrid.tsx
@mystiqor-build/src/lib/constants/tools.ts
@mystiqor-build/src/app/(auth)/dashboard/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mystiqor-build/src/lib/constants/tools.ts:
```typescript
export interface PrimaryTool {
  readonly id: string;
  readonly name: string;        // Hebrew name — used for the overlay
  readonly description: string; // Hebrew description — used for the overlay
  readonly href: string;
  readonly imagePath: string;
  readonly Icon: LucideIcon;
  readonly gradient: string;
}
```

From mystiqor-build/src/components/features/shared/ToolGrid.tsx (PROVEN PATTERN to replicate):
```tsx
// Lines 89-93 — tool-card-fade overlay + name/description:
<div className="absolute inset-0 tool-card-fade" />
<div className="absolute bottom-0 inset-x-0 px-4 pb-4 pt-2 text-center">
  <h3 className="font-headline font-bold text-foreground text-lg drop-shadow-[0_1px_3px_rgba(0,0,0,0.8)]">{tool.name}</h3>
  <p className="font-body text-sm text-muted-foreground mt-1 drop-shadow-[0_1px_2px_rgba(0,0,0,0.8)]">{tool.description}</p>
</div>
```

From mystiqor-build/src/app/globals.css:
```css
.tool-card-fade {
  background: linear-gradient(to bottom, transparent 20%, rgba(13,11,30,0.5) 50%, rgba(13,11,30,0.95) 75%, rgba(13,11,30,1) 100%);
}
```

From github-source/src/pages/Home.jsx (BASE44 hero gradient to replicate):
```jsx
<span className="font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400">{firstName}</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tool name overlay to HeroToolCard + align hero gradient</name>
  <files>mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx, mystiqor-build/src/app/(auth)/dashboard/page.tsx</files>
  <read_first>
    - mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
    - mystiqor-build/src/components/features/shared/ToolGrid.tsx (lines 89-93 for proven pattern)
    - mystiqor-build/src/lib/constants/tools.ts (PrimaryTool interface — has name and description fields)
    - mystiqor-build/src/app/(auth)/dashboard/page.tsx (lines 305-318 for hero section)
    - mystiqor-build/src/app/globals.css (search for "tool-card-fade" to confirm utility exists)
  </read_first>
  <action>
**File 1: HeroToolCard.tsx**

Add tool name and description overlay using the PROVEN tool-card-fade pattern from ToolGrid.tsx. The PrimaryTool interface already has `name` (Hebrew) and `description` fields — no type changes needed.

Inside the inner `<div className="w-full h-full relative flex items-center justify-center rounded-xl overflow-hidden">`, AFTER the existing hover overlay div (`<div className="absolute inset-0 bg-transparent group-hover:bg-white/5 ..."/>`), add these two new divs:

```tsx
{/* שכבת דעיכה לשם כלי */}
<div className="absolute inset-0 tool-card-fade" />
{/* שם ותיאור הכלי */}
<div className="absolute bottom-0 inset-x-0 px-2 pb-2 text-center">
  <p className="font-headline font-bold text-foreground text-xs sm:text-sm drop-shadow-[0_1px_3px_rgba(0,0,0,0.8)] leading-tight">
    {tool.name}
  </p>
  <p className="font-body text-[10px] sm:text-xs text-muted-foreground mt-0.5 drop-shadow-[0_1px_2px_rgba(0,0,0,0.8)] leading-tight line-clamp-1">
    {tool.description}
  </p>
</div>
```

Text sizing rationale: HeroToolCards are smaller than ToolGrid cards (6-column vs 4-column grid), so use `text-xs sm:text-sm` for name (vs ToolGrid's `text-lg`) and `text-[10px] sm:text-xs` for description (vs ToolGrid's `text-sm`). Use `line-clamp-1` on description to prevent overflow on small cards.

Do NOT change:
- The Link, motion.div, Image, or any other existing elements
- The PrimaryTool interface or tools.ts
- The aspect-[4/5] ratio, rounded-2xl, or outer card structure
- Any framer-motion props or useReducedMotion logic

**File 2: dashboard/page.tsx**

In the hero section (around line 312), change the name gradient from `text-gradient-gold` to the BASE44-faithful purple-to-pink gradient:

Replace:
```tsx
<span className="font-bold text-gradient-gold">
```

With:
```tsx
<span className="font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400">
```

This is a single class replacement on one `<span>` element. Do NOT change anything else in dashboard/page.tsx — no Supabase queries, no React Query hooks, no stat cards, no chart components, no imports, no types.

Also in the hero badge (around line 306), change the badge to use BASE44's purple accent:

Replace:
```tsx
<div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary/10 border border-primary/20 text-primary text-sm font-medium">
```

With:
```tsx
<div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-purple-500/10 border border-purple-500/20 text-purple-300 text-sm font-medium">
```
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tool-card-fade" mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
    - grep -q "tool.name" mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
    - grep -q "tool.description" mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx
    - grep -q "from-purple-400 to-pink-400" mystiqor-build/src/app/(auth)/dashboard/page.tsx
    - grep -q "bg-purple-500/10" mystiqor-build/src/app/(auth)/dashboard/page.tsx
    - grep -q "text-purple-300" mystiqor-build/src/app/(auth)/dashboard/page.tsx
    - TypeScript compilation passes with zero errors
    - HeroToolCard still exports function HeroToolCard
    - No changes to any imports, types, hooks, or Supabase queries in dashboard/page.tsx
  </acceptance_criteria>
  <done>
    - Every HeroToolCard renders tool.name in a text-xs/sm overlay at the card bottom with tool-card-fade gradient
    - Every HeroToolCard renders tool.description in text-[10px]/xs below the name
    - Dashboard hero greeting shows purple-to-pink gradient on user's first name (from-purple-400 to-pink-400)
    - Dashboard hero badge uses purple-500 accent colors matching BASE44
    - Zero TypeScript errors
    - All existing functionality preserved (links, animations, data fetching)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — HeroToolCard overlays + hero gradient</name>
  <files>mystiqor-build/src/components/features/dashboard/HeroToolCard.tsx, mystiqor-build/src/app/(auth)/dashboard/page.tsx</files>
  <action>
Human verifies the visual output of Task 1: tool name overlays on all 6 HeroToolCards, plus BASE44-faithful purple-to-pink gradient on the greeting name and badge.

Steps:
1. Run: cd mystiqor-build and npm run dev
2. Open http://localhost:3000/dashboard
3. Check all 6 HeroToolCards show tool name + description at card bottom with gradient fade
4. Check hero greeting name shows purple-to-pink gradient (not gold)
5. Check badge has purple accent
6. Test hover animations and card navigation
7. Test mobile responsive view (2-column cards)
  </action>
  <verify>Human visual inspection confirms all overlay text is readable, gradient is correct, and cards function properly</verify>
  <done>User approves visual output of HeroToolCard overlays and hero gradient changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a pure visual/CSS change adding text elements that already exist in the PrimaryTool data constants. No new data inputs, no API calls, no form submissions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I (Info Disclosure) | HeroToolCard overlay | accept | Tool names are public information already visible in sidebar and tool pages — no sensitive data exposed |
| T-29-02 | T (Tampering) | dashboard hero classes | accept | No change to data flow or logic — purely visual class changes on existing elements |
</threat_model>

<verification>
1. TypeScript compilation: `cd mystiqor-build && npx tsc --noEmit` passes with 0 errors
2. HeroToolCard.tsx contains `tool-card-fade`, `tool.name`, and `tool.description`
3. dashboard/page.tsx contains `from-purple-400 to-pink-400` (not `text-gradient-gold`) for the name
4. dashboard/page.tsx contains `bg-purple-500/10` for the hero badge
5. All 6 PrimaryTool names render correctly in the overlay
6. Card links still navigate to correct tool pages
7. No regressions in dashboard data (stat cards, charts, mood trends still load)
</verification>

<success_criteria>
- All 6 HeroToolCards display tool name and description with fade overlay
- Hero greeting uses BASE44's purple-to-pink gradient on the name
- Zero TypeScript errors, zero functional regressions
- Human verification confirms cards are readable and visually balanced
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-rebuild-faithful-base44-design-recreation/29-01-SUMMARY.md`
</output>
