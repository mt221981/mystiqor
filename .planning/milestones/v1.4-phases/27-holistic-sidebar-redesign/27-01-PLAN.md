---
phase: 27-holistic-sidebar-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mystiqor-build/src/components/layouts/Sidebar.tsx
autonomous: false
requirements:
  - SIDE-01
  - SIDE-02
  - SIDE-03

must_haves:
  truths:
    - "הסיידבר נראה כחלק אורגני מהאפליקציה — רקע שקוף עם blur שמתמזג עם הרקע הכוכבי"
    - "הלוגו משולב טבעי בראש הסיידבר — קומפקטי (w-40), עם blend-luminous ו-mask vignette"
    - "קטגוריות מופרדות בקווים עדינים (border-outline-variant/5) ולא בגבולות כבדים"
    - "פריט פעיל בולט עם bg-primary/10 text-primary ופס צד (side bar indicator)"
    - "hover על פריטים מראה bg-white/5 עם מעבר חלק"
    - "כל הקוד הפונקציונלי (localStorage, toggles, UsageBar) נשמר ללא שינוי"
  artifacts:
    - path: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      provides: "Holistic mystical sidebar with refined visual design"
      contains: "bg-surface/60"
  key_links:
    - from: "mystiqor-build/src/components/layouts/Sidebar.tsx"
      to: "layout-client.tsx"
      via: "Sidebar component export — unchanged API"
      pattern: "export function Sidebar"
---

<objective>
Redesign the sidebar to feel like an organic part of the mystical cosmic app — not a separate pasted component. Apply semi-transparent blur background, refined category separators, active/hover states with purple accents, and compact logo integration.

Purpose: The sidebar currently looks like a distinct panel bolted onto the app. This redesign merges it visually with the cosmic dark theme, making navigation feel like part of the mystical journey.
Output: Single modified Sidebar.tsx with holistic mystical styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-holistic-sidebar-redesign/27-CONTEXT.md

@mystiqor-build/src/components/layouts/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic sidebar visual redesign</name>
  <files>mystiqor-build/src/components/layouts/Sidebar.tsx</files>
  <action>
Modify ONLY the Tailwind classes and minor structural elements in Sidebar.tsx. Do NOT change any logic (NAV_SECTIONS data, localStorage, toggleSection, usePathname, useSubscription, UsageBar calculations). All functional code stays identical.

**1. Aside container (the root `<aside>`)** — per SIDE-01:
- Replace `bg-surface/95 glass-panel` with `bg-surface/60 backdrop-blur-md`
- Keep `border-e border-outline-variant/10` as is
- Keep `flex h-full w-80 flex-col`

**2. Logo area (the Link wrapping Image)** — per SIDE-02:
- Change Image width to `w-40` (was `w-44`) and keep `h-auto object-contain blend-luminous`
- Keep the mask-image/WebkitMaskImage radial-gradient vignette style exactly as is
- Change padding to `py-4 px-3` (was `px-4 py-5`) — more compact
- Keep the subtitle "המסע המיסטי שלך" as is
- Keep `border-b border-primary/15` separator and `hover:bg-surface-container-high/30 transition-colors`

**3. Category section headers (CollapsibleSection button)** — per SIDE-01/SIDE-03:
- Replace `text-lg font-bold uppercase` with `text-sm font-semibold uppercase tracking-wider`
- Replace `text-gold hover:text-gold-bright font-label border-b border-gold/10 pb-2` with `text-on-surface-variant/60 hover:text-on-surface-variant/80 font-label`
- Change padding from `px-3 py-3` to `px-3 py-2`
- Keep `flex w-full items-center justify-between` and `transition-colors duration-200`
- Keep `aria-expanded={isOpen}`

**4. Category separator** — per SIDE-01:
- On the wrapping `<div className="mb-3">` of each CollapsibleSection, add a bottom border: change `mb-3` to `mb-1 border-b border-outline-variant/5 pb-2`
- This creates subtle thin separators between categories

**5. Chevron icons** — make smaller to match refined headers:
- Change `h-5 w-5` to `h-4 w-4`

**6. Nav item links (inside CollapsibleSection)** — per SIDE-03:
- **Active state**: Replace the current active classes:
  `bg-primary-container/20 text-primary shadow-sm shadow-primary-container/20 border border-primary/10`
  with:
  `bg-primary/10 text-primary border-s-2 border-s-primary`
  (This adds the side bar indicator via border-s-2 on the start side, and uses bg-primary/10 per decision)
- **Inactive/hover state**: Replace:
  `text-on-surface-variant hover:bg-surface-container-high/60 hover:text-on-surface`
  with:
  `text-on-surface-variant hover:bg-white/5 hover:text-on-surface`
  (Uses bg-white/5 per decision)
- Change icon size from `h-7 w-7` to `h-5 w-5` — less bulky, more refined
- Change link padding from `gap-4 rounded-lg px-3 py-3` to `gap-3 rounded-md px-3 py-2` — tighter spacing
- Change `text-lg font-medium` to `text-sm font-medium` — smaller nav text for refinement

**7. Nav list container** — tighten spacing:
- In the nav element: keep `flex flex-col gap-1 px-2`

**8. UsageBar** — keep ALL logic and calculations identical. Only minor class refinement:
- No changes needed to UsageBar — it already uses outline-variant/10 borders and fits the scheme

**9. Scrollbar area** — keep `scrollbar-thin` on the overflow container

IMPORTANT: Do NOT change:
- NAV_SECTIONS array (labels, hrefs, icons)
- useState/useCallback/useEffect logic
- localStorage persistence
- UsageBar component logic
- Any TypeScript types or interfaces
- Import statements
- Component export signature
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
- Sidebar.tsx compiles with zero TypeScript errors
- aside uses bg-surface/60 backdrop-blur-md (not bg-surface/95 glass-panel)
- Logo uses w-40 with py-4 px-3 padding
- Category headers use text-sm font-semibold with outline-variant separators
- Active nav items show bg-primary/10 text-primary with border-s-2 border-s-primary side bar
- Hover shows bg-white/5
- Nav item icons are h-5 w-5, text is text-sm
- All functional logic (localStorage, toggles, UsageBar) unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Holistic sidebar redesign — transparent blur background merging with cosmic theme, refined category separators, purple active state with side bar indicator, compact logo, smaller refined nav items</what-built>
  <how-to-verify>
    1. Run the dev server: cd mystiqor-build && npm run dev
    2. Open http://localhost:3000 in browser
    3. Check sidebar background — should see the cosmic background bleeding through with blur (semi-transparent, not opaque)
    4. Check logo area — should be compact (w-40), with luminous blend and vignette mask
    5. Check category headers — should be small, uppercase, subtle text (not bold gold)
    6. Check category separators — thin subtle lines between sections (barely visible)
    7. Hover over nav items — should see bg-white/5 (very subtle white highlight)
    8. Click a tool to navigate — active item should show purple background (bg-primary/10) with a purple side bar on the start edge
    9. Expand/collapse categories — should still work with localStorage persistence
    10. Check bottom usage bar — should still display correctly
    11. Overall feel: sidebar should look like it belongs to the app, not pasted on
  </how-to-verify>
  <resume-signal>Type "approved" or describe what needs adjustment</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this is a pure CSS/styling change on an existing client component. No data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | T (Tampering) | Sidebar nav links | accept | No change to href values or navigation logic — purely visual |
| T-27-02 | D (DoS) | backdrop-blur-md | accept | CSS blur is GPU-accelerated, negligible performance impact on modern browsers |
</threat_model>

<verification>
1. TypeScript compilation passes with zero errors
2. Sidebar renders with transparent blur background (bg-surface/60 backdrop-blur-md)
3. Logo area is compact (w-40, py-4 px-3) with blend-luminous preserved
4. Active state shows bg-primary/10 text-primary with border-s-2 side indicator
5. Hover shows bg-white/5
6. Category separators use border-outline-variant/5
7. All existing functionality preserved (localStorage, toggles, UsageBar, navigation)
</verification>

<success_criteria>
- The sidebar visually merges with the cosmic app theme (transparent + blur)
- Navigation active/hover states are clear and purple-accented
- Logo is naturally integrated, not oversized
- Category separators are subtle, not heavy
- Zero TypeScript errors, zero regressions in navigation functionality
- Human verification confirms the sidebar feels "organic" and "mystical"
</success_criteria>

<output>
After completion, create `.planning/phases/27-holistic-sidebar-redesign/27-01-SUMMARY.md`
</output>
