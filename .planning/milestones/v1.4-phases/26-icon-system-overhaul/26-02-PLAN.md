---
phase: 26-icon-system-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/app/(auth)/tools/numerology/page.tsx
  - src/app/(auth)/tools/astrology/page.tsx
  - src/app/(auth)/tools/tarot/page.tsx
  - src/app/(auth)/tools/dream/page.tsx
  - src/app/(auth)/tools/palmistry/page.tsx
  - src/app/(auth)/tools/graphology/page.tsx
  - src/app/(auth)/tools/drawing/page.tsx
  - src/app/(auth)/tools/human-design/page.tsx
  - src/app/(auth)/tools/compatibility/page.tsx
  - src/app/(auth)/tools/personality/page.tsx
  - src/app/(auth)/tools/career/page.tsx
  - src/app/(auth)/tools/relationships/page.tsx
  - src/app/(auth)/tools/document/page.tsx
  - src/app/(auth)/tools/timing/page.tsx
  - src/app/(auth)/tools/synthesis/page.tsx
  - src/app/(auth)/tools/daily-insights/page.tsx
  - src/app/(auth)/tools/page.tsx
autonomous: true
requirements:
  - ICON-02
  - ICON-03

must_haves:
  truths:
    - "Every tool page header shows the correct Lucide icon matching the sidebar"
    - "No tool page imports from react-icons/gi"
    - "Icons on tool pages match their centralized mapping in tool-icons.ts"
    - "Secondary/decorative Gi icons on tool pages also replaced with Lucide equivalents"
  artifacts:
    - path: "src/app/(auth)/tools/numerology/page.tsx"
      provides: "Hash icon for numerology header"
      contains: "from 'lucide-react'"
    - path: "src/app/(auth)/tools/astrology/page.tsx"
      provides: "Orbit icon for astrology header"
      contains: "from 'lucide-react'"
    - path: "src/app/(auth)/tools/page.tsx"
      provides: "BookMarked icon for tools index"
      contains: "from 'lucide-react'"
  key_links:
    - from: "src/app/(auth)/tools/*/page.tsx"
      to: "lucide-react"
      via: "direct Lucide icon imports for headers"
      pattern: "import.*from 'lucide-react'"
---

<objective>
Migrate all 17 tool page files from react-icons/gi to lucide-react icons.

Purpose: Ensures tool page headers display the same Lucide icons as the sidebar (ICON-02), completing the tool-level icon consistency. Each tool page has 1-3 Gi imports for its header icon and optional decorative icons.

Output: 17 tool page files migrated, zero Gi imports in tools/ directory.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-icon-system-overhaul/26-CONTEXT.md
@.planning/phases/26-icon-system-overhaul/26-RESEARCH.md
@.planning/phases/26-icon-system-overhaul/26-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: centralized mapping created -->
From src/lib/constants/tool-icons.ts (created in Plan 01):
```typescript
import type { LucideIcon } from 'lucide-react';
export const TOOL_ICONS: Record<string, LucideIcon>;
export function getToolIcon(toolId: string): LucideIcon;
```

Tool pages pass icons as JSX to StandardSectionHeader:
```typescript
// Pattern: <StandardSectionHeader icon={<IconComponent className="h-6 w-6" />} title="..." />
```
Tool pages should import icons directly from lucide-react (not from tool-icons.ts) since they need JSX elements, not component references. The centralized mapping is for programmatic lookups (Sidebar, ToolGrid).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 9 tool pages (numerology through human-design)</name>
  <files>src/app/(auth)/tools/numerology/page.tsx, src/app/(auth)/tools/astrology/page.tsx, src/app/(auth)/tools/tarot/page.tsx, src/app/(auth)/tools/dream/page.tsx, src/app/(auth)/tools/palmistry/page.tsx, src/app/(auth)/tools/graphology/page.tsx, src/app/(auth)/tools/drawing/page.tsx, src/app/(auth)/tools/human-design/page.tsx, src/app/(auth)/tools/compatibility/page.tsx</files>
  <action>
For each file: remove the `react-icons/gi` import line, add the Lucide replacement to the existing `lucide-react` import (or create one if none exists), and replace every Gi icon usage in JSX.

**File-by-file mapping:**

**1. numerology/page.tsx:**
- Remove: `import { GiAbacus } from 'react-icons/gi'`
- Add `Hash` to lucide-react import (file already imports ChevronDown, ChevronUp, Heart from lucide-react)
- Replace: `GiAbacus` -> `Hash` in all JSX usages

**2. astrology/page.tsx:**
- Remove: `import { GiAstrolabe } from 'react-icons/gi'`
- Add `Orbit` to lucide-react import
- Replace: `GiAstrolabe` -> `Orbit` in all JSX usages

**3. tarot/page.tsx:**
- Remove: `import { GiCardRandom, GiCrystalBall } from 'react-icons/gi'`
- Add `Layers, Gem` to lucide-react import (Gem for GiCrystalBall secondary usage, distinct from Sparkles)
- Replace: `GiCardRandom` -> `Layers`, `GiCrystalBall` -> `Gem` in all JSX usages

**4. dream/page.tsx:**
- Remove: `import { GiDreamCatcher } from 'react-icons/gi'`
- Add `Moon` to lucide-react import
- Replace: `GiDreamCatcher` -> `Moon` in all JSX usages

**5. palmistry/page.tsx:**
- Remove: `import { GiHand } from 'react-icons/gi'`
- Add `Hand` to lucide-react import
- Replace: `GiHand` -> `Hand` in all JSX usages

**6. graphology/page.tsx:**
- Remove: `import { GiQuillInk } from 'react-icons/gi'`
- Add `PenTool` to lucide-react import
- Replace: `GiQuillInk` -> `PenTool` in all JSX usages

**7. drawing/page.tsx:**
- Remove: `import { GiPaintBrush } from 'react-icons/gi'`
- Add `Palette` to lucide-react import
- Replace: `GiPaintBrush` -> `Palette` in all JSX usages

**8. human-design/page.tsx:**
- Remove: `import { GiDna1 } from 'react-icons/gi'`
- Add `Dna` to lucide-react import
- Replace: `GiDna1` -> `Dna` in all JSX usages

**9. compatibility/page.tsx:**
- Remove: `import { GiHearts, GiYinYang } from 'react-icons/gi'`
- Add `Heart, CircleDot` to lucide-react import
- Replace: `GiHearts` -> `Heart`, `GiYinYang` -> `CircleDot` in all JSX usages
- PITFALL: If this file already imports `Heart` from lucide-react, do NOT duplicate. Check before adding.

For ALL files: preserve everything else -- structure, comments, logic, other imports. Only touch the icon import line and icon usages in JSX.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -r "react-icons/gi" src/app/\(auth\)/tools/numerology/ src/app/\(auth\)/tools/astrology/page.tsx src/app/\(auth\)/tools/tarot/ src/app/\(auth\)/tools/dream/ src/app/\(auth\)/tools/palmistry/ src/app/\(auth\)/tools/graphology/ src/app/\(auth\)/tools/drawing/page.tsx src/app/\(auth\)/tools/human-design/ src/app/\(auth\)/tools/compatibility/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>9 tool pages migrated. Zero react-icons/gi imports in these files. TypeScript passes. Each tool page header shows the correct Lucide icon.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 8 remaining tool pages (personality through tools/page.tsx)</name>
  <files>src/app/(auth)/tools/personality/page.tsx, src/app/(auth)/tools/career/page.tsx, src/app/(auth)/tools/relationships/page.tsx, src/app/(auth)/tools/document/page.tsx, src/app/(auth)/tools/timing/page.tsx, src/app/(auth)/tools/synthesis/page.tsx, src/app/(auth)/tools/daily-insights/page.tsx, src/app/(auth)/tools/page.tsx</files>
  <action>
Continue migration for the remaining 8 tool page files.

**File-by-file mapping:**

**10. personality/page.tsx:**
- Remove: `import { GiBrain } from 'react-icons/gi'`
- Add `Brain` to lucide-react import
- Replace: `GiBrain` -> `Brain` in all JSX usages

**11. career/page.tsx:**
- Remove: `import { GiBriefcase, GiStarFormation } from 'react-icons/gi'`
- Add `Compass, Star` to lucide-react import
- Replace: `GiBriefcase` -> `Compass`, `GiStarFormation` -> `Star` in all JSX usages

**12. relationships/page.tsx:**
- Remove: `import { GiTwoCoins } from 'react-icons/gi'`
- Add `Users` to lucide-react import
- Replace: `GiTwoCoins` -> `Users` in all JSX usages

**13. document/page.tsx:**
- Remove: `import { GiScrollQuill } from 'react-icons/gi'`
- Add `FileSearch` to lucide-react import
- Replace: `GiScrollQuill` -> `FileSearch` in all JSX usages

**14. timing/page.tsx:**
- Remove: `import { GiHourglass } from 'react-icons/gi'`
- Add `Clock` to lucide-react import
- Replace: `GiHourglass` -> `Clock` in all JSX usages

**15. synthesis/page.tsx:**
- Remove: `import { GiAllSeeingEye } from 'react-icons/gi'`
- Add `Merge` to lucide-react import
- Replace: `GiAllSeeingEye` -> `Merge` in all JSX usages

**16. daily-insights/page.tsx:**
- Remove: `import { GiStarSwirl } from 'react-icons/gi'`
- Add `Sun` to lucide-react import
- Replace: `GiStarSwirl` -> `Sun` in all JSX usages

**17. tools/page.tsx (tools index):**
- Remove: `import { GiSpellBook } from 'react-icons/gi'`
- Add `BookMarked` to lucide-react import
- Replace: `GiSpellBook` -> `BookMarked` in all JSX usages

For ALL files: preserve everything else -- structure, comments, logic, other imports. Only touch the icon import line and icon usages in JSX.
  </action>
  <verify>
    <automated>cd mystiqor-build && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -rn "react-icons/gi" src/app/\(auth\)/tools/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 17 tool pages migrated. Zero react-icons/gi imports in entire src/app/(auth)/tools/ tree. TypeScript passes. Every tool page header uses correct Lucide icon matching sidebar mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. Icon component swaps only -- no user input, data flow, or API changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | Icon imports | accept | Same as Plan 01 -- icons bundled from npm lockfile at build time |
</threat_model>

<verification>
```bash
cd mystiqor-build

# Zero Gi imports in all tool pages
grep -r "react-icons/gi" src/app/\(auth\)/tools/ 2>/dev/null | wc -l
# Expected: 0

# TypeScript compiles cleanly
npx tsc --noEmit
# Expected: 0 errors
```
</verification>

<success_criteria>
1. All 17 tool page files use lucide-react icons (not react-icons/gi)
2. Each tool page header icon matches the sidebar mapping from Plan 01
3. Secondary/decorative icons (GiCrystalBall in tarot, GiYinYang in compatibility, GiStarFormation in career) replaced with appropriate Lucide equivalents
4. `npx tsc --noEmit` passes with zero errors
5. `grep -r "react-icons/gi" src/app/(auth)/tools/` returns empty
</success_criteria>

<output>
After completion, create `.planning/phases/26-icon-system-overhaul/26-02-SUMMARY.md`
</output>
