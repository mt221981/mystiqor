---
phase: 01-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/numerology/gematria.ts
  - src/services/numerology/calculations.ts
  - src/services/numerology/compatibility.ts
  - src/services/analysis/rule-engine.ts
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "calculateGematria('שלום') returns 376"
    - "reduceToSingleDigit(11) returns 11 (master number preserved)"
    - "calculateLifePath('1990-05-15') returns 3"
    - "evaluateCondition uses === not == (strict operators throughout)"
    - "All 4 files compile with tsc --noEmit 0 errors"
  artifacts:
    - path: "src/services/numerology/gematria.ts"
      provides: "GEMATRIA constant, cleanHebrewText, calculateGematria"
      exports: ["GEMATRIA", "cleanHebrewText", "calculateGematria"]
    - path: "src/services/numerology/calculations.ts"
      provides: "reduceToSingleDigit, calculateLifePath, calculateNumerologyNumbers"
      exports: ["reduceToSingleDigit", "calculateLifePath", "calculateNumerologyNumbers"]
    - path: "src/services/numerology/compatibility.ts"
      provides: "COMPATIBILITY_MATRIX, calculateNumerologyCompatibility"
      exports: ["COMPATIBILITY_MATRIX", "calculateNumerologyCompatibility"]
    - path: "src/services/analysis/rule-engine.ts"
      provides: "RuleOperator type, evaluateCondition, applyRules"
      exports: ["RuleOperator", "evaluateCondition", "applyRules"]
  key_links:
    - from: "src/services/numerology/calculations.ts"
      to: "src/services/numerology/gematria.ts"
      via: "import { calculateGematria, cleanHebrewText, HEBREW_VOWELS }"
      pattern: "import.*gematria"
    - from: "src/services/numerology/compatibility.ts"
      to: "src/services/numerology/calculations.ts"
      via: "import { calculateNumerologyNumbers }"
      pattern: "import.*calculations"
---

<objective>
Migrate GEM 2 (Hebrew gematria + numerology) and GEM 3 (rule engine) to typed TypeScript service modules.

Purpose: These are pure calculation functions — no DB, no React, no network. They run in 0 dependencies and can be tested immediately. They are also the first files other services (LLM prompts, compatibility tool) will import.

Output: 4 typed service files with JSDoc Hebrew comments, strict TypeScript, zero `any`, all exported for use by API routes in Wave 7.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OWNER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
@02b_GEMS.md
@src/types/analysis.ts
@src/types/numerology.ts
</context>

<interfaces>
<!-- Source: 02b_GEMS.md — GEM 2 exact values to preserve -->
GEMATRIA constant (verified values):
- א=1, ב=2, ג=3, ד=4, ה=5, ו=6, ז=7, ח=8, ט=9
- י=10, כ=20, ך=20 (final kaf), ל=30, מ=40, ם=40 (final mem), נ=50, ן=50 (final nun)
- ס=60, ע=70, פ=80, ף=80 (final pe), צ=90, ץ=90 (final tsadi), ק=100, ר=200, ש=300, ת=400

HEBREW_VOWELS = ['א', 'ה', 'ו', 'י', 'ע']

COMPATIBILITY_MATRIX (from 02b_GEMS.md):
```
1: { 1:70, 2:85, 3:90, 4:60, 5:95, 6:75, 7:55, 8:80, 9:65, 11:80, 22:70, 33:75 }
2: { 1:85, 2:80, 3:70, 4:90, 5:60, 6:95, 7:75, 8:85, 9:80, 11:90, 22:85, 33:95 }
```
Full matrix in source — implement all 12x12 values from 02b_GEMS.md.

From src/types/numerology.ts (use exact interface names):
```typescript
export interface NumerologyResult { ... }
export interface GematriaResult { ... }
```

Rule operators from 02b_GEMS.md GEM 3:
'equals' | 'not_equals' | 'greater_than' | 'less_than' | 'greater_or_equal' | 'less_or_equal' | 'contains' | 'in'
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate GEM 2 — gematria.ts and calculations.ts</name>
  <read_first>
    - D:/AI_projects/mystiqor-build/02b_GEMS.md (GEM 2 section — exact GEMATRIA values, cleanHebrewText, calculateGematria, reduceToSingleDigit, calculateLifePath, calculateNumerologyNumbers)
    - D:/AI_projects/mystiqor-build/src/types/numerology.ts (NumerologyResult interface)
    - D:/AI_projects/mystiqor-build/tests/services/numerology.test.ts (tests that must pass)
    - D:/AI_projects/mystiqor-build/.planning/phases/01-core-infrastructure/01-RESEARCH.md (§ Pitfall 2 about normalize, §Code Examples for GEM 2)
  </read_first>
  <files>
    src/services/numerology/gematria.ts,
    src/services/numerology/calculations.ts
  </files>
  <behavior>
    - Test: calculateGematria('שלום') === 376 (ש=300+ל=30+ו=6+ם=40)
    - Test: cleanHebrewText('שָׁלוֹם') === 'שלום' (niqqud stripped, unicode range u0591-u05C7)
    - Test: cleanHebrewText('דוד  לוי') === 'דודלוי' (spaces removed)
    - Test: reduceToSingleDigit(11) === 11 (master number preserved)
    - Test: reduceToSingleDigit(22) === 22 (master number preserved)
    - Test: reduceToSingleDigit(33) === 33 (master number preserved)
    - Test: reduceToSingleDigit(29) === 2 (29→11→2, but 11 is master, so actually 29→11 but 11 is master... check source: 29: 2+9=11, and 11 IS master, so reduceToSingleDigit(29)=11 NOT 2)
    - CORRECTION: reduceToSingleDigit checks BEFORE the while loop — if result of sum IS a master number, return it. Source: `if (num === 11 || num === 22 || num === 33) return num` applies at TOP of each iteration.
    - Test: calculateLifePath('1990-05-15') — split by '-', reduce each part, sum them
  </behavior>
  <action>
Create `src/services/numerology/gematria.ts`:
- Export `GEMATRIA: Readonly<Record<string, number>>` with ALL 27 Hebrew letter values from GEM 2 (including final forms ך ם ן ף ץ)
- Export `HEBREW_VOWELS: readonly string[]` = ['א', 'ה', 'ו', 'י', 'ע']
- Export `cleanHebrewText(text: string): string` — removes niqqud (regex /[\u0591-\u05C7]/g), Hebrew punctuation /[״׳־]/g, spaces /\s+/g
- Export `calculateGematria(text: string): number` — sums GEMATRIA[char] ?? 0 for each char in text
- All functions: JSDoc Hebrew comment explaining what + why
- File must be under 100 lines

Create `src/services/numerology/calculations.ts`:
- Import `{ calculateGematria, cleanHebrewText, HEBREW_VOWELS }` from `./gematria`
- Export `reduceToSingleDigit(num: number): number`:
  - If num === 11 || num === 22 || num === 33, return num immediately (checked at EACH iteration, not just start)
  - While loop: sum digits
  - Check master number after each sum — if result is 11/22/33, stop and return it
- Export `calculateLifePath(birthDate: string): number`:
  - Split 'YYYY-MM-DD' by '-', get [year, month, day] as numbers
  - Reduce each part individually, then sum all three, then reduce total
- Export `calculateNumerologyNumbers(person: { fullName: string; birthDate: string })`:
  - Returns: { name, life_path, destiny, soul } matching NumerologyResult interface
  - destiny = reduceToSingleDigit(calculateGematria(cleanHebrewText(fullName)))
  - soul = vowels only: filter chars where HEBREW_VOWELS.includes(char), then calculateGematria
  - personality = consonants only: filter chars where !HEBREW_VOWELS.includes(char)
  - personal_year = reduceToSingleDigit(day + month + currentYear)
  </action>
  <verify>
    <automated>cd D:/AI_projects/mystiqor-build && npx vitest run tests/services/numerology.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 7 tests in numerology.test.ts pass; gematria.ts and calculations.ts compile with 0 tsc errors; no `any` types; all functions have Hebrew JSDoc</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate GEM 2 compatibility.ts and GEM 3 rule-engine.ts</name>
  <read_first>
    - D:/AI_projects/mystiqor-build/02b_GEMS.md (GEM 2 COMPATIBILITY_MATRIX section; GEM 3 evaluateCondition section)
    - D:/AI_projects/mystiqor-build/.planning/phases/01-core-infrastructure/01-RESEARCH.md (§Code Examples GEM 3 — strict === rule)
    - D:/AI_projects/mystiqor-build/tests/services/rule-engine.test.ts (tests that must pass)
    - D:/AI_projects/mystiqor-build/src/types/analysis.ts (InsightType, Insight, Provenance interfaces)
  </read_first>
  <files>
    src/services/numerology/compatibility.ts,
    src/services/analysis/rule-engine.ts
  </files>
  <behavior>
    - rule-engine: evaluateCondition(5, 'equals', 5) === true
    - rule-engine: evaluateCondition('Aries', 'equals', 'Aries') === true
    - rule-engine: evaluateCondition('Aries', 'in', ['Aries', 'Leo']) === true
    - rule-engine: evaluateCondition(5, 'greater_than', 3) === true
    - rule-engine: evaluateCondition('Aries Sun', 'contains', 'Aries') === true
    - rule-engine: unknown operator returns false (no throw)
    - compatibility: overallScore = lifePathScore * 0.4 + destinyScore * 0.3 + soulScore * 0.3
  </behavior>
  <action>
Create `src/services/numerology/compatibility.ts`:
- Import `{ calculateNumerologyNumbers }` from `./calculations`
- Export `COMPATIBILITY_MATRIX: Readonly<Record<number, Record<number, number>>>`:
  - Full 12x12 matrix from 02b_GEMS.md — all entries for keys 1-9, 11, 22, 33
  - Exact numeric values from source — DO NOT invent values
- Export interface `CompatibilityResult { person1: string; person2: string; life_path_score: number; destiny_score: number; soul_score: number; overall_score: number; description: string }`
- Export `calculateNumerologyCompatibility(person1: {...}, person2: {...}): CompatibilityResult`:
  - Calculate numerology for both persons
  - Look up COMPATIBILITY_MATRIX[p1.lifePath][p2.lifePath] for each dimension
  - overallScore = Math.round(lifePathScore * 0.4 + destinyScore * 0.3 + soulScore * 0.3)
  - description: Hebrew string based on overall score ranges (≥80: "תאימות גבוהה מאוד", ≥60: "תאימות טובה", <60: "תאימות מאתגרת")

Create `src/services/analysis/rule-engine.ts`:
- Export `type RuleOperator = 'equals' | 'not_equals' | 'greater_than' | 'less_than' | 'greater_or_equal' | 'less_or_equal' | 'contains' | 'in'`
- Export interface `RuleCondition { feature_key: string; operator: RuleOperator; value: string | number | string[] }`
- Export interface `RuleMatch { rule_id: string; insight_template: Record<string, unknown>; weight: number; base_confidence: number; sources: string[]; tags: string[] }`
- Export `evaluateCondition(featureValue: string | number, operator: RuleOperator, conditionValue: string | number | string[]): boolean`:
  - Use STRICT === throughout — no == anywhere
  - For numeric comparisons: `const nf = typeof featureValue === 'number' ? featureValue : parseFloat(String(featureValue))`
  - For 'equals': `featureValue === conditionValue` — if both strings match OR both numbers match
  - For 'in': `Array.isArray(conditionValue) && conditionValue.includes(featureValue as string)`
  - Default: return false (no throw)
- Export `applyRules(features: Record<string, string | number>, rules: RuleMatch[]): { matched: RuleMatch[]; topInsights: RuleMatch[] }`:
  - Filter rules where evaluateCondition matches
  - Sort by weight * base_confidence DESC
  - topInsights = first 3
  </action>
  <verify>
    <automated>cd D:/AI_projects/mystiqor-build && npx vitest run tests/services/rule-engine.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 10 tests in rule-engine.test.ts pass; compatibility.ts and rule-engine.ts compile with 0 tsc errors; no == operators (only ===); no `any` types</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd D:/AI_projects/mystiqor-build && npx vitest run tests/services/ --reporter=verbose` — all tests pass
2. `npx tsc --noEmit` — 0 errors
3. `grep -rn "==[^=]" src/services/analysis/rule-engine.ts` — must return empty (no loose equality)
4. `grep -rn " any" src/services/numerology/ src/services/analysis/rule-engine.ts` — must return empty
</verification>

<success_criteria>
- calculateGematria('שלום') returns 376
- reduceToSingleDigit(11/22/33) returns the number unchanged
- evaluateCondition uses === strictly
- All 4 files: typed interfaces, JSDoc Hebrew comments, under 300 lines each
- npx vitest run tests/services/ — all tests pass
- npx tsc --noEmit — 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-02-SUMMARY.md`
</output>
