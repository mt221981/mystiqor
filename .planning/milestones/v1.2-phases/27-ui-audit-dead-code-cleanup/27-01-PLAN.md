---
phase: 27-ui-audit-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/nebula-button.tsx
  - src/components/ui/glass-input.tsx
  - src/components/ui/glass-card.tsx
  - src/components/features/insights/ExplainableInsight.tsx
  - src/components/features/shared/AnalysisHistory.tsx
  - src/components/features/shared/ToolPageHero.tsx
  - src/components/forms/BirthDataForm.tsx
  - src/hooks/useMobile.ts
  - src/hooks/useAnalytics.ts
  - src/services/analysis/rule-engine.ts
  - src/app/actions/auth.ts
  - src/components/features/tarot/TarotCardDetailModal.tsx
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "Zero orphaned components remain in codebase — all 11 files deleted"
    - "TarotCardDetailModal renders without GlassCard import — uses plain div with equivalent CSS"
    - "TypeScript compiles with 0 errors after all deletions"
    - "No broken imports reference any deleted file"
  artifacts:
    - path: "src/components/features/tarot/TarotCardDetailModal.tsx"
      provides: "Updated tarot modal without GlassCard dependency"
      contains: "mystic-card-gold"
  key_links:
    - from: "src/components/features/tarot/TarotCardDetailModal.tsx"
      to: "globals.css"
      via: "CSS class mystic-card-gold applied directly on div"
      pattern: "mystic-card-gold"
---

<objective>
Delete all 11 orphaned files (components, hooks, services, server actions) per user decision D-01 through D-06, and update TarotCardDetailModal to remove its GlassCard import.

Purpose: Clean dead code from the codebase so that Wave 2 verification reports reflect the actual clean state. These 11 files total ~866 lines of unused code that inflate the codebase and confuse import searches.

Output: 11 files deleted, 1 file updated, 0 TypeScript errors, 0 broken imports.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ui-audit-dead-code-cleanup/27-CONTEXT.md
@.planning/v1.2-MILESTONE-AUDIT.md

<interfaces>
<!-- TarotCardDetailModal currently imports GlassCard. After deletion, replace with plain divs using the CSS classes directly. -->

From src/components/ui/glass-card.tsx (TO BE DELETED):
```typescript
// variant="gold" resolves to: 'mystic-card-gold rounded-xl p-6'
// variant="default" resolves to: 'bg-surface-container rounded-xl p-6 border border-outline-variant/5'
```

From src/components/features/tarot/TarotCardDetailModal.tsx (lines 83-101):
```typescript
// Line 83: <GlassCard variant="gold" className="p-4">  -->  <div className="mystic-card-gold rounded-xl p-4">
// Line 94: <GlassCard variant="default" className="p-4">  -->  <div className="bg-surface-container rounded-xl border border-outline-variant/5 p-4">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all 11 orphaned files and update TarotCardDetailModal</name>
  <files>
    src/components/ui/nebula-button.tsx,
    src/components/ui/glass-input.tsx,
    src/components/ui/glass-card.tsx,
    src/components/features/insights/ExplainableInsight.tsx,
    src/components/features/shared/AnalysisHistory.tsx,
    src/components/features/shared/ToolPageHero.tsx,
    src/components/forms/BirthDataForm.tsx,
    src/hooks/useMobile.ts,
    src/hooks/useAnalytics.ts,
    src/services/analysis/rule-engine.ts,
    src/app/actions/auth.ts,
    src/components/features/tarot/TarotCardDetailModal.tsx
  </files>
  <read_first>
    src/components/features/tarot/TarotCardDetailModal.tsx,
    src/components/ui/glass-card.tsx
  </read_first>
  <action>
    **Per D-01 through D-06: delete ALL orphaned files, no exceptions.**

    Step 1 — Delete 11 files (use rm or equivalent):
    1. `src/components/ui/nebula-button.tsx` (38 lines, 0 imports) — per D-02
    2. `src/components/ui/glass-input.tsx` (32 lines, 0 imports) — per D-02
    3. `src/components/ui/glass-card.tsx` (38 lines, 1 import in TarotCardDetailModal) — per D-02
    4. `src/components/features/insights/ExplainableInsight.tsx` (168 lines, 0 page imports) — per D-02
    5. `src/components/features/shared/AnalysisHistory.tsx` (122 lines, 0 page imports) — per D-02
    6. `src/components/features/shared/ToolPageHero.tsx` (127 lines, untracked by git) — per D-02
    7. `src/components/forms/BirthDataForm.tsx` (120 lines, 0 page imports) — per D-02
    8. `src/hooks/useMobile.ts` (35 lines, 0 imports) — per D-03
    9. `src/hooks/useAnalytics.ts` (64 lines, 0 imports from pages/layout) — per D-03
    10. `src/services/analysis/rule-engine.ts` (142 lines, 0 imports from API routes) — per D-04
    11. `src/app/actions/auth.ts` (orphaned signOut action, Header uses @/lib/supabase/auth instead) — per D-05

    Also delete the now-empty `src/app/actions/` directory after removing auth.ts.

    Step 2 — Update TarotCardDetailModal.tsx (per D-06):
    - Remove the `import { GlassCard } from '@/components/ui/glass-card'` line (line 14)
    - Replace `<GlassCard variant="gold" className="p-4">` (line 83) with `<div className="mystic-card-gold rounded-xl p-4">`
    - Replace `</GlassCard>` (line 90) with `</div>`
    - Replace `<GlassCard variant="default" className="p-4">` (line 94) with `<div className="bg-surface-container rounded-xl border border-outline-variant/5 p-4">`
    - Replace `</GlassCard>` (line 101) with `</div>`
    - The 'use client' directive and all other imports remain unchanged.
    - Note: `mystic-card-gold` and `bg-surface-container` are CSS classes defined in globals.css — they do NOT depend on glass-card.tsx.

    Step 3 — Verify no broken imports remain:
    - Run `grep -r "glass-card\|nebula-button\|glass-input\|ExplainableInsight\|AnalysisHistory\|ToolPageHero\|BirthDataForm\|useMobile\|useAnalytics\|rule-engine\|actions/auth" src/ --include="*.ts" --include="*.tsx" -l`
    - Expected: 0 matches (all references removed)

    Step 4 — Verify TypeScript compiles clean:
    - Run `npx tsc --noEmit`
    - Expected: 0 errors
  </action>
  <verify>
    <automated>cd D:/AI_projects/MystiQor/mystiqor-build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 11 orphaned files no longer exist on disk (ls each path returns "not found")
    - src/app/actions/ directory no longer exists
    - TarotCardDetailModal.tsx has zero references to "glass-card" or "GlassCard"
    - TarotCardDetailModal.tsx contains "mystic-card-gold rounded-xl p-4" (gold variant replacement)
    - TarotCardDetailModal.tsx contains "bg-surface-container rounded-xl border border-outline-variant/5 p-4" (default variant replacement)
    - grep for deleted module names across src/ returns 0 matches
    - tsc --noEmit exits with 0 errors
  </acceptance_criteria>
  <done>
    All 11 orphaned files deleted, TarotCardDetailModal updated with equivalent CSS classes,
    zero broken imports, TypeScript compiles clean. AUDIT-02 (orphans resolved) and AUDIT-03
    (rule-engine and analytics hooks resolved via deletion per D-01) are satisfied.
  </done>
</task>

</tasks>

<verification>
1. **File deletion:** `ls` each of the 11 deleted paths — all return "not found"
2. **Import cleanup:** `grep -r "glass-card\|nebula-button\|glass-input\|ExplainableInsight\|AnalysisHistory\|ToolPageHero\|BirthDataForm\|useMobile\|useAnalytics\|rule-engine\|actions/auth" src/ --include="*.ts" --include="*.tsx" -l` returns empty
3. **TypeScript:** `npx tsc --noEmit` exits 0
4. **TarotCardDetailModal:** `grep "mystic-card-gold" src/components/features/tarot/TarotCardDetailModal.tsx` returns 1 match
5. **Test suite:** `npx vitest run` still passes (no regressions from deletions)
</verification>

<success_criteria>
- 11 orphaned files removed from codebase (~866 lines of dead code eliminated)
- 1 file (TarotCardDetailModal.tsx) updated to use direct CSS classes instead of deleted GlassCard component
- 0 TypeScript errors
- 0 broken imports
- Test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-audit-dead-code-cleanup/27-01-SUMMARY.md`
</output>
