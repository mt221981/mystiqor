---
phase: 02-core-features
plan: 09
type: checkpoint:human-verify
wave: 5
depends_on: [02-06, 02-07, 02-08]
files_modified: []
autonomous: false
requirements: [ONBR-01, ONBR-02, ONBR-03, TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05, TOOL-06, TOOL-07, TOOL-08, TOOL-09, TOOL-10, TOOL-11, TOOL-12, TOOL-13]
must_haves:
  truths:
    - "All 13 tool pages render without 500 errors"
    - "BirthChart SVG renders with visible planet dots and zodiac ring"
    - "Onboarding wizard can be completed end-to-end (4 steps → profile saved)"
    - "tsc --noEmit passes with 0 errors"
    - "next build completes with 0 errors"
  artifacts:
    - path: "src/app/(auth)/tools/numerology/page.tsx"
      provides: "Numerology tool page"
      contains: "NumberCard"
    - path: "src/components/features/astrology/BirthChart/index.tsx"
      provides: "BirthChart SVG container"
      contains: "ZodiacRing"
  key_links:
    - from: "All 13 tool pages"
      to: "Their respective API routes"
      via: "useMutation fetch calls"
      pattern: "fetch.*api/tools"
---

<objective>
Human verification of all Phase 2 work: all 13 tools render, onboarding completes, build is clean.

Purpose: Before marking Phase 2 complete, a human must verify that all tools render correctly in the browser and the build passes. Automated tests confirm logic; human verification confirms UX, RTL layout, and visual correctness.

Output: Human-confirmed sign-off on Phase 2 features.
</objective>

<execution_context>
@C:/Users/OWNER/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build validation + test suite</name>
  <files></files>
  <read_first>
    None — run commands only.
  </read_first>
  <action>
Run the full validation sequence before presenting the checkpoint:

1. `npx tsc --noEmit` — must complete with 0 errors
2. `npx vitest run --reporter=verbose` — run the full test suite
3. `npx next build` — must complete with 0 build errors (warnings OK)

Report results to user before proceeding to the human verification step.

Fix any TypeScript errors found before presenting the checkpoint. If test failures are found, report which tests failed and their error messages.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 TS errors" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits with 0 TypeScript errors
    - next build completes without "Build failed" message
    - All vitest tests pass (or failures are documented)
  </acceptance_criteria>
  <done>Build is clean, tests pass or failures documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 2 features: 4-step onboarding wizard, real-data dashboard, home page with ToolGrid, and all 13 mystical tools with API routes and pages.

Tools built:
1. TOOL-01: Numerology (5 Hebrew number cards)
2. TOOL-02: Astrology Birth Chart (5-part SVG)
3. TOOL-03: Solar Return (VSOP87 binary search)
4. TOOL-04: Transits (real ephemeris, REBUILT from mocked)
5. TOOL-05: Synastry (compatibility score)
6. TOOL-06: Astrology Readings (8 reading types)
7. TOOL-07: Graphology (RadarChart comparison)
8. TOOL-08: Drawing Analysis (DigitalCanvas + Koppitz)
9. TOOL-09: Palmistry (image URL + vision AI)
10. TOOL-10: Tarot (cards from DB + AI interpretation)
11. TOOL-11: Human Design (9-center SVG + LLM simulation)
12. TOOL-12: Dream Analysis (async fire-and-forget)
13. TOOL-13: Compatibility (numerology + astrology combined)
  </what-built>
  <how-to-verify>
Run `npm run dev` and check the following:

**Onboarding (ONBR-01):**
- [ ] Visit /onboarding → see 4 steps
- [ ] Reach step 3 "הבנת הניתוחים" → Next button disabled until both checkboxes checked
- [ ] Complete all 4 steps → profile saved → redirected to /tools

**Dashboard + Home (ONBR-02, ONBR-03):**
- [ ] Visit /dashboard → 4 stat cards visible with numbers (not zeros after having done any analysis)
- [ ] Visit / → ToolGrid visible with Hebrew tool names

**Spot-check 3 tools:**
- [ ] /tools/numerology → enter שם מלא "ישראל ישראלי" + birth date → submit → 5 number cards appear
- [ ] /tools/astrology → enter birth data + city → submit → BirthChart SVG renders with zodiac ring + colored planets
- [ ] /tools/tarot → submit with 3 cards → cards display with Hebrew names

**Build health:**
- [ ] `npx tsc --noEmit` → 0 errors
- [ ] Check any of the 13 tool pages for RTL layout issues (text aligns right, buttons align correctly)
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found (e.g. "birth chart SVG is blank" or "step 3 checkbox doesn't work")</resume-signal>
</task>

</tasks>

<verification>
Human sign-off required. Build validation runs automatically in Task 1.
</verification>

<success_criteria>
- Phase 2 human-verified: all 13 tools render + onboarding complete
- tsc --noEmit: 0 errors
- next build: passes
- RTL layout correct in spot-checked pages
- PHASE SCORE ≥ 52/60 (to be scored by executor after human approval)
</success_criteria>

<output>
After human approval, update `.planning/ROADMAP.md` Phase 2 status to "Complete" and create `.planning/phases/02-core-features/02-09-SUMMARY.md`
</output>
