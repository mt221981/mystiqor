---
phase: 02-core-features
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/onboarding/OnboardingWizard.tsx
  - src/app/(public)/page.tsx
autonomous: true
gap_closure: true
requirements: [ONBR-01, ONBR-03]
must_haves:
  truths:
    - "After completing the onboarding wizard, the user is redirected to /tools (not /dashboard)"
    - "The public home page at / renders ToolGrid for authenticated users instead of silently redirecting"
  artifacts:
    - path: "src/components/features/onboarding/OnboardingWizard.tsx"
      provides: "Post-onboarding redirect to /tools"
      contains: "router.push('/tools')"
    - path: "src/app/(public)/page.tsx"
      provides: "Home page with ToolGrid for authenticated users"
      contains: "ToolGrid"
  key_links:
    - from: "src/components/features/onboarding/OnboardingWizard.tsx"
      to: "/tools"
      via: "router.push on wizard completion"
      pattern: "router\\.push\\('/tools'\\)"
    - from: "src/app/(public)/page.tsx"
      to: "src/components/features/shared/ToolGrid.tsx"
      via: "ToolGrid import and render"
      pattern: "import.*ToolGrid"
---

<objective>
Close 2 specification-alignment gaps identified during Phase 02 verification:

1. ONBR-01: OnboardingWizard redirects to /dashboard after completion; plan spec says /tools
2. ONBR-03: Public home page is redirect-only; plan spec says it should render ToolGrid

These are small, targeted fixes that align working code with the original requirements. No feature logic changes -- just redirect target and page rendering.

Purpose: Ensure code matches the documented specification so verification passes without reclassification workarounds.
Output: Two modified files with spec-aligned behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-features/02-VERIFICATION.md
@.planning/phases/02-core-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix post-onboarding redirect target (ONBR-01)</name>
  <files>src/components/features/onboarding/OnboardingWizard.tsx</files>
  <read_first>
    - src/components/features/onboarding/OnboardingWizard.tsx (full file -- find the router.push call near line 155)
    - src/app/(auth)/onboarding/page.tsx (server component already redirects completed users to /tools at line 32 -- confirm consistency)
  </read_first>
  <action>
In `src/components/features/onboarding/OnboardingWizard.tsx`, change the post-completion redirect from `/dashboard` to `/tools`.

Specifically, find the line (currently around line 155):
```typescript
router.push('/dashboard');
```

Change it to:
```typescript
router.push('/tools');
```

This aligns the client-side wizard completion redirect with:
- The original plan spec (ONBR-01 must_have: "redirect goes to /tools")
- The server-side onboarding page which already redirects completed users to /tools (line 32 of onboarding/page.tsx)

Do NOT change anything else in the file. The toast message, error handling, and all other logic remain identical.
  </action>
  <verify>
    <automated>grep -n "router.push('/tools')" src/components/features/onboarding/OnboardingWizard.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep for `router.push('/tools')` in OnboardingWizard.tsx returns exactly 1 match
    - grep for `router.push('/dashboard')` in OnboardingWizard.tsx returns 0 matches
    - No other lines in the file are modified (diff shows exactly 1 changed line)
    - TypeScript compilation passes: `npx tsc --noEmit --pretty 2>&1 | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>OnboardingWizard redirects to /tools after completion, consistent with server-side redirect and original ONBR-01 spec.</done>
</task>

<task type="auto">
  <name>Task 2: Add ToolGrid to public home page (ONBR-03)</name>
  <files>src/app/(public)/page.tsx</files>
  <read_first>
    - src/app/(public)/page.tsx (current redirect-only implementation)
    - src/components/features/shared/ToolGrid.tsx (the ToolGrid component to render -- check its Props interface and export)
    - src/components/features/dashboard/DailyInsightCard.tsx (check its Props interface -- it needs birthDate prop)
    - src/app/(auth)/dashboard/page.tsx (reference for how HeroToolGrid + DailyInsightCard are used on dashboard)
    - src/lib/supabase/server.ts (createClient import for server component auth check)
  </read_first>
  <action>
Replace `src/app/(public)/page.tsx` from its current redirect-only implementation to a server component that:

1. Checks auth via `createClient()` + `getUser()`
2. If NOT authenticated: `redirect('/login')` (same as current behavior)
3. If authenticated: Renders a page with the ToolGrid component and a simple welcome heading

The page should follow the existing code patterns (Hebrew JSDoc, RTL, Tailwind, server component). Keep it under 50 lines -- this is a simple page.

Implementation:

```typescript
/**
 * דף הבית — מציג את רשת הכלים למשתמשים מחוברים, מפנה אנונימיים ל-login
 */

import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { ToolGrid } from '@/components/features/shared/ToolGrid';

/** דף הבית — server component */
export default async function HomePage() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();

  if (!user) redirect('/login');

  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-on-surface mb-6">
        ברוכים הבאים למיסטיקור
      </h1>
      <ToolGrid />
    </div>
  );
}
```

Key decisions:
- Server component (no 'use client') -- auth check runs server-side, same as before
- ToolGrid imported from shared components (already exists, takes optional className prop)
- No DailyInsightCard here -- that requires birthDate prop and complex client state; it lives on the dashboard which already has it
- Hebrew heading per RTL conventions
- Container + padding matches existing page patterns
  </action>
  <verify>
    <automated>grep -n "ToolGrid" src/app/\(public\)/page.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - grep for `ToolGrid` in (public)/page.tsx returns at least 2 matches (import + JSX render)
    - grep for `redirect('/login')` in (public)/page.tsx returns 1 match (unauthenticated redirect preserved)
    - The file does NOT contain 'use client' (remains a server component)
    - The file is under 50 lines
    - TypeScript compilation passes: `npx tsc --noEmit --pretty 2>&1 | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>Public home page renders ToolGrid for authenticated users, redirects unauthenticated to /login. ONBR-03 spec satisfied -- ToolGrid visible at / route.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **ONBR-01 check**: `grep "router.push('/tools')" src/components/features/onboarding/OnboardingWizard.tsx` shows the /tools redirect
2. **ONBR-03 check**: `grep "ToolGrid" src/app/\(public\)/page.tsx` shows ToolGrid import and render
3. **Build check**: `npx tsc --noEmit` passes with 0 errors
4. **No regressions**: `npx vitest run tests/components/onboarding.test.tsx tests/components/tool-grid.test.tsx` passes
</verification>

<success_criteria>
- OnboardingWizard.tsx redirects to /tools after completion (not /dashboard)
- (public)/page.tsx renders ToolGrid for authenticated users
- (public)/page.tsx redirects unauthenticated users to /login (preserved behavior)
- TypeScript compiles with 0 errors
- Existing onboarding and tool-grid tests still pass
- Total changes: exactly 2 files, minimal diff
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-features/02-10-SUMMARY.md`
</output>
