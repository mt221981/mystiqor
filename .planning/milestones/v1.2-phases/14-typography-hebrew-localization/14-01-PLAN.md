---
phase: 14-typography-hebrew-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hebrew body text renders at line-height 1.7 globally without per-component overrides"
    - "Hebrew body and label text have zero letter-spacing globally"
    - "Numeric values in Hebrew context render with tabular-nums"
    - "Font loading does not cause cumulative layout shift (CLS) — fallback fonts are size-adjusted"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global .font-body and .font-label typography overrides"
      contains: "line-height: 1.7"
    - path: "src/app/layout.tsx"
      provides: "CLS-preventing adjustFontFallback on all font declarations"
      contains: "adjustFontFallback"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "body element has font-body class, globals.css defines .font-body rules"
      pattern: "font-body"
---

<objective>
Add global typography overrides for Hebrew text and prevent font-loading CLS.

Purpose: Establish correct line-height (1.7), zero letter-spacing, and tabular-nums on all Hebrew body text via CSS cascade from globals.css. Enable adjustFontFallback on all next/font/google declarations to prevent layout shift during font loading. These global rules eliminate the need for per-component typography fixes.

Output: Updated globals.css with .font-body/.font-label rules, updated layout.tsx with adjustFontFallback on all 4 font declarations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-typography-hebrew-localization/14-CONTEXT.md
@.planning/phases/14-typography-hebrew-localization/14-UI-SPEC.md
@src/app/globals.css
@src/app/layout.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global Hebrew typography overrides to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current state — see existing @layer base block and utility classes)
    - src/app/layout.tsx (line 112 — body has className="font-body ..." confirming cascade root)
    - tailwind.config.ts (lines 26-29 — font-body resolves to Heebo via --font-hebrew)
  </read_first>
  <action>
    Add a new rule block inside the existing `@layer base { }` section of globals.css, AFTER the `body { ... }` rule (after line 118) and BEFORE the closing `}` of `@layer base`.

    Add these exact CSS rules:

    ```css
    /* === טיפוגרפיה עברית גלובלית — D-01, D-02, D-03 === */
    .font-body {
      line-height: 1.7;
      letter-spacing: 0;
      font-variant-numeric: tabular-nums;
    }

    .font-label {
      letter-spacing: 0;
    }
    ```

    Per D-01: `line-height: 1.7` on `.font-body` — Hebrew body text gets correct spacing automatically.
    Per D-02: `letter-spacing: 0` on BOTH `.font-body` AND `.font-label` — prevents any Tailwind tracking utility from affecting Hebrew text.
    Per D-03: `font-variant-numeric: tabular-nums` on `.font-body` — all numeric values (gematria, scores, dates) are tabular-aligned.

    Do NOT touch any existing rules. Do NOT modify the body rule. These are ADDITIONAL rules that use Tailwind's font-family class names as CSS selectors.
  </action>
  <verify>
    <automated>cd "D:/AI_projects/MystiQor/mystiqor-build" && grep -n "line-height: 1.7" src/app/globals.css && grep -n "letter-spacing: 0" src/app/globals.css && grep -n "font-variant-numeric: tabular-nums" src/app/globals.css && grep -c "\.font-body" src/app/globals.css && grep -c "\.font-label" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `.font-body { line-height: 1.7; letter-spacing: 0; font-variant-numeric: tabular-nums; }`
    - globals.css contains `.font-label { letter-spacing: 0; }`
    - Both rules are inside `@layer base { }`
    - No existing rules were modified or removed
    - File still has valid CSS syntax (no unclosed braces)
    - `grep -c "line-height: 1.7" src/app/globals.css` returns 1
    - `grep -c "font-variant-numeric: tabular-nums" src/app/globals.css` returns 1
    - `grep "letter-spacing: 0" src/app/globals.css` returns 2 lines (one for .font-body, one for .font-label)
  </acceptance_criteria>
  <done>Global .font-body rule has line-height 1.7, letter-spacing 0, tabular-nums. Global .font-label rule has letter-spacing 0. Both inside @layer base.</done>
</task>

<task type="auto">
  <name>Task 2: Enable adjustFontFallback on all next/font/google declarations in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (lines 18-47 — all 4 font declarations: plusJakartaSans, inter, manrope, heebo)
  </read_first>
  <action>
    Per D-04: Add `adjustFontFallback: true` to the options object of ALL four `next/font/google` font declarations in layout.tsx:

    1. `plusJakartaSans` (Plus_Jakarta_Sans) at ~line 18 — add `adjustFontFallback: true` after `display: 'swap'`
    2. `inter` (Inter) at ~line 25 — add `adjustFontFallback: true` after `display: 'swap'`
    3. `manrope` (Manrope) at ~line 32 — add `adjustFontFallback: true` after `display: 'swap'`
    4. `heebo` (Heebo) at ~line 39 — add `adjustFontFallback: true` after `display: 'swap'`

    Each declaration currently ends with `display: 'swap',` followed by `});`. Add `adjustFontFallback: true,` on a new line before the closing `});`.

    Example — the `heebo` declaration should become:
    ```typescript
    const heebo = Heebo({
      subsets: ['hebrew', 'latin'],
      weight: ['300', '400', '500', '600', '700'],
      variable: '--font-hebrew',
      display: 'swap',
      adjustFontFallback: true,
    });
    ```

    Do NOT change any other properties. Do NOT change the className on body or html. Do NOT change metadata or viewport.
  </action>
  <verify>
    <automated>cd "D:/AI_projects/MystiQor/mystiqor-build" && grep -c "adjustFontFallback" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adjustFontFallback" src/app/layout.tsx` returns exactly 4
    - `grep "adjustFontFallback: true" src/app/layout.tsx` shows 4 lines
    - All 4 font declarations (Plus_Jakarta_Sans, Inter, Manrope, Heebo) have the property
    - No other changes to layout.tsx — body className still has "font-body", html still has lang="he" dir="rtl"
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>All 4 next/font/google declarations in layout.tsx have adjustFontFallback: true. Fallback fonts are size-adjusted to prevent CLS during font loading.</done>
</task>

</tasks>

<verification>
1. `grep -n "line-height: 1.7" src/app/globals.css` — confirms D-01
2. `grep -n "letter-spacing: 0" src/app/globals.css` — confirms D-02 (2 results: .font-body and .font-label)
3. `grep -n "font-variant-numeric: tabular-nums" src/app/globals.css` — confirms D-03
4. `grep -c "adjustFontFallback: true" src/app/layout.tsx` — returns 4, confirms D-04
5. `npx next build 2>&1 | head -20` — build succeeds without errors
</verification>

<success_criteria>
- globals.css has .font-body rule with line-height: 1.7, letter-spacing: 0, font-variant-numeric: tabular-nums
- globals.css has .font-label rule with letter-spacing: 0
- layout.tsx has adjustFontFallback: true on all 4 font declarations
- Build passes with zero errors
- No existing rules modified or removed
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-hebrew-localization/14-01-SUMMARY.md`
</output>
